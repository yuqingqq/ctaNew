program: P-2026-003-polymarket-5min
name: Polymarket Crypto 5-min Markets
phase: in-progress
status: active
branch: mm-research            # all work lives here, not main; pushed to origin
updated: 2026-08-20 (session 2 — sigma review applied, spec repaired, code HOLD)

flags:
  collector_running: true    # since 2026-08-19 ~14:32 UTC. MNAR bug fixed
                             # 2026-08-20 (see incident log in HANDOFF): 27 of
                             # 47 WS disconnects were OUR OWN slow-consumer
                             # overflow, 32 of 47 on BTC = loss concentrated in
                             # the busiest intervals. Post-fix: 0 drops vs 28.
                             # Data before the fix is MNAR — never pool a
                             # pre/post-fix unpaired statistic.
  settlement_verified: true  # E-M6 PASSED 2026-08-20: S60(T) vs S60(t0)
                             # reproduces 1465/1465 winners at 99.8% (99.9% at
                             # |margin|>0.5bp). w = 60 s CONFIRMED; the
                             # full-300s reading is REFUTED at 86.9%.
                             # Knowledge-time read 99.3% — the 0.5pp gap is the
                             # look-ahead a careless backtest would bank.
  g2_model_beats_book: null  # NOT DECIDED. The 2026-08-20 run said the book
                             # wins at every horizon (+2.5..3.2 Brier pts), but
                             # that model was MIS-ANCHORED (see anchor_bug
                             # below), so the verdict is WITHDRAWN, not held.
                             # Must be re-read on the corrected specification.
  anchor_bug: found+diagnosed  # E_t[X_T] used the trailing S60, which lags spot
                             # by ~w/2 = 30 s, paired with a CONDITIONAL
                             # variance law. sigma_eff came out ~2.6x too small
                             # at r=30. Nowcast P_hat = 2*S30 - S60 fixes it:
                             # -0.0101 Brier pooled, at EVERY horizon. The
                             # anchor explains a large share of the deficit;
                             # the residual model-vs-book verdict is unadjudicated.
  flb_edge: downgraded       # WAS "+3.6 c/share at p in [0.15,0.35), stable" —
                             # the prior that recommended the Option-B re-scope.
                             # That was measured on `book` snapshots which are
                             # p90 6.2 s STALE. Rebuilt from
                             # price_change.best_bid/ask (executable quotes):
                             # b_hat 1.145 (stale inflated it to 1.182),
                             # walk-forward gain 0.0004 Brier, and the effect is
                             # ONE-SIDED = a drift signature, not a bias.
  stale_book_contamination: true  # EVERY book-derived number in PM_DEEP_REVIEW
                             # inherits this defect, including the "+95 bps
                             # maker gross / +136 with rebate". Treat all of
                             # them as UNVERIFIED until re-measured on
                             # price_change quotes.
  decision_pending: true     # Option A (mechanism program) vs Option B (FLB
                             # harvest re-scope) — STILL OPEN, but B has lost
                             # the measurement that recommended it. Re-frame
                             # before choosing; do not re-read PM_MM_PLAN §17's
                             # recommendation as current.
  siting_decision: suspended # measured latency 1,700 ms of which 1,440 ms is
                             # PM-side publish delay (untouchable). CORRECTED
                             # 2026-08-20: the 1.7 s is the SIGNAL clock only —
                             # the book itself arrives in 47 ms. Siting matters
                             # much less than the FATAL-1 finding implied.
  fee_structure_known: false # fee_rate_bps = 0 on all trades observed so far;
                             # conflicts with docs (7%) and CLOB base_fee.
  structure_converged: false # 12 review iterations; replay stands at
                             # LOCAL 11 / SPREADING 1 / STRUCTURAL 1.
  sigma_plan_finalized: true # SIGMA_PLAN.md, commit 80f823e. Phase 0 gated.
  sigma_plan_review: applied # SIGMA_PLAN_REVIEW.md, commit 6bea435. All six
                             # MUST-FIX ACCEPTED and applied in SIGMA_PLAN.md
                             # REVISION 2 + sigma_kernels.py + contracts v13.
                             # ESTIMATOR CODE STILL ON HOLD — Phase 0A steps
                             # 4-6 (consumer matrix, S30/S60 semantics, anchor
                             # helper) remain. One remedy DECLINED with reasons
                             # (R1): unit space is normalised ARITHMETIC
                             # returns, not log — the settlement mark is an
                             # arithmetic mean, so log coordinates demote every
                             # TWAP identity to an approximation. Not a
                             # magnitude argument (Jensen gap 0.00059 bps =
                             # 0.024% of sigma_eff(30)); an exactness one.
  anchor_ledger_closed: true # omega_P is NOT a free parameter: P_hat =
                             # 2*S30-S60 is a fixed linear functional of the
                             # same latent path as sigma, so its error is
                             # exactly 3.0845*sigma on the 1s grid (3.36 bps
                             # for BTC; continuous sqrt(10)). Gives S3 its
                             # floor/ceiling bracket without identifying latent
                             # Chainlink spot. sigma_eff was UNDERSTATED at
                             # every horizon: r=30 goes 1.77 -> 2.44 bps.
                             # sigma_kernels.py, 13 selftests.
  d2_retired: true           # "c(r) will breach [0.8,1.25], budget for
                             # redesign" is WITHDRAWN. It was computed against
                             # an incomplete ledger: c(30)=2.17 on the
                             # diffusion line alone, 1.14 with the anchor line
                             # in = IN BAND. This does not prove c(r) is in
                             # band (2.6 bps is provisional, w_hat~47s
                             # untouched) — it removes the prediction. No
                             # redesign budget is reserved on its account.
  contracts_version: 13      # M13-1. BE-Uncertainty module added; PathLaw was
                             # {kind: str, params: dict[str,float]} which could
                             # not carry a unit space, horizon domain, fit
                             # cutoff or refusal. Now typed: UnitSpace,
                             # HorizonDomain, AnchorErrorBudget (floor/ceiling,
                             # identified=false), CalibrationCurve (status
                             # DIAGNOSTIC until >=10 day clusters), and a
                             # settlement_var/increment_var protocol that
                             # REFUSES out of domain. 2 migration records;
                             # contract_check.py passes, 0 unexplained changes.
                             # STILL OPEN: BE-Belief absent from modules: —
                             # that belongs to the structure review loop.

focus: >
  Build and test the model, mechanism-first, no PnL/capacity estimation
  (user, 2026-08-20). Entry point is live/pm_research/PM_ARCHITECTURE.md (v12)
  with contracts/contracts.yaml as the machine-readable source of truth.
  IMMEDIATE: Phase 0A steps 4-6. Steps 1-3 are DONE (unit space frozen, typed
  BE-Uncertainty/PathLaw carrier landed at contracts v13, discrete kernels and
  the closed anchor ledger derived and unit-tested in sigma_kernels.py). Next:
  freeze the consumer matrix including the BE-Belief fallback's own loss
  function; VERIFY S30/S60 SEMANTICS against the 1 s Binance tape — this can
  still invalidate the anchor and the nowcast together and must precede the
  helper; then implement the anchor helper and measure the omega_P ceiling.
  Estimator implementation is on HOLD. Only after Phase 0A may the simple
  per-symbol baseline and complete-ledger c(r) diagnostic run.

standing_rules:
  - No design decision that a measurement on existing data could settle may be
    recorded as settled until that measurement is run.
  - Dedup prices by (timestamp, symbol) and raw by message identity — recv_ns
    differs per process, so exact-line dedup does NOT catch duplicate
    collectors. Check collectors with `ps -eo pid,etimes,cmd | grep
    live/pm_research` (pgrep patterns must include the pm_research/ segment).
  - Read book state from `price_change.best_bid/ask`, never from `book`
    snapshots — the latter are p90 6.2 s stale and quietly inflate any
    edge measured against them.
  - Read everything at knowledge time (recv_ns), never payload timestamps.
    Measured cost of getting this wrong is 0.5 pp on E-M6.

tasks:
  - id: e0-collector
    phase: 0
    name: collect_pm.py — CLOB WS book/trades + Gamma metadata + CLOB resolutions
    status: done
    notes: |
      LIVE 2026-08-19. Three bugs found and fixed: (1) Gamma outcomePrices is
      live prices not resolution (is_final guard); (2) Gamma DELETES resolved
      5-min markets — resolution source switched to
      clob.polymarket.com/markets/<conditionId>; (3) 2026-08-20 the hot loop
      allocated an asyncio timer per message, which at BTC's rate backed the
      server send buffer up into 1013 slow-consumer disconnects. Rewritten to a
      persistent recv task + deadline task, 2000-line write batches.
  - id: e0-audit
    phase: 0
    name: EXP-E0 data integrity audit (7 checks)
    status: done
    notes: exp_e0_data_audit.py. Quantifies the duplicate-collector overlap, the
      ~16 min market-side outage, the 8 malformed resolution rows, restart
      shards, TWAP gaps, knowledge-time lag, and the up-rate drift confound.
  - id: em6-settlement
    phase: 0
    name: EXP-M6 settlement truth — THE FOUNDATION GATE
    status: done
    notes: PASSED at 99.8% on 1465 windows. w=60 confirmed, 300 s refuted.
      exp_m6_settlement.py; results in EXP_RESULTS_2026-08-20.md.
  - id: architecture
    phase: 0
    name: Modular architecture + machine-readable contracts
    status: done-not-converged
    notes: |
      PM_ARCHITECTURE.md v12, 6 planes; contracts/contracts.yaml 85 types with a
      structural diff checker and 13 selftests; migrations.yaml binds each
      migration to (operation, key, old, new, version). Two of my own artefacts
      were PROVEN UNSOUND during review and replaced: the first contract checker
      (invalid ref exited 0, generics invisible, narrowing produced an identical
      inventory) and the path-keyed allowlist (M11-1: authorised ANY change at
      that path — reproduced CouplingSource -> CompetitionState passing).
      Review loop is at 12 iterations and NOT converged.
  - id: sigma-model
    phase: 1
    name: Volatility estimator — spec repaired (REVISION 2), Phase 0A 4-6 next
    status: spec-repaired-hold
    notes: |
      Three estimators were built and each violated a decision already made
      (static pooled -> static per-coin -> rolling per-window with POOLED blend
      weights). Root cause was not sigma at all: the ANCHOR was wrong. Plan
      written and finalized (SIGMA_PLAN.md, 80f823e), reviewed at 6bea435, and
      all six MUST-FIX now APPLIED in REVISION 2. Estimator code still on HOLD.
      DONE: unit space frozen (normalised arithmetic returns, bps — R1 declines
      the review's log-coordinate remedy because the settlement mark is an
      arithmetic mean and log space demotes every TWAP identity to an
      approximation); typed BE-Uncertainty + PathLaw carrier at contracts v13;
      discrete kernels unit-tested, continuous at r=w (v1 MIXED conventions, so
      its r>w kernel should be r-39.4972 not r-40); the anchor ledger closed in
      CLOSED FORM, which retires D2; MNAR pairing claim struck; output renamed a
      physical-volatility FORECAST with overlap-aware day-block validation;
      c(r)/H-3 given estimands, domains and a frozen inference rule.
      ALSO FOUND (neither v1 nor the review caught it): the in-window anchor
      coefficient is not (r/w)^2*omega_P^2 in general — inside the window the
      trailing (w-r)s TWAP is reconstructed from the SAME S30/S60 pair, so the
      errors correlate and add. Exact at r=30 and r=60 (today's only in-window
      grid points), 24% understated at r=45. Live the moment the decision grid
      densifies toward per-second quoting.
      NEXT: Phase 0A 4 (consumer matrix incl. the fallback's loss function),
      5 (VERIFY S30/S60 SEMANTICS — can invalidate anchor + nowcast together;
      must precede the helper), 6 (anchor helper + measure the omega_P ceiling).
  - id: e-x1-bookbeat
    phase: 1
    name: Re-read the model-vs-book verdict on the corrected anchor
    status: pending
    notes: Decides the programme's identity (alpha vs pure market-making) and is
      a precondition for SIGMA_PLAN §0's argument that sigma is only plumbing.
      Pairing does NOT repair the load-correlated missing regimes. Rebuild dense
      top of book from price_change, require cause-aware gap admissibility and
      prefer post-repair data. Descriptive until day-clustered inference exists.
  - id: g-ff4-queue
    phase: 1
    name: Queue-position bracket (Q_ahead)
    status: pending
    notes: POTENTIALLY PROGRAM-ENDING. If the bracket on Q_ahead is wide enough,
      MM profitability is not knowable from the data we can collect. Plan in
      plans/BE_FLOWANDFILLS_PLAN.md.
  - id: e1-dataset
    phase: 1
    name: Aligned dataset — binary book vs Binance F_t at 100ms, per window
    status: pending
  - id: e3-screen
    phase: 2
    name: Run the pre-registered gates on >=2 weeks of windows
    status: pending
