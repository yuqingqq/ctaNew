DA 117 — coordinator — GO, one batch: LIGHT first, then ONE heavy launch; fetch origin (DE 117 (GO #6) sealed the 09-06 day: receipt @@RECEIPT@@; BE 72's book @@BOOK@@, structure v4 naming it; the deploy pin v2 current). (1) The 09-06 LIGHT pre-read against the receipt as the chain head (your resolver names it): the gate, the seal census by KEY under the list in force at its emit (the eleven), provenance by pair (params v15, design v23), `n_days_complete` through the read gate's resolver, `n_arms_agreeing` None where the row loop is skipped; nothing sealed, never a reason text; the anti-echo declaration v1 in force. (2) `da117book06` — the open-book heavy half on the 09-06 book through the structure head v4 (the guard BEFORE the open), unique unit name, pre-state by inode and `not-found`, the capture record with `mapped_by` and the leaf read (the peak of record) beside the verbatim property, the population half by name (both arms recomputed from `asm.by_arm`, seeds re-derived), superseding (1)'s record by the pair; one commit for both; STOP on any refusal or failure. Report ONE message: per half the record path + sha + pair, the verdicts by name, the unit's five fields + id and leaf peak; your Q-DA row through the script; times from `date`.
