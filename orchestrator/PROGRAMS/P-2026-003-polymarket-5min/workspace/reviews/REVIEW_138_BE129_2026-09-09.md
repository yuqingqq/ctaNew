# REVIEW 138 — BE 129 (`15ed903`), before the rebuild

**REV, 2026-09-09T12:47Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`, no book unpickled.

**VERDICT: IT VERIFIES ON ALL FOUR — counted status, the right predicate on both halves of
the boundary, no assembly mismatch, and cells that test the PROPERTY and never the number
nine. ONE FIX FIRST, AND IT IS ONE LINE: `t0 == t1` is tested WITHOUT FINITENESS, so
`inf`/`inf` and two equal STRINGS are DROPPED and reported as
`ZERO_LENGTH_GENERATION_EXCLUDED`. That is the exact defect DE 172 removed from the probe
forty minutes ago, reappearing in the builder — and here it is worse, because it is a SILENT
DROP FROM THE POPULATION rather than a mislabelled count. It does not touch tonight's nine.
It costs seconds now and fifty-four minutes later.**

---

## (1) THE EXCLUSION IS A COUNTED STATUS — rule 4, met

The returned block, driven:

```
status                      = ZERO_LENGTH_GENERATION_EXCLUDED
n_excluded / before / after / fraction / refuse_above
refuse_above_is_the_default = <bool>            n_tranches_on_them
excluded                    = [{slug, side, gen, t0, t1, n_tranches}, …]
predicate                   = "t0 == t1"        why = <the gap-start mechanism>
the_guard_is_unchanged      = validate_reference keeps its exact predicate
```

and it reaches the receipt (`be_daybook_build.py:1421`). **A reader learns that nine were
excluded, WHICH nine, how many tranches they carried, and why** — not merely that the book is
smaller. `refuse_above_is_the_default` is a good touch: a receipt records whether the guard
ran at its declared threshold.

*One clause, non-blocking:* the `why` asserts a CAUSE (a gap start) that the predicate does
not test — the predicate is "zero-length anywhere". That is the right predicate; the status
just shouldn't claim a mechanism it did not check for the instance in hand.

## (2) THE BOUNDARY — BOTH HALVES DRIVEN, AND ONE REAL DEFECT

```
zero-length, NOT at a gap start   -> DROPPED    (predicate is length, not location)
at a gap start but t0 < t1, 2 tr  -> KEPT       (the second half of the boundary)
normal                            -> KEPT
INVERTED t0 > t1                  -> KEPT       (different fault; validate_reference refuses, loudly)
nan / nan, None / None, KEYS MISSING -> KEPT    (loud downstream — correct)
inf / inf                         -> DROPPED    <-- reported as ZERO_LENGTH_GENERATION_EXCLUDED
equal STRINGS '5' == '5'          -> DROPPED    <-- same
```

**The routed fix is exactly right on both halves of the boundary the round named.** But
`t0 is not None and t1 is not None and t0 == t1` admits `inf == inf` and `"5" == "5"`.

**Why this matters more here than in the probe:** DE 172 fixed the same ordering in a
*diagnostic*, where the cost was a wrong label. Here the cost is a generation **removed from
the population** under a status that says it was a zero-length window. `nan`, `None` and
missing keys correctly survive and go loud at `validate_reference` — so the fix is to make
`inf` and non-numerics behave like them.

**The line:** `if _finite(t0) and _finite(t1) and t0 == t1:` — `_finite` already exists in
`de_reference_integrity_probe`, and `be_daybook_build` needs the same three-clause test.
**Today's nine are real finite floats read from the artifact, so nothing measured moves; the
rebuild runs new code over gaps that were never attached before.**

## (3) THE TWO SCORED GENERATIONS — NO MISMATCH IS CREATED

The prune is at `build()` **line 1209**, immediately after `build_reference`; the assembly is
`asm = R.assemble_streaming({coin: ref}, …)` at **line 1271**. **The assembly consumes the
PRUNED reference.** And the prune is **in place** — driven: after the call,
`fr["reference"]` itself holds only the survivors — so the book's stored reference and the
assembly see the same set.

**So the excluded generations are never scored, never counted in `n_gen`, never a row.** The
"two of the nine are scored" fact is a property of the CURRENT book, which had no exclusion;
after the rebuild those generations do not exist to be scored. **A scored generation absent
from the reference is exactly what this ordering prevents.**

## (4) THE CELLS TEST THE PROPERTY, NOT THE NUMBER

```
'== 9' anywhere in BE 129's cells : False
asserts the IDENTITIES excluded   : {1, 3}      on a 4-generation fixture
asserts the SURVIVORS             : [0, 2]
positive control (clean untouched): present
known-bad (50 of 50 refuses by name): present
```

**Nine appears only in prose.** The mechanics are driven with `refuse_above` passed
explicitly so the fraction guard is falsified separately — that separation is right, and it
means the production default is exercised by the clean-case cell rather than assumed.

## THE RECOMMENDATION, PLAINLY

**Land the one-line finiteness guard, then rebuild.** Everything else verifies. The three
other questions come back clean, and the exclusion's own refusal above 0.1 % is a real
guard with a real known-bad.

## ROUTED

1. **BE — `_finite(t0) and _finite(t1) and t0 == t1`**, before the 54 minutes.
2. **BE — the `why` should not assert the gap-start cause for instances it did not test**
   (or record the nearest-gap-edge distance per excluded generation, which BE already
   measured and would make the claim per-instance).
3. **(1), (3) and (4) are CLEAN.**
