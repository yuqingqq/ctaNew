# REVIEW — DE 83 **closes §1.6 / REV 43 §4.3–4.4**, driven on the exact case I constructed then; and DA 66 closes **both** §C.4 gaps, with its container census reproducing bit-for-bit under my own independent implementation. **One thing the coordinator should expect before DE 84's receipt is read: the peak-stage predicate now REFUSES, and its first real measurement is that run.**

**Filed** 2026-09-06T08:24Z (clock read before composing) · reviewer seat (pm-codex)
· tip `fae41cc` · **LIGHT AND LOCK-FREE.** Nothing was driven over the book, the tape
or the fragment. My heaviest steps: the runner battery **13.23 s / 54 MB**, the design
battery 0.95 s, and four in-process census walks over an 85 KB receipt.

**Rule 20.** The lock was free at 08:20:32Z and is **HELD at 08:24:33Z** (pid 3049131,
`WRITE`) — DE 84's smoke, as dispatched. **I did not take it and nothing of mine
competes with it.**

**ROUTING — CHECKED.** Every number below is recomputed or driven by me.

## VERDICT

**(1) §1.6 IS CLOSED**, and not by inspection: I re-drove **the exact stages dict I
constructed in REV 43 §4.3** — a 7,000 MB transient inside `S4_null` that frees before
the mark. Then, the current-RSS argmax said `S1_load` and the predicate **AGREED** on a
real day. Now the delta argmax says `S4_null`, `the_two_readings_agree: false`, and the
predicate **REFUSES**. The declared stage has no default and is read from the stage
table's `[DECLARED PEAK]` marker — one source, closing §4.4. Battery **152** under my
run. **§1.**

**(2) THE FIXTURE'S `S0_verify` DISAGREEMENT IS EXPECTED AND ITS RESOLUTION IS DE 84.**
On the fixture the deltas are `S0_verify 13.01 · S1_load 1.25 · S4_null 3.07` against a
27.09 MB baseline — S0 is the interpreter and import cost, and a few-hundred-KB book
cannot outweigh it. On a real day S0 carries a **290.8 MB** transient
(`verify_book_against_builder_receipt` hashes via `read_bytes()`, `:1758`) and S1
unpickles a book whose reference alone BE measured at **2.006 GB**, so S1 should
dominate by an order of magnitude. **DE is right — and this is now a real test that can
refuse a correct day, so §2.3 says what to do if it does.**

**(3) DA 66 CLOSES BOTH §C.4 GAPS.** I re-implemented the container census
independently: **33 containers, a byte-identical profile, 1,167 numeric leaves** — and
my own plant of an economic value under an innocuous name inside a list is **missed by
both name nets and moves the container census by one**. DA also found something I did
not: the narrow list-residency reading gives **0** on this receipt. The resource block
carries two stamped live samples with their provenance and the direction of the bound
stated correctly. **§3.**

---

# 1. DE 83 — THE HIGH-WATER DELTA

## 1.1 My own REV 43 case, re-driven at the current tip

```
stages: S0 hw 10 cur 10 | S1 hw 900 cur 900 | S4 hw 7000 cur 120
                                              (7,000 MB allocated inside S4 and freed before the mark)

  current-RSS argmax : S1_load          <- what REV 43 saw, and the predicate AGREED
  DELTA argmax       : S4_null          <- what it sees now
  deltas             : {S0_verify 0.0, S1_load 890.0, S4_null 6100.0}
  the_two_readings_agree : False
  assert_peak_stage(fixture=False) -> REFUSES: "the memory plan declares S1_load as the
                                     peak and the measured peak is S4_null"
```

The deltas are exactly the numbers I predicted in REV 43 (`S4 = 7000 − 900 = 6100`).
**The invisible transient is visible, and the day that would have proceeded now stops.**

**And the positive control admits:** a case whose peak really is at `S1_load`
(deltas `0 / 2090 / 50`) gives `measured_peak_stage: S1_load`, `asserted: True`,
`agrees: True`. Both directions — not a guard shown only to refuse.

## 1.2 The declaration is one source — §4.4 closed

```
peak_stage_predicate(stages, *, declared: str)      <- NO DEFAULT
declared_peak_stage()  -> "S1_load"
DAY_STAGE_PEAK_MARKER  -> "[DECLARED PEAK]"          <- read from the DAY_STAGES table
```

The predicate cannot disagree with the declaration because it has nowhere else to get
the value. That was §4.4 verbatim, and the shape is better than the `PEAK_STAGE`
constant I suggested: the marker lives **in the stage table's own prose**, so the
declaration and the machine-readable fact are the same text.

**The baseline is real too:** `_mark("S_start")` at `:2195`, and the battery asserts the
recorded stage set is exactly `DAY_STAGES ∪ {S_start}` — so `S0_verify`'s delta is
growth from a mark taken before it, not from itself.

## 1.3 DE's own finding — checks that pinned a transient STATE

Two rehearsal checks went red because they asserted a fact about the world at authoring
time ("the book does not exist"; three hardcoded qualifying counts). They are now
**relations** evaluated live:

```
a_days == qual · b_days <= a_days · every_SET_B_day_is_untouched
every_ruled_closed_day_qualifies · holm_consistent_with_own_G ("2^-G <= alpha/2")
and the rehearsal check: "AGREES WITH THE FILESYSTEM in whichever state it is in"
```

with the reason recorded: *"at 00:06Z on 09-07 the 09-06 verdict lands and the
qualifying count changes… rewritten precisely so that event does not break it."*

**This is a class worth the register.** It is the mirror of three findings I filed
against BE this session — the literal `23` beside a constant that grew to 38 then 49,
`what_this_build_did` still describing the build before the correction, the hardcoded
`.v2/.json` receipt search. **A check that asserts a fact goes red when the world moves;
a check that asserts a relation survives it.** DE found this one in its own battery.

## 1.4 §1.6 — CLOSED

Both halves: the delta argmax (§4.3) and the single-source declaration (§4.4). This was
my last open DE ledger item and it is the one I named as *"what remains open for DE"*
three rounds running.

---

# 2. THE FIXTURE'S `S0_verify` — WHAT IT MEANS FOR THE REAL DAY

## 2.1 What the fixture measured

```
baseline highwater 27.0859 MB
deltas: S0_verify 13.0078 · S1_load 1.2500 · S2 0.0 · S3 0.0 · S4_null 3.0703 · S5 0.0
measured_peak_stage           S0_verify      (delta argmax)
measured_peak_stage_by_current_rss  S4_null   (the other reading, reported beside it)
declared_stage_is_the_measured_peak  false
assertion: asserted false, recorded_not_refused_because_fixture true
```

**On a fixture this is the correct answer, not a defect.** `S0_verify` imports BE's
cascade and numpy and verifies the models; `S1_load` unpickles a book of a few hundred
KB. 13 MB of imports beats a fraction of a MB of book, and the predicate says so
instead of pretending otherwise. It is recorded and not refused **because it is a
fixture** — which is the same asymmetry as the battery's `offline=fixture`, and right
for the same reason.

## 2.2 Why S1 should dominate on a real day — with the numbers

| stage | what it allocates on the 09-03 book | order |
|---|---|---|
| `S0_verify` | `verify_book_against_builder_receipt` hashes the book via **`book_path.read_bytes()`** (`:1758`) — a **290,758,834-byte** transient — plus the cascade import (~13 MB, measured on the fixture) | **≈ 300 MB** |
| `S1_load` | `mod.load(book_path)` reads the bytes into a **second** 290 MB buffer and `pickle.loads` it; BE measured the reference alone at **2.006 GB** while building it, and `asm` carries 297,379 keys × 2 heads | **≈ 2 GB** |

So the expected delta ordering is `S1_load ≫ S0_verify` by roughly an order of
magnitude. **DE's "a real day would refuse it… the 2 GB reference load should dominate"
is sound, and the arithmetic above is the basis rather than the assertion.**

## 2.3 **What the coordinator should expect, and what to do if it happens**

The predicate is now a **real test that can refuse a real day**, and DE 84 is its first
measurement. Two outcomes:

1. **`S1_load` is the delta argmax** — the memory plan's central claim is confirmed by
   measurement for the first time, and the 8 GiB ceiling has an evidential basis rather
   than a stated one.
2. **It is not** — the day REFUSES with the artifact unwritten. **That is the predicate
   working**, and the correct response is a **declaration act, not a relaxation**:
   re-declare the peak stage from the measured deltas (moving the `[DECLARED PEAK]`
   marker in `DAY_STAGES`, which moves both the declaration and the predicate at once),
   record why, and re-run. **Never widen the predicate and never raise the cap** —
   R-174. The refusal costs one run; a predicate quietly loosened after seeing costs
   the claim the ceiling rests on.

*One observation, not a finding.* The 290 MB double read in S0 → S1 is deliberate — DE
hashes independently and `be_cancel_axis_null.load` hashes its own buffer, and `run_day`
compares the two, which is a genuine cross-check between two independent reads. It does
mean **S0 carries a 290 MB transient that competes in the delta argmax**, so if the
outcome above is close, that is where the headroom is: one read, `sha256` fed from the
same buffer, would remove ~290 MB from S0's delta and sharpen the test.

---

# 3. DA 66 — BOTH §C.4 GAPS CLOSED

## 3.1 The container census — reproduced bit-for-bit by my own implementation

I wrote the walk again rather than reading DA's numbers:

```
n_containers      mine 33     DA says 33
total numeric     mine 1,167  (my REV 44 walk: 1,167)
profiles identical: True      differing entries: NONE
```

And on the **landed v1 bytes**, my REV 44 walk reproduces exactly: **1,759 leaves /
1,167 numeric / 1,119 list-resident**, over 141 leaf-path groups (33 at DA's coarser
container granularity, which is the better axis — a container census is about *where* a
number sits, not what it is called).

## 3.2 **DA found something I did not** — the narrow list-residency reading is 0

```
list_residency_rule: "a numeric leaf is LIST-RESIDENT if ANY level of its container
   path is a list, not only its immediate parent. On this receipt the two readings are
   1,119 and 0"

my check: numeric leaves whose IMMEDIATE parent is a list -> 0
          broad reading (any list level)                  -> 1,119
```

**Every list in this receipt holds dicts, not bare numbers.** So the obvious
implementation of "is it in a list" reports a clean bill on the region where 1,119 of
the 1,167 numbers live. That is a rule-16 shape found before it could fire, and it is a
better catch than the gap I filed.

## 3.3 The plant — my own, and it confirms the claim

```
PLANTED  .symbols.BTCUSDT.admissions[0].aux_q7 = -12.3456
  marker net           0 leaks   <- MISSES it
  economic-shape net   0 leaks   <- MISSES it
  CONTAINER census      moved -> {"symbols.BTCUSDT.admissions[].aux_q7": 1}
```

**Both name nets miss it and the container count moves by one.** That is exactly the
independent axis §C.4 asked for: the name nets need a reader to have anticipated the
name; the container count moves whatever the leaf is called. **§C.4 gap 1: CLOSED.**

*One number a future reader will trip on, so I state it:* a census of the **whole** v1
receipt gives 95 names, while the receipt records **91**. Not a discrepancy — the
receipt's census was computed on `openr` **before** `sealed_payload` was attached, and
censusing v1 minus `sealed_payload` gives exactly **91**. Checked.

## 3.4 The resource block — every number recomputed, and the bounds are right

```
peak_gib        7.788829803466797   = 8,363,192,320 / 2^30      (recomputed, exact)
headroom_bytes  226,742,272         = 8,589,934,592 − peak      (exact)
headroom_gib    0.21117019653320312                             (exact)
fraction_of_cap 0.9736037254333496                              (exact)
anon 1.1106 GiB · file 5.1716 GiB · events max 0 · oom 0
v1 carried only  max_rss_gib 2.717388153076172                  (my REV 44 number)
```

**The provenance is stated where I asked for it:** *"read by the DA seat from
/sys/fs/cgroup and `systemctl --user show` while `da63smoke.scope` was live. NOT
emitted by the run, NOT recomputable from the v1 receipt's bytes, and NOT a substitute
for the instrument."* Two samples with `as_of` stamps (07:24:17Z, 07:25:16Z).

**And the direction of the bound is correct in both places, which a reader might think
clashes:** the peak is a **LOWER** bound on the run's final peak (`memory.peak` is
monotone and the last sample is ~3.5 min before the end), and the headroom is therefore
an **UPPER** bound on the true headroom. Both are stated; they agree. **§C.4 gap 2:
CLOSED** — and better than asked: `the_cap_was_never_enforced` computes
`memory.events:max = 0` and `oom = 0`, so *"the peak was near the cap and the cap never
bit"* is a predicate rather than a reassurance.

**The best thing in the block is a control that fires.**
`observation_at_the_EMITTERS_OWN_process` measures the emitter's own ambient scope and
reports `status: AMBIENT_SCOPE_NOT_THE_RUNS_OWN`, `in_research_slice: false`,
`is_this_runs_own_memory: false`, `memory_peak_gib: 15.33`. A number many times the
smoke's, correctly refused rather than attributed. **The ambient-scope hazard is
demonstrated, not described.**

## 3.5 The wall attribution and the 0/261 line

```
100.55 + 262.35 + 268.9 = 631.80 = wall_s_total          (recomputed)
262.35 + 268.9         = 531.25 = symbol_block_wall_s     (recomputed)
```

The 268.9 s inside the symbol block but in no stage is named: **the admission legs,
chiefly the rule-5 era leg reading 534,499,718 `recv_ns` rows over the 15
pre-admissible days**, while `resource_observation` records the 11 ADMITTED days only —
*"a reporting gap this line exists to close, not a discrepancy"*. That answers a
question I had not asked (why the per-day stage walls do not sum to the symbol wall)
and answers it against the artifact.

**And the 0/261 reading is sharper than mine.** I wrote that the control did not fire
because its case was absent. DA says the same and then says what a reader must not do
with it: *"read the other way it would retire the v7 narrowing as unnecessary, on
evidence that cannot support it: the pre-v7 runner would have survived THIS run, which
is not the same as the pre-v7 trigger being sound."* That is the correct handling of a
control that did not fire, and it belongs in the register.

**Rule 13 is followed exactly:** v2 supersedes v1 by path **and sha256**
(`ec50c888…`), `untouched: true`, `nothing_was_re_run: true`,
`the_sealed_payload_was_not_opened: true`, and the emitter carries its own digest with
`producing_code_is_the_committed_bytes: true`.

---

# 4. VERDICT

**DE 83: §1.6 / REV 43 §4.3–4.4 CLOSED**, driven on my own constructed case in both
directions, with the declaration and the predicate reduced to one source. My DE ledger
now has no open item.

**DA 66: §C.4 CLOSED, both gaps**, with the container census reproducing bit-for-bit
under an independent implementation and my own plant confirming what the name nets
cannot see. DA found a defect in the obvious implementation of its own new instrument
before it could fire.

**Carried to DE 84, not blocking it:** the peak-stage predicate is now a test that can
refuse a correct-looking day, and §2.3 states the two outcomes and the response to
each — **re-declare from the measurement, never widen the predicate, never raise the
cap.** I would rather that be written down before the receipt exists than argued after.

Nothing here scored an arm, drew a null, opened a sealed file, read a gate, or took the
lock.

---

## CONTEXT

Approximately 76%. Approaching the threshold; I expect to report the 80% crossing on my
next round.
