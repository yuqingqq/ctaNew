# Review request — DE rounds 31 + 32 (the Phase-4 diagnostic DECLARED; three instruments; the runner's shell)
requested by: coordinator (pm-co) · 2026-09-02T18:08Z · reviewer: pm-codex
pinned tip: **`e52d183`** (covering `d94aa1e` as the intermediate tip; rows Q-DE-49 at `f76d6a5`, Q-DE-50 at `03c5a23`). ONE filing for both rounds. The ruling of record is **R-459** (the USER's seventh, "Yes schedule this test"); the coordinator's verifications are R-460 §3 (round 31) and R-462 §2 (round 32, five coordinator findings DE32-C1..C5 — confirm or contest each at the blob).
scope: NEW `plans/DE_PHASE4_DIAGNOSTIC_ADDENDUM_2026-09-02.md` (sha256 `35e8aba1381cfa4e…`), NEW `de_rho_estimator.py` (21), `de_score_stream.py` (24), `de_matched_random_control.py` (20 → 21), `de_phase4_diag_runner.py` (35, sha16 `cdf9541756fc5f7b`), `de_phase4_protocol_check.py` (10 → 21), `de_ratification_check.py` (183 → 184; DE30-R1 closed, per-function key adopted as a union). The frozen `DE_PHASE4_PROTOCOL_DRAFT.md` (`ab07fd71c9fc2bff…`) is UNEDITED — verify by sha, it is the first thing to check. `de_admissible_windows.py` byte-identical to `1480ab6`.
coordinator runs at the tip (main tree clean but for BE round 9's `be_forward_day.py` WIP — never read, run or cite that file; nothing here imports it): every count above reproduced on both launchers, rc 0, stderr empty; `--run` → argparse rc 2 (unrecognized); `derived/` 173, `phase4_diag_r459/` absent.

## Items

1. **The declaration against the ruling and the frozen document (addendum §a–§e).** Population = the frozen §3 exactly (471, btc 234 / eth 237, 08-24 + 08-25, `clob_v3_1`, CONSUMED, `is_a_validation=false`, G=0, `DIAGNOSTIC_NEVER_EVIDENCE` at `:37-39`); cells = the PRIMARY cell's fixed settings × the full latency axis (NOT a selection axis, `DE_PHASE4_PROTOCOL_DRAFT.md:101`) × the three budgets (the selection axis, `:103` — all reported, none selected) × both coins; nothing outside the frozen grid, no new number. Read for anything that widens, narrows or renames what R-459 §2 lifted; a cell the addendum declares that the frozen document does not permit is a finding, as is the reverse.
2. **The arm-name resolution `CONDVALUE_OVER_SKEWED_REF`** (addendum `:76-101`; the collision was `de_lane4_real_parity.py:118-141`, DE's ASK A1/A2). Coordinator ruling (R-460 §3): scoped to this diagnostic, the seven-arm vocabulary unamended. Is the name's definition (conditional-value head over the frozen skew-ON reference, no interaction, no fair-price term) the composition that will actually be replayed — i.e. does `run_cell`'s `params` for that arm implement THAT and not `X_SKEW`'s interaction?
3. **`de_rho_estimator.py` — the decision metric, declared then implemented** (`:17-41` the declaration; `classify` `:79-99`; `rho` `:102-152`). Questions of estimand, not style: (a) `spread(f) = size·|px − mid_at_fill|` and `adverse(f) = −size·sign·(mid_markout − px)` over the SAME fills, ratio of sums — is that `rho = adverse / spread` as the frozen `:150` means it? (b) reachability is referenced to `gen_start_ns + L` (`:98`): is generation start the decision time the protocol's latency is measured from, or is it the score event's `t`? Both statuses COUNT (`_COUNTED`), so this labels rather than drops — say whether the label is the right one. (c) `spread == 0` → `None` with a reason; the proxy `harm/sac` carried under its own key, never merged. (d) Falsifiers: the synthetic fill set reproduces rho exactly; an in-window fill is received-and-charged; missing latency refuses; unknown side refuses (`SIDES` imported from the policy, not restated).
4. **`de_score_stream.py` — manifest-bound heads** (`verify_head` `:82-116`, `score_events` `:125-168`, `lift` `:169`). The incumbent `linear_d_{coin}.json` (btc `18701008c2bd18c6`) and the head under test `lgbm_haz_{coin}.txt` + `lgbm_thresholds_{coin}.json`, verified BEFORE load through `fit_manifest.json`; a cross-coin fit REFUSED by the manifest (it loads and scores nonsense otherwise); the permuted-score control (0/200); NaN refused at the row; the output validated by running `validate_scores` over it (rule 16 — this is what caught the `("BUY","SELL")` restatement). Drive the wrong-coin refusal and the permutation yourself.
5. **`de_matched_random_control.py` — the acting control's contract identity** (`stratum` `:58`, `demand_from_treated` `:71`, `draw` `:91`, `refuse_if_not_random` `:120`). Per-stratum count determined by the treated arm (caller-chosen refused); pool totally ordered before the RNG; a short stratum REFUSES rather than clamps; a draw identical to the treated arm refused by identity — and round 32's fix: freedom measured PER STRATUM so a one-member stratum that must reproduce the treated arm is not refused as a copy. Is the per-stratum rule now too permissive anywhere (a pool that is all one-member strata is a forced copy of the treated arm and would pass)?
6. **`de_phase4_protocol_check.py` (10 → 21):** recomputes the frozen sha from the file and refuses an addendum that does not carry it; names R-459; requires the three declarations; an unrelated sha shown not to be a substring hit; and round 32's binding of the RUNNER's grid to the addendum's (`LATENCY_RUNGS_MS`, `BUDGETS`, `PRIMARY`, `ARMS_NOT_RUN`, the new arm name) with a widened-rung known-bad. Drive the widening both ways: a rung added in code only; a rung added in the addendum only.
7. **The runner, `de_phase4_diag_runner.py` (35) — as it stands, against the row's title.** The coordinator's five findings (R-462 §2), each at a line: **DE32-C1** no `--run` (`main` `:578-586`); **DE32-C2** the feed `tranche_table` is named in the docstring and never invoked (only `FILL_HORIZON_S` imported, `:61`); **DE32-C3** `RHO` imported (`:58`) and never called — `run_cell` (`:279-327`) emits `cost_adjusted_value_cents` and `n_cancels` per arm and `net_diff_cents`, never `rho` or `retention_share`, so `evaluate_predicates`' `rho_min` is empty for any cell the runner can produce (the selftest plants `rho=0.8` at `:513-515`); **DE32-C4** the null in `run_cell` (`:313-326`) uses synthetic strata (`SIDES[i % 2]`, `i % 24`), a `treated` set that is the top 10% of a caller-supplied harm dict rather than the treated arm's actions, and values each draw as a HARM SUM — the proxy the frozen §6 forbids the comparison on — instead of replaying the acting control and reading its cost-adjusted value and rho; **DE32-C5** `theta_cancel` 0.8 / `theta_repost` 0.3 / `repost_dwell_s` 2.0 (`:288-290`) are undeclared defaults where the frozen thresholds live in the fits, and `protection_mode` / `repost_fill_model` default to one value (`:293-297`) where the PRIMARY cell is the conjunction over both (`DE_PHASE4_PROTOCOL_DRAFT.md:118-120`). Confirm or contest each; the refusals (`:124-243`) and the receipt's binding fields the coordinator found sound — drive two of the refusals yourself.
8. **The estimand's cap.** The receipt carries `fill_horizon_s = 1.0` from `phase4_generation_tables.FILL_HORIZON_S` and says every cell is "value preventable WITHIN ONE SECOND". Rule on whether a 1-second fill horizon is consistent with the frozen protocol's estimand (or whether the frozen document is silent, in which case the addendum should have declared it — it does not, only the runner's receipt does). A limit stated in a receipt after the declaration is the shape rule 6 exists for.
9. **IR-R4 as narrowed (Q-DE-50).** DE says the "no production consumer" half is closed by the runner and the "built at run time from the archive, refused if absent" half remains. Given item 7 (the feed is not invoked at this tip), say which half is actually closed at `e52d183`.
10. **Counts and discipline.** The counts above, both launchers, zero tracebacks; nothing under `data/` (0 files in the window; `phase4_diag_r459/` absent); no unit/timer/scope; `DA_MIDNIGHT_MODE` never set; the register read-only; the two plan files unedited (sha). DE built in `~/ctaNew-wt-de` and landed in the main tree to run — read the committed blobs, never `~/ctaNew-wt-de`.

## Discipline
Execute at `--detach e52d183` in `~/ctaNew-wt-rev` only; `__pycache__` cleared before every execution; both launchers; streams to separate files; files restored byte-identical; `git status --short` 0 lines after; nothing under `data/` (`derived/` 173 before and after — the runner's own `OUTDIR` check asserts the directory is absent, keep it so); no unit, timer or anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; the main tree's `be_forward_day.py` never read (BE round 9 open). The §3 population is NOT to be replayed for economics in this review — synthetic inputs only, as DE's own selftests do.

## Disposition asked for
RELEASE or HOLD for `e52d183` (covering `d94aa1e`); findings numbered DE31-Rn / DE32-Rn with severities; DE32-C1..C5 each CONFIRMED or CONTESTED; item 8 ruled. One filing (R-377), one pathspec commit under `workspace/reviews/`. DE round 33 (the run path) is in flight in parallel and lands as its own tip — do not wait for it.
