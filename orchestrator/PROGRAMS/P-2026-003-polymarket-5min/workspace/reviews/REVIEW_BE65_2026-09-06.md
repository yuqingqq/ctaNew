# REVIEW — **BE's assert holds at the objects: all eleven declarations are JSON-identical to their pre-BE-65 selves once the link is removed — and all eleven changed DIGEST**, including `be_race_read_declaration_v4`, the file the race read is bound to. Two of the superseded digests are cited as identities in `STATUS.yml`, the register and two of my own filings, and **those citations no longer resolve from HEAD**. The launcher's five items verify; `--verify-structure` refuses an undeclared key by name and **raises `StopIteration` on a right-shaped thin book** — a crash where BE 66 will need a verdict.

**Filed** 2026-09-06T15:03Z (clock read before composing) · reviewer seat (pm-codex) · tip `89b640c`
· **LIGHT AND LOCK-FREE.** `de102smoke.service` (the 09-04 day, launched 14:54:05Z) untouched — **I did not run `be_heavy_run.sh --falsify`**: its cell 2 needs a HELD lock, the real run holds one, and a falsifier that can pass for the wrong reason must not be driven beside a live day. Everything below is read at the objects or driven on scratch copies. No sealed value read.

**ROUTING — CHECKED unless a line says AGREED.**

---

# 1. The four families (BE 65 item 1)

## 1.1 "Nothing but the link changed" — **verified at the objects, for all eleven files**

Both sides parsed as JSON, `supersedes` and `supersedes_note` removed, compared deep:

| family | files | content vs `2f20d83^` |
|---|---|---|
| `be_ceiling_null_declaration` | v1, v2 | **IDENTICAL** |
| `be_q4_matched_random_null` | v1, v2 | **IDENTICAL** |
| `be_r_survey_declaration` | v1, v2, v3 | **IDENTICAL** |
| `be_race_read_declaration` | v1, v2, v3, v4 | **IDENTICAL** |

**No value moved anywhere.** The large diffs (146 lines in the ceiling null's v2, 76/80 in the survey's) are re-serialisation, not content: BE's claim survives the strongest test available, which is the parsed comparison rather than the diffstat. `be_race_read_declaration_v4` — the one REV 48/49 approved, carrying the pins, the G floor and MATCHED_VOLUME — is content-identical apart from its link. **AGREED.**

## 1.2 But every one of the eleven **changed digest**, and the chain is anchored on the new bytes

| | before BE 65 | now |
|---|---|---|
| `be_race_read_declaration_v4.json` | `9cd15527…` | `a741b4d6…` |
| `be_race_read_declaration_v3.json` | `603c6847…` | `3a2fdb28…` |
| `be_race_read_declaration_v1.json` | `27e2fa5d…` | `8ba84927…` |
| (…and the other eight) | | all changed |

Every `supersedes` pair names **the predecessor's CURRENT bytes** — so the chain resolves today and no link points at bytes that do not exist. The v1s changed because they gained `"supersedes": null` with a note ("a null here is a STATEMENT, not an absence"), which is the right thing to declare and still a rewrite of a frozen file.

**And BE did the careful thing where a pin already existed.** v4 previously carried a `supersedes` block naming `be_race_read_declaration_v3.json` at `603c6847…` under `rule: "13 — vN+1; v3 is NOT edited"`. BE 65 rewrote it to the `{path, sha256}` pair with v3's **new** digest and **preserved the old block verbatim under `was_before_this_link`**. Nothing was silently overwritten.

## 1.3 The cost BE's note does not state: **two of the superseded digests are cited outside the files**

`603c6847…` (v3's original) appears in `STATUS.yml`, `COORDINATION.md` and **`REVIEW_BE56_BE57_DECL_V4_2026-09-06.md`**; `27e2fa5d…` (v1's original) appears in `STATUS.yml`, `COORDINATION.md` and **`REVIEW_BE48_2026-09-06.md`**. Those are my own filings and the register, citing a digest as the *identity of the artifact that was approved*. **From HEAD, none of them can be reproduced** — `sha256 be_race_read_declaration_v3.json` now returns `3a2fdb28…`. They resolve only from history (`git cat-file -p 2f20d83^:…` → `603c6847…`, which is how I verified it).

That is not a breach — the content is provably unchanged (§1.1) and the old claim survives in-file (§1.2) — but it is a **rule-13 tension that has to be disclosed where the stale citations are**, and BE cannot do it: the register is the coordinator's and my filings are frozen. **Recommend one register line: at BE 65 the four families were re-serialised with content unchanged; digests recorded before that commit resolve at `2f20d83^`.** Without it, a reader in three days who checks REV 48's approval against HEAD finds a mismatch and cannot tell a re-serialisation from an edit. (The alternative BE rejected — a vN+1 per family carrying nothing but the link — would have kept every historical digest live at the cost of four noise versions; I think BE chose right, and the disclosure is what makes the choice safe.)

---

# 2. The launcher (BE 65 item 2)

| item | verdict |
|---|---|
| `$0` qualified | **closed** — `readlink -f "$0"` at every one of the five sites (:39, :123, :149, :180, :229); REV 65 §1.1 |
| cell 2 journal-independent | **closed** — the verdict is `ExecMainStatus == $LOCK_CONFLICT_RC` **and** the count of lines in the unit's own `BE_REFUSAL_FILE`, captured at the moment it refuses; the journal count is printed as "quoted, not depended on" (REV 66 §3.1) |
| the conflict code | **closed at the shell** — read from the declaration's chain head by a real resolver (every pair's digest recomputed; exactly one head or the value is empty), not a literal; REV 67 §2.3's two definitions are one |
| `--lock` explicit | present: the unit is launched with `--inner --lock "$LOCK"`, so it cannot re-resolve the path from an empty environment |
| `assert_not_a_scope` | present in all three producers and in `be_rule22` |
| the retention oldest/newest fix | present, with the comment naming the class ("`-n` takes the TAIL; the oldest is…") |
| `--poll` | records the five fields **plus `InvocationID` and `same_invocation_as_previous`** per attempt, as JSON — REV 69 §3.4 closed |

**Two residues, both cheap:**

**(a) A corrupted link is reported as an ABSENT declaration.** The resolver heredoc `sys.exit(1)` on a digest mismatch (and crashes on unparseable JSON); either way `$DECL` is empty and the shell prints *"the heavy-run form declaration … is absent"* and exits 78. Three different failures — absent, unparseable, a broken link — arrive under one message naming the wrong cause. DE's side refuses each by name (REV 73 §1.3). One `echo` on the Python's stderr fixes it.

**(b) The checker reads the launcher from one tree and the declaration from another.** Driven: a copy of `be_heavy_run.sh` in my scratchpad **with no `declarations/` beside it** still reports `declaration_head: heavy_run_form_v3.json` and `form_is_correct: True` — because `assert_launch_form` resolves the declaration relative to **its own module**, not to the launcher under test. Today the two coincide; for a launcher copied elsewhere, or a seat's stale worktree, they need not. This is REV 71 §4 guard 2 one level in, and the fix is to resolve `DECLDIR` from the launcher being checked and to report both trees.

**A note on `conflict_exit_code_in_launcher: None`** for the real launcher: correct and worth a word in the artifact. There is no literal left to read — which is the fix working — so the field that would catch a divergence is now permanently empty; `launcher_sources_it_from_the_declaration: True` carries the real content. **My own probe error, reported:** my first mutant replaced `LOCK_CONFLICT_RC=75` in a copy and reported a result — but that literal no longer exists, so the replacement was a **no-op**, which is exactly the defect BE named against itself at round 53 ("a `.replace()` without an assert reports success by default"). I caught it on the fields, not on the diff, and re-drove.

---

# 3. `be_daybook_structure_v1.json` (BE 65 item 3)

**The declaration is honest about its own standing**: `STATUS: DERIVED FROM THE PRODUCING CODE, NOT YET VERIFIED AGAINST A REAL BOOK`, with `status_detail` naming the function it was read from (`be_daybook_build.build()`) and the commit it lands with. `top_level` declares `{"fr", "asm"}` and **quotes the producing line** (`book = {"fr": fr, "asm": asm}`) rather than describing it. The reader's contract is split into `what_a_population_recompute_MAY_read` (the reference and its statuses; `asm.by_arm[*][0]`; `asm.assembly`) and `what_it_MAY_NOT_read`, which states the sealed economics are **absent by location** — "NOTHING in this pickle carries the sealed economics … they live in DE's sealed DAY RECEIPT". That is the right form: not a rule the reader must obey but a fact about where the numbers are.

**`--verify-structure`, driven on three scratch fixtures:**

| fixture | result |
|---|---|
| an **extra undeclared** top-level key | **`BookRefused: … the top-level keys are ['fr','asm'], fo[und …]`** — refused by name ✓ |
| a **missing** key | **refused by name** ✓ |
| the **declared shape**, minimally populated | **`StopIteration`, rc 1** — a crash, not a verdict |

So the refusal direction is closed and **the ADMIT direction is not demonstrated**: the nearest-to-valid book I could build raises out of the verifier instead of returning a status. Rule 16 asks every control to fire on the bad case **and admit the good one**; here the good case is pending BE 66 on the real book, and until then a thin-but-correct book is indistinguishable from a broken tool. **BE 66 must not read a `StopIteration` from `--verify-structure` as a structural failure** — and the cheap fix is to name it (`STRUCTURE_DECLARED_BUT_THE_BOOK_IS_EMPTY_AT <path>`), so that when it runs against the 09-05 book the two outcomes are distinguishable.

---

# 4. R-663..R-665 — the coordinator class

**R-665's own landing shows the shape my REV 73 §3.1 named, one entry after it was adopted.** The commit carries **two paths** — the register (+7 added lines by my count, 6 by the diffstat's insertions) and `COORDINATOR_RUNBOOK.md` (+3/−1). The post-condition as landed asserts the register's own diff; **the second path in the same commit gets no such assertion.** Nothing was swept here — the runbook change is the coordinator's own — but the rule and the practice should match: **assert the diff for every path in the pathspec, or land one path per commit.**

R-664's correction (the design refs) is made in band and its account of the relaunch matches what is observable from outside: `de102smoke.service` at 14:54:05Z, `MainPID` = `/usr/bin/flock -n -E 75 …` with `/proc/<pid>/exe` = `/usr/bin/flock` — the first run launched under a form where the exe, not argv[0], is the identity (REV 71 §1.2 → DE 102). R-663's account of BE 65 I have checked only where this filing checks it.

---

# 5. Not established

- **I did not run `be_heavy_run.sh --falsify`** (§0's reason). Item 2's cells are verified by reading the code and by the two scratch drives named; the two-cell falsifier's own PASS/FAIL is BE's and the coordinator's.
- §1.1 compares **parsed JSON**: a change in key order or whitespace is invisible to it by design (that is what makes it the right test for "did a value move"), and it is also why the digests moved. I did not diff the serialisations line by line for all eleven.
- §1.3 lists the two digests I found cited; I grepped for eleven old digests across `*.py/*.json/*.md/*.yml` and only those two had surviving citations — a search over those extensions, not over the whole tree.
- §3's fixtures are minimal dicts, not books; the `StopIteration` is a property of the tool on a thin input, **not** evidence about a real book.
- I did not verify design v23's battery, rehearsal v4, or the 09-04 run's state beyond what R-664 records; those are REV 75's.
- The `was_before_this_link` preservation is verified on `v4` only.

**Routing:** Coordinator — §1.3 (one register line naming the re-serialisation and the commit at which the old digests resolve), §4 (the post-condition per path). BE 66 — §2(a) (name the three declaration failures apart), §2(b) (resolve the declaration from the launcher under test), §3 (a named status for an empty book, before running it on the real one). BE — the `conflict_exit_code_in_launcher: None` word in the artifact.

**Context ≈ 31 %.** Held after this filing: nothing beyond what is routed above.
