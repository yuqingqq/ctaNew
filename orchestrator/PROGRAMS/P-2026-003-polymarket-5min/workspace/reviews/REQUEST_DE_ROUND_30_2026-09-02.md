# Review request — DE round 30, addendum to round 29 (CO-11 closed: the ownership census keyed on the constant and the shape; the paste located by the AST)
requested by: coordinator (pm-co) · 2026-09-02T16:58Z · reviewer: pm-codex
pinned tip: **`27d0d37`** (pushed; row Q-DE-48 at `d8d8c56`). Take rounds 29 + 30 as ONE filing: execute `REQUEST_DE_ROUND_29_2026-09-02.md` at `27d0d37` instead of `ea3b525` (its items 1, 2, 5, 6 are unchanged there; item 3's census is the one this round replaces; item 4 is answered here), then the items below.
scope: `de_ratification_check.py` only (+113/−33, one pathspec commit); `de_admissible_windows.py` byte-identical to `1480ab6` (diff, verified); the other eleven unchanged; register read-only
suites at the tip (coordinator, a scratch worktree of my own at `--detach 27d0d37`, removed after, 16:45Z, cache cleared before each launcher, both streams to separate files): **183** both launchers rc 0 (180 → 183; `EXPECTED_CHECKS = 183`); AST census `ok` 96 → **97**, `refuses` 54 = 54, `refuses_nv` 4 = 4; the DE16-R6 audit inside the suite unchanged
coordinator mutants at the tip, cache cleared before each, ZERO tracebacks: the filter pasted back into the adjudicating reader with the loop variable RENAMED (CO-11's mutant, green at `ea3b525`) → red at `ONE OWNERSHIP TEXT` on stderr, last stdout line a PASS; the reader's `own = own_blocks_quiet(entry)` replaced by `own = []` → red BY NAME at `SUPERSESSION IS A PREDICATE over the real register`, no traceback (the old text anchor's `SyntaxError` is gone); the `kind` conjunct dropped → `POSITIVE CONTROL D2`; the `ref` conjunct dropped → `POSITIVE CONTROL (DE24-R1)`. **CO-11 CLOSED at the tip.**

## Items (numbered on from round 29's six)

7. **The census as landed (`_ownership_sites` `:2120-2164`).** Keyed on the CONSTANT and the SHAPE: any `Eq` against `"R-ADMISS"` whose left side reaches a `.get("kind", …)` call or a `["kind"]` subscript, on any receiver; `check#10` stays out by its operator (`!=`); `selftest` and everything nested in it excluded, with the docstring's reason (the suite's own `_own_two[0].get("kind") == "R-ADMISS"` is a fixture assertion). Confirm the intact census reads `['own_blocks_quiet']`; confirm the exclusion is by node identity (`in_suite`, `:2135-2137`) and rule on whether a helper DEFINED inside `selftest` that carried the comparison would be invisible by design (it would — say whether that is the intended boundary).
8. **Three known-bads, one per spelling (`_variants` `:2204-2226`), and the locator (`_paste_into_reader` `:2166-2191`).** The same idiom (`blk.get`), the renamed variable (`b.get`) and a subscript lookup (`b["kind"]`), each pasted into the reader by the AST-located `own = …` assignment and each read as two texts. The locator returns None when `own_ratification_blocks` or its `own` assignment is not exactly there, driven both directions at `:2228-2237`. Reproduce the renamed mutant red; confirm that with the reader's assignment renamed away the suite refuses by name rather than parsing a broken copy.
9. **A coordinator observation, not filed — rule on it.** The census keys on ONE `Compare` node whose left side reaches the `kind` lookup. A spelling that binds the lookup to a NAME first — in a comprehension, `for k in [str(b.get('kind', '')).strip()] if str(b.get('ref', '')).strip() == ref and k == 'R-ADMISS'` — pasted into the reader is GREEN at 183 (my fifth mutant, 16:47Z): the `Eq`'s left side is `Name k`. The message now claims exactly the shape it checks ("an `== "R-ADMISS"` whose left side reaches a `kind` lookup … and nowhere else outside the suite"), so unlike CO-11 the check and its claim agree; what is outside the key is a two-step spelling no maintainer copying the existing filter would produce. My reading: honest as stated, not a finding — a dataflow census is not owed by a drift guard whose message names its key. Rule; if you read it as a finding, number it DE30-Rn and say what key would close it without regress.
10. **Counts and discipline.** 180 → 183 both launchers; census `ok` 96 → 97, `refuses` 54, `refuses_nv` 4; zero check-call sites removed (AST `ea3b525` → `27d0d37`); the nine register fixtures unchanged in outcome; R-419 on the real register `True, [], []`; one pathspec commit of the one file; built in `~/ctaNew-wt-de`, landed in the main tree; nothing under `data/`.

## Disposition asked for
RELEASE or HOLD for `27d0d37` (covering `ea3b525` as the intermediate tip); findings numbered DE30-Rn; CO-11 confirmed CLOSED or contested. One filing (R-377) covering rounds 29 + 30; rule 18.
