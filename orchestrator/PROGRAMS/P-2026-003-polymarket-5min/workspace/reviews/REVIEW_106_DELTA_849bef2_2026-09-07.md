# REVIEW 106 — DELTA GATE: `850c166` → the seventh head (`+ 849bef2`, the USER's fix)

**REV, 2026-09-07T17:08Z.** Read-only; no lock, no heavy unit, nothing written under
`data/`. The seventh head does not exist yet, so I built it in a scratch clone —
`850c166` with `de_multiday_gate1_runner.py` and `de_decision_ledger.py` taken from
`849bef2` — and drove that.

**VERDICT: NO-GO ON ONE ARTIFACT — `live/pm_research/de_multiday_gate1_runner.py`, the new
KNOWN-BAD cell beside the cascade positive control (`:8082–8088`). It does not fire on the
composition; it FAILS. The battery goes red at check 8 of 395, and the battery runs IN-RUN
at stage `S0b_battery`, so every one of R1..R4 would die in its first minutes.**
Everything else in the delta is sound and strictly strengthens the gate — including the
`files` question the chain was stopped on, which is confirmed in §1.

**ON THE COORDINATOR'S RULING (the conditional skip): NOT WRONG, BUT WEAKER THAN
NECESSARY, AND I HAVE DRIVEN THE ALTERNATIVE.** A known-bad that constructs its own bad
input instead of reading the tree's state gives **PASS 395 / 0 disarmed / 0 skipped in
BOTH trees** — phase4 at the pin AND phase4 moved — with no conditional, no skip and the
same count. §4b gates the ruling as asked and §4a gives the alternative. Either way,
**GO the moment that one cell is settled; nothing else in the delta blocks.**

---

## (1) THE COORDINATOR'S REASON FOR STOPPING THE CHAIN — CONFIRMED, with one refinement

**Confirmed: an artifact produced under `850c166` could never satisfy the new gate.**
Measured at `850c166`:

- `chainlink_streams(...)` **does** return `files` — keys `files, files_digest, hours,
  n_hourly_files, narrowed_by, reader_module`. The producer has the list.
- `verify_winners_against_chainlink(...)`'s `stream_provenance` sub-block — **the block
  that lands in the artifact** — carries only `files_digest, hours, n_hourly_files,
  reader_module`. **`files` is dropped on the way through.**
- `849bef2` adds `"files": (stream_provenance or {}).get("files")` to that sub-block, and
  `chainlink_stream_provenance_check` requires `files_list_present` before
  `f_provenance_complete`, hence before `is_final`.

So the refinement is only in where the gap was: not "850c166 did not record `files`" but
"850c166's verification block did not carry them through". The consequence is exactly as
you read it — **a run under `850c166` records a verification that the new checker can
never pass, while `850c166`'s own weaker gate would have stamped it
`is_final_for_quotation: True`. Stopping four hours of runs was right**: the alternative
was four artifacts whose finality verdict the next commit would withdraw.

**And the new check is real, not shape-checking.** Driven on a fixture root with honest
files: a **forged `files_digest`** and a **file that is not there** each refuse
`SETTLEMENT_VERIFICATION_PROVENANCE_INCOMPLETE`. It re-reads the named files from
`<data_root>/pm_5min/prices/<topic>/<name>`, rejects absolute paths, `..`, wrong arity and
unknown topics, recomputes the digest over name+bytes, and compares the reader module's
sha to the file on disk **now**.

## (2) THE LEDGER READER — NOTHING FALLS BACK TO D_E0 SILENTLY

`recompute` now reports `primary_result_field` and `primary_result_cents` explicitly:

- SETTLEMENT rows present → primary is **`D_E_settle`**, and `D_E0_role` reads
  `DIAGNOSTIC_ONLY_SETTLEMENT_ROWS_PRESENT`.
- The arm has no `SETTLEMENT_SCALARS` row while the ledger has settlement rows →
  **REFUSES `DECISION_LEDGER_SETTLEMENT_SCALARS_ABSENT_FOR_ARM`** ("cannot infer the ruled
  endpoint from another arm or from the 5-second D_E0").
- The row is there but `D_E_settle` is null → **REFUSES
  `DECISION_LEDGER_SETTLEMENT_VALUE_ABSENT_FOR_ARM`**.
- No settlement rows at all (the landed v2 ledgers) → primary is `D_E0` with
  `D_E0_role: PRIMARY_WHEN_NO_SETTLEMENT`. **That is the only fallback, it is correct for
  pre-endpoint ledgers, and it is NAMED in the output rather than implied.**

The reader also indexes `settlement_by_arm` and returns the per-slug legs, so a reader
holding the ledger re-forms the ruled P&L without the receipt. **Answer to the question as
asked: no, nothing falls back to the 5-second D_E0 as a day's ruled result.**

## (3) THE PLACEMENT-LATENCY WALK — the leaf rule is right, and neither refusal can fire on tonight's four

The walk no longer records a `placement_latency` **block** as if it were a value: it
records leaves whose key names a placement latency, plus `L_place_ms` /
`placement_latency_ms` leaves **under** a placement-latency parent. That is the correct
shape — phase4's own block would otherwise have offered a dict where a number belongs, and
`proposed_in_the_draft_ms: 250.0` sitting beside `placement_latency_ms: 0.0` would have
read as two values. It ignores the proposal and the default and takes the one live value.

`850c166` returned `L_place_ms: None, ambiguous: True` for both bad shapes; `849bef2`
**REFUSES** — `SETTLEMENT_BOOK_PLACEMENT_LATENCY_MALFORMED` (non-numeric) and
`_AMBIGUOUS` (not exactly one value). Right direction: a day quoted at an L nobody can
parse is worse than no day.

**Pre-flighted against the four real books' builder receipts** so a run does not discover
it: 09-03, 09-04, 09-05, 09-06 all return `L_place_ms 0.0`, `source THE BOOK'S BUILDER
RECEIPT DECLARES NONE`, `found_at {}`. **Neither new refusal can fire on tonight's four.**

## (4) EXPECTED_CHECKS 394 → 395 — THE NEW CELL IS THE ONE THAT FAILS

The new cell is, beside the cascade positive control:

```python
refuses(lambda: verify_be_module(P),
        "KNOWN-BAD: the frozen params refuse on the moved phase4 cascade module …",
        "BE_CASCADE_DIFFERS")
```

with the positive control changed to `verify_be_module(_with_current_cascade(P))`.

**It asserts a property of the TREE, and the two trees this code legitimately runs in
disagree.** On mm-research phase4 has moved (`52e76689…`), so the frozen params refuse and
the cell fires. **On the composition phase4 is held at v19's pin (`ee4034c1…`) — which is
the property REVIEW 105 cleared and the property the runs need — so `verify_be_module(P)`
ADMITS and the cell FAILS.** Driven on the assembled seventh head:

```
[de_multiday_gate1_runner] FAIL: KNOWN-BAD: the frozen params refuse on the moved
phase4 cascade module … -- ADMITTED          (at check 8 of 395)
```

This is the recurring class — **a literal that must track a moving thing** — one level up:
the cell's known-bad is the tree's state rather than something it constructs.

## (4a) THE ALTERNATIVE I RECOMMEND — one cell, no conditional, driven in both trees

Build the known-bad from mutated params, exactly as the neighbouring `actual_sha="0"*64`
cell already does:

```python
def _with_moved_cascade(params):
    out = json.loads(json.dumps(params))
    for _m in (out.get("be_cascade") or {}).get("modules") or []:
        if _m["path"].endswith("de_phase4_diag_runner.py"):
            _m["sha256"] = "0" * 64
    return out
refuses(lambda: verify_be_module(_with_moved_cascade(P)), …, "BE_CASCADE_DIFFERS")
```

**Driven, both trees, same cell, same count:**

| tree | phase4 | battery |
|---|---|---|
| the composition (`850c166` + `849bef2`'s two files) | `ee4034c1…` — at v19's pin | **PASS 395 / 0 disarmed / 0 skipped** |
| the same, phase4 taken from mm-research | `52e76689…` — moved | **PASS 395 / 0 disarmed / 0 skipped** |

It fires on both, which is what a known-bad is for, and it needs no conditional to be
counted.

**Do NOT take the other route.** The battery would also go green if the composition took
mm-research's phase4 — but that breaks the cascade pin REVIEW 105 cleared, changes the
null producer the earlier runs used (phase4 is one of the ten cascade modules, on the draw
path), and makes the four re-runs not like-for-like with the artifacts they supersede.
Hold phase4 at the pin and fix the cell.

## (4b) THE COORDINATOR'S RULING, GATED ON (a)–(d)

The ruling — fire when phase4 differs from v19's declared digest, SKIP BY NAME as a
declared conditional when it equals the pin — **is sound and it unblocks tonight.** I do
not say NO-GO on it. Gated as asked, it must have all four properties:

- **(a) the predicate.** It must be exactly *the sha256 of
  `live/pm_research/de_phase4_diag_runner.py` on disk ≠ the `sha256` this params file
  declares for that path in `be_cascade.modules`* — **read from the params, computed from
  the file, with no environment variable, no flag, no `try/except`, and no fallback that
  turns an unreadable file into a skip.** Anything else is a second escape.
- **(b) it must still fire where phase4 has moved.** Drive it there, not only at the pin —
  the whole failure this round was a cell measured in one tree and run in the other.
- **(c) the skip must be visible as a CONDITIONAL in the count**, the way phase4's own
  battery already reports `214 run + 4 conditional = 218` — never folded into the passes.
  A skipped control counted as a pass is the shape rule 15 exists to forbid, and the
  status must say what the ruling says: *the control cannot fire while the cascade is at
  its declared pin, and this is not a pass.*
- **(d) nothing else in `849bef2` depends on the moved cascade.** Checked: the other three
  touched sites (`verify_be_module(_with_current_cascade(P))`,
  `verify_run_inputs(_with_current_cascade(live))`, `import_be_cascade(_with_current_
  cascade(live), …)`) all RE-POINT the pins to the files on disk, so they pass in either
  tree; and the four real books' builder receipts return `L_place_ms 0.0` in either tree.
  **No other cell reads the tree's cascade state.**

**WHY I STILL PREFER §4a.** Under the ruling, on the composition — *the tree that actually
runs* — the known-bad is skipped and the positive control is
`verify_be_module(_with_current_cascade(P))`, which re-hashes the files it is about to
check and therefore cannot fail. **Both cascade cells are then inert on the tree that
runs.** §4a leaves a control that fires there. The two are also complementary: keep the
conditional if you want the tip's state recorded, and add the constructed known-bad so
something fires at the pin.

**AND ONE THING THE RULING'S SECOND MEASUREMENT MAKES ME REPORT.** In the composed bytes I
can find **no production caller of `verify_run_inputs`** — `verify_be_module` is called in
production only from `import_be_cascade`, and there with `actual_sha` set, which SKIPS the
ten-module cascade block entirely; v19's `be_module` names the cascade's ENTRY POINT
(`be_cancel_axis_null.py`), not phase4. So by my reading a moved phase4 would not be
refused by any production path in these bytes — with `_with_current_cascade` in place it
would not be refused by the battery either. **I could not reproduce "run_day's own
preflight then refuses on the frozen params"; if DE has the transcript, it names a guard I
did not find and I would like to see it.** The conclusion is unchanged and, if anything,
firmer: **hold phase4 at the pin — because the four re-runs must execute the same cascade
as the artifacts they supersede, which is a comparability requirement the composition
enforces and no guard does.**

## (5) DOES THE USER'S COMMIT WEAKEN OR DUPLICATE ANYTHING REVIEW 105 CLEARED?

**It FIXES the one seam REVIEW 105 routed.** §1 of that filing said
`chainlink_verification.status` was only ever upgraded by finality, so a 287-of-288 case
read `VERIFIED_AGAINST_CHAINLINK` beside `is_final_for_quotation: False`. `849bef2` sets
the status in both directions; driven on the seventh head, that case now reads
**`VERIFICATION_NOT_FINAL`**. Routed item closed.

**My six USER-gate cases all still refuse BY NAME on the seventh head**, and two more join
them:

| case | seventh head |
|---|---|
| the USER's reproduction (empty `per_slug`) | `SETTLEMENT_VERIFICATION_EMPTY` |
| 287 of 288, all agreeing | `SETTLEMENT_VERIFICATION_SLUG_SET_MISMATCH` |
| …without `require_verified` | no refusal — **`VERIFICATION_NOT_FINAL`, `is_final` False** |
| one DISAGREE of 288 | `SETTLEMENT_WINNER_DISAGREES_WITH_CHAINLINK` |
| a convention that is not the pinned one | `SETTLEMENT_CONVENTION_NOT_THE_PINNED_ONE` |
| zero hourly files | `SETTLEMENT_CHAINLINK_NO_FILES_READ` |
| **a forged `files_digest`** | **`SETTLEMENT_VERIFICATION_PROVENANCE_INCOMPLETE`** |
| **a named file that is not there** | **`SETTLEMENT_VERIFICATION_PROVENANCE_INCOMPLETE`** |
| 288/288 with honest provenance | no refusal, **`is_final_for_quotation` True** |

And the real days re-read their own files: **09-04, 09-05, 09-06 each 288/288, files count
✓, digest ✓, reader sha ✓, `is_final` True.**

**Three loosenings, all deliberate, none blocking, one worth a routed line:**

1. **The cascade positive control is now tautological.** `verify_be_module(_with_current_
   cascade(P))` re-hashes the files it is about to check, so it cannot fail. Paired with a
   known-bad that does not fire on a pinned tree, **neither cascade cell tests anything on
   the composition.** The `actual_sha="0"*64` cell still fires, so the mechanism stays
   falsifiable — but the ten declared pins are no longer exercised there. My fix in (4)
   restores the pair: keep `verify_be_module(P)` as the positive control (it passes on a
   pinned tree, which is the tree that runs) and construct the known-bad.
2. **The R-648 unit-outcome cell** dropped `ExecMainStatus == "0"` and `Result ==
   "success"` for `the_triple[0] != "loaded"`. That is a portability fix with its reason
   stated in the message (this host returns None where others return systemd's defaults),
   and the property it establishes — unreadable ⇒ VOID — is unchanged.
3. **The flock cell** now retries for 5 s instead of failing on the first
   `BlockingIOError`. A flake fix; the cell observes the wrapper rather than detecting
   contention.

## (6) WHAT STANDS FROM REVIEW 105

Everything, except where this delta touches it. Unchanged and still true of the seventh
head: phase4 at the pin, ten cascade pins 0 mismatched, rule 11 (09-03..06 DESIGN_DATA,
09-07 NOT_ADMISSIBLE), the supersession targets, **`--supersedes` is not optional**, DA
must verify with the LANDED verifier (DA 131), any reader of the new ledgers must know
schema 3, and **09-03 has 287 markets with one DISAGREE at 21:05 UTC, so it will not be
quotable as final.**

## (7) ROUTED

1. **DE / the composition** — settle the one cell before the seventh head is cut
   (§4a preferred, §4b's four properties if the conditional is kept), then re-run the
   battery **on BOTH trees**: this defect is invisible on the tip, and its mirror image
   would be invisible at the pin.
2. **DE / coordinator** — restore an in-run control that reads the DECLARED cascade
   digests on the tree that runs; after `_with_current_cascade` nothing does (§4b).
3. **Standing** — a KNOWN-BAD must construct its own bad input. Two of the three defects
   this programme has hit today are a check whose subject was the tree it happened to run
   in.
