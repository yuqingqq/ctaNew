# REVIEW — **three porcelain parsers, driven side by side, DISAGREE on 4 of 12 lines** — so R-235's argument is settled by measurement and my own REV 66 remedy was right about the class and **wrong about the direction**; the census "fails the moment either starts deriving a path" passes on **4 of 9** deriving shapes; and MEM 175's discriminator **does not exist in the programme's ruled launch form** — I measured a refusal and a crash with identical `ExecMainStatus=1`

**Filed** 2026-09-06T13:28Z (clock read before composing) · reviewer seat (pm-codex)
· tip `ee9d8c1` (BE 64a `5f5f76c`, MEM 175 `7864cb8`, rule texts `689c911` / `63e601a`, R-641 `7a2ee28`, R-642 `a7dfc94`)
· **LIGHT AND LOCK-FREE.** `de95smoke.service` was active before and after every drive (MainPID 3384217, `ActiveState=active` at 13:28:51Z); every unit I launched was a `rev67*` scratch name against a **scratch lock in my scratchpad**; the real heavy lock was never taken, `wt-de2` never touched.

**ROUTING — CHECKED unless a line says AGREED.**

---

# 0. The two corrections I owe first

**(0.1) My REV 66 §1.1 remedy — "the durable fix is the one DA has already built: import `parse_porcelain`" — was right about the class and wrong about which implementation to standardise on.** I drove all three implementations against one battery (§1.2). BE's new parser is the **only one of the three that is right on all twelve lines**. Had BE followed my recommendation instead of writing its own, it would have imported two defects DA's parser has and BE's does not. **R-641(B) routes DE 96 to do exactly that, and DE 96 has not landed** — this is the round to change the instruction, not after.

**(0.2) My own probe was wrong twice this round and I caught both before a claim** (`ls /proc/<pid>/task/<pid>/children` handed to `awk` as a path, and `journalctl -n 1 --reverse` read as the *oldest* entry when it is the newest). Both re-driven; the numbers below are from the corrected probes. Recording it because the harvest says to suspect the probe first, and twice in one round is the rate, not the exception.

---

# 1. BE 64a (`5f5f76c`) — the slice half, the second parser, the census

## 1.1 The slice half: **VERIFIED, and better than the item asked for**

`be_rule22.parse_porcelain_line` (L96–128) no longer assumes the offset. Driven at the tip:

| line | returns |
|---|---|
| `" M live/pm_research/x.py"` | `(' M', 'live/pm_research/x.py')` |
| `"?? data"` | `('??', 'data')` |
| `"R  a -> b"` | `('R ', 'b')` — **the rename line R-641 named** |
| `"M live/pm_research/x.py"` (the strip half's output) | `PorcelainMalformed`, **refused by name** |
| `'RM "odd name.py"'` | path `odd name.py` |

The red case is the point: the shifted line is **refused**, not sliced one character short. `Capture.classify_dirt` (L306, the real identity — R-641(B) names it correctly) consumes it, so BE's half of REV 66 §1.1 is closed at the artifact, not at the report. **The 85 checks run under my drive.**

## 1.2 R-235 and the second parser — the argument does NOT close, and the driven rename result is not what closes it

The coordinator asks whether my "a porcelain parser is infrastructure, not a statistic" binds here, or whether BE's correct rename result closes it. **Neither.** It is settled by measurement. The programme has **three** implementations — `be_rule22.parse_porcelain_line`, `da_root.parse_porcelain`, `ev_replay_seam.parse_porcelain` — and I drove all three on one twelve-line battery:

| line | BE | DA | EV |
|---|---|---|---|
| the three falsifier lines | ✅ ✅ ✅ | ✅ ✅ ✅ | ✅ ✅ ✅ |
| `"M live/x.py"` (shifted) | **REFUSED by name** | `malformed` (reported) | **`'ive/x.py'` — silently corrupted** |
| `"## mm-research...origin/…"` (`status -sb`) | **REFUSED by name** | path = the branch line | path = the branch line |
| `"?? a -> b"` (a file *named* `a -> b`) | `'a -> b'` | **`'b'`** | `'a -> b'` |
| `"?? trailing "` | `'trailing '` | `'trailing '` | **`'trailing'`** |

**4 of 12 lines: the three disagree.** Each is wrong somewhere and no two are wrong in the same place — the exact shape of "two implementations corroborate nothing". So the class argument **holds and is now measured**, and it points the other way from my REV 66 remedy:

- **DA's** splits on `" -> "` regardless of the status code, so an untracked or modified file whose *name* contains ` -> ` is truncated to its tail; and it accepts a `--branch` header as a path.
- **EV's** has no shape check at all: handed a shifted line it returns `ive/…`, **the very defect its own docstring says it fixed**, and its known-bad (L1301–1307) only asserts that stripping the input *changes* the answer — it proves the fixed direction and never the residual (rule 16's shape).
- **BE's** is the only one that refuses both malformed shapes by name and splits renames only for `R`/`C`.

**Regime, stated:** no caller passes `-b`/`-sb` today (checked: the only `-b` in the tree is `git init -b main`), no tracked path contains ` -> ` or a trailing space, and EV's caller reads raw (`strip=False`). **So none of the four divergences is live today** — this is a measurement of drift, not an outage.

**Recommendation (a ruling for the coordinator, not mine to make): one parser, and it should be BE's shape** — shape-checked, alphabet-validated, refuses by name, rename split gated on the code — with DA's row structure (`malformed` reported rather than raised) if callers prefer statuses to exceptions. **Whatever is chosen, DE 96 must not be told to import the weaker one.** Owner: coordinator; then DE 96, BE 65, and whoever owns `ev_replay_seam`.

## 1.3 The census — its claim is stronger than its artifact

The commit says the AST check "fails the moment either starts deriving one". I lifted the check verbatim and drove it against nine synthetic readers that all derive a path:

| shape | census |
|---|---|
| `out = git(…"--porcelain"); p = out[3:]` | **FAIL** (caught) |
| `out = …; lines = out.splitlines(); p = lines[0][3:]` | **FAIL** (caught) |
| `for line in git(…"--porcelain").splitlines(): p = line[3:]` | **PASS** — no `Assign`, and the split's value is a `Call`, not a `Name` |
| `F = "--porcelain"; out = git("status", F); p = …[3:]` | **PASS** — the constant is still counted, `out` is not bound |
| `ARGS = ["status"] + ["--porcelain"]; out = git(*ARGS); p = out[3:]` | **PASS** |
| `p = re.findall(r"^..\s(.*)$", out, re.M)` | **PASS** — only subscript/split/partition/splitlines count |
| `--porcelain=v1` / `--short` / a wrapper in another module | **FAIL**, fail-closed — but because the *flag* is not found, not because a path is derived |

**2 of 9 caught, 3 fail closed for the wrong reason, 4 pass while deriving a path.** The check is a true statement about *today's* two files and a weak regression guard; the sentence in the commit and in the round's row should say which. The cheap hardening is one line: resolve names transitively (any `Name` assigned from an expression containing a bound name) and treat a `Call` receiver as bound.

**One more thing the message says that the code does not:** it prints "N porcelain call(s)". For `be_forward_day.py` **N = 3, and two of them are `git worktree list --porcelain`** (L4116, L4124) — a completely different output format. The count is of a string constant, not of porcelain-status readers. Owner: BE 65 (cosmetic; the audit's scope is unaffected).

## 1.4 **Can a reader that never slices still be wrong? YES — one of the two, and it is emitted**

This is the sharpest half of the item, and the census cannot see it because it is not R-637's defect.

- **`be_forward_recon.py:307` — SAFE, but for a reason the census does not give.** `bool(git("status","--porcelain","--", rel))` is **pathspec-scoped to one file**. Driven in three trees: `False` everywhere. It is the pathspec that saves it, not the boolean.
- **`be_forward_day.py:281–286` — WRONG NOW.** `status = git("status","--porcelain")` over the whole tree, `working_tree_dirty = bool(status.strip())`. Driven at 13:20Z: in `wt-rev` and `wt-be` the **entire** porcelain output is `?? data` — the R-553 ledger symlink — so **`working_tree_dirty` is `True` in every seat worktree, structurally, with no dirt.** It is emitted at **L2180 under `"producing_code"`**, and it names no paths at all — weaker than the daybook receipt's `dirty_paths` that REV 63 §2 already flagged and BE 62 closed with `dirty_code`/`exempt_entries`.
- **Why nobody has seen it:** the provenance cells (L4014–4075) drive `_provenance` against a **`git worktree add` scratch tree**, which has no ledger symlink. The control runs in the one tree shape where the ambient is absent — REV 53 §0's diagnosis one level down ("it can only fail in the configuration nobody tests").

**Owner: BE 65.** The fix is the one BE already built: `classify_dirt` over the porcelain rows, `dirty_code` beside `dirty`, the exempt entries named. **Not a blocker for the 09-05 book** — `be_forward_day` is not on the book path.

---

# 2. MEM 175's measurement — **who reads the state**

## 2.1 The measurement reproduces; the sentence generalises one step too far

MEM 175: *"at the unit level a refusal and a failure are the same state; the only discriminator is `ExecMainStatus`, which is what `flock -E 75` exists for."* **CHECKED and true of the unit MEM measured.** Driven on three scratch units against a scratch lock at 13:22Z:

| unit | form | lock | ExecMainStatus | Result | ActiveState |
|---|---|---|---|---|---|
| `rev67de` | **the ruled form** (`-- flock -n <lock> cmd`) | **HELD** | **1** | exit-code | failed |
| `rev67crash` | the ruled form | free, payload exits 1 | **1** | exit-code | failed |
| `rev67be` | BE's launcher form (`flock -n -E 75`) | HELD | **75** | exit-code | failed |

**A refusal and a genuine failure are byte-identical in every unit property under the programme's own ruled launch form.** The discriminator exists only where `-E` was passed, and rule 20's published command does not pass it. Their journals differ only in the `Started …` line's command text — and rule 20 as amended now forbids a verdict that depends on journal retention, so **under the ruled form there is no rule-20-legal discriminator at all.**

**And in the other direction, measured on the live run at 13:28:51Z:** `de95smoke.service` is `ActiveState=active` with **`ExecMainStatus=0`** — a *running* unit reports the same status as a completed success. `ExecMainStatus` alone answers neither question; the pair (`ActiveState`, `ExecMainStatus`) is the minimum.

**Owner: DE (with the 09-04 smoke), coordinator for the GO text.** One flag — `-E 75` in `THE_ONE_COMMAND` — and DE's refusals become distinguishable from DE's crashes. This is live for the next launch, not hypothetical: if the lock is held at GO #5, the unit exits 1 and so does a runner that raises.

## 2.2 Every reader of a unit's outcome, and what it can tell

| reader | reads | 75 vs failure | rule 20 (journal) |
|---|---|---|---|
| `be_heavy_run.sh --inner` (L41–47) | `$?` vs `$LOCK_CONFLICT_RC` | **by code** (it *produces* it) | writes the line; reads none |
| `be_heavy_run.sh --falsify` cell 2 (L89–91) | `ExecMainStatus` == `"75"` **AND** a journal grep ≥ 1 | by code, **only 75** — a DE-form refusal (1) reads FAIL | **verdict depends on retention — against rule 20** (REV 66 §3.1, still open, BE 65) |
| `de_launch_form_probe.py` leg 3 (L170–186, 253) | `exited_nonzero = status not in ("","0")` **+ a file the payload would have created** | **no code discrimination — deliberately**; it distinguishes *ran* from *did-not-run*, which is the property it needs | journal kept as a 3-line `journal_tail` **evidence** field only — **safe, and the only reader that rejected the journal-marker approach on the record** |
| `de_multiday_gate1_runner.py:4988` (battery comment) | declares "a HELD lock → `ExecMainStatus` **1**" | asserts the other literal | — |
| `de_multiday_design_declaration.py:1863,1879` | "driven: status **1**, Result exit-code" | asserts the other literal | — |
| **SEAT_PROTOCOL rule 20** (L128, L139) | "the lock REFUSES (exit **1**…)" | asserts the other literal, **for all seats** | — |
| `be_daybook_build.py:1541` | `_lfB["conflict_exit_code"] == 75` | see §2.3 — **it does not read the launcher** | — |
| `ops/pm_lane_health.py:132–153` | ActiveState/SubState/Result/ExecMainStatus/NRestarts | records `exit_status`; classifies on ActiveState | — |
| `o1_boundary_preflight.py:80`, `v5_boundary_preflight.py:149`, `pm_shadow_observer.py:411`, `scripts/resource_monitor.sh:28` | `is-active` only | **not at all** — a refusal, a crash and a finished run are all "not active" | — |
| DA (`da_process_budget_audit`, DA 83's polling) | `/proc/locks`, cgroups — **no unit-outcome reader anywhere** | n/a — a seat that polls the LOCK never needs the discriminator | — |

**The seam:** three artifacts state the conflict code as **1** (rule 20 twice, DE's declaration, DE's runner comment) and four state it as **75** (§2.3). Both are true of their own launch form; **rule 20 states one of them as the fact for every seat**, and BE's units are the ones that emit the other.

## 2.3 Where 75 lives — **two definitions and two bare literals, across two languages, and the check cannot see the one that runs**

| site | what it is |
|---|---|
| `be_heavy_run.sh:36` `LOCK_CONFLICT_RC=75` | **the value the running unit actually uses** (L41) |
| `be_rule22.py:134` `LOCK_CONFLICT_RC = 75` | a second, independent definition |
| `be_rule22.py:254` `"conflict_exit_code": LOCK_CONFLICT_RC` | publishes the **Python** one into receipts |
| `be_heavy_run.sh:91` `[ "$RC2" = "75" ]` | a bare literal in the falsifier, not `$LOCK_CONFLICT_RC` |
| `be_daybook_build.py:1541` `… == 75` | a bare literal in a check |

**Driven** (`assert_launch_form` handed a copy of the launcher whose conflict code is **76**, everything else identical):

```
mutant: form_is_correct=True  conflict_exit_code=75
the shell now says: LOCK_CONFLICT_RC=76
be_daybook_build.py:1541 asserts == 75 -> True on a launcher that refuses with 76
```

`assert_launch_form` **reads the launcher's source** for eight tokens and then returns the *Python* constant as `conflict_exit_code`. So the check's own sentence — "THE BOOK BUILDER'S LAUNCHER IS THE SERVICE FORM TOO, with a distinct lock-conflict exit code (75)" — attributes to the launcher a number the launcher was never read for, and a receipt would publish 75 for a unit that emits 76. **This is BE's own recurring class, in the instrument built to close it.** Owner: BE 65 — parse `LOCK_CONFLICT_RC=` out of the launcher and assert the Python constant equals it; make L91 use `$LOCK_CONFLICT_RC`.

---

# 3. The two rule texts as artifacts — satisfying the words without the property

## 3.1 SEAT_PROTOCOL rule 20's last paragraph (`689c911`)

> *"A number read from the journal is copied into an artifact at the moment it is read, with the source's retention state named; a receipt or record carries its own journal lines at emit; no control's verdict may depend on journal retention."*

**(a) "with the source's retention state named" is satisfiable by a typed string.** Nothing requires the retention state to be *measured*, or to carry its own as-of — and it moves faster than the artifacts that quote it. Three independent reads of the same quantity:

| read at | oldest user-journal entry | window |
|---|---|---|
| DA 84, 12:44Z | 08:45Z | ~3 h 59 m |
| MEM 175, ~13:08Z | 09:00:20Z | ~4 h 08 m |
| me, 13:26Z | **09:15:13Z** | ~4 h 11 m |

The window's *start* advanced ~15 minutes in the 18 minutes between MEM's read and mine. **A retention state named once and re-quoted an hour later is stale.** MEM 175 satisfies the stronger form (it names 13:05:10Z and 13:08:25Z as the read moments); **the rule does not require what MEM did.** Fix: the retention state is a measurement with its own as-of and the query that produced it.

**(b) "carries its own journal lines" — the unit name is not an identity.** `journalctl --user -u be64book` returned **23 `Started` lines at 13:26:45Z** (MEM recorded five at 13:10:39Z — accurate then, and the number moves every poll). A record quoting "be64book's journal line" without an invocation id names 23 runs. The discriminator exists and `systemd-run` prints it at launch: **`InvocationID`** (`be64book` currently `4829ed38…`), and `journalctl --user _SYSTEMD_INVOCATION_ID=<id>` scopes to exactly one run. **Recommendation: a receipt that quotes journal lines names the invocation id it filtered on, and a check compares it to the unit's `InvocationID`.** Owner: DE 96/97, BE 65, DA 86.

**(c) Two facts for the run in flight, measured at 13:26–13:28Z.** `de84smoke.scope` is down to **one** line and it is the **`Consumed`** line (09:46:29Z, `1h 24min 20.439s CPU, 2.3G memory peak`) — R-641's statement is **confirmed at the artifact**. The protection is not that `Consumed` lines are durable; it is that the *oldest* line goes first, so **a run's launch stamp is always the first evidence to be lost**. And `de95smoke.service` holds **exactly one journal line at 13:26:45Z — its `Started` line.** At the observed drift it survives a few more hours; **the receipt due ≈14:00Z must copy it at emit, or the launch record of the sealed day is gone.** That is REV 68's first check.

## 3.2 RUNBOOK §6's PPid test (`63e601a`)

> *"The test is the payload's PPid, measured never argued: under a transient service it is `systemd --user` (pid 1004 here); under a `--scope` it is the harness (`claude`)."*

**The property is right; the words name the wrong object.** Measured on the live run:

```
de95smoke.service  MainPID 3384217 = /usr/bin/flock -n …    PPid 1004      cgroup leaf: de95smoke.service
                   child   3384218 = …python3 …runner.py    PPid 3384217
```

`flock` is the unit's ExecStart, so **the payload is a grandchild: its PPid is `flock`, not 1004.** A seat applying the test as written to the run's python process reads 3384217 and concludes the form is wrong. The test holds of `MainPID`, which is what R-634 actually measured.

**And the unambiguous test needs no parent at all.** Driven, from inside:

```
scope  : 0::/user.slice/user-1001.slice/user@1001.service/research.slice/rev67scope.scope
service: 0::/user.slice/user-1001.slice/user@1001.service/research.slice/rev67svc.service
```

The **cgroup leaf's suffix** decides it, from the process's own `/proc/self/cgroup`, with no parent, no journal, no ambient, and no literal that moves — which is the runtime guard already open as **BE 65 / DE 97 (REV 65 §1.2, REV 62 §3)**. Two other ways the words can be satisfied without the property: **pid 1004 is a literal** (a user-manager restart makes it name someone else — "here" carries the caveat, a check would not), and a `--scope` launched from a detached or `setsid` process has neither `claude` nor 1004 as the payload's parent. **Recommendation: §6 keeps the PPid test as the *outside* diagnostic, naming `MainPID` explicitly, and cites the cgroup leaf as the decidable property.** Owner: coordinator (§6 text); BE 65 / DE 97 (the guard).

---

# 4. R-641 and R-642 — the coordinator class

**(4.1) R-641's headline contradicts R-641's own body, in band.** The headline: *"DE's and BE's remaining `[3:]` halves pass its first two and FAIL THE RENAME"* — present tense, about the code. The body, four lines down: *"BE's half of §1.1 is closed at BE 64a, which landed after the reviewer's tip; DE's `[3:]` remains for DE 96."* BE 64a landed 13:03:36Z; R-641 is stamped 13:06Z and the coordinator had already driven the fix. **A reader who scans headlines carries "BE fails the rename" as current, and it is false at the entry's own moment.** The correction is one clause — *"at the reviewer's tip `d4c190b`; BE's half closed at BE 64a"* — and this is the class the harvest names for this seat: the headline is looser than the artifact it names. Everything else in R-641 that I can check is **exact**: `classify_dirt` is the real identity, the three-line return values reproduce, the seven journal readers enumerate, and the `Consumed`-line claim is confirmed at the journal (§3.1c).

**(4.2) R-642(A)'s account of my stop is accurate wherever I can check it**, and I say plainly where I cannot. Checkable and correct: the brief was sent at tip `7864cb8`; my confirmation was made from the files after the refresh script; the three deltas are the three I reported (the filing list one short, DA no longer nothing-open, REV 63 §4 already said at R-640(A) §5); the sealed-receipt rules were restated unprompted. **Not checkable by me:** everything before the clear — "wt-rev clean at `d4c190b`", the four answers at 13:06Z, the scrollback overwrite and the 13:10Z re-send. That session's context is gone; the coordinator verified those from the main tree and the pane, and I neither confirm nor dispute them. One nit of fact: R-642 gives my confirmation as 13:14Z; the clock I read before composing it was **13:12:33Z**, so 13:14Z is the arrival, not the reading — immaterial, recorded because times are the thing this programme has slipped on four times.

**(4.3) One statement of MEM's to hold as bounded, not corrected.** "By commit time be64book had started five times, every one status 75" was true at 13:10:39Z and is 23 `Started` lines by 13:26:45Z. MEM named its as-of, which is exactly what makes the drift readable — this is the rule working, not a defect.

---

# 5. Not established

- **No book, tape, fragment or sealed receipt was opened.** Nothing economic was read; no sealed field is quoted anywhere above.
- **The four parser divergences are latent, not live** (§1.2 regime). I did not census the tree for a path containing ` -> ` or a trailing space; I checked that no caller passes `-b`.
- **The journal's rotation *mechanism* is not established.** I measured the window's start advancing (three reads, §3.1a) and that the fs has 1.1 T free with no explicit `SystemMaxUse` in `journald.conf`; which cap is actually binding I could not determine, and "read it quickly" is therefore not a defence I can price.
- **`be_forward_day`'s selftest was not run** (it is a BE driver; the runbook forbids me driving it). §1.4's claims are: the porcelain expression driven directly in three real trees, and the cells' scratch-worktree isolation read at the code — not the suite's verdict.
- **`ev_replay_seam.parse_porcelain`'s callers were not audited** beyond the one at L891 that reads raw. Its shipped defect is latent for that caller and I did not check every other.
- The mutant in §2.3 was driven on a **copy** of the launcher in scratch; no repo file was modified.

**Routing:** BE 65 — §1.3 (the census's transitive names), §1.4 (`working_tree_dirty`), §2.3 (the two definitions and two literals), and the already-open §1.1/§1.2 of REV 65 plus the falsifier's journal grep. DE 96/97 — **§0.1 first** (do not import the weaker parser), §3.1b (the invocation id), the cgroup-leaf refusal. DE + coordinator — §2.1 (`-E 75` in `THE_ONE_COMMAND`, before GO #5). Coordinator — §1.2 (which parser is the one), §3.2 (§6's wording), §4.1 (the headline correction), and rule 20's "exit 1" now being true of one launch form and false of the other. DA 86 — §3.1a/b for its two remaining readers.

**Context ≈ 9 %.** Held after this filing: nothing beyond what is routed above.
