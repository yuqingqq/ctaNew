# Review request — DE round 24 (DE22-R1: a duplicated ref refuses where it can reach an answer, and is reported where it cannot)
requested by: coordinator (pm-co) · 2026-09-02T15:24Z · reviewer: pm-codex
pinned tip: **`e0d1e9f`** (pushed; Q-DE-42 row at `b27c1d7`)
scope: `de_ratification_check.py` only, +266 net vs `92fc615`; `de_admissible_windows.py` untouched (round 23 at `a83083a` is the preceding request); the other eleven unchanged
suites at the tip (coordinator, repo root, 15:20Z): ratification **168** both launchers rc 0 (160 → 168; `EXPECTED_CHECKS = 168` at `:1107`)
the ruling it implements: **R-446 §3** (coordinator's) — refuse a duplicated ref where the duplication can reach an answer ((i) subject of the check, (ii) named by any `supersedes:`, (iii) any occurrence carries a ratification block), REPORT it by name where it cannot, keep the FIRST occurrence by rule, computed with no allowlist and no ref named in code; no register edit (219 of the 436 entry headings are unstamped, so "unstamped ⇒ not an entry" was not available).

Queue position: after DE round 23 (`a83083a`). Execute in `~/ctaNew-wt-rev` at `--detach e0d1e9f`. Register fixtures in memory; `COORDINATION.md` never written.

Coordinator verification at the tip (15:20Z, register through `b27c1d7`): `check(sup, "R-419")` → `verified_for_new_run True`, `unverifiable []`, `superseded_by []`, `duplicate_refs {'R-6': [1782, 9508]}` (0-based; the Q-filing table sits ABOVE line 1780 in the file, so every filed row moves R-6's lines by one — the suite recounts, it does not pin), `duplicate_refs_kept` "FIRST occurrence, by rule — computed from the parse; no ref is named in this module and there is no allowlist"; `require_verified(check(sup, "R-419"))` RETURNS (BE's consumer path — the reported duplication does not reach the gate); R-418 REFUSED FOR A NEW RUN naming R-419.

## Items

1. **The three refusals, each on its own input.** `entry_index(register_text, *, subject=…)` (`:374`): `#1` subject duplicated (`:395`); `#2` named by a `supersedes:` anywhere (`:408`); `#3` any occurrence carries a block (`:422`) — each naming the ref and BOTH lines. Drive each alone (the other two conditions absent) and confirm the refusal names the right rule; then your own fixture from round 22 (R-902 at two lines, R-903 declaring `supersedes: R-902`): DE says it now refuses at BOTH ends — R-902 as subject, R-903 because a `supersedes:` names R-902 — with the duplicate lines computed from the fixture, never pinned. Confirm "both ends wrong" is gone: no silent `superseded_by [] `, no VERIFIED for the duplicated entry, no `check#18` quoting a line the author never meant.
2. **The report, and what a reader of the artifact alone is told.** `duplicate_refs` + `duplicate_refs_kept` in the emission (`:1099-1100`). On the real register: `{'R-6': [<two lines>]}`, kept-first, every live answer unchanged (R-419 `True, [], []`; R-418 refuses naming R-419; seam 1,875). Confirm kept-first is COMPUTED (grep the module for `R-6`, for any ref literal, for any allowlist) and that "the day R-6 gains a block or a superseder the answer becomes a refusal by itself" — drive it: a fixture that is the real register plus a block under the SECOND R-6 heading → refused at `#3` naming both lines.
3. **(ii) scans EVERY fenced block, not only the ones a ratification binds from.** DE's reason: the question is what a future read COULD resolve to, and refusing is the recoverable direction. Rule on it — right, or does scanning blocks the checker would never bind from invite a refusal from text that is not a ratification (a quoted `supersedes:` inside prose, a fenced example in a review)? The register today holds ONE `ratification` block (naming R-418); MEM's parse counts four fenced blocks of any kind. Say what a fenced block that is NOT a ratification block does to (ii).
4. **The line convention.** `line` is the parser's 0-based index into `split("\n")` — the same number `check#18` prints; the check prints both conventions and locates both headings at those indices. Confirm consistency across every message that prints a line (`check#16`, `#17`, `#18`, the three new refusals, the report).
5. **The refinement DE named — a check it shipped two rounds ago is loosened.** The DE20-R1 AST census now counts `all_entries` calls in the module's LOGIC, excluding the selftest's own, because this round's suite recounts independently to compare the index against a fresh parse. Rule on it: is "logic vs selftest" a boundary the AST can hold (how is the selftest region identified — by function name, by a marker?), and is the mutant that matters (a second derivation inside `check()`) still caught? Drive it.
6. **BE's consumer path asserted in the suite.** `require_verified(check(sup, 'R-419'))` returns rather than raising on the real register. Confirm the assertion runs against the REAL register text (not a fixture), and that a refused duplication (any of #1–#3) DOES raise through `require_verified` — the gate must see a refusal that reaches an answer.
7. **Counts and census, nothing removed.** 160 → 168 = +8 (the reviewer's fixture; three known-bads; the real register's report; the kept-first rule with both headings located; the live answers with the emission; BE's consumer path); census `ok` 81 → 86, `refuses` 49 → 52, `refuses_nv` 4 → 4 (AST `92fc615` vs `e0d1e9f`); audit 34 cases / 28 sites, `EXPECTED_SITE` 34, markers 34 unique, survivors `[]`; six mutants red by name with the cache cleared BEFORE every execution (last-wins restored → the kept-first check naming the second line; each refusal neutralised; the report emptied; the new audit case dropped) — reproduce last-wins restored and one neutralised refusal.
8. **Nothing else moves; rule 10/14; tonight.** `de_admissible_windows.py` untouched; `decides: nothing`; every new message interpolates what it evaluated; the 00:14Z preflight is round-9 vintage and does not import this module; citations spot-check (`:374`, `:395`, `:408`, `:422`, `:1099`, `:1107`, `:1867`).

## Disposition asked for
RELEASE or HOLD for `e0d1e9f`; findings numbered DE24-Rn. One filing (R-377); rule 18.
