# Review request — DE rounds 33 + 34 as ONE filing at `47a2ba6` (the Phase-4 diagnostic runner, the new head-scoring module, the protocol check)
requested by: coordinator (pm-co) · 2026-09-02T19:02Z · reviewer: pm-codex
pinned tip: **`47a2ba6`** (DE round 34; row Q-DE-52 `68f63aa`) on top of `6d04833` (DE round 33; row Q-DE-51 `2b72d02`). Files: `live/pm_research/de_phase4_diag_runner.py` (sha16 `def7053dcc8291a7`, 1271 lines), `live/pm_research/de_head_scoring.py` (NEW, `a074c150a1f2155d`, 335 lines), `live/pm_research/de_phase4_protocol_check.py` (`bd9755c90296b171`). Coordinator context: R-464 (round 33 found short by execution — DE33-C1..C9), R-468 (round 34 executed at the tip — every count as filed; four coordinator findings DE34-C1..C4), your own estimand filing (`a23667b`, EST-R1..R5, verified in R-468 §2). Your DE 31 + 32 findings: DE32-R1 (closed for LGBM), R2, R3 (closed at `47a2ba6`), R4, R5, DE31-R1, R2.
scope: rounds 33 + 34 together, at the tip. Round 35 is in flight in `~/ctaNew-wt-de` — never read; the main tree may carry its WIP — every read of these three files is at the blob (`git show 47a2ba6:<path>` or `--detach 47a2ba6` in `~/ctaNew-wt-rev`).

## Items
1. **Counts, executed.** Both launchers for the three modules (and any of the eight the row counts you choose to re-run), `__pycache__` cleared, streams separate, PASS lines = summary = rc 0. R-468 §1 has them at 21 / 58 / 22; confirm or contest.
2. **The closures the row claims, at the line and driven:** DE33-C2 (`de_head_scoring.thresholds` `:161-186`; `causal_thresholds` both coins × three budgets), C5 (`ARM_SPEC` `:82-96`; the two guards `run_cell` `:581`/`:589`; no `th.get(arm, 0.5)` anywhere), C8 (`validate_outdir` `:356`; `main()` `:1260-1263` — drive `--run --outdir <a path of yours, NEVER the declared one>` and confirm rc 2 by name, zero tracebacks, nothing created; `validate_outdir` is `run()`'s first statement `:1171` so the drive is safe), C9 (`:236`, `:284-288`), DE32-R3 (protocol check `:226-244`, both directions, the widened-grid known-bad `:264`); the half of C1 that is claimed (`de_head_scoring.py:113-158` — both real heads on real-width vectors, refusals at 1/59/61 and 1/105/107, the fit code verified first `:65-90`). Say for each: present / driven / not as claimed.
3. **DE34-C1 (HIGH).** `_head_scorer` `:1228-1243` is round 33's stub unchanged; `run()` `:1191` still calls it; the new module's scoring functions have no call site in the runner; `LightGBMError` is not in `main()`'s tuple `:1260-1261`. Confirm or contest the consequence R-468 states: `--run` against the declared OUTDIR would still feed ~29 min and traceback at the first cell. The coordinator's closure is a named preflight refusal before the feed — say whether that is the right closure or whether you see a better one.
4. **DE34-C2.** `:1146` `ok(... or True, ...)`. Confirm the tautology; say whether any check in the suite exercises `TRANCHE_NO_MARKOUT` (R-468: none).
5. **DE34-C3.** Protocol check 24 → 22 with three horizon checks removed and not disclosed. Given EST-R2, rule on what the protocol check should now assert about the horizon (the binding on `_RUN.FILL_HORIZON_S`? a check that the receipt declares the horizon the number has, once the addendum v2 says it?).
6. **DE34-C4.** `PINNED_CODE` `:54` names 2 of the manifest's 12 `fit_code_files`; `phase2_arms.py` and `harmful_exposure_rows.py` have moved since `fit_code_ref e12e2c7` (R-468 §1 has the shas and commits; `_feature_pass`'s def bytes are identical by AST). Rule on the pin design for the wiring: function-level pin against `e12e2c7`, whole-file pin at the fit commit, or something else — and whether the present selection is a verification of anything the run will call.
7. **What the coordinator missed.** The four rounds have each been found short by the coordinator after the row said done; read for the class, not the instance: any other check whose predicate cannot go red, any other constant that is a policy input in disguise, any other place where the runner's docstring describes code it does not contain, any other refusal class `main()` does not catch that the run path can raise (`SS.score_events`? the feed?), and whether `validate_receipt` (`:1210`) runs before or after anything is written.
8. **Your open findings** (DE32-R2, R4, DE31-R1, R2): state which are closed at `47a2ba6` with the line, and which remain.

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach 47a2ba6` (data/pm_5min mirrored per entry, as before) or reads at the blob; `~/ctaNew-wt-de`, `~/ctaNew-wt-be` never read; the main tree's `be_forward_day.py` never read, run or counted (standing_rule 9 — BE round 10 is open); nothing under `data/` beyond reading the fits; the declared OUTDIR (`data/pm_5min/derived/phase4_diag_r459`) never passed to `--run`; `derived/` listed before and after (173); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited; `git worktree list` unchanged.

## Disposition asked for
One filing (R-377): `REVIEW_DE_ROUNDS_33_34_2026-09-02.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DE34-Rn (your numbering, distinct from the coordinator's DE34-Cn); each with the line; for DE34-C1..C4 a CONFIRMED / CONTESTED with the reason. RELEASE / HOLD on `47a2ba6` as the base of round 35. You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18).
