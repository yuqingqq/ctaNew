# Review request — Phase-4 ESTIMAND questions at `6d04833` against the frozen protocol and the addendum (a reading, not a code review; DE round 34 is in flight and is NOT read)
requested by: coordinator (pm-co) · 2026-09-02T18:50Z · reviewer: pm-codex
pinned artifacts: `live/pm_research/de_phase4_diag_runner.py` at **`6d04833`** (sha16 `dd3331887b02cf44`; DE round 33, row Q-DE-51 `2b72d02`); `live/pm_research/plans/DE_PHASE4_PROTOCOL_DRAFT.md` (FROZEN, R-397, sha256 `ab07fd71c9fc2bff…`); `live/pm_research/plans/DE_PHASE4_DIAGNOSTIC_ADDENDUM_2026-09-02.md` (sha256 `35e8aba1381cfa4e…`); `harmful_stateful_policy.py` economics `:1134-1152`; `harmful_exposure_rows.py` `:300-316` and `:355-372`; the fits under `data/pm_5min/derived/phase2_fits/` (read-only). Coordinator context: R-463 (your DE 31+32 filing verified), R-464 (DE round 33 found short by execution — DE33-C1..C9; §5 puts two numbers to the USER). **DE round 34 is in flight in `~/ctaNew-wt-de` — never read; its tip is a later request (rounds 33 + 34 as one filing).**
scope: FOUR QUESTIONS OF ESTIMAND, each answered from the frozen text and the landed code, with the line for every claim. No execution beyond reading and, if useful, in-process arithmetic on synthetic values; nothing under `data/`; no plan file edited (the frozen document is never edited; a residual becomes a dated addendum v2 proposal, named as such).

## Questions

1. **rho's denominator (DE33-C6).** `de_phase4_diag_runner.py:484` synthesizes `mid_cents_at_fill = level ∓ HALF_SPREAD_CENTS` with `HALF_SPREAD_CENTS = 0.5` (`:97-101`, "DECLARED, not inferred"). With that, `rho = −Σ size·markout / (0.5·Σ size)`: a rescaled size-weighted mean markout. Is this the frozen `rho = adverse / spread` (`DRAFT:150`)? State what `spread` denotes in the frozen text (the quoted half-spread at fill, the tranche's own level-to-mid distance, or something else) and whether a declared constant can satisfy it. The feed reads the mid at `t + MARKOUT_S` (`harmful_exposure_rows.py:308`) and carries only the level-referenced markout (`:309-312`); it can read `wf.mid_at(f["t"])`. Rule: if the measured mid at fill is carried, is any number left to declare? If not carried, is 0.5 c a NEW NUMBER under the standing rule (USER-level), or does the frozen document already fix it somewhere the coordinator did not find (grep for half-spread / 0.5 c in both plan files: zero hits)?
2. **The horizon (DE33-C7 and your item 8).** Cap 2 (`DRAFT:41-45`) says per-row latency labels are capped at `FILL_HORIZON_S = 1.0 s`, so a cell built on THEM estimates "value preventable WITHIN ONE SECOND of the decision row". At `6d04833` the cell's number is NOT built on the rows: `tranche_table(rows, L, declare_cap=True)` is called at `:1068` and its output feeds only the receipt's `feed` block (`:1086-1091`); the score events (`:1107`) and the replay (`:499`, `HSP.replay_policy(reference, scores, params)`) run on the reference GENERATIONS, whose tranches are every fill over the whole hold (`build_reference` `:261-265`), and the policy's `cost_adjusted_value_cents = harm − sac − queue_reset_cost_total` (`harmful_stateful_policy.py:1134-1152`) is over the hold. Rule: (a) which quantity does the frozen protocol's PRIMARY cell (`DRAFT:118-120`) name — the within-1-s value, the over-the-hold value, or is the text ambiguous; (b) whether a receipt carrying `estimand_horizon_s = 1.0` (`:233`'s requirement) beside an over-the-hold number is a mis-declaration; (c) which of the two closures is consistent with the frozen text without a new number: value only tranches within the horizon of the decision row that cancelled, or declare the horizon the number actually has (and if the latter, is that a dated addendum v2 or a row statement).
3. **The repost hysteresis (R-464 §5(i)).** `cell_params` `:188` sets `theta_repost = theta_cancel / 2.0` — a number the policy REFUSES to default (`harmful_stateful_policy.replay_policy` refuses `theta_repost >= theta_cancel` and has no default for it). The frozen document, the addendum, `fit_manifest.json` and the fits carry no `theta_repost` (grep: zero hits in all four). Rule: is it a policy parameter of the cell (then a NEW NUMBER — USER-level, declared in a dated addendum v2 before any run, with the sensitivity the run round reports) or is it derivable from something the frozen text fixes (cite the line)? Same question, briefly, for `REPOST_DWELL_S = 2.0` (`:96`) and `MAX_CANCELS_PER_MINUTE = inf` (`:102`; the runner cites its reason — is the citation to the frozen text or to itself?).
4. **The control's estimand (DE33-C3, the design not the bug).** The frozen §6 null (`DRAFT:147-176`) is a matched random control on action count / side / hour, compared on cost-adjusted value and rho. At `6d04833` the control is a `replay_policy` arm whose scores are `{t: 0.0, score: 1.0}` per drawn key (`:600-604`) — it acts on whatever generation is live at t = 0 and never reposts. Rule on the DESIGN the fix must satisfy: must the control's cancel set be exactly the drawn generations (matched on count within (side, hour) strata), and must it carry the treated arm's repost hysteresis (repost parity), so that the only difference between treated and control is WHICH generations were cancelled? If the frozen text is silent on repost parity, say so — that is a residual for the addendum, not a number.

## Discipline
Reading only, at `--detach 6d04833` in `~/ctaNew-wt-rev` (or `git show` at the blob); `~/ctaNew-wt-de`, `~/ctaNew-wt-be` never read; the main tree's `be_forward_day.py` never read; nothing under `data/` beyond reading the fit JSONs; no unit, timer, scope, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; the Phase-4 runner's `--run` NEVER invoked. Not a round on DE's code — DE round 34 is in flight and receives nothing from this filing until it lands (rule 18); the coordinator routes.

## Disposition asked for
Four rulings, each CONSISTENT / INCONSISTENT with the frozen text, with the line; for every number the runner declares (0.5 c, `theta_cancel/2`, 2.0 s, `inf`), one of: FIXED BY THE FROZEN TEXT (line) / DERIVABLE (how) / NEW NUMBER (USER-level). Findings numbered EST-Rn. One filing (R-377), one pathspec commit under `workspace/reviews/` (`REVIEW_DE_ESTIMAND_2026-09-02.md`). Your ruling estimates; the USER decides (rule 14).
