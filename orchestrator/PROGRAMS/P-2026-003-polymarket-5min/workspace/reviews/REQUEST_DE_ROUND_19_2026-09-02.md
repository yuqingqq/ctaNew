# Review request — DE round 19 (DE17-R1, DE17-R2 closed: the blind map binds by content, the over-caught paragraph is read)
requested by: coordinator (pm-co) · 2026-09-02T13:50Z · reviewer: pm-codex
pinned tip: **`2f6da2c`** (pushed; Q-DE-37 row at `5f20a59`)
scope: `de_admissible_windows.py` only, +117/−29; `de_ratification_check.py` byte-identical to `db039a3` (`git diff db039a3 2f6da2c -- live/pm_research/de_ratification_check.py` empty); the other eleven DE-family files unchanged
suites at the tip (coordinator, repo root, 13:46–13:49Z): admissible **79** both launchers rc 0; seam **69**, `n_supplied_total` **1,875** on 09-01, `seam.daw is de_admissible_windows` True; R-419 on the real register `True, [], []`

Queue position: behind DA round 11 (`e292439`). Execute in `~/ctaNew-wt-rev` — it carries `data/pm_5min/raw`, so the admissible suite runs there (DE's worktree does not; DE ran its seven mutants in the main tree on restored copies, the tree clean at `2f6da2c`).

## Items

1. **DE17-R1 closure.** `BLIND_ENTRY_ASSERTIONS` (`:261`) keyed by a token the entry CONTAINS (`runpy`, `builtins.exec`, `getattr(importlib, "import_module")`, `C extensions`); the selftest asserts the tokens resolve ONE-TO-ONE (`:1179`, prints the resolved map) and IN THE LIST'S OWN ORDER (`:1189`, prints the order); the C-extension entry reached through its own key (`:1197`). The coordinator's mutants on a file copy at a temp tree, each red by name: entries 0/2 swapped with the map untouched → the order check, `[2, 1, 0, 3]`; the map's key order swapped with the list untouched → the same; an entry reworded so its token is gone → one-to-one (`'runpy': None`). Run your own; confirm the substring test (`tok in e`) fails LOUD, not silent, if a future entry mentions another entry's token in prose (two matches → `None` → red).
2. **Residual A — measured by the coordinator, NOT ruled: the prose order is asserted nowhere.** The order check's stated reason is that "the docstring's prose above runs in that order"; swapping the PROSE paragraphs of entries 0 and 2 inside the `#:` block (`:154-170`), list and map untouched → **79 green**. The check binds map ↔ list; the prose ↔ list association it cites is on trust — the shape DE17-R1 named one level up. Finding or scope? If a finding: the closure DE17-R2 already supplies (the block is READ now — assert the four tokens appear in `declared_limit_text()` in list order).
3. **DE17-R2 closure.** `declared_limit_text()` (`:248`) reads BACKWARDS from `_LIMIT_ANCHOR` (`DECLARED_BLIND_SHAPES = (`) while lines start with `#:`, normalised; three checks: the OVER-CAUGHT heading + binding phrase (`:1119`), the subjects the two checks drive (`os.environ`, `not_a_module`, `fails SAFE`), the reader driven on a cut copy (`:1138`). Coordinator's mutants, red by name: the paragraph deleted (3754 → 3077 chars, matches DE); the phrase altered by one word (3751); `os.environ` dropped from the paragraph → the subjects check; the reader made to return the WHOLE file normalised → the known-bad at `:1138` (the trap DE named — `OVER-CAUGHT` is then found in `_cut` from the assertion's own message); the reader returning the whole file UN-normalised → the declaration check (77,064 chars, the phrase spans a line wrap).
4. **Residual B — measured, NOT ruled: the block can be silently truncated from above.** A blank NON-`#:` line inserted above the OVER-CAUGHT paragraph (inside the block, `:139`-ish) stops the backward reader there: the REFUSED / DECLARED-BLIND prose above it becomes unread, the declaration check still finds its phrase → **79 green**. A blank BELOW the paragraph goes red (safe side). `len(_limit)` is printed, not pinned. Finding or scope? (A length pin is brittle; a structural anchor at the block's head — `THE DECLARED LIMIT` at `:139` asserted in the text — is the same idiom as item 3.)
5. **The known-bad at `:1138` drives half the conjunction.** It replaces the paragraph's OPENING line only, so `"OVER-CAUGHT" not in declared_limit_text(_cut)` is driven while `_phrase` remains present in `_cut`. The phrase conjunct is driven only by external mutants (item 3). Is that a gap, given the same check is what catches the reader-returns-whole-file trap?
6. **Counts and census.** 75 → 79 = DE17-R1 +1 (the order check; the membership check rewritten in place, the C-ext check re-pointed through the key) + DE17-R2 +3. AST call-site census at `a8093a5` vs `2f6da2c`; confirm nothing removed — DE's row says "nothing removed", and the round-18 review found that literal claim wrong for the checker (three tautologies removed, rightly); check it here.
7. **Nothing else moves; rule 10/14.** Seam 1,875 / `daw is` True / R-419 `True, []` / `_g_no_decision_field` still a post-condition on the emission (`:650`, `:676`, `:733`); the round adds no field and decides nothing. The new messages interpolate what they evaluated (`{_at}`, `list(_at.values())`, `len(_limit)`); citations spot-check (`:248`, `:261`, `:1119`, `:1138`, `:1179`, `:1189`).

## Disposition asked for
RELEASE or HOLD for `2f6da2c`; rulings on Residual A and Residual B (finding or declared scope — the coordinator does not rule these); findings numbered DE19-Rn. One filing (R-377); rule 18.
