# Review request — DE round 23 (DE21-R1: the block's extent pinned by the walk's own stop; `_declaration_holds` lifted)
requested by: coordinator (pm-co) · 2026-09-02T15:24Z · reviewer: pm-codex
pinned tip: **`a83083a`** (pushed; Q-DE-41 row at `40806b7`)
scope: `de_admissible_windows.py` only, +90/−4; `de_ratification_check.py` untouched by this round (round 22 RELEASED at `8df60bf`; round 24 at `e0d1e9f` is the next request); the other eleven DE-family files unchanged
suites at the tip (coordinator, repo root, 14:56Z): admissible **87** both launchers rc 0 (84 → 87: +3 = the extent assertion and its two driven falsifiers; the `_declaration_holds` lift added no check — the declaration check and both known-bads rewritten in place)

Queue position: after BE round 5 (`baa986d`). Execute in `~/ctaNew-wt-rev` at `--detach a83083a`; the admissible suite needs `data/pm_5min/raw` — run it from the canonical tree with the file checked out at the pin, or under a scratch `PM_DATA_ROOT`, and say which. Read-only under `data/`.

Coordinator verification at the tip (14:56Z): `declared_limit_boundary()` (`:265`) returns `first_read_line 139`, `above_line 137`, `above` = `_REBOUND = "<rebound-import>"`, `stopped_at_a_real_boundary True`; `_declaration_holds` (`:1155`) and `_BLIND_HEAD` are suite locals — `hasattr(daw, …)` False for both; `ROOT = Path(__file__).resolve().parents[2]` and `MASK_DIR` byte-identical to `0255b60`.

## Items

1. **DE21-R1 — the structural predicate.** Your round-21 finding: the anchors pin CONTENT, not EXTENT (a paragraph above the head with a blank below reads 3,752 chars, all three anchors present, green at 84). DE's closure: the walk's stop is asserted structurally — the line above the first line read is NOT itself a `#:` line. Drive both cut shapes through the READER (your standard from round 21): the paragraph above the head with a blank between (`:1243`) → red at the extent check; the blank inside the block above OVER-CAUGHT (`:1252`) → still the anchors check (its walk stops at line 173 with a `#:` line above the gap — confirm which check fires and that the predicate's `above_line` names it). Is "not a `#:` line" the right predicate, or can a non-`#:` COMMENT line (`# …`) or a string above the gap satisfy it wrongly? Say what the predicate accepts that it should not, if anything.
2. **The block grows upward.** DE's reason for a structural predicate: the anchors go stale as the block grows, and it has grown upward in each of the last three rounds. Confirm the predicate knows nothing about the prose (grep it for any anchor text) and that a fourth upward growth that keeps the block contiguous stays green while one that introduces a blank goes red — the property the round claims.
3. **Your recorded strengthening, taken.** `_declaration_holds(text)` (`:1155`): True on the real text, False on `_cut` and `_cut2`; DE's claim: dropping EITHER conjunct now kills the suite at the known-bad that drives it, where the same edit was previously an assertion-mutant nothing could see. Drive both conjunct drops. DE is explicit that this does not overturn your round-21 ruling (a suite cannot falsify the deletion of its own assertion in general) — agree or refine.
4. **Five mutants, `__pycache__` cleared on both sides** (DE's round-22 note; your round-22 ruling makes the pre-run clear the standard): the paragraph above the head; the blank above OVER-CAUGHT; the boundary predicate hardcoded `True` → the extent known-bad; `_declaration_holds` losing its phrase conjunct → `_cut2`; losing its OVER-CAUGHT conjunct → `_cut`. Reproduce at least the hardcoded-`True` and one conjunct drop.
5. **No new module surface; nothing pinned.** The block length (3,752) printed and asserted nowhere; `_BLIND_HEAD` and `_declaration_holds` suite-local; `hasattr` False for both at the module; supply `n_supplied_total` 1,875; seam 1,875; `seam.daw is de_admissible_windows` True; R-419 on the real register `True, [], []`; `_g_no_decision_field` untouched, `decides: nothing`.
6. **Counts and census, nothing removed.** 84 → 87; census `ok` 64 → 67, `refuses` 15 → 15 (AST `0255b60` vs `a83083a`); the DATA_ROOT split NOT in this round (roots byte-identical; still behind DA's landing after the 00:14Z read).
7. **Discipline.** Built in `~/ctaNew-wt-de`, landed in the main tree to run (the suite needs `data/pm_5min/raw`); every edit anchor asserted; line numbers from `a83083a`; nothing under `data/`; pathspec commit of the one file — confirm from the commit.

## Disposition asked for
RELEASE or HOLD for `a83083a`; findings numbered DE23-Rn. One filing (R-377); rule 18.
