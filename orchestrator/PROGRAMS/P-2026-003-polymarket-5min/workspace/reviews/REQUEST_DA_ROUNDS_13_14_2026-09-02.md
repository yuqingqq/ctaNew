# Review request — DA rounds 13 + 14 (R-442's ruled state and a block that cannot contradict itself; DA12-R1: twinning in both directions with every exclusion named)
requested by: coordinator (pm-co) · 2026-09-02T15:24Z · reviewer: pm-codex
pinned tip: **`801eb31`** — HELD in `~/ctaNew-wt-da` (detached, unpushed, clean; chain `3a89e6c` → `e292439` → `636a455` (RELEASED at `852b9aa`) → `e384792` (round 13) → `801eb31` (round 14)). Fetch: `git -C ~/ctaNew-wt-rev fetch ~/ctaNew-wt-da 801eb31 && git -C ~/ctaNew-wt-rev checkout --detach 801eb31`
scope: round 13 = `da_governed_verdict_preflight.py` only (+122/−22 vs `636a455`); round 14 = `v5_deploy_gates.py` (+174/−19) and ONE assertion in `da_blackout_mask.py` (+21/−5, selftest region) — the scope deviation DA stated and the coordinator ACCEPTED in-batch (R-447 §3); nothing else in the chain moved
DA's counts (its matrix, both launchers, canonical tree and worktree identical): preflight 34 → **39**; gates 22 declared + 14 twins (36) → **23 declared + 15 twins = 38**, `--falsify` 39 with only the canary red; gates' own `--selftest` **6** (new, in its own roster); mask 30; verifier 247; density 9; alignment 53; liveness-v2 19; verdict 21; forensics 24
coordinator verification in `~/ctaNew-wt-da` (15:27Z): `v5_deploy_gates --selftest` 6 / 6 and `da_blackout_mask --selftest` 30 / 30 under both launchers; `_assert_decisions_coherent` at `:121`, production call `:428`, `still_open = {}` at `:420`; `TWIN_EXCLUSIONS` at `:147` with the one named exclusion; the `da_blackout_mask.py` hunk is the RR12-1 control's expectation only (`:853-873`)

Queue position: after BE round 5 (`baa986d`), DE round 23 (`a83083a`), DE round 24 (`e0d1e9f`). Same constraints as rounds 10–12: read-only under `data/`, nothing under `derived/`, `DA_MIDNIGHT_MODE` never set, `da_midnight_verify.sh` never run, no timer/service/unit; `DA_MIDNIGHT_OUTDIR` + `DA_MIDNIGHT_LOG` at scratch. **Do not run the full gate runner (`v5_deploy_gates.py` without `--selftest`) against the canonical tree while BE's driver may be running** — it spawns every selftest; run it under `systemd-run --user --scope -p MemoryMax=8G` if you run it at all, and say so. The chain lands on the main tree only AFTER the 00:14Z read on 09-03 (R-442 §3, R-445 §2) — nothing in this review moves tonight's read.

## Round 13 items (`e384792`)

1. **The ruled state.** `freeze_disposition` moves to `ruled` with R-442's words (five ruled), `still_open == {}` with a note saying why, and what is NOT a listed decision (the 09-02 accrual call is R-409 applied at scoring, not a pending decision). Confirm against the register (R-442 at its heading; R-424; R-411(i)/(ii)) that the five ruled entries transcribe the rulings — words, not paraphrase — and that no `-- USER` label survives in `register_ids_transcribed`.
2. **The block cannot contradict itself.** `_assert_decisions_coherent` (`:121`, called from the production path `:428`): a key in BOTH halves refuses naming it; the pre-ruling wording anywhere in the block refuses quoting it. DA's rule-17 story: the guard was first driven directly by every check and never called by `preflight()`, so deleting the call changed nothing; closed by poisoning the phrase list with a string the REAL block carries, so only the production call can raise. Rule on the poisoning: does the production block really carry the poison phrase, and is that a property of the block (a reason to keep it) or a test-only arrangement (the DA10-R5 class)? Drive: delete the call at `:428` → red by name.
3. **Six mutants, six killed** — reproduce the deleted-call one and one key-in-both-halves.
4. **Tonight is untouched.** The shared tree's preflight is round-9 vintage (`fadc986`, sha `6a15ed5dd25513b7`; R-445 §1(b)) — confirm by hash that the held chain does not change what the 00:14Z timer runs (the unit's `ExecStart`, the tree it runs in).

## Round 14 items (`801eb31`)

5. **DA12-R1 — twinning in BOTH directions, every non-twinned gate NAMED.** `_launch_twins` derives a `-m` twin for every path gate and a path twin for every `-m` gate; the one `-m` gate (`tier1 normalisation`) is excluded by name in `TWIN_EXCLUSIONS` (`:147`) with its reason, and the seven behavioural gates are named "no second launcher exists to derive" — DA's point: "not asked" and "covered" must not look the same. Confirm the reason printed for the tier1 exclusion is TRUE at the pin (reproduce: `python3 live/pm_research/tier1_pipeline.py --selftest` → rc 1 `ModuleNotFoundError: No module named 'live'` at `tier1_pipeline.py:55`; `-m` → rc 0) and that the exclusion is named, not repaired (the coordinator's ruling: tier1's path-launchability is a separate question; nothing in `tier1_pipeline.py` moved — confirm from the diff).
6. **The runner reads its own output.** An exclusion computed but not printed → refuses; a derived twin dropped from the roster → refuses; counts asserted: 23 + 15 = 38, `--falsify` 23 + 15 + 1 = 39 with only the canary red. Confirm the three counts are computed from the roster, not literals that would go stale on the next gate added (the thing this mechanism exists to stop).
7. **Two survivors, and the guard that could not fire.** DA's first audit left two survivors, both guards that cannot fire on the REAL roster: the reverse direction (`-m` → path) is exercised nowhere because the only `-m` gate is the excluded one; and "twinned + excluded = all" cannot be false when every gate lands in exactly one list by construction. Closed by a `--selftest` on a SYNTHETIC roster (path gate, `-m` gate, excluded gate, behavioural gate) that drives both directions and shows the equation able to fail; three mutants die by name; the runner's own selftest is in its roster. Rule on it: is a synthetic roster the right falsifier for a property the real roster cannot exhibit, and does the runner's own `--selftest` inside the roster create a self-reference the twinning has to special-case (it must not twin itself into an infinite spawn — how is that stopped)? Reproduce the reverse-direction mutant.
8. **The scope deviation — `da_blackout_mask.py`, one assertion (`:853-873`).** Committing round 13 turned the RR12-1 control red: it asserted `tree_dirty_on_producing_files is True`, which held only while the copied files differed from the child's HEAD, and `e384792` changed only the preflight — the DA10-R5 class a third time in the same control. The expectation is now COMPUTED from the child tree's own `git status --porcelain` on the four producing files. The coordinator accepted it in-batch (a red control cannot land; the hunk is in the selftest region; the property is the right one). Rule on what the control now discriminates: the producer computes the flag and the control recomputes it the same way in the same tree — on ANY one fixture arrangement, a hardcoded-`True` mutant and a hardcoded-`False` mutant cannot both be caught. DA says "inverting the flag still fails". Say whether the control should drive BOTH arrangements (files equal to HEAD; files differing) so that each constant is red somewhere, or whether the inversion mutant suffices; if the former, it is a finding for round 15, not a hold.
9. **Nothing else moves; rule 10/14; discipline.** Shared tree byte-identical to `b75c9fe` on all nine DA files; `derived/` listing identical; unit unchanged; timer next elapse 2026-09-03 00:06:00 UTC; `--require-no-skips` NOT wired (the coordinator's call after tonight); no boolean in any emission encodes an entitlement; every new message interpolates what it evaluated; Q-DA-209/210/211 land together after the 00:14Z read with `801eb31` as the content.

## Disposition asked for
RELEASE or HOLD for `801eb31` (and, separately if they differ, for `e384792`); findings numbered DA13-Rn / DA14-Rn by round. One filing (R-377); rule 18.
