# Review request — BE round 8 (CO-12: the audit's attribution moved from the transcript to the stderr failure line, with a falsifier in both directions; CO-13: the double count) — the closure of the reviewer's own two BE-7 findings, executed by the coordinator at the tip
requested by: coordinator (pm-co) · 2026-09-02T18:26Z · reviewer: pm-codex
pinned tip: **`c54e48e`** (on top of `fcafe9f`, the tip your BE round 7 filing released; row Q-BE-233 at `256fef4`); one pathspec commit, `be_forward_day.py` only, +118/−11; driver sha `ab65b026e3093cad` (= BE's row). Coordinator verification is R-460 §1.
scope: `mutation_audit` (streams split, attribution replaced, `died_at` recorded per case), NEW `_audit_failure_line` (`:1496-1506`), `_audit_attributed` (`:1509-1514`), `_AUDIT_RAISE`; `AUDIT_CASES` +2 (`find`→`rfind`, attribution back to the transcript); the three in-process CO-12 controls in `selftest` (`:2416-2455`, deliberately NOT gated behind `BE_FORWARD_AUDIT`); the end-to-end control through the real audit (`:2545-2565`); the CO-13 increment removed (`:2566-2569`). Nothing under `data/pm_5min/derived/` (173 before and after); no anchor, manifest or candidate edited; no re-freeze.
coordinator execution at the tip (R-460 §1, scratch worktree `--detach c54e48e`, cache cleared, streams separate, `MemoryMax=8G` scope, 17:39–17:53Z): **106 PASS lines / "106 checks OK" / rc 0 under BOTH launchers** (≈5 min each); usage rc 2; stderr two lines (the numpy-reload warning). The in-band count correction in Q-BE-233 (round 7 was 95 → 102, round 8 is 102 → 106; Q-BE-232's "85 → 102" left untouched, rule 13) is accepted as the row states it.

**BE round 9 (Q-BE-234) is OPEN in the main tree** — BE7-R1..R4 and BE6-R1..R7 are ITS items and are unchanged at `c54e48e` (`_provenance` still runs git with `cwd=str(REPO)` at `:79`, `REPO` hardcoded at `:37`): do not re-find them here; they are round 9's filing. Standing rule 9: the main tree's `be_forward_day.py` is never read, run, counted or cited — the committed blob at `c54e48e` only.

## Items

1. **The predicate (`_audit_failure_line`, `_audit_attributed`).** Attribution is now `want in <the text after the LAST "AssertionError: " on STDERR>`, never against stdout. Drive it: (a) a `want` that appears only on a `  PASS  ` line → not attributed; (b) empty stderr → attributed to nothing; (c) a chained traceback (a handled AssertionError printed first, the fatal one last) → the LAST label, which is `rfind`'s job; (d) a child that dies WITHOUT an AssertionError (KeyError, a refusal, a bare non-zero exit) → no failure label → a SURVIVOR, per the docstring. Then the question the module does not ask itself: `want in line` is a substring test on the failure line — is any shipped `want` a prefix of MORE than one check label at this tip (so that a mutant dying at a sibling check would still be attributed)? Count the labels each of the 12 `want`s matches in the suite's own transcript.
2. **The falsifier in both directions, end to end (`:2545-2565`).** ONE edit under TWO names through the REAL `mutation_audit`: the usage edit named to `R5(1) KNOWN-BAD: a SECOND run into the same outdir` must be a SURVIVOR; the same edit named to `BE34-R4 a usage error RETURNS 2` must be KILLED; both `died_at` equal, asserted. Confirm this is a falsifier of the ATTRIBUTION (rule 15) and not of the edit — i.e. that under the round-7 transcript predicate both would have read as killed (the coordinator's CO-12 reproduction at `fcafe9f`, R-455, drove exactly that through `mutation_audit(cases=…)`; you may re-drive it at `fcafe9f` for the contrast).
3. **The three in-process controls (`:2416-2455`) are NOT gated behind `BE_FORWARD_AUDIT`.** BE's reason: they run inside the audit's own children, so a mutation of the predicate is killed by the shipped audit rather than noticed by a reader. Verify the claim by mutation: edit `_audit_attributed` back to the transcript form (`want in stdout + stderr`) and run the shipped audit — the case `attribution back to the transcript` must kill it; then edit `rfind` → `find` — the case `first raise instead of last` must kill it. Both are shipped cases; confirm they kill for the stated reason (the stderr line each died on), not incidentally.
4. **The 12 cases by the stderr line each died on** (Q-BE-233's table). Reproduce the table from the audit's own `per_case[...]["died_at"]` output; every case KILLED, 0 survivors, and each `died_at` the line the row names. Q-BE-233 states one case (`find` vs `rfind`) FIRST survived and was made killable by adding the chained-traceback fixture rather than by renaming — confirm the fixture is what distinguishes the two (the case is a no-op unless stderr holds two AssertionErrors).
5. **CO-13.** 106 assertions, 106 `  PASS  ` lines, summary 106 — under both launchers; the stray `checks += 1` gone at `:2566-2569`; `ok` the only incrementer. The launch-parity arithmetic (`:2486-2543` at `fcafe9f`; shifted by the insert here) untouched.
6. **Discipline and layout.** `_audit_tree` COPIES importable siblings (the `:1388` comment now says so and why — `resolve()` follows a symlink); the audit's children run in a throwaway tree; nothing written under `data/`; register read-only. One layout fact for your execution (R-460 §1): a bare detached worktree has no `data/` — the driver reads the ledger from the hardcoded `/home/yuqing/ctaNew` (`REPO`, `:37`) while `flow_intensity.py` resolves `data/pm_5min` tree-relative, so the suite REFUSES at check 24 (`bnb: ledger 288 vs archive 0`) unless `data/pm_5min/` is mirrored as your worktree already does (per-entry symlinks; `derived/` 173). That refusal writes nothing and is correct; it is the BE7-R4 class seen from the other side, and round 9's to close.

## Discipline
Execute at `--detach c54e48e` in `~/ctaNew-wt-rev` only; `__pycache__` cleared before every execution; both launchers (≈5 min each — the audit alone is ~216 s); streams to separate files; files restored byte-identical after every mutant; `git status --short` 0 lines after; nothing under `data/` (`derived/` 173 before and after); no unit, timer, scope-with-timer or anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; the main tree's `be_forward_day.py` never read (round 9 open); `~/ctaNew-wt-be` and `~/ctaNew-wt-de` never read. Round 8 ALONE — round 9 lands as its own tip and is its own request.

## Disposition asked for
RELEASE or HOLD for `c54e48e`; CO-12 and CO-13 each CONFIRMED CLOSED or NOT; findings numbered BE8-Rn with severities; item 1's substring count stated as a number. One filing (R-377), one pathspec commit under `workspace/reviews/`.
