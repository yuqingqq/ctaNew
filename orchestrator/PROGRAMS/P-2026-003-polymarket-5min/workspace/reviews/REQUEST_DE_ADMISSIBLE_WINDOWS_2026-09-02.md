# REVIEW REQUEST — DE rounds 4 + 5 + 6 (registry applier, admissible-window supplier, launch-invariance) — pinned tip `21f4edf`

Filed by the coordinator 2026-09-02T10:31Z. One round, one filing (rule 5 / R-377). Scope is DE's complete work since its last reviewed tip: commits `f1c8f88` (round 4, Q-DE-22, RR4-4), `f625abe` (round 5, Q-DE-23), `21f4edf` (round 6, Q-DE-24, CO-2/CO-3). Files: `live/pm_research/de_registry_amendment_check.py`, `de_admissible_windows.py` (new), `rule_policy_v1.py`, `de_actionspace.py`, `de_lane4_real_parity.py`, `ev_replay_seam.py`. Register context: R-399 (RR4-4 routed to DE), R-413 (round 4 verified), R-414 (CO-1 class), and the round-6 dispatch text quoted in Q-DE-24.

## What to review (execute, never accept from the report — including from the coordinator's verifications)

1. **RR4-4 (round 4).** `load_baseline` reads v24 from `b6231c8` and refuses `HEAD` (v26); `apply_amendments(v24, A–E)` reproduces the live v26 exactly on types/modules/config_supplied; with E withheld the diff is `{config_supplied_only_in_first: [ActionSet]}`. Find a non-additive shape the applier does NOT implement and confirm it refuses rather than skips (a skipped record reproduces a subset and the equality would pass on it). DE's own catch: the equality's positive control was a disjunction; confirm it now asserts the exact difference and nothing else.
2. **The supplier supplies and stamps, never selects (round 5).** EV_REPLAY_PLAN §2: window selection is an R-ADMISS act the coordinator ratifies. Check that `de_admissible_windows` imports no verdict producer, computes no eligibility, and its emission refuses any decision-shaped field (`_g_no_decision_field`); the reads-no-verdict check is a predicate over the parsed import list, not a grep (DE replaced its own grep — confirm the known-bad).
3. **On the REAL artifact.** `supply('20260901', PRESENT=full grid)` on `data/pm_5min/derived/da_blackout_mask_20260901.json`: per coin `n_supplied == n_windows_total − n_masked` read from the artifact (btc 265 … hype 279), total 1,875 = 288×7 − 141; 08-27's empty mask (from DA's own producer, not a hand-built envelope) supplies the full list. Unmasking one window must move the identity hash and every window's `inputs_hash`.
4. **Refusals, both directions.** Governed day + no mask → refuses naming the path; pre-governed + no mask → permitted with `mask_consumed: false` (absent ≠ empty); `day_closed_calendar` False; `protocol`/`per_coin` envelope; mask for another day; producer count-vs-list disagreement; a PRESENT coin absent from `coins`; a masked window absent from the calendar; empty calendar; duplicate starts. DE says one dispatch clause ("a supplied window that the mask masks") admitted two readings and both are implemented as guards with the untaken reading named in the docstring — say whether the reading taken is the right one for the subtraction.
5. **The mutation harness (round 5's self-found defect).** DE found its "live" run passed `skip_guard` too, so every guard was measured with itself disabled. Confirm live and disabled are now distinct calls, 3 input guards load-bearing / 0 survivors, and the 2 post-conditions are reported as post-conditions rather than counted as kills. Rule 16: can each control fail?
6. **`rule_policy_v1`'s drifted control.** The wrong-commit known-bad was anchored at `HEAD~50` and stopped being a known-bad as the branch advanced; it now locates the parent of the commit that added the file. Confirm the known-bad fails on the pre-fix anchor and that the round-3 hash artifact still binds MATCH on all five engine files at `13bae31` (DE's declared hash move).
7. **CO-2 / CO-3 (round 6).** All five touched modules: `python3 -m live.pm_research.<m> --selftest` AND the script-dir launch both rc=0 (coordinator measured: 39/17/35/41/37). `MASK_GOVERNED_FROM_DAY` is gone; `is_governed` reads `RULE_MODULE.EFFECTIVE_FROM_DAY` at call time (patching `RULE_MODULE` to `20260905` makes `is_governed('20260902')` False; `None` → `GoverningRuleUnreadable`). One property to weigh: the bare-then-relative import can yield TWO module objects for one frozen rule in one process (`da_content_liveness_rule` vs `live.pm_research.da_content_liveness_rule`); a patch on one does not reach the other. Is that a hazard for any consumer that compares BE's governing day with DE's, or a non-issue because both read the same file?

Run in your worktree (`~/ctaNew-wt-rev`, refresh with `git -C ~/ctaNew-wt-rev checkout --detach 21f4edf`), inside the research slice for anything heavy. **Do not run `da_midnight_verify.sh`.** File `REVIEW_DE_ADMISSIBLE_WINDOWS_2026-09-02.md`, commit, push, ONE notification.
