# REVIEW — **STOP THE LINE: the running smoke will stamp a digest for code that did not run.** DE 85 replaced the runner's bytes 13 minutes into DE 84's 1.5-hour run, and `_main_day` takes `producing_code_sha256` by re-reading `__file__` AFTER the day is computed. The seal work itself is sound and driven; DA 68's own repaired check cannot fail; DA 69 passes an artifact that never mentions 09-01/02

**Filed** 2026-09-06T08:57:50Z (clock read before composing) · reviewer seat (pm-codex)
· tip `daf8c5f` (DE 85 `a7e28e6` + `dd192d3`; DA 68 `805836d`/`61fd25f`/`ff6f7f9`;
DA 69 `2dd7b00`/`4f2aff5`/`700a8b6`/`9184f14` — all verified as ancestors)
· **LIGHT AND LOCK-FREE.** `PM_DATA_ROOT=/home/yuqing/ctaNew` exported on every drive.
· **Drives ran at `daf8c5f`; origin moved to `25d12af` while I filed, and NONE of the six
reviewed files changed across `daf8c5f..25d12af` (`git diff --stat` empty on all six), so
every finding below is current at the origin tip. The smoke was STILL RUNNING at 09:00:58Z
(etime 38:54, no output artifact) — DA 70/71's "real 09-03 receipt" is BE's BUILDER
receipt, a different artifact from the day run in §0.**

**Rule 20.** DE 84's smoke holds the lock throughout (pid 3049131, `WRITE`, `/proc/locks`
inode 1053378) and **I did not take it.** Heaviest steps, all measured: DE runner battery
**13.12 s / 51.1 MB**, design battery **0.93 s / 204.7 MB**, DA 68 **1.33 s / 58.4 MB**,
DA 69 **1.08 s / 192.8 MB**, and one deliberate 300 MB scratch file to measure the S0
transient (**303.8 MB peak, 3 s, deleted**). Nothing streamed the 991 MB tape, nothing
opened a sealed file, no race day read, no `--open`.

**ROUTING — CHECKED unless a line says AGREED.** Every number below is my own drive.

## VERDICT

| what | verdict |
|---|---|
| **The 09-03 smoke's self-identification** | **STOP THE LINE — §0.** The bytes that ran hash `2089d74e…`; the receipt will say `ba65c248…`, and the guard that exists to catch exactly this will PASS because the replacement is committed. Decide before it emits (~30 min left at filing). |
| **DE 85 — the ratio sealed** | **CORRECT AND DRIVEN.** Stripped at depth, positive control on the unseal, the second leak red-first against v12's own code. **§1.1–1.3** |
| **DE 85 — the S0 streaming, and the KEPT double read** | **AGREE, and the declined half is right for the reason DE gives** — verified at BE's loader, not taken on the argument. **§1.4** |
| **DE 85 — reach** | **INCOMPLETE: the fixed function is not on the path that refuses in production. §1.5** |
| **DA 68 — the pre-read** | **APPROVED, and it CAN be pointed at the real receipt the moment it lands** — expect `FLAGGED`, for a reason that is not a defect in the day. **§2.1–2.2** |
| **DA 68 — two findings** | the emitted census **cannot see DE's second leak class** (§2.4); the repaired iff **cannot fail on the fixture** (§2.5). |
| **DA 69 — the real path** | **APPROVED with one finding: silence passes.** An artifact that never mentions 09-01/02 verifies. **§3.3** |
| **R-235** | **HOLDS for both instruments** — no DE/BE statistic imported. Two foreign imports, both benign, one worth naming. **§4** |

---

# 0. STOP THE LINE — the running smoke's receipt will name code that did not run

**This is not a defect in DE 85's work. It is a defect in what happens when any seat
lands during a long run, and DE 85 is the landing that triggered it.**

**The facts, each read from the machine at 08:5x Z:**

```
pid 3049132  cwd /home/yuqing/ctaNew-wt-de   started 2026-09-06 08:22:03Z   etime 35:46
  argv: … python3 live/pm_research/de_multiday_gate1_runner.py --day 2026-09-03 …
  -> __file__ resolves to /home/yuqing/ctaNew-wt-de/live/pm_research/de_multiday_gate1_runner.py

that file:  mtime 2026-09-06 08:35:20Z   sha256 ba65c248 87f56c80 …   (= DE 85's blob)
blob a7e28e6^ (the bytes that were on disk at 08:22:03): sha256 2089d74e ecb1dcac …
blob a7e28e6  (DE 85):                                   sha256 ba65c248 87f56c80 …
DE's worktree: clean at 4f2aff5, and a7e28e6 IS an ancestor of it
output artifact: DOES NOT EXIST YET (checked 08:56:27Z)
```

**And the code that stamps it** (`de_multiday_gate1_runner.py:4343-4347`, inside
`_main_day`, **after** `day_split_residency_proof()` has run the whole day):

```python
payload["source_identity"] = {
    "producing_code_sha256": hashlib.sha256(
        Path(__file__).resolve().read_bytes()).hexdigest(),   # a FRESH disk read
    **carrying_commit_block(Path(__file__).resolve()), }
```

**AST census of the day path — there is no earlier capture to fall back on:**

```
day_split_residency_proof [2585-2623]: lines mentioning __file__ -> []
run_day                   [2214-2426]: lines mentioning __file__ -> []
_main_day                 [4316-4411]: -> [4344, 4346, 4347]      (post-run)
module level:             -> [44]      (sys.path.insert, not a digest)
```

**Three consequences, in order of severity:**

1. **The receipt will assert a digest for bytes that never executed.** `2089d74e…` ran;
   `ba65c248…` will be stamped. `2089d74e…` is not my inference — **DA 68 independently
   recorded it as the runner's digest at DE 84** (R-599(A), 08:28Z, before DE 85 landed),
   and I reproduce it from the pre-DE-85 blob. Two independent records of the executed bytes.
2. **The guard designed to catch this will PASS, and passes *because* of the
   replacement.** `_main_day:4363` refuses a real day when
   `producing_code_is_the_committed_bytes` is false. v13 **is** what HEAD holds, so the flag
   is TRUE. Had DE left the worktree dirty, this run would have refused after 1.5 hours.
   A guard that is satisfied by the very act it exists to detect is rule 16 from a
   direction none of the four named instances had.
3. **DE's own claim is refuted at the code.** `a7e28e6`'s message says "The running smoke
   is untouched and **cites the runner digest at launch**". There is no launch-time capture
   on the day path. The behavioural half of that sentence is TRUE — the process holds v12's
   parsed bytes and its arithmetic is unaffected (§1.4 shows the S0 change cannot reach it) —
   **the provenance half is false.**

**A falsifiable prediction, written before the artifact exists** (the REV 47 §2.3 pattern):
when the receipt lands it will carry `source_identity.producing_code_sha256 = ba65c248…`
**while** `sealed_field_names` has **7** entries and `admissibility.sd_over_abs_mean` is
**present** — both v12 fingerprints, because v12 is what ran. Those two fields inside the
same artifact are the internal proof of the mismatch, and they are non-economic, so they can
be read before 2026-09-09 without touching the seal.

**Not my call to fix, and the two routes are not symmetric.** Restoring v12's bytes at
that path before the emit would make the digest true and would then make
`producing_code_is_the_committed_bytes` FALSE, so the run refuses at 4363 and **1.5 hours
of the only sealed Gate-1 day is lost**. Letting it emit costs a receipt that misnames its
own producer, correctable in band (rule 13) against a digest two seats already hold. I state
the prices; the coordinator and DE own the choice. The durable repair is a launch-time
capture (`_main_day` entry or module import), and it is not a repair that can be applied to
this run.

**And the general form, which outlives this instance:** the runner reads its own source at
EMIT time, so **any landing to the runner during a run rewrites that run's provenance** —
BE 58 is landing 09-04 inputs and MEM lands every round. Nothing in the register or the
runbook says "do not land the runner while a day is running", and nothing in the code
notices.

---

# 1. DE 85 — the seal, the second leak, and the transient

## 1.1 The ratio is sealed — driven at depth, with the positive control

`ECONOMIC_FIELDS` is 8 names now; `_strip_economic` is key-based at every depth. On my own
plant (not DE's fixture): `sd_over_abs_mean` inside a nested list inside a dict is removed,
and after `seal(x, 6, 6)` it is present again — **the seal withholds, it does not delete.**
Runner battery **159 checks / 0 failures**, design **84 / 0**, both under my run at `daf8c5f`.

## 1.2 The second leak — RED-FIRST against v12's own code, not against a description

I lifted `arm_day_admissible` verbatim from the pre-fix blob (`a7e28e6^`) and ran both on the
same input (`n=51`, 501 draws all `5555.5555`, so sd = 0 against a non-zero mean):

```
v12: 'null sd 0 < 0.25 * |mean 5555.56| = 1388.89; Z explodes as sd -> 0'
v13: "the null's dispersion is below the declared floor of 0.25 * |mean| … THE VERDICT ONLY:
      sd, mean and their ratio are SEALED and are not quoted here, in a key or in a string"
```

**The mean and the derived floor were in plain text under `reasons`, a key the stripper
cannot reach.** The finding is real, it was DE's own, and the fix removes it. The decision
half keeps `decisions 7 < declared minimum 30` — a count and a bar, both population facts,
correctly retained.

## 1.3 The census the round asked for — can any OTHER string carry a sealed number?

**Two answers, and they are different.**

**By construction: YES, any of them can.** `_strip_economic` traverses dicts and lists and
`return o` for everything else. A string is returned untouched at every depth. **Nothing in
the seal closes this class** — only emitter discipline does, and no check enforces it.

**In fact, today: NO, none does.** Two censuses:

* **The emitter, driven.** I built arm-days through `arm_day()` with sentinel nulls
  (mean 8888, sd 4444, ratio 0.5, observed 424242.4242 — `Z = 93.464…`), sealed them, and
  scanned every string leaf for every formatted form of every sealed quantity. The
  admissible sealed arm-day has **13 string leaves** — `day`, `arm`, `status`,
  `admissibility.status`, `seal_status`, and the 8 `sealed_field_names` — and **zero
  needle hits**. Same for both refusal shapes.
* **DE's own receipt.** `p003_de_multiday_gate1_fixture_run_v14__20260906T083511Z.json`
  (as of 08:35:11Z): **459 leaves, 265 of them strings, 16 economic values** present in the
  two unsealed FIXTURE-3 arms. **No economic value appears inside any string.** The sealed
  arms' 68 string leaves come from exactly 8 field names: `arm`, `book_digest`, `day`,
  `draw_source`, `module_sha256`, `seal_status`, `sealed_field_names`, `status` — all
  identifiers, digests, dates and constants.

**So the surface is clean and the class is open.** `reasons` was the only string field ever
composed from a computed quantity, which is why it was the one that leaked. **What is
missing is the rule-15 instrument:** a check that scans the emitted artifact's strings for
the formatted forms of the sealed values would have caught the second leak without DE
having to notice it, and would catch the next string field. I wrote it in ~15 lines to
produce the census above; it belongs in the battery, not in a review.

**One caveat on coverage:** the v14 fixture contains **no refused arm-day** (all six are
`status: OK`, `reasons: []`). The refusal text is exercised by the battery and by my drive,
but **no receipt on disk demonstrates it** — so the artifact-level evidence for the fix is
mine and DE's batteries, not a landed receipt.

## 1.4 The S0 transient — MEASURED, and the double read is right

Streamed vs whole-file, same 300 MB file, in-process `ru_maxrss` deltas:

| reading | digest | baseline RSS | peak RSS | delta |
|---|---|---:|---:|---:|
| `sha256_streamed` (8 MiB chunks) | `bf5020dd3b66826e` | 17.6 MB | 33.6 MB | **+16.0 MB** |
| `hashlib.sha256(read_bytes())` | `bf5020dd3b66826e` | 17.6 MB | 303.8 MB | **+286.1 MB** |

Identical digest, **286.1 MB of highwater removed**. DE's ~290 MB figure for the real book
is the right order and the mechanism is exactly as described.

**The declined half — AGREE, and I checked the mechanism rather than the argument.**
DE says the double read is "the two ends of a check-and-use pair". At `be_cancel_axis_null.py:213-215`:

```python
buf = p.read_bytes()
digest = hashlib.sha256(buf).hexdigest()
c = pickle.loads(buf)              # THE SAME OBJECT
```

BE's loader genuinely hashes the buffer it unpickles, and `run_day` refuses when
`bk["source_sha256"] != book_sha`. **These are two independent attestations, not one read
counted twice**, and collapsing them would delete the second. DE is right to decline, and
right that changing BE's signature is not DE's to do. The remaining transient now lives in
S1 where the book is actually held — which is the declared peak, so it strengthens rather
than weakens the peak-stage story.

**And it cannot reach the running smoke**: the process parsed v12's bytes at 08:22:03Z, so
its S0 still does the whole-file read. REV 47 §2.3's two outcomes stand unchanged for
the 09-03 day.

## 1.5 **FINDING — the fixed function is not on the path that refuses in production**

`run_day` (the REAL day path) never calls `arm_day_admissible` for a too-few-decisions
refusal. It builds its own dict:

```python
adm = DESIGN.arm_day_admissible(pop["decisions"], [0.0] * 501)   # line 2297
if pop["decisions"] < params["min_decisions_per_arm_day"]:
    results.append({… "admissibility": {"admissible": False,
                                        "n_decisions": pop["decisions"],
                                        "bar": params["min_decisions_per_arm_day"]}, …})
```

**AST over `run_day` [2214-2426]: `adm` has 1 Store and 0 Loads.** It is dead, and it is
computed against a fabricated null (`[0.0] * 501`) that would report `sd_meets_floor: True`
on a null nobody drew. Dead, so harmless — but it reads as an admissibility determination
and is not one.

**The live half:** on a real day the refusal that actually fires publishes
**neither of R4's two booleans** and names the bar `bar` instead of
`min_decisions_per_arm_day`. R-599's ruling — publish R4's verdicts — is implemented in the
function the review item named, and the production refusal path routes around it. This is
the same asymmetry DE fixed in R-572(B)(3) (a consumer keying on a name gets `None` and
cannot tell "absent" from "misspelled"), one function away. DA's pre-read is immune because
it **recomputes** `dec_ok` from the book (§2.1) — which is R-235 earning its keep — but a
consumer keying on DE's published field is not.

## 1.6 **FINDING — params v5 still pins design v12 while the code is v13**

`declarations/de_multiday_gate1_params_v5.json` pins
`…design_v12__20260906T074622Z.json`, sha `c32c7245…`, and the runner's `P3_design`
precondition compares that file's digest to that pin. **Nothing anywhere binds
`DESIGN.VERSION` (now 13) to the declaration the receipt cites.** Correct for the running
smoke, which is v12 in code and in citation. From 09-04 on, a v13 run will emit a receipt
citing a v12 declaration that does not contain R17 or R18 — the pointer that must track
something that moves, one level up from the literals BE keeps being caught by.

---

# 2. DA 68 — the pre-read

## 2.1 Reproduced, and the design is right where it counts

26 checks / 0 failures under my run. On my own fixture (not DA's): a clean sealed receipt
gives `PRE_READ_VERIFIED`, `economic_absence.sealed: true`, census `clean`. The population
comes from `dec = da_decisions(scored, theta)` — **arithmetic on the book's own scores** —
and `dec_ok` is recomputed against params, never read from DE's field. That is why §1.5
cannot hurt it.

## 2.2 **YES — it can be pointed at DE 84's real receipt the moment it lands. Expect `FLAGGED`, and read two fields, not the status**

Driven. I built the receipt shape the smoke will produce — v12's, where the ratio survives
the seal — and ran the pre-read on it:

```
clean (v13-shaped) receipt  -> PRE_READ_VERIFIED  absence.sealed True   census clean
v12-shaped (ratio present)  -> FLAGGED            absence.sealed False
   leaked_field_paths: ['per_day_sealed_artifacts[0].admissibility.sd_over_abs_mean']
   the ratio's VALUE anywhere in the emission?  False
   census: 1 receipt economic value, 0 echoed, clean
```

**It runs, it is safe, and it does not echo the number.** But its verdict will be `FLAGGED`
for a reason that is **not a disagreement about the day**: DA reads DE's field list from the
SOURCE (v13, 8 fields) while the receipt was produced by v12 (7 fields). A reader taking
`FLAGGED` at face value will think the population or the seed disagreed.

**The two fields to read instead of `status`:** `n_arms_agreeing == n_arms_declared`
(the population/seed/status half) and `economic_absence.leaked_field_paths` (the seal half).
If the only leak path is `…admissibility.sd_over_abs_mean`, the FLAG is the version fact
from §0/§1.6 and nothing else. I state this before the receipt exists so it is not decided
after seeing it.

## 2.3 What it must NOT echo, given DE's second leak

**`admissibility.reasons`.** Under v12 — the code that is producing the 09-03 receipt right
now — an sd-refused arm-day composes `null sd … < 0.25 * |mean …| = …` into that key, and
`reasons` is not in `ECONOMIC_FIELDS`, so it survives the seal. **The pre-read does not echo
it today** (it emits `receipt_admissibility_status` only, a status string). That is
correct — but it is correct **by omission, not by a check**, and §2.4 shows the check that
would have to notice cannot.

**R-599(B)(i) needs one word added.** The ruling says nobody quotes `sd_over_abs_mean` from
the 09-03 receipt. On the same receipt, `admissibility.reasons` carries the mean and the
floor in text on any sd-refused arm-day. **Nobody quotes that either** — and it is not
covered by the ruling as written, because the second leak was found after it.

## 2.4 **FINDING — the emitted census is blind to exactly the class DE 85 found**

`emitted_census` proves two things: no economic NAME in the emission, and no numeric VALUE
from the receipt in the emission. **Driven, on a receipt carrying `null_sd = 0.0001234` and
`null_mean = 5555.5555`:**

```
emission carrying BOTH numbers as TEXT (a verbatim echo of v12's `reasons`)
   -> names 0, receipt values 2, echoed 0, clean: TRUE
the same number as a FLOAT
   -> echoed 1, clean: FALSE
```

`econ_vals` is built with `isinstance(v, (int, float))` and intersected against the
emission's numeric leaves; **a number inside a string is in neither set.** The one class of
leak this programme discovered today is the one class this check cannot see.

**Second, smaller, and already disclosed:** on a correctly sealed receipt the economic
fields are absent, so `econ_vals` is **empty** and the value half is vacuous — `clean`
reduces to the name check. DA publishes `n_economic_values_from_the_receipt` (0 in my clean
drive, 1 in the v12-shaped drive), so the vacuity is visible in the artifact. That is
rule 10 satisfied; I note it so the "two independent checks" sentence is not read as two
checks in the case that matters.

## 2.5 **FINDING — the repaired `iff` cannot fail on the fixture, and the one state where it is false is the real 09-03 receipt**

DA's repair (`9184f14`) is right in kind: a count-pin became a floor-pin, a state-pin became
a consistency-pin. DA's own commit message names the class exactly — "a check that pins
today's value of something another seat owns … the same class as a control that cannot
fail, arriving from the other direction." **The repair lands an instance of it.**

The check asserts `present is not ratio_sealed_by_DE`. The fixture's receipt is produced by
`_sealed_de_shape_receipt` → `_strip_like_DE`, which strips using **the same
`ECONOMIC_FIELDS` the check compares against**. Driven both ways:

```
DE list carries the ratio = True   -> receipt presence False -> check True
DE list carries the ratio = False  -> receipt presence True  -> check True
```

Both reachable fixture states pass; **the check has no failing input.** The state where the
iff is genuinely false — presence True with DE's list True, i.e. **a v12-produced receipt
read under v13's list, which is precisely DE 84's 09-03 receipt** — is never constructed.
The instrument itself handles that state correctly (§2.2); it is the control that cannot
fire. A receipt stripped by a **fixed literal 7-name list**, independent of the list under
test, restores both directions.

## 2.6 **FINDING — the design pin is hardcoded to v12 and never bound to the receipt**

`_design_v12_check()` globs `p003_de_multiday_gate1_design_v12__*.json` and compares an
**8-hex prefix** (`c32c7245`). Driven: it returns `matches: True` today; **v13 is on disk**
(`…design_v13__20260906T083511Z.json`); and `"receipt" in _fn_source("_design_v12_check")`
is **False** — the check never looks at which design the receipt cites, though DE's receipts
carry it with a full sha256 (`design_declaration.sha256 = c32c7245…3ff5`, protocol
`…_V12`). `prov_ok` gates the verdict, so from 09-04 a v13-produced day will be verified
against a v12 declaration and report `provenance_all_matched: true`.

Two smaller notes in the same block: `params.expected_prefix = "306bfdb0"` is a hand-kept
copy of DE's constant compared by **8-hex prefix**, and this is the pattern REV 45 found and
BE 56 was made to fix at full length with `hmac.compare_digest`. Same standard, other seat.

---

# 3. DA 69 — the race-read verifier's real path

## 3.1 Reproduced on my own synthetic pins and feeds (3 readable + 2 absent, 24 rows/day)

| drive | result |
|---|---|
| baseline, artifact agrees | **VERIFIED**, 0 mismatches, floor `G=3, m=2, best_p 0.25, clears_0_05 False` |
| artifact's floor computed at **G = 5** | **FLAGGED** — `permutation_floor.best_possible_adjusted_p` |
| 09-01 carrying a number | **FLAGGED** — `unrecoverable_days_carrying_a_number: ['20260901']` |
| foreign protocol, 0 numeric mismatches | `is_BEs_declared_shape False` → **not a verification** |
| read artifact absent | refuses **by name**: `READ_ARTIFACT_ABSENT_THE_READ_HAS_NOT_BEEN_OPENED` |

**The gate is the artifact's existence, with no clock and no flag** — verified at the code
and by the refusal. `da_hash_and_parse_feed` accumulates the digest over exactly the lines
the parser consumes, in one open; the caller checks the pin before any number is used.

## 3.2 It would catch BE's open §1.6 defect — at the floor

My REV 48 §1.6 finding is that BE's reader takes `G` from `len(paths)`, i.e. from the
invocation. **DA takes `G` from the pins' own `exists` flags** (`g = len(readable)`), which
is where R-592's form (3) puts it. Driven: an artifact whose floor was computed at G = 5
while the pins declare 3 readable is **FLAGGED on the floor field**. The two seats' seam is
covered from DA's side; BE 59 still has to close it on BE's.

## 3.3 **FINDING — silence passes: an artifact that never mentions 09-01/02 VERIFIES**

Driven. The verifier checks that no unrecoverable day **carries a number**
(`has_number` over `_leaves(blk)`; `blk` is `None` when the day is simply absent) and that
`days_in_the_artifact == readable`. An artifact whose `days` are the three readable and
which **never names 09-01 or 09-02 anywhere** returns **VERIFIED**:

```
status VERIFIED | unrecoverable from pins ['20260901','20260902']
                | carrying a number []      | artifact mentions them? False
```

**Form (3) is a disclosure form.** R-592 ruled the five named days stay the population and
09-01/02 are **disclosed READ-BUT-UNRECOVERABLE**. The verifier enforces "not a number" and
not "disclosed", so the omission — the failure mode that quietly shrinks the population from
five named days to three — reads as a pass. Rule 11: absence must never read as a pass. The
predicate needed is `every unrecoverable day appears in the artifact with a
READ-BUT-UNRECOVERABLE status`, and it needs a known-bad that omits one.

## 3.4 The refusal order — the informative refusal sits behind a generic one

Driven, on a feed whose bytes do not match the pin **and** whose rows lack
`score_incumbent`:

```
one-arm + unpinned -> "REFUSED: feed row 1 carries no `score_incumbent`. This is a ONE-ARM feed…"
two-arm + unpinned -> "REFUSED — THE PIN DOES NOT HOLD for feed.jsonl: … hashes to c5a61039…"
```

The parse runs to completion before the pin is checked, so a **moved file** is reported as a
**malformed feed**. Nothing unsafe follows — one pass, and nothing parsed is used — but on
the race read that message points a reader at BE's emitter when the true fact is that the
bytes are not the pinned bytes. Same shape as REV 48 §1.6(2), other module.

---

# 4. R-235 — the import graph, static and at runtime

AST census of every `import`/`from` in both instruments, plus a runtime `sys.modules` diff
across a full pre-read run:

| instrument | foreign imports | runtime |
|---|---|---|
| `da_gate1_day_verdict.py` | `de_data_root` (line 1585, inside `_derived_dir`) | loaded during a pre-read |
| `da_race_read_verify.py` | `be_race_reader` (line 423, inside `selftest`) | not loaded outside the battery |

**No DE or BE statistic is imported by either instrument.** DE's field list is read from the
source **by AST** (`de_economic_fields_at_source`), which is the pattern, and it is what let
DA see R-599 land within the hour. `be_race_reader` is imported only as the **A/B
comparator**, and only `RR.day_matched_volume(feed)` on a synthetic feed DA wrote to a temp
dir; DA's own numbers come from `da_day_matched_volume`. DA's prose about BE's path is
accurate — `day_matched_volume` delegates to `be_read_cells.load_two_arm_feed` and
`C.matched_volume`.

**Two things to say out loud.**

1. **`de_data_root` is DE's code inside DA's instrument.** It is a path resolver, not a
   statistic, so R-235's substance holds — but DA and DE now agree on *where to look* by
   construction, and a root-resolution error would be invisible to both. Scoped, not a
   violation; worth a line in the receipt.
2. **`da_race_read_verify.py --selftest` imports BE's reader, and that module is one call
   away from consuming the race.** Import is safe — the only module-level call in
   `be_race_reader.py` is `main()` under an `if __name__ == "__main__"` guard (AST over
   module-level statements; my first probe walked into function bodies and said otherwise —
   corrected). But `RR.read(RR.sealed_feeds())` in that battery would open three real feeds,
   and the battery is run routinely by three seats. The runbook §6 note (`9af4785`) covers
   the reader; **it does not cover the fact that DA's battery now imports it.**

---

# 5. VERDICT, and what I did NOT establish

**DE 85 — the ruling is implemented and the second finding is DE's own, driven red.**
§1.5 and §1.6 are open: the production refusal path does not carry R4's booleans, and params
v5 still points at v12. **§0 is not DE 85's defect but it is DE 85's landing**, and it is
the only item on this filing with a clock on it.

**DA 68 — APPROVED**, and the answer to the round's question is **yes, point it at the real
receipt**, reading `n_arms_agreeing` and `leaked_field_paths` rather than `status`, and
never echoing `admissibility.reasons`. Two instrument findings (§2.4, §2.5) and one pin
(§2.6).

**DA 69 — APPROVED** with §3.3 to close before the read opens, since the day set is exactly
what form (3) is about.

**What I did not establish.** I did not read the 09-03 receipt — it does not exist — so
every statement about it is a prediction with its observable fields named. I did not open a
sealed file, a race day, or the tape; the digest attacks and the census were re-driven on
scratch fixtures and on a 300 MB file I made and deleted. I did not run the v12/v13 A/B on a
real book, so the S0 numbers are from a 300 MB synthetic, not from the 290 MB book. I cannot
verify that `2089d74e…` is what the running process holds in memory — I verify that it is
what was on disk at that path when the process started, that the file changed at 08:35:20Z,
and that DA recorded the same digest at DE 84 independently. And §1.3's "no other string
field leaks" is a statement about **today's** emitter on **today's** receipt; the class stays
open until a check enforces it.

**My own probe errors this round, both caught before they reached a claim:** my first
sentinel arm-day was inadmissible (sd 1111 against a 0.25×8888 floor) and I re-drove it; my
first module-level I/O census walked into function bodies and would have reported
`be_race_reader` as unsafe at import.

**Context: ≈12%** — 119k tokens of the 1M window by my own count. My pane's status line
carries no `% context used` field in this build (it shows the turn's token count and the
permission mode), so this is my count, not the pane's.
