# REVIEW — **all eight conjuncts are enforced and every one drives both ways; the horizon is named, not applied; and the two seats resolve the SAME supersession link by DIFFERENT FIELDS**, so exactly one of them will call the R-603 correction a day that ran twice

**Filed** 2026-09-06T09:43Z (clock read before composing) · reviewer seat (pm-codex)
· tip `f8632f1` (DE 87 `21d5df5`/`3b5ce97`/`ca2916b`/`2afae75`/`1df6015`/`66a546d`;
DA 74 `7582ebd`/`1be42c9`/`8083309`; **DA 75 `df7529b`/`9dd7957`/`90db091`/`528edbe`, which
landed since the brief was written**; rule 22 amended `0efc5a5`)
· **LIGHT AND LOCK-FREE.** `PM_DATA_ROOT=/home/yuqing/ctaNew` on every drive.

**Rule 20.** DE 84's smoke holds the lock and **I did not take it.** Heaviest steps: runner
battery **14.12 s / 50.7 MB**, design **0.98 s / 205.0 MB**, DA verdict **1.95 s / 65.7 MB**,
DA race **1.30 s / 193.3 MB**, DA book **1.00 s / 53.8 MB**. Every gate drive on synthetic
roots in `/tmp`. No sealed file, no race day, no `--open`.

**In flight:** the smoke at **1:21:45**, `active`, cgroup peak **2.532 GB** (unmoved since
08:24:41Z). Zero `de_gate1_day_run*` artifacts.

**ROUTING — CHECKED unless a line says AGREED.**

## VERDICT

| what | verdict |
|---|---|
| **The eight conjuncts** | **ENFORCED AND DRIVEN BOTH WAYS.** Positive control opens at G = 6; each of the six drivable conjuncts refuses alone; both directions of the id/evaluator correspondence refuse; a params file without the field refuses. **§1.1** |
| **The chain-head resolver** | **CORRECT, four cases driven** — and my REV 51 §1.5 is closed: a `.v2` chained to the landed digest is admitted **and** conjunct 3 still refuses a re-roll. **§1.2** |
| **The horizon** | **NAMED, NOT APPLIED**, driven a minute either side. **§1.3** |
| **REV 51 §0** | **CLOSED where it blocked** — `rehearse_smoke('2026-09-04')` blocks on the two book preconditions only; 09-03 is `READY`. **§1.4** |
| **…and still wrong where nobody looks** | design v15's `parameters.path` **still names params v2** while its digest names v8; two checks either side of it read the digest and neither reads the path. **§0** |
| **DA's id binding** | **CLOSED AT MY TIP** — all eight `bound by ID` against DE's real v8, none unevaluable, an unknown id leaves the gate closed. At DA 74 (`8083309`) it was keyword-bound to the two old strings. **§2.1** |
| **THE SEAM** | **DE resolves `supersedes` by DIGEST; DA resolves it by NAME.** Driven three ways on the same bytes: exactly one seat refuses unless the `.v2` carries **both** fields, and nothing declares that it must. **§2.3** |
| **REV 50 §1.3** | **CLOSED by name and driven.** **§2.2** |
| **REV 50 §3.4** | **a third adjacent case closed; the filed case is still open** — and part of that is my wording. **§2.5** |

---

# 0. The pin chain — closed where it blocked, still wrong where nothing reads it

**Fixed, and I verified it:** params v8 names `…design_v15__20260906T094500Z.json`, which
**exists**; `P3_design` now checks the flipped direction end to end — *the design file the
params name pins the params digest the runner reads* — and it **holds**.

**Not fixed:**

```
design v15 -> parameters.path   = live/pm_research/declarations/de_multiday_gate1_params_v2.json
              parameters.sha256 = 8c3f26766bea3af4…   <- which is params **v8**
   v2 on disk  ce46b5775d8f17a4   |   v8 on disk  8c3f26766bea3af4
```

The digest moved to v8; **the path is still the typed constant naming v2** — the second
round running, and DE's own `DECLARATION_CHAIN` comment names this exact recurrence ("that
is how v7 came to name v2").

**Why every check passes anyway.** At `de_multiday_design_declaration.py:1280` the `path` is
a literal; at `:2650` the battery's own `_pp` is a **second literal**
(`…params_v8.json`) and the check compares `d["parameters"]["sha256"]` against **that
file** — never against the file the `path` field names. `grep` for any read of
`parameters["path"]` across the runner and the design module: **no matches.** So there are
now **three literals that must move together on a params bump** — the emitted `path`, the
runner's `PARAMS_REL`, and the battery's `_pp` — two were updated and the one that is
*published in the artifact* was not, and the check written to prove "the pin is real" is
blind to it.

Not blocking anything today. Wrong in the artifact, and a consumer following the field lands
on a four-version-old declaration. **One predicate closes it for good: the file at
`parameters.path` hashes to `parameters.sha256`** — red-first against exactly today's state.

---

# 1. DE 87

## 1.1 The eight conjuncts — the positive control ADMITS, and each conjunct refuses alone

Six synthetic sealed receipts, synthetic landing records, an injected ledger, a real commit
and the runner's real blob digest for conjunct 6:

```
BASELINE (six clean days)  -> may_open True, G_if_opened 6, all seven evaluated conjuncts hold
                              declared ids 8 -> evaluators 8
```

| drive | `may_open` | failing |
|---|---|---|
| clock one minute **before** the bar | False | `1_clock` |
| 09-08's receipt removed | False | `2…`, `3…`, `4…`, `6…` |
| 09-05 **re-run after landing** (digest moved) | False | `3_digest_matches_the_landing_record` |
| **every arm inadmissible** on all six | False | `4_each_day_has_an_admissible_arm` |
| 09-07 fails `day_quality_pass` in the ledger | False | `5_ledger_verdict_holds` |
| `producing_code_sha256` in no commit | False | `6_producing_code_is_locatable` |

And the id binding, both directions plus the missing field:

```
a declared id with NO evaluator   -> REFUSED "…Declared with no evaluator: ['a_bar_nobody_evaluates']"
an evaluator with NO declared id  -> REFUSED "…evaluated but not declared: […]"
params with no conjunction field  -> REFUSED "…carries no `read_gate.the_bar_is_a_CONJUNCTION`…"
```

**Every one of my §3.3 eight is now a conjunct with an evaluator, and every evaluator has a
declared id.** The four that were open at v14 — an all-inadmissible day, the ledger verdict,
the horizon, the two-sided bar — are closed. Item 2 is closed in the strong form: the digest
is compared against **DA's pre-read as the landing record**, not against whatever is on disk
at read time.

## 1.2 The chain-head resolver — four cases, and my §1.5 is closed without losing item 2

```
a single receipt            -> PRESENT              head = the file
v1 + a CHAINED .v2          -> PRESENT_CHAIN_HEAD   head = the .v2
two UNCHAINED receipts      -> AMBIGUOUS            n = 2
a .v2 superseding NOTHING   -> DANGLING_SUPERSEDES  supersedes_sha256 = 000000000000…
```

And the property that matters, driven together: with the **v1's digest** in the landing
record and the `.v2` chained to it, `may_open` is **True** and 09-03's
`landing.matches_landing` is **True** — so rule 13's own form is admitted **while** a
re-run after landing still refuses (§1.1 row 3). That is exactly the shape I asked for and
it did not cost the anti-re-roll property.

## 1.3 The horizon — named, not applied

Five landed, the sixth absent:

```
one minute BEFORE  -> may_open False, reached False, opens_at_G_minus_one False, G_if_opened None
one minute AFTER   -> may_open False, reached True,  opens_at_G_minus_one True,  G_if_opened 5
                      the_sixth_day_is_disclosed_as ['2026-09-08']
                      verdict_if_opened_here "DIRECTIONAL ONLY -- 2^-5 = 0.03125 is not significant at m = 2"
```

`may_open` stays **False** at the horizon: the fallback is a named, dated, disclosed option,
not an automatic opening. `horizon_utc = 2026-09-09T12:00:00Z` is in params v8, declared
before any day is read. That is the stopping rule removed from the space of things that can
be chosen after seeing.

*One property worth stating because it is a deliberate narrowness:* `opens_at_G_minus_one`
requires **exactly** five landed and all four per-day conjuncts holding on all five. Four
landed at the horizon opens nothing — correct, and it means the horizon cannot be reached
half-way.

## 1.4 The rehearsals on today's real state

```
2026-09-04 -> status NOT_READY_P2_book_exists_P2_builder_receipt_exists
              blocking ['P2_book_exists', 'P2_builder_receipt_exists']
2026-09-03 -> status READY, blocking []
```

`P3_design` is gone from `blocking`. **REV 51 §0's live half is closed.**

## 1.5 DE's five self-caught defects — which a reviewer would have caught

| defect | verdict |
|---|---|
| **the shadowed parameter** (`per_day, ledger_rows = {}, {}` resetting the parameter) | **READABLE.** The signature says `ledger_rows: dict | None = None` and the next lines rebind it — a one-screen contradiction. A reviewer cross-reading signature and body catches it; the author does not, because they wrote both. |
| **the completeness check refusing the horizon case** | **READABLE, by cross-referencing two lists.** `_EVAL` maps `horizon_fallback_G5_directional → "horizon"`, and there is no `_c("horizon", …)` — the horizon is computed outside `C` by design. Exactly the comparison a reviewer makes and a writer skips. |
| **the fixture touching `data/`** | **RUN-ONLY.** Nothing in the source shows which paths a process opens; it takes the data-free instrument, which is why DE has one. |
| **`P3_params` comparing a digest with itself** | **READABLE — AND I READ PAST IT.** `_digest(repo / PARAMS_REL) == _digest(repo / PARAMS_REL)` sat two lines above the `P3_design` I filed as REV 51 §0, in the same screen I quoted. A control that cannot fail is the class I check for, and I missed it because I had already found what I came for. My miss, recorded. (Fixed: it states the digest and reports existence.) |
| **a battery emitted from stale code after an aborted checkout, twice** | **RUN-ONLY, and it is REV 49 §0's own class** — an artifact naming code that did not produce it. Nothing static reveals it; the identity fields or a re-run do. That DE caught it twice is the import-time capture doing its job on its author. |

**Three of five were readable and two needed a run** — which is roughly the split this seat
should expect, and the honest note is that I had one of the three in front of me.

---

# 2. DA 74 and DA 75

## 2.1 The id binding — the brief's question, answered at my tip

**At DA 74 (`8083309`) the conjuncts were bound by KEYWORD** against the two old sentence
strings (`CONJUNCT_EVALUATORS`, kept as the v7-and-earlier fallback). **DA 75 landed while
this round was open and rebinds by identity.** Driven against DE's **real** params v8:

```
DA reads params: de_multiday_gate1_params_v8.json   (PARAMS_PATH const: …_v8.json)
all eight conjuncts: binding = {'bound by ID'}   declared 8, evaluated 8, unevaluable []
a planted unknown id -> open: False, conjuncts_not_evaluable ['a_bar_nobody_evaluates'],
                        n_evaluated 8 of 9
```

**So the gap the brief asks about is closed**, and the "a conjunct nobody evaluates CLOSES
the gate" property is real on DA's side as well as DE's. DA's params pin is also now v8 — my
REV 51 §2.6 closed, and closed better than I proposed: it is bound to the **receipt's** own
params citation, with INCOMPLETE rather than a fallback when the receipt names none.

## 2.2 REV 50 §1.3 — closed by name, driven

```
builder_index_call(at_commit='deadbeef…').source
  -> "working tree (FALLBACK -- the requested commit did not resolve: deadbeef…)"
check_seam(receipt naming that commit)
  -> PROVENANCE_INCOMPLETE_BUILDER_COMMIT_UNRESOLVED, contradicts_the_code: None
```

The fallback is labelled where it happens and the verdict refuses to be computed from it.
That is the repair I asked for, and the message no longer names a commit it did not read.

## 2.3 **FINDING — the two seats resolve the same supersession link by different fields**

DE (`find_sealed_day_receipt`) follows `supersedes.sha256`. DA (`_resolve_day`) follows
`supersedes.path` or `supersedes.artifact` — **a NAME**. Driven on identical bytes, with each
seat's own resolver:

| `supersedes` carries | DA says | DE says |
|---|---|---|
| `{sha256}` — DE's requirement, and the shape DE's battery drives | **AMBIGUOUS** | PRESENT_CHAIN_HEAD |
| `{path}` — DA's requirement | CHAIN_HEAD | **AMBIGUOUS** |
| `{sha256, path}` | CHAIN_HEAD | PRESENT_CHAIN_HEAD |

**Unless the `.v2` carries both fields, exactly one seat calls the R-603 correction "a day
that ran twice" — and the correction is due as soon as the smoke lands.** On DA's side that
is not a cosmetic disagreement: conjunct 2 reports `holds: false`, so DA's independent read
of the gate says the bar is not met while DE's says it is.

Nothing declares the shape. Params v8's `receipt_naming` fixes the FILENAME convention and
says nothing about `supersedes`. The fix is a declared shape — both fields, with the digest
authoritative and the path informative — and a check on each side that **refuses a
`supersedes` block missing the field that side resolves by**, rather than silently treating
it as no link. Two implementations are the point (R-235); two implementations of an
undeclared shape is how they agree perfectly on nothing.

## 2.4 A second seam in the same conjunct: the landing record has no name and no correction path

Conjunct 3 compares against **DA's pre-read**, located by
`glob("p003_da_gate1_pre_read*.json")` plus a matching `day` field. Two things follow, and
neither is declared anywhere:

* **DA's real per-day pre-read filename is not in any declaration.** Params v8 declares the
  runner's own receipt naming and says nothing about the landing record. If DA emits under a
  name outside that glob, conjunct 3 fails for **every** day and the gate never opens.
* **`landing_record_for` returns AMBIGUOUS on two matches for a day, and there is no chain
  resolution for landing records** — the very fix DE just built for its own receipts. A
  re-run pre-read, or a `.v2` of one, closes the gate.

*Checked today:* the only file matching the glob is DA's battery receipt
`p003_da_gate1_pre_read_fixture__20260906T082803Z.json`, which carries **no `day` field**, so
`landing_record_for('2026-09-03')` correctly returns `NO_LANDING_RECORD` — no false landing
record exists. The hazard is forward-looking, and it is the first thing conjunct 3 will meet.

## 2.5 §3.4 — a third adjacent case closed, the filed case still open, and part of that is mine

DA 75 closed the **CLI** cases — an absent pins file, an unreadable one, one carrying no
days, an unreadable read artifact — all previously bare tracebacks, now refusing by name
with exit 2 and driven through real subprocess invocations with a good invocation as the
positive control. That is a real improvement and the third distinct instance of "an
informative refusal behind a generic one" to be fixed.

**The case I filed is untouched. Driven at this tip:**

```
a feed that is PRESENT, whose bytes do NOT match the pin, whose rows are one-arm
  -> "REFUSED: feed row 1 carries no `score_incumbent`. This is a ONE-ARM feed…"
```

`da_hash_and_parse_feed` still raises the shape refusal inside the parse loop, before
`da_day_from_pinned_feed` compares the digest. **A moved file is still reported as a
malformed feed.**

**And I should own my share.** My §3.4 stated the class ("the informative refusal sits behind
a generic one") in its title and the case only in its drive. Two seats have now read the
title and fixed a neighbouring instance in good faith. From here I will name the case in the
item, not the class: *"a present file whose bytes do not match the pin refuses as ONE-ARM
before the pin is compared."*

## 2.6 §2.2 restated on the calendar — **AGREED, not re-derived.** DA 74 restates my REV 50
§2.2 (the serial chain is not binding; each of 09-06..09-08 is gated by its own calendar
completion) on the calendar. I verified that reading once, at DA 72's own schedule, in REV 50;
I did not re-drive it this round.

---

# 3. VERDICT, and what I did NOT establish

**DE 87 — APPROVED.** R-604's eight are declared with stable ids, each has an evaluator, each
evaluator has a declared id, both directions of that correspondence refuse, and six of the
eight drive to failure individually with a positive control that opens. The horizon is
declared with its date and named rather than applied. §0 is the one thing left, and it is a
field nothing reads and no check covers.

**DA 74/75 — APPROVED**, with §2.3 as the gating item: it is a cross-seat shape disagreement
on the artifact the whole read gate now depends on, and it fires the moment the R-603
correction lands. §2.4 is the same conjunct's other end. §2.5 stands open, named properly
this time.

**What I did not establish.** No real day, book, tape or sealed receipt — none exists; every
gate drive was on synthetic receipts, synthetic landing records, an injected ledger and a
14-byte "book" in `/tmp`. Conjunct 5's production path reads the ledger through DESIGN's own
reader, which refuses any root but the canonical one, so I drove it **only** through the
injected `ledger_rows` seam — the real path is AGREED from the code, not driven. I did not
re-derive DA 72's calendar (§2.6). I did not run the book tier. And my §2.3 table is what
each seat's resolver does with a `supersedes` block I constructed; which shape DE will
actually emit for the 09-03 `.v2` is not yet decided — that is why it is worth declaring
before it is written.

**Two probe errors of my own this round**, both caught before they reached a claim: I keyed
DA's `per_day` by `2026-09-03` when it is keyed by `20260903`, and my first extraction of
DA's conjunct rows guessed the wrong container and reported `None` where the answer was
`AMBIGUOUS`.

**Context: ≈21%** — 215k tokens of the 1M window by my own count; this build's pane status
line carries no `% context used` field, so it is my count, not the pane's.
