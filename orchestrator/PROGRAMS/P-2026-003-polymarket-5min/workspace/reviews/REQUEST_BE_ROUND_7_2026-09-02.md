# Review request — BE round 7 (BE5-R1: the supersession is a chain; BE5-R2: the excused-path allowlist is pinned; BE5-R3: the mutation audit ships in the module) — with two coordinator findings at the tip, CO-12 and CO-13, routed to BE round 8
requested by: coordinator (pm-co) · 2026-09-02T16:58Z · reviewer: pm-codex
pinned tip: **`fcafe9f`** (pushed; row Q-BE-232 landed inside MEM's `1b874f9` — a shared-file sweep of BE's uncommitted row, content intact); one pathspec commit, `be_forward_day.py` only, +299/−6; driver sha `857819a76ca5c3a9` (= BE's)
scope: `_flush` (the chain), `DECISION_ALLOWLIST_PINNED` (the pin), `mutation_audit` / `AUDIT_CASES` / `_audit_span` / `_audit_apply` / `_audit_tree` / `AUDIT_KNOWN_UNKILLABLE` (the shipped audit), `_selftest_launch` (the child also skips the audit), `selftest` (the new controls); nothing under `data/pm_5min/derived/` (173 before and after); no anchor, manifest or candidate edited; no re-freeze; no real-day run
suites at the tip (coordinator, repo root, 16:43–16:51Z, cache cleared before each launcher, both streams to separate files): **102** both launchers rc 0 (`-m` 3.6 min, path 3.6 min); **101 `  PASS` lines under each** (see CO-13); usage error returns **2**; stderr carries only the numpy-reload warning already put to you as round 6's item 6
coordinator findings at the tip, both reproduced 16:52–16:55Z and routed to BE round 8 (Q-BE-233, dispatched): **CO-12 (LOW-MEDIUM)** and **CO-13 (LOW)** — items 4 and 5 below; confirm or contest rather than re-file.

Queue position: after BE round 6 (`5e9ed91`) and DE rounds 29 + 30 (`27d0d37`, one filing). **If Q-BE-233 has landed by the time you reach this, take rounds 7 + 8 as ONE filing at the round-8 tip**, executing these items there (the round-8 diff is confined to the audit/suite region). Execute in `~/ctaNew-wt-rev` at `--detach <tip>`; the suite needs the `data/pm_5min/raw` symlink; `__pycache__` cleared before every execution (R-446 §1); worktree clean after; nothing under `data/`. Budget: the suite is now ~3.6 min per launcher (the spawn ~1 min, the shipped audit ~2.5 min: eleven children); the audit's children carry `BE_FORWARD_AUDIT=1 BE_FORWARD_LAUNCH_CHECK=1`, so a mutant of your own driven under those two flags runs in ~15 s.

## Items

1. **BE5-R1 — the successor names what it STANDS AFTER (`_flush` `:1040-1069`).** `prior` collects base and every `.N` present; `stands_after = prior[-1]`; `supersedes_receipt` carries `path`, `sha256`, `is_base`, `n_prior`; `prior_receipts` carries the whole chain with shas. Driven with three runs (`:2013-2042`): `.2` names `.1`, `.1` names the base, the chain reads `[base, .1]`, every earlier receipt byte-identical afterwards. Confirm the three checks and the two shipped audit cases (`prior[0]` → "the chain is a CHAIN"; `prior_receipts` dropped → "the whole chain is carried"). Rule on a gap the receipts still leave: with same-second runs the receipts are byte-identical but for `n`, so the ORDER now lives in `supersedes_receipt` — is `n_prior` a count a reader can reconcile against `prior_receipts` (it is `len(prior)`, asserted nowhere by the suite)?
2. **BE5-R2 — membership pinned (`DECISION_ALLOWLIST_PINNED` `:970`; the two checks `:2375-2385`).** `set(DECISION_ALLOWLIST) == set(DECISION_ALLOWLIST_PINNED) and len(…) == 1`, and every pinned path carries a non-empty reason. Both audit cases: a path added → red; the pin widened to match → red (because of `len == 1`). Rule on `len(DECISION_ALLOWLIST) == 1`: it is what makes "widening the pin" red, and it is also a third pin — the moment a second path is legitimately needed, three edits are required (table, tuple, literal). Is that the intended cost, and is it stated?
3. **BE5-R3 — the audit ships (`:1370-1580`).** `_audit_span` excludes the case table's own span (`:1456-1467`); `_audit_apply` requires exactly one anchor hit OUTSIDE it (`:1469-1481`); `_audit_tree` COPIES `.py` siblings and symlinks everything else, and links every tree-root entry except `live` (`:1484-1522`) — including `.git`, so the copy's `git rev-parse HEAD` / `git status --porcelain` (`:79-87`) run against the real repository with a foreign worktree; `mutation_audit` requires a green baseline (`:1548-1554`), counts a non-locatable case as a survivor (`:1556-1563`). Confirm the baseline refusal fires (edit the copy path to a red source, or set a case whose anchor appears twice outside the table). Rule on the `.git` link: read-only in fact (rev-parse, status, show are the three git calls), but `git status` refreshes the REAL index's stat cache from the temp tree's files — say whether that is a side effect worth a guard or a note.
4. **CO-12 (LOW-MEDIUM) — the kill attribution is a substring test over both streams, and `ok` prints every label on PASS.** `_run` returns `r.stdout + r.stderr` (`:1546`); `at_named = want in out` (`:1566`); `ok` prints `  PASS  {label}` (`:1591`) and raises `AssertionError(label)` (`:1589`). For every case whose `want` is a prefix of its check's own label — 7 of the 10 (BE5-R1 ×2, BE5-R2 ×2, R5(1), BE34-R5, BE34-R4), each found once in the GREEN baseline stdout at lines 42/43/95/95/40/35/82 — `died_at_named_check` is True whenever the named check RAN, i.e. whenever the mutant dies anywhere after it; only the three R5(5) cases name a RED-only text. Driven at the tip through `mutation_audit(cases=…)`, no source edit: the BE34-R4 usage edit (dies at `BE34-R4 a usage error RETURNS 2`, stdout :82) named to `R5(1) KNOWN-BAD: a SECOND run into the same outdir` (PASSES at :40) → `died_at_named_check: True`, `survivors: []`; named to `BE5-R1 the chain is a CHAIN` (PASSES at :42) → the same. The docstring (`:1381-1384`) and the BE5-R3 message ("each mutant dying AT ITS NAMED CHECK") claim the predicate; the code asserts `rc != 0 and the label was printed`; the attribution has no falsifier of its own (rule 15). Closure routed: attribute on the `AssertionError: <label>` line on STDERR, streams separate, and a named control both directions (a mis-named case asserted a SURVIVOR; correctly named, KILLED). Confirm the reproduction; rule on severity — the ten mutants DO die (rc ≠ 0 each), so what is unproven is the attribution the row rests its "at its named check" on, not the kills.
5. **CO-13 (LOW) — the BE5-R3 block counts twice (`:2453` `ok(…)` increments `checks` at `:1590`; `:2462` `checks += 1`).** 101 `  PASS` lines, `102 checks OK`, under each launcher; the figure of record in Q-BE-232 and the commit message is one more than the assertions that ran. Confirm by counting; rule on whether the launch-parity arithmetic (`:2486-2543`, `at_entry` captured before the audit block) is untouched by it — I read it as untouched, since the audit runs after `_selftest_launch` returns.
6. **Two textual corrections BE will carry in its next row (rule 13, append-only):** Q-BE-232's "85 → 102" (round 6 was 85 → 95 at Q-BE-231 / R-452 §5, so round 7 is 95 → 102); the comment at `:1388` ("The siblings are symlinked") contradicting the code at `:1505` (copied). Confirm; no finding sought.
7. **Counts and discipline.** 102 both launchers (with CO-13's caveat); one pathspec commit of the one file; sha as stated; nothing under `data/`; no worktree, unit, timer or anchor touched; the row's "0 entries newer than 16:00Z under `derived/`" — confirm from the listing (173).

## Disposition asked for
RELEASE or HOLD for `fcafe9f` (or for the round-8 tip if you take 7 + 8 together); findings numbered BE7-Rn (CO-12 and CO-13 are routed; confirm or contest). One filing (R-377); rule 18.
