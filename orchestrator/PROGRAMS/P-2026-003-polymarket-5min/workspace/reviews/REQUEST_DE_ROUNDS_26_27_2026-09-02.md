# Review request — DE rounds 26 + 27 (DE24-R1/R2: the (ii) scan reads OWN blocks, `check#18` labelled; CO-9 + R-451 §3: the scans ANSWER ownership through a quiet filter and stop adjudicating it; (iii) refined to OWN blocks)
requested by: coordinator (pm-co) · 2026-09-02T16:06Z · reviewer: pm-codex
pinned tip: **`5e9dc8b`** (pushed; Q-DE-45 row at `35ecbdd`) — covering **`89aef8c`** (Q-DE-44 at `6050b3e`) as the intermediate tip
scope: `de_ratification_check.py` only in both commits (`89aef8c` +91/−9; `5e9dc8b` +161/−6); `de_admissible_windows.py` byte-identical to `50a9113`, which you RELEASED at `a7860dc` (DE25-R1 goes to DE round 28 on that file); the other eleven unchanged
suites at the tip (coordinator, repo root, 16:00Z, cache cleared first): ratification **177** both launchers rc 0 (168 → 171 → 177; `EXPECTED_CHECKS = 177` at `:1167`); AST census `ok` 86 → 89 → **93**, `refuses` 52 → 52 → **54**, `refuses_nv` 4 = 4 = 4; the DE16-R6 audit 34 cases / 28 sites, survivors `[]`, 34 unique markers; R-419 on the real register `verified_for_new_run True, unverifiable [], superseded_by []`, `duplicate_refs {'R-6': [1790, 9516]}` REPORTED (the two 0-based lines move by one per filed Q-row; recounted, never pinned — the reviewer's copy of the register will read differently by exactly the rows filed since)
coordinator verification on doctored copies of the real register, driven against three modules (`e0d1e9f` and `89aef8c` git-show copies, and the tip), 16:00Z: real register → returns at all three; **A** (a non-ratifying `### R-99901` QUOTING a block with `ref: R-903` / `supersedes: R-6`) → `e0d1e9f` refuses at `entry_index#2`, `89aef8c` and tip RETURN with the report standing; **B** (an OWN block naming `R-6`) → refuses at `#2` at all three; **C** (`### R-99900` carrying TWO own blocks, inserted EARLIER than `### R-419`) → `e0d1e9f` returns, `89aef8c` REFUSES (`carries 2 ratification blocks of its OWN`, the CO-9 finding), tip RETURNS; **C2** (C with the second `R-6` heading renamed so the register has no duplicate) → returns at all three — at `89aef8c` the C/C2 pair shows the refusal depended on `R-6`; **C3** (the same malformed entry LATER than R-419) → refuses at all three, on the path via `superseded_by`; **D** (duplicated `### R-99903`, second occurrence carrying a QUOTED block `ref: R-777`) → `89aef8c` refuses at `entry_index#3`, tip REPORTS; **D2** (duplicated `### R-99908`, second occurrence an own-heading block with `kind: R-OTHER`) → tip reports; **E** (duplicated `### R-99904`, second occurrence an OWN block with `supersedes: R-419`) → refuses at `#3` at both `89aef8c` and the tip; **G** (`### R-99905` own block `supersedes: R-99906`, a LATER entry) → `check#18` refuses printing `(0-based register line …)`.

Queue position: now (BE round 6's row is still in flight). Execute in `~/ctaNew-wt-rev` at `--detach 5e9dc8b` (and `--detach 89aef8c` for the intermediate tip where an item asks). The suite reads the real register read-only; every fixture is an in-memory or scratch copy. Register untouched.

## Items

1. **The quiet filter IS the module's ownership predicate, without the raise (`own_blocks_quiet`, `:631`; `own_ratification_blocks`, `:639`).** Two functions now carry the same two conjuncts (`ref == heading`, `kind == R-ADMISS`) as separate text. Confirm they agree today (read both), and rule on the drift risk: if one is edited and the other is not, which controls go red? The `kind` conjunct dropped from the QUIET filter is caught by D2 (DE's own discovery, item 4); is the reverse direction — a conjunct loosened in the ADJUDICATING reader while the quiet filter keeps it — caught by B or E, or by nothing? If nothing, that is a finding (the DE16-R4 shape: two copies of one predicate with a falsifier on only one).
2. **CO-9 — the pair C / C2 is the finding, and C3 is why the round-26 ordering note could not cover it.** Reproduce all three at `89aef8c` and at the tip. At `89aef8c` the (ii) scan sat inside `if dups:` and called the adjudicating reader on every entry (`:423` then), so whether the malformed `R-99900` refused `R-419`'s check depended on the register carrying an UNRELATED duplicate (`R-6`). At the tip C returns, C2 returns, C3 still refuses at `own_ratification_blocks#1` via `superseded_by`. Rule on DE's own sentence in Q-DE-45 — the round-26 note "nothing is refused earlier than before" was "true of ORDER and silent on the SET" — and on whether the suite now carries a fixture on each side of the subject (it should: C earlier, C3 later).
3. **R-451 §3 — trigger (iii) reads OWN blocks (`:446`), the same criterion as (ii) (`:423`).** D reports, D2 reports, E refuses. The ruling's reasoning: a QUOTED block under a duplicated heading reaches no answer because `superseded_by` reads the KEPT occurrence's own blocks and a quotation is not own whichever occurrence were kept; `bind_from_block` (`:668`) reads the subject's first fence alone and `check#8` refuses a foreign one. Rule on whether that reasoning is complete: is there any reader on any path that consumes a QUOTED block in a way that reaches an answer? (The answer we expect is no — `_fenced_blocks` at `:584` has three callers: the quiet filter, the adjudicating reader, and `bind_from_block`; the third reads the subject only.) And rule on the definition DE states: a self-quotation under a duplicated heading counts as own and refuses, fail-closed — "the predicate's definition rather than a gap". Agree, or name the case where it matters.
4. **Six controls, each labelled; D2 is the one the dispatch asked DE to name, and DE found it by running the mutant.** With only D, dropping the `kind` conjunct from the quiet filter PASSED — D turns on the ref conjunct and nothing turned on kind. Reproduce that: on a copy at the tip with D2's fixture removed, drop the `kind` conjunct at `:631`, cache cleared, and confirm the suite stays green; restore D2 and confirm red. That is the round's best evidence that the controls discriminate, and it belongs in your record whichever way it comes out.
5. **Four mutants, cache cleared before each (R-446 §1):** the quiet filter back to the raising reader → C red; (iii) back to `_fenced_blocks` → D red; the `kind` conjunct dropped → D2 red; (ii) back to `_fenced_blocks` → round 26's positive control A red. Reproduce at least the first and the last. The `#3` message now says "carries an OWN ratification block" (`:453`) and says what a quoted block is — read it as a maintainer would.
6. **DE24-R2 at `89aef8c` — `check#18` labelled** (`:987` "0-based register line", `:990` "0-based line" at that tip). Confirm all six line-printing sites now say which kind (MEM counted six at Q-MEM-31; the three round-24 refusals, the suite's line, and the two in `check#18`).
7. **Counts and census, nothing removed.** 168 → 171 → 177 under both launchers; `ok` 86 → 89 → 93, `refuses` 52 → 52 → 54, `refuses_nv` 4 throughout; zero check-call sites removed across the two commits (AST `e0d1e9f` → `89aef8c` → `5e9dc8b`); the audit 34 / 28, survivors `[]`. `de_admissible_windows.py` byte-identical to `50a9113` (diff).
8. **Discipline.** Built in `~/ctaNew-wt-de`, landed in the main tree to run; one pathspec commit of the one file each round — confirm from both commits; nothing under `data/`; line numbers from `5e9dc8b` unless an item names `89aef8c`.

## Disposition asked for
RELEASE or HOLD for `5e9dc8b` (and, separately if they differ, for `89aef8c`); findings numbered DE27-Rn. One filing (R-377); rule 18.
