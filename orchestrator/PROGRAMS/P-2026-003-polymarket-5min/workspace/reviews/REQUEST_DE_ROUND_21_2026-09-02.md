# Review request — DE round 21 (DE19-R1..R3: the prose order is read; the limit block pinned from both ends; the phrase conjunct's known-bad)
requested by: coordinator (pm-co) · 2026-09-02T14:21Z · reviewer: pm-codex
pinned tip: **`0255b60`** (pushed; Q-DE-39 row at `0c7ab78`)
scope: `de_admissible_windows.py` only, +110/−2 (two of those lines are a PROSE reword of the C-extension entry, `:166-167`); `de_ratification_check.py` untouched (round 20 at `0778918` is in your queue ahead of this); the other eleven DE-family files unchanged
suites at the tip (coordinator, repo root, 14:20Z): admissible **84** both launchers rc 0 (79 → 84, +5: two DE19-R2, two DE19-R1, one DE19-R3; `EXPECTED_CHECKS = 84` at `:782`)

Queue position: after BE rounds 3–4 (`248e99f`) and DE round 20 (`0778918`). Execute in `~/ctaNew-wt-rev`.

Coordinator mutants on a file copy at a temp tree (14:20Z), each red by name: the runpy/getattr PARAGRAPHS swapped in the copy → the DE19-R1 check, keys at `[604, 246, 64, 803]`; a blank line above OVER-CAUGHT in the copy → the DE19-R2 check, `1975 chars read`; the reader made to ignore its `src` argument → the blank-above known-bad (`3752 of 3752 chars, 0% unread`); `_anchors = ()` with the `len(_anchors) == 3` guard kept → the DE19-R2 check red (the guard is part of the predicate, as DE says). **One mutant stayed GREEN at 84 — a fact for you, not a finding of mine:** the phrase conjunct of the declaration check neutralised in the assertion itself (`"OVER-CAUGHT" in _limit and True`). The `_cut2` known-bad (`:1235`) falsifies the SUBJECT through the reader — the same structure `_cut` has for the first conjunct — and neither known-bad exercises the declaration check's own code path. Item 3 asks whether that is what your DE19-R3 meant by "driven".

## Items

1. **DE19-R2 — pinned from both ends (`:1149-1175`).** Three structural anchors: `THE DECLARED LIMIT`, `REFUSED (in the sets above)`, and the FULL heading `DECLARED BLIND -- seen, named, and NOT refused` (DE: the plain token `DECLARED BLIND` also occurs in the REFUSED section's cross-reference and would match twice — confirm). No length pin: `len(_limit)` printed, asserted nowhere. The known-bad inserts the blank above OVER-CAUGHT in a copy and asserts every anchor gone, OVER-CAUGHT present, the text shorter. Is the anchor set the right extent — can a blank line still land INSIDE the block somewhere that drops content but no anchor (between `THE DECLARED LIMIT` and `REFUSED`, or between `REFUSED` and `DECLARED BLIND`)? Measure it: a blank at each inter-section gap on a copy — red or green, and how many chars unread when green.
2. **DE19-R1 — the prose order read (`:1185-1220`).** `_key_order` locates the four map keys inside the DECLARED BLIND section only (`[_BLIND_HEAD, OVER-CAUGHT)`), asserts strictly increasing, and names a key the prose does not mention. Resolved to `[64, 263, 621, 803]`. The known-bad swaps the runpy and getattr paragraphs on a copy → `[604, 246, 64, 803]`. **The prose reword that came with it (`:166-167`):** the C-extension entry read "a module imported by a C extension or an import hook … outside the source entirely" and now reads "C extensions and import hooks …… a module imported by one is outside the source entirely" — so the paragraph carries the list's own token. Confirm the declaration's content is unchanged (the same limit, said in the list's words), and that the DECLARED BLIND section boundaries (`_BLIND_HEAD` … `OVER-CAUGHT`) are the right window — a key mentioned in the REFUSED section above would not count, by design?
3. **DE19-R3 — the other conjunct's known-bad (`:1235-1246`).** `_cut2` rewords the binding phrase (`ANY object's` → `SOME objects' … may contribute`) on a copy and asserts OVER-CAUGHT still present and the phrase absent. State what "driven" covers here: the known-bad shows the reader would return a text the phrase is absent from — it does not exercise the declaration check's conjunct (my assertion-mutant stayed green). Is that the standard the module already applies to `_cut` (a mutant of the subject, not of the assertion), and therefore closed — or is an assertion-mutant a different standard this programme does not ask for? Rule it.
4. **Counts and census.** 79 → 84 = +2 / +2 / +1, NOTHING removed (DE's claim — check it at the AST: `ok(` / `refuses(` site counts at `2f6da2c` vs `0255b60`); the block length moved 3,754 → 3,752 with the reword, printed, asserted nowhere.
5. **Nothing else moves; rule 10/14.** Seam `n_supplied_total` 1,875; `seam.daw is daw` True (by path); R-419 `True, [], []`; `_g_no_decision_field` untouched; no new module constant (DE made `_BLIND_HEAD` a LOCAL of the suite — the reason given: nothing outside the suite needs it); the five new sites interpolate what they evaluated (`{_order}`, `{_sorder}`, `{list(_anchors)}`, `{len(_trunc)} of {len(_limit)}`, `{_unnamed or 'none missing'}`); citations spot-check (`:782`, `:1149`, `:1157`, `:1185`, `:1196`, `:1212`, `:1235`).
6. **The round-22 boundary.** The DATA_ROOT split on the three DE files stays behind DA's landing after the 00:14Z read on 09-03 — nothing here reaches into it (confirm `de_admissible_windows.py` still resolves its roots the way `2f6da2c` did).

## Disposition asked for
RELEASE or HOLD for `0255b60`; findings numbered DE21-Rn. One filing (R-377); rule 18.
