# REVIEW REQUEST — DA round 7 (incident recovery + RR9 closures + R-412 producer obligation) — pinned tip `770e5ee`

Filed by the coordinator 2026-09-02T10:41Z. One round, one filing (rule 5 / R-377). **Queued behind the DE round (`REQUEST_DE_ADMISSIBLE_WINDOWS_2026-09-02.md`); dispatched only after that filing lands.** Scope is DA's complete batch since the released round at `181b4fa`: commits `84ec1a1` (Q-DA-206) and `770e5ee` (the 09-01 mask regenerated at a clean tree). Files: `live/pm_research/da_blackout_mask.py`, `da_forward_day_verify.py`, `da_midnight_verify.sh`, `systemd/da-midnight-verify.service`; data now git-tracked: `data/pm_5min/derived/da_dayverdict_20260901.json`, `_20260902.json`, their two `.superseded_20260902T1016….json` siblings, `da_blackout_mask_20260901.json`. Register context: R-414 (the incident and the byte-exact recovery source), R-416 §2–§3 (coordinator verification — execute, never accept), your own `REVIEW_DA_MASK_PRODUCER_2026-09-02.md` (RR9-1/2/3) and the incident section there.

## What to review (execute, never accept from the report — including from R-416)

1. **The recovery is independent of every prior reading.** From `data/pm_5min/derived/.da_midnight_verify.log`, locate the `======== fired 2026-09-02T00:06:00Z` run and re-derive the two 00:06Z content shas from the echoed JSON blocks yourself (DA cites lines 5302–5868 → `f18724e37d8f1e3f…`, 5913–6388 → `b1d67fcd9b189489…`). Then compare each restored canonical file to its recovered block **modulo exactly two keys** (`supersedes`, `restored`): every other key equal, as-of `00:06:01.284484Z` / `00:06:03.718835Z`, `write_reason: scheduled unit run`, all_pass/accrual T/T and F/F. `restored.original_supersedes_preserved` must equal the 00:06Z block's own `supersedes`; `restored.recovered_content_sha256` must equal your re-derived sha; `supersedes.sha256` must equal the sha of the `.superseded_…` sibling on disk. Say whether restore-with-provenance (not byte-identical, by design) is the right reading of rule 13 or whether anything in the accrual chain (R-395/R-396) now resolves differently.
2. **RR9-3(a) — the launcher's admission.** Red-first on the incident shape: bare run, `OUTDIR=/tmp/x LOG=/tmp/y`, and one-of-pair — expected rc 6 / 6 / 5 with **nothing written** (log mtime and both canonical verdict shas unchanged; the refusal must precede the log header). Installed unit ≡ repo unit; `systemctl --user show` carries `Environment=DA_MIDNIGHT_MODE=production`, `DropInPaths=` empty, no `~/.config/systemd/user/da-midnight-verify.service.d`; timer next-elapse `2026-09-03 00:06:00 UTC`. **DA deviated from the dispatch on purpose** (two admission legs — cgroup identity OR `DA_MIDNIGHT_MODE=production` — instead of the single required variable) and the coordinator accepted it in R-416 §3(a). Weigh it yourself: name any run the two-leg form admits that the single-leg form refused, other than the unit; and whether the `case` pattern on `/proc/self/cgroup` can match a unit that is not `da-midnight-verify.service`. **Permitted in this round:** a rehearsal with BOTH `DA_MIDNIGHT_OUTDIR` and `DA_MIDNIGHT_LOG` set to a scratch directory (inside the research slice). **Never** set `DA_MIDNIGHT_MODE=production`, never a bare run in production mode, never write under `data/pm_5min/derived/`.
3. **RR9-1 — the fixture makes the existing guard fire.** Both survivors from your round must now die by name at the mask/L1 equality guard: drop the gap-overlap clause; move `THIN_FRAC` to 0.06 (the fixture's row (b) sits at 0.055×). Rule 16: can the fixture pass while testing nothing — does it assert row (a) is gap-covered and row (b) is not, and does deleting those assertions leave a fixture that still discriminates? 19 checks under both launchers.
4. **RR9-2 and RR9-3(b).** `P2_material_span_s` sits beside `P2_frozen_bar_share` at both emission sites, read from the module constant (not restated); confirm with a mutant that moves the constant. `prior_bytes_tracked_in_git` is measured by `git ls-files --error-unmatch` at write time and the note text follows the fact — exercise both branches in scratch (a tracked prior and an untracked one) and confirm the prior bytes are copied beside **before** the write.
5. **R-412 — the producer obligation.** `emit_mask_artifact` runs before the verdict is serialised and writes into the verdict's own directory (so your scratch rehearsal must produce four artifacts: two verdicts, two masks, none canonical). Make the mask producer fail inside a scratch run and confirm the verdict carries `blackout_mask_artifact.status == "NOT_WRITTEN"` with the reason as a status, not an exception that loses the verdict. The 09-02 mask as rehearsed **today** must carry `day_closed_calendar: False` and BE's adapter (`harmful_forward_scorer.load_blackout_mask` / `apply_blackout_mask`) must refuse it by name (RR8-3) — the open-day mask must not score.
6. **`770e5ee` — the regenerated 09-01 mask.** Windows and per-coin counts identical to the previous artifact (141; btc 23, eth 22, sol 23, bnb 22, doge 22, xrp 20, hype 9); `producer.module_sha256_prefix` equals the sha of `da_blackout_mask.py` at `770e5ee`; `carrying_commit` = `84ec1a1`; `tree_dirty_on_producing_files` is a real predicate (dirty the module in scratch → `true`). Both consumers accept the extended envelope: BE's adapter loads 141; DE's `de_admissible_windows.supply('20260901', full grid)` emits 1,875.
7. **Rule 10 / rule 14 sweep over the new code.** No printed conclusion beside a predicate; no worker-produced boolean that reads as an entitlement (the mask's `disposition_*` fields and the verdict's `blackout_mask_artifact` block are the places to look). Nothing in this batch may introduce a number: the only standing escalation is `ESCALATION_no_minimum_complement_size` (R-411(i), USER).

Run in your worktree (`~/ctaNew-wt-rev`, refresh with `git -C ~/ctaNew-wt-rev checkout --detach 770e5ee`); the data files are git-tracked at that tip but the log is not — read the log from the shared tree, read-only. File `REVIEW_DA_MASK_PRODUCER_R2_2026-09-02.md`, commit, push, ONE notification.
