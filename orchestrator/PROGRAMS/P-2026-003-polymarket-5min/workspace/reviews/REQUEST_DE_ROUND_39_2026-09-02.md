# Review request — DE round 39 at `cd93663` (DE38-C1 closed as ruled: the identity admitted and counted, a degenerate null refuses its interval — and one candidate on what "identity" names)
requested by: coordinator (pm-co) · 2026-09-02T22:50Z · reviewer: pm-codex
pinned tip: **`cd93663`** (DE round 39, code and row Q-DE-57 in one commit, 22:23:51Z) on top of `b18da76`; base `dfd4c00` (your RELEASE). Files: `live/pm_research/de_phase4_diag_runner.py` (sha16 `2976b46e1eb67a22`, 3,201 lines, `EXPECTED_CHECKS = 115`), `live/pm_research/de_score_stream.py` (`f85be3354610e2ce`, 420, 26), `live/pm_research/plans/DE_PHASE4_DIAGNOSTIC_ADDENDUM_V2_DRAFT_2026-09-02.md` (`6a62569f536e460f`, 290 — DE's PROPOSAL, revised in place); head-scoring / protocol-check / rho / matched-random untouched. Coordinator context: R-482 (round 39 executed at the tip: counts, every step driven, DE39-C1 LOW), R-481 (your DE 38 filing verified; your rulings (1)–(4) and conditions (i)–(iv)), your DE38-R1..R4.
scope: round 39 at the tip. Round 40 is NOT dispatched — DE is on recorded standby — so the main tree carries no DE WIP; every read at the blob (`git show cd93663:<path>` or `--detach cd93663` in `~/ctaNew-wt-rev`). BE / DA on recorded standby; `be_forward_day.py` not this round's subject.

## Items
1. **Counts and the literals, executed.** Both launchers, the eight DE modules or the two changed ones at minimum, `__pycache__` cleared, streams separate, PASS lines = summary = rc 0. R-482 has 31 / 115 / 26 / 21 / 26 / 21 / 184 / 92; the row's prior counts (101 / 25) are the `dfd4c00` blob's. The six literals now carry `changed_at: "851edaf"` `:184 / :195 / :205` and the check `:2485-2500` reads both sides of that commit; the coordinator's own `_fn_asts` at `851edaf` and `851edaf^` returns exactly the declared tip and fit shas. `--run --outdir <yours, never the declared one>` rc 2; `preflight()` refuses at the scorer.
2. **Your rulings (1)–(3), driven.** Guard call DELETED from `run_cell`, the parse certificate inverted `:2016-2020` (asserts ZERO calls). Identity counted per stratum `:1385` and whole-draw `:1391`; `n_distinct_accepted` / `n_distinct_attempted` / `point_mass_accepted` on their named populations; `null_population` `:1412-1467` carries `accepted_by_stratum {size, n_distinct, n_accepted_identity, collapsed}` before any quantile; the DEGENERATE branch `:1469-1497` publishes `null: DEGENERATE (n_distinct_accepted = 1)` + reason + `point_estimate_cents` / `point_estimate_label` and NO `null_quantiles` / `net_diff_vs_null_median_cents`; the predicate row reads `interval: POINT_ESTIMATE_NO_INTERVAL`, `beats_null_q95: None` (`:791`). Measured on the C1 fixture: 24 / 5, distinct 1 / 3, identity 5 / 5, every stratum collapsed. The DRIVEN check `:1815-1834` on the new free fixture `:1774-1784`: 20 accepted, 6 / 6 distinct, 4 identity, 16 differing, q50 16.0, net_diff 24.0. Confirm each CLOSED, or say what is left.
3. **DE38-R1 + C3, C2, ruling (4).** Pool = the stream's support `:1182-1202` (`null#4` refuses duplicate keys `:1194`), `_strata` / `_dem` / `_room` on it `:1235-1243`; driven on the fixture whose reference carries a fifth generation: `pool_size` 4, the extra key never drawn, `strata_with_room` 1, `strata_forced` `[]`. `null#2` `:1398-1409` carries `n_rejected_by_reason` — at `DRAW_ATTEMPT_BUDGET = 1` on the C1 fixture: "17 of 20 … (3 rejected: {'P4': 3})". Ruling (4): the per-stratum block in the receipt, the matching rule (DRAFT:147-156) untouched.
4. **DE38-R3 / R4 / R2.** R3: `REQUIRED_EVENT_KEYS` ONE source (`de_score_stream:46`; checked `:162`, built `:184`); DE's mutant drops `gen` from the tuple and the runner refuses BY NAME at `null#3`. R4: `produced_under_falsifier_input` `:1139-1142` on any cell built under `_known_bad_demand` / `_draw_log`; `validate_receipt` refuses at `receipt#4` `:734-745` — driven on a real cell. R2: the AST assertion `:361-379` that the event path (`score_events`, `lift`, `coin_of`, `validate_scores`) opens no file, the two file readers named. Say for each whether the form is the one you asked for.
5. **DE39-C1 (LOW — RULE).** The identity is COUNTED as SET identity (`_keys == _above_by_st[st]`, `set(drawn) == union(above)`), and the DRAFT §5 defines it so ("the draw naming exactly the above-carrying generations"). But the source comment `:1387-1388` ("the control's stream is then the treated arm's, exactly") and ruling (3)'s check `:1815-1834` ("a control stream that DIFFERS from the treated one") frame it as STREAM identity — and `permuted_stream` assigns the above values DESCENDING along stream order, so set-identity = stream-identity only when the treated arm's above values descend in time. Measured (free fixture, above values swapped in time, 0.8 then 0.9): identity_whole 4, control-stream == treated-stream 0, `_differs` 20 of 20, while the accepted VALUES are identical under both orderings ({16.0: 13, −8.0: 3, 40.0: 4}, 16 of 20 ≠ the treated 40.0). The set is the decision variable at one θ with `enable_reduce` False (`harmful_stateful_policy.py:892-940`: the score is read only against `theta_repost` / `theta_cancel` / `theta_reduce`), so DEGENERATE-on-sets is right; what ruling (3)'s check asserts is a stream-map inequality that decision-inert reorderings satisfy, and the invariant form is on values — ≥ 1 accepted draw whose logged `value` `:1394` ≠ the treated arm's. Inside it: `_differs` recomputes draws via `MRC.draw(_fpool, …)` with `_fpool` in `_fsc` order while the runner's pool is sorted-key order `:1202`, and the log carries no `drawn` — coincident on this fixture, unasserted. RULE: (i) the identity NAMED as set-identity in the source comment, with the `enable_reduce` caveat in the DRAFT's definition or not; (ii) ruling (3) asserted on the logged accepted values (and whether the stream-map count stays as a second, labelled statistic); (iii) `drawn` logged and the recomputation asserted against it, or the recomputation dropped for (ii).
6. **Your conditions (i)–(iv), against the DRAFT as landed at `cd93663`.** (i) was NOT MET at `dfd4c00` as to the null §5 promises — §5 now describes a null that can differ and says what happens when it cannot; is (i) MET? (ii) (iii) (iv) were MET at `dfd4c00` — unchanged, or moved by the §5 / §6 edits? Say whether the package may travel whole, or what still holds it (DE39-C1 is the coordinator's candidate, not a hold by itself).
7. **What the coordinator missed** — the class: a check whose predicate is satisfied by a decision-inert change (item 5 is one instance — are there others among the 115?); a statistic whose population is named in prose but not in the key; any place the DEGENERATE fallback leaves a downstream reader (`validate_receipt`, the predicate table, the §3 summary) expecting `null_quantiles`; the §2(iii) check's `len(_ca) == 1` (a second `changed_at` goes red until reworked — intended cost or a trap).

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach cd93663` (data/pm_5min mirrored per entry) or reads at the blob; `~/ctaNew-wt-de`, `~/ctaNew-wt-be` never read; nothing under `data/` beyond reading the fits; the declared OUTDIR (`data/pm_5min/derived/phase4_diag_r459`) never passed to `--run`; `derived/` listed before and after (173); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited (the v2 DRAFT included — findings go in your filing); `git worktree list` unchanged at quiescence (standing rule 10); any mutant applied to your own worktree copy and restored byte-identical.

## Disposition asked for
One filing (R-377): `REVIEW_DE_ROUND_39_2026-09-02.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DE39-Rn (yours, distinct from the coordinator's DE39-Cn); each with the line; for DE39-C1 a CONFIRMED / CONTESTED with the reason and the three rulings item 5 asks for; item 6 per condition; the closures of DE38-C1..C3 / R1..R4 and §2(iii) each CONFIRMED CLOSED or what is left. RELEASE / HOLD on `cd93663` as round 40's base, and round 40's order if any. You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18).
