# Review request — DE round 41 at `8479b67` (DE40-R1, DE40-R2, DE40-R3 closed and driven; the package untouched in the USER's hands)
requested by: coordinator (pm-co) · 2026-09-03T00:52Z · reviewer: pm-codex
pinned tip: **`8479b67`** (DE round 41, code and row Q-DE-59 in one commit, 23:46:12Z; the row's heading names DE's pre-landing sha `d156efc` — index by `8479b67`) on base **`35452c0`** (your RELEASE at `c3f8743`). Files: `live/pm_research/de_phase4_diag_runner.py` (sha16 `384c64dee08de879`, 3,455 lines, `EXPECTED_CHECKS = 124`, +171 / −44) — the ONLY file changed; the v2 DRAFT (`cb693000880c3d94`, 307 lines) NOT edited; score-stream (`f85be3354610e2ce`, 26) untouched. Coordinator context: R-487 (A) (round 41 executed at the tip, the coordinator's four mutants, one LOW observation), R-484 (the package FORWARDED at `35452c0`), your DE 40 filing (`c3f8743`: DE40-R1 LOW-MEDIUM, DE40-R2 LOW, DE40-R3 LOW; item 6's answer on ask (5)).
scope: round 41 at the tip, BEHIND the package: the USER holds the DRAFT and its five asks (ask (5) with your cost reading, R-485); nothing here re-opens the forwarding. Round 42 is NOT dispatched — DE is on recorded standby, so the main tree carries no DE WIP; every read at the blob (`git show 8479b67:<path>` or `--detach 8479b67` in `~/ctaNew-wt-rev`). BE round 12 and DA round 20 are OPEN in their own worktrees on `c511750` (your RELEASE) — `~/ctaNew-wt-be` and `~/ctaNew-wt-da` never read; `be_forward_day.py` never run (standing rule 9). Your step 5 (the assembly's expensive half) stays USER-gated.

## Items
1. **Counts and the literals, executed.** Both launchers, the eight DE modules, `__pycache__` cleared, streams separate, PASS lines = summary = rc 0. R-487 (A) has 31 / 124 / 26 / 21 / 26 / 21 / 184 / 92 — including `de_admissible_windows` 92, which DE could not measure in its own worktree (no `raw/`; it refuses BY NAME `CONTENT_LIVENESS_UNRESOLVED for 20260827 (no raw directory)`); the row's prior counts (119 / 26) are the `35452c0` blob's. `--run --outdir <yours, never the declared one>` rc 2, nothing created; `preflight()` refuses at the scorer.
2. **DE40-R1, driven on two groups.** `declaration_groups(declared=None)` `:507-537` — injectable for the fixture only, the run passes the real map; the fixture `:2728-2757` holds the real `851edaf` group (three functions) and a REAL second declaring commit, `46ab455 → label_rows` in `harmful_exposure_rows.py` (tip `905975dceed925f0`, parent `4a4403ee715d88f7`, both recomputed from the blobs), with the known-bad editing the FIT sha of the SECOND group and the mismatch named on that group alone (`['label_rows@46ab455^']`) while the first stays green. Coordinator's mutant: `break` after the first group → red at *DE40-R1, DRIVEN ON TWO GROUPS … yields 1 groups*. Is the falsifier the one you asked for — does "check only the first group" now die, and is `46ab455` the right second group (it is that file's prior change in `git log`)?
3. **DE40-R2, the cell says what it asked for.** `run_cell` records `n_draws_requested` `:1211`; `_null_status` `:804-832` reads it: 0 → `NO_NULL_REQUESTED`; >0 + DEGENERATE → `NULL_COLLAPSED`; >0 + quantiles → `NULL_SAMPLED`; field absent → REFUSE at `pred#1` `:815`; >0 with neither → REFUSE at `pred#2` `:827` (unreachable from the runner while `null#2` is unchanged). Driven on 20 / 20 / 0 requested plus both known-bads (PASS lines 37–39). Coordinator's mutant (the round-40 derivation restored) passes the three-state check and dies at the `pred#1` known-bad. Your item-5 question — a cell that requested draws and accepted none — now reads `NULL_COLLAPSED` (requested >0, DEGENERATE): is that the state the reader needs, or is the fourth state still missing?
4. **DE40-R3, the whole block minus one named statistic.** The invariance `:1974-2024` compares EVERY field of `null_population` with `n_accepted_stream_differs` excluded BY NAME (`_ORDER_DEPENDENT`), the key sets, and the predicate row (a refusal while building it lands in THIS check, not a traceback): "ALL 20 of the 21 fields identical", the excluded field asserted to DIFFER (16 → 20). Coordinator's mutants: `n_draws_requested` not recorded → red at DE40-R3 through the predicate branch, by name; a 22nd order-dependent field added → red with the assertion unedited (the block grows to 22). **One LOW observation to weigh, not a round:** under that last mutant the FAIL label reads "leaves ALL 21 of the 22 `null_population` fields identical" — the count is computed from the filtered block BEFORE the comparison, so a red line reports the invariant as holding; the label should carry the number of fields that DIFFER. Say whether R3 is closed as you measured it, and whether the label is a round-42 item or noise.
5. **What the coordinator missed — the class.** Any field of the receipt outside `null_population` whose value depends on the order of decision-inert inputs; whether `declaration_groups`' grouping key (`changed_at`) can merge two declaring commits that touch the same function at different shas; whether the six DE-40 mutants plus the coordinator's four leave any of the three closures with a single falsifier only.
6. **Round 42.** RELEASE / HOLD `8479b67` as round 42's base. Round 42 waits for the USER's ruling on the five asks; your DE 40 item-6 answer (what in the runner must move for each possible answer to ask (5)) — say whether it still holds at `8479b67` line for line, or what moved.

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach 8479b67` (data/pm_5min mirrored per entry — the `de_admissible_windows` 92 needs `raw/`) or reads at the blob; `~/ctaNew-wt-da`, `~/ctaNew-wt-be`, `~/ctaNew-wt-de` never read; `be_forward_day.py` never run; nothing under `data/` beyond reading; the declared Phase-4 OUTDIR `data/pm_5min/derived/phase4_diag_r459` never passed to `--run`; `derived/` listed before and after (178 on the main tree at 00:47Z); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited (the v2 DRAFT is the USER's); `git worktree list` unchanged at quiescence (standing rule 10); any mutant applied to your own worktree copy and restored byte-identical.

## Disposition asked for
One filing (R-377): `REVIEW_DE_ROUND_41_2026-09-03.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DE41-Rn; each with the line; items 2–4 as CLOSED / what is left; item 6 as RELEASE / HOLD. You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18).
