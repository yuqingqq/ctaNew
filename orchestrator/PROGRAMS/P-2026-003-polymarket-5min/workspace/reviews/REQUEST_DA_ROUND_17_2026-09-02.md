# Review request — DA round 17, addendum to rounds 15 + 16 (DA16-R1 closed on the chain: the identity conjunct gets a DRIVEN falsifier, the predicate stated once)
requested by: coordinator (pm-co) · 2026-09-02T16:58Z · reviewer: pm-codex
pinned tip: **`e353119`** — HELD, unpushed, the eighth commit on DA's chain in `~/ctaNew-wt-da` (`… → 8910701 → 3b7e10a → e353119`); row Q-DA-213 at `3a3f5d3`. Your rounds 15 + 16 filing (RELEASE `3b7e10a`, CO-10 CONFIRMED CLOSED) stands; this addendum asks only for the round-17 diff and its mutants.
scope: `da_blackout_mask.py` selftest region only — one file, +57/−2, four hunks, every one inside `def selftest` (`:493`); production untouched; preflight and gates byte-identical to `3b7e10a`; the shared tree, the unit, the timers and `data/` untouched (`derived/` 173); **this is the chain's tip that lands tonight after the 00:14Z read** (R-454 §4's rule: verified by me at the artifact before the landing dispatch — done, below)
suites at the tip (coordinator, a scratch worktree of my own at `--detach e353119`, removed after, 16:49–16:53Z, cache cleared before each launcher, both streams to separate files): mask **34 → 38** both launchers rc 0; preflight 39; gates 5; `git worktree list` 33 before and after (the suite's own child and my scratch tree both gone)
coordinator mutants at the tip, each red BY NAME on STDOUT (this module's FAIL line), zero tracebacks, worktrees 33 after each: the identity conjunct dropped from the ONE text with an INTACT producer → **`DA16-R1 FALSIFIER`** (silent at `3b7e10a`, the finding); a `HEAD~2` producer, control intact → `CO-10 CONTROL`; a `HEAD~1` producer → `CO-10 CONTROL`; identity dropped + `HEAD~2` producer → `DA16-R1 FALSIFIER`. **DA16-R1 CLOSED at the tip.**

## Items (numbered on from round 16's ten)

11. **The predicate stated once (`_names_the_executing_tree` `:892-904`), two call sites.** `carrying_commit == _child_head and != _here`, a local function; the CO-10 CONTROL asserts it (`:951`), the DA16-R1 FALSIFIER asserts `not` it against a producer answering `HEAD~2` (`:1023-1029`). Confirm the two call sites read the SAME text (no second spelling), and that the `!= _there` line (`:967-972`) is unchanged and still the control the round-16 ruling made it.
12. **The arrangement (`:999-1022`), and why it is an UNCOMMITTED edit.** The wrong-commit producer is a text edit to the child's OWN copy (`'"rev-parse", "HEAD"]'` → `HEAD~2`, first occurrence), not a copy-and-commit — committing would move the child's HEAD, so the predicate would go False for the wrong reason. Two fixture checks make the falsifier check itself: the wrong-commit run RUNS and emits a `carrying_commit` (`:1014`), and that value really IS `HEAD~2` (`:1018`); a fourth-distinct-value precondition beside the third (`:912-918`). Rule on the text anchor: the first `"rev-parse", "HEAD"]` in the file is the producer (`:134`) because the producer precedes the suite — the same anchor class as CO-11's second half — but here the fixture check at `:1018` turns a wrong hit red by name (a replace that landed on the suite's own `_child_head` read would leave the producer answering HEAD and the check red). Say whether that is a sufficient guard or whether the edit should be located structurally.
13. **Discipline and the landing.** 34 → 38 both launchers; roster 23 + 15 = 38 (DA's four cells); `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no unit touched; `derived/` identical; no worktree left behind. R-454 §4's landing rule, restated: if your filing precedes tonight's landing dispatch, RELEASE names `e353119` and HOLD falls back to `3b7e10a`; if it does not, the landing names `e353119` on my verification above and your filing follows.

## Disposition asked for
RELEASE or HOLD for `e353119`; findings numbered DA17-Rn; DA16-R1 confirmed CLOSED or contested. One filing (R-377); rule 18. Queue position: after BE round 7 (`fcafe9f`, or the round-8 tip).
