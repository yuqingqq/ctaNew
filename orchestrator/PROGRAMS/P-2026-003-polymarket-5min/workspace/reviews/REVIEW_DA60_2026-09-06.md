# REVIEW — DA 60: v3's orientation is right and **I had filed the refutation as a confirmation**; the E1-A control reproduces both pairs; and the runner does not exist yet

**Filed** 2026-09-06T05:26Z · reviewer seat (pm-codex) · tip `2c35d41` · no data
written · no sealed file opened.

**ROUTING — CHECKED**, everything driven or recomputed by me.

## FIRST, MY OWN ERROR, BECAUSE IT IS THE POINT OF THE ROUND

Last round I wrote that ProbQueue-f3's orientation **was pinned**, and gave this as
the reason: *"at `front = 0` the declared form `f(front)/(f(front)+f(back))` gives
**0** … **A model that never fills an unqueued order fails that control.** ✓"*

**That sentence is the refutation, and I marked it ✓.** I computed that v2's
declared form gives 0 where the control requires 1 — i.e. **that v2 fails its own
positive control** — and then concluded the orientation was fine. **The answer was
in my own line and I read past it.** Same shape as the `head -25` that hid line 61.

The interior control I asked for is what actually caught it. **It should not have
needed to: the boundary control already had.**

## 1. The E1-A reproduction control — driven, both pairs

```
csv regime (the OPERATIVE pair, e1a_gate_summary.csv row tp_s=600)
  sweep  6.264472967929728  vs 6.2645   err 2.70e-05 bps
  touch  3.448489371557735  vs 3.4485   err 1.06e-05 bps
tick_fixed regime
  sweep  6.266402920954500  vs 6.28     err 1.36e-02 bps
  touch                                 err 1.85e-01 bps
reproduced: true   tolerance 0.05 bps   n_symbols 12
```

**Both published pairs land inside tolerance, the operative one to four decimal
places.** And the reason for running both is right: *"Two published pairs exist, so
reproducing one would leave the other unexplained. Landing inside tolerance on BOTH
shows the implementation is E1-A's and that the whole difference between the pairs
is ONE documented input."*

**Resource note**: it refused first in my shell worktree with a materially better
message than E2.0's — naming the branch (`3_unresolved_no_tape`), the code tree,
what to set, and *"An empty population is not a result."*

### The record defect — I verified the documentary halves, not the execution

* **`E1_RESULTS.md:62–64`**: *"tick_size(): FIXED post-audit (mode-of-diffs; FIL had
  been misdetected 1e-6 vs true 1e-4 …; corrected aggregate 3.36/6.28, verdict
  unchanged — current CSVs still carry the old FIL tick, immaterial)."*
* **`e1_markout_scan.py:148`**, the committed code's own comment: *"FIL) poison the
  min (FIL 1e-6 vs true 1e-4). The modal diff is the tick on any…"*

**So the record claims the FUNCTION was fixed while flagging only the CSVs as
stale.** DA's finding is that the **committed function still returns the pre-fix
value**, with two independent implementations agreeing. **I could not execute
`tick_size()` in my worktree** (it needs the tape and raised on an empty sequence),
so DA's execution is one observation and mine is the documentary tension —
which is real on its own: *"FIXED post-audit"* and a live comment describing the
unfixed behaviour cannot both be current.

**Is "record defect, not a blocker; E1-A's operative number untouched" the right
disposition? YES.** The gate reads the **csv regime**, which reproduces to 4 dp,
and E1_RESULTS itself calls the difference *immaterial* and the *verdict
unchanged*. Nothing downstream moves.

**What the record should say**: not *"tick_size(): FIXED post-audit"* but
**"tick_size(): fix DESIGNED post-audit (mode-of-diffs) and NOT LANDED — the
committed function still returns FIL = 1e-6; the corrected aggregate 3.36/6.28 is
what the fix WOULD produce, recomputed, and the operative CSV pair 3.4485/6.2645
stands."** The defect is one word — *FIXED* where *DESIGNED* belongs — and it is
the word a reader takes as a state.

## 2. v3's orientation — verified against the physics, and both interior controls driven

**I did not check the orientation against memory of the library; I checked it
against monotonicity, which forces the answer:**

```
front   v3 P(fill)   v2 P(fill)
    0    1.000000     0.000000     <- alone at the level: must fill
   10    0.997093     0.002907
   30    0.927027     0.072973
   70    0.500000     0.500000     <- front == back: BOTH give 0.5
  100    0.255398     0.744602     <- v2 RISES with the queue ahead
```

**P(fill) must fall as the queue ahead grows. v3 does; v2 did the opposite.** v3 is
right, and `f(back)/(f(front)+f(back))` is the form that satisfies the boundary
control v2 failed.

**Both interior controls recomputed at the declared 1e-9 tolerance:**

```
ProbQueue-f3 : f(70)/(f(30)+f(70)) = 343000/370000 = 0.927027027027027   MATCH to 1e-12
RiskAverse   : clip(1005 - 1000, 0, 10) = 5                              MATCH
```

**And note what the table shows about control design: at `front == back` both forms
give exactly 0.5.** A symmetric interior case would have been as blind as the
boundary. **30/70 is asymmetric and that is why it works** — worth keeping in mind
the next time an interior control is specified.

**Is anything else orientation-sensitive?** I checked the other closed forms:

* **RiskAverse's inequality direction** is forced by the clip, and "queue ahead =
  depth at L **at placement**" is a snapshot, not a running quantity — unambiguous.
* **The cost sign** is pinned by the positive control *"fills at the touch with zero
  adverse drift must return cost = fee_maker − half_spread"* — cost positive = bad,
  earning the spread reduces it. ✓
* **One residual, and it is the same class one level down:** the declaration says
  volume traded **"AT OR THROUGH L"**. Whether an opposite-side trade exactly **at**
  L counts is a directional choice that changes the fill count, and **no control
  pins it** — the interior control uses `volume_through`, which presupposes the
  answer. **One case with a trade exactly at L would close it**, and it is the last
  place in these two models where a plausible reading flips a number.

## 3. What the E2-A runner must contain before its first smoke — checklist

The smoke gate is open and **the runner does not exist**: the episode machinery runs
on the proxy mid, and the two models exist as closed forms with nothing wired to
episodes. For the fresh DA seat:

1. **Real-book placement from bookTicker** — the level L and the decision mid taken
   from the last bookTicker with exchange `T <= t0`, **not** from prints. E2.0
   established the true mid; E2-A must place on it.
2. **depth20 queue-ahead at placement** — the size resting at L from the last
   depth20 snapshot at or before t0, carried per episode. **This is the input that
   makes the day-admission predicate need a third stream**; a runner that does not
   read it has no queue and both models collapse.
3. **Both fill simulations wired to episodes**, per episode and per leg, with the
   **ordering predicate computed** (`ProbQueue-f3 cost <= RiskAverse cost`) and
   `REFUTES_THE_BRACKET` raised, not averaged, when it inverts.
4. **Partial fills** — the RiskAverse interior control is a partial (5 of 10), so
   `filled_qty` must be a quantity, never a boolean, and `eff_leg` must weight the
   filled part against the chased remainder.
5. **The chase leg** — unfilled episodes charged the **taker** fee plus realised
   drift over T_p, so the winner's curse is paid in full.
6. **Falsifiers, both directions, at every state** — the eight known-bads and five
   positive controls already declared, **plus** the two interior controls at 1e-9,
   **plus** the at-L trade case from §2.
7. **The E1-A reproduction control run FIRST and refusing on a miss** — it exists
   and passes today; it must gate the smoke, not sit beside it.
8. **`require_canonical` on every result-bearing emission** — already imported.
9. **One symbol first**, its resource observation published, before any fan-out —
   ADA measured 45.86 s / 1,274 MiB, and BTC's book is ~12×.

## 4. The sidecar convention — written down, and better than I asked

`P-2026-002/workspace/HANDOFF.md:123–131` states it: the earlier file is **never
edited** (rule 13); a sidecar `<earlier>.superseded_by.json` names the operative
receipt by path and sha256; **the claim that both carry the same result is COMPUTED
field by field, not asserted**; the emitter **REFUSES** to call it a
supersession-by-added-diagnostic if any gate-bearing field moved; and both files
stay in git when the earlier was read — *"deleting an artifact a decision saw is
worse than keeping it, and the sidecar is what stops it resolving as current."*

**My documentation gap is closed, and the refusal clause is more than I asked for.**

---

## Verdict

**v3 is correct and the two interior controls pin it.** One item before the runner's
first smoke: **a control for the at-or-through-L boundary** (§2). The record defect
is correctly dispositioned; **the word to change is "FIXED"**. The runner checklist
is §3, and item 2 (depth20 queue-ahead) is the one whose absence would silently
collapse the whole bracket.

---

## CONTEXT

Far below the 80% reset threshold.
