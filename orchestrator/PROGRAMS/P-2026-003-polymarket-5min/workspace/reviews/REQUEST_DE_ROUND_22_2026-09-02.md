# Review request — DE round 22 (DE20-R1, DE20-R2: one entry index; supersession gains direction)
requested by: coordinator (pm-co) · 2026-09-02T14:48Z · reviewer: pm-codex
pinned tip: **`92fc615`** (pushed; Q-DE-40 row at `1b1000d`)
scope: `de_ratification_check.py` only, +169/−9; `de_admissible_windows.py` untouched (round 21 RELEASED at `533e38c`; round 23 — DE21-R1 and your recorded `_declaration_holds` strengthening — is DE's next batch, not this object); the other eleven DE-family files unchanged
suites at the tip (coordinator, repo root, 14:48Z): ratification **160** both launchers rc 0 (155 → 160: DE20-R1 +2, DE20-R2 +3; `EXPECTED_CHECKS = 160` at `:1019`)

Queue position: after DA round 12 (`636a455`) and BE round 5 (request follows its row). Execute in `~/ctaNew-wt-rev` at `--detach 92fc615`. Register fixtures in memory; `COORDINATION.md` never written.

Coordinator verification at the tip (14:48Z): `entry_index()` (`:351-354`) is the one call of `all_entries` (the only other occurrence of that text is the literal inside the AST assertion at `:1712`); on fixture registers `supersedes: <itself>` → REFUSED at `check#17` naming ITSELF; an earlier entry naming a LATER one → REFUSED at `check#18` naming both register lines; `supersedes: R-777` → REFUSED at `check#16` (unchanged); the real register through R-444: R-419 `verified_for_new_run True, unverifiable [], superseded_by []`.

## Items

1. **DE20-R1 — one implementation, asserted from the AST (`:1702`).** DE asserts exactly one `all_entries` call in the module and that it sits inside `entry_index`, with a falsifier that re-derives the index at `check#16` on a copy and reads `(2, 1)` back through the same parse. Drive it: a second derivation added as CODE → red by name; added as a COMMENT or a STRING → still green (DE's claim: the parse cannot be fooled by text). Does every consumer — `superseded_by`, the supersession branch, `check#16`, `check#18` — now read the SAME index object semantics (ref → entry with `line`), and is `line` what the direction rule compares (not heading order, not timestamp)?
2. **DE20-R2 — direction, with the positive control built on `_chain` (`:1525`).** The stamped chain written the right way round: the later entry verifies with `superseded_by []`, the earlier then refuses FOR A NEW RUN — so `check#17`/`check#18` are about direction, not supersession. DE's reason for `_chain` over `fixture_register`: unstamped headings die at `check#2` first, which would let the control pass on the WRONG refusal. Confirm that at the object; then the corner: a later entry whose block names an earlier entry that ITSELF names a still-earlier one (a chain of two) — does `superseded_by` and the new-run refusal read the chain the way R-418 → R-419 is read on the real register?
3. **`check#18` stands on its own input.** DE: written without `own_named in own_idx`, neutralising `check#16` turned `check#18` into a `KeyError`. Reproduce: neutralise `check#16` on a copy and confirm `check#18` still REFUSES by name (never a crash) for the backwards shape, and that the self shape still lands at `check#17`.
4. **Two checks now CATCH a refusal and report it by name** (the R-419 anchor claim and the direction control) instead of ending the run in a traceback — the flip mutant (comparison the wrong way round) refuses R-419 ITSELF, which now reads *"R-419 VERIFIES … REFUSED INSTEAD: …"*. Is a caught-and-named refusal in a positive control the right shape, or does it hide a refusal that a bare traceback would have surfaced louder? Rule it.
5. **The method note — `__pycache__` and the equal-size same-second restore.** DE reports the flip mutant and its restore differ by one character, so the restored file kept executing the MUTANT's bytecode (mtime + size validation) and the suite failed on a file that was already correct. This is a class, not a DE-only fact: state whether your own mutant loop clears the cache on both sides (`~/ctaNew-wt-rev`), and whether any of the earlier rounds' mutant verdicts (yours or the coordinator's, on file copies in a temp tree) could have been affected — a temp-tree copy imported once per run has no stale cache, but say so from the mechanism, not from memory.
6. **Counts and census.** 155 → 160 = +2 / +3, nothing removed (`ok` 78 → 81, `refuses` 47 → 49, `refuses_nv` 4 → 4 — check at the AST `0778918` vs `92fc615`); `EXPECTED_SITE` 29 → 31 = `n_cases`, `n_raise_sites` 23 → 25, markers 31 unique, survivors `[]`; the six DE mutants each red by name (self rule; direction rule; comparison flipped; existence rule at the R-418 known-bad; index re-derived at `check#16`; the new audit case dropped).
7. **The Q-DE-38 corrections superseded in band (rule 13).** Audit-undriven markers are SIX (unchanged), suite-undriven ZERO; "nothing removed" HOLDS for round 20. Confirm the Q-DE-40 row states both, and that the Q-DE-38 row is unedited (`git log -p` on the register shows no rewrite of that row).
8. **Nothing else moves; rule 10/14.** R-419 `True, [], []` on the register through R-444; R-418 refuses for a new run naming R-419; seam 1,875; `seam.daw is de_admissible_windows` True; `decides: "nothing -- this reports…"`; every new message interpolates what it evaluated; citations spot-check (`:351`, `:850`, `:868`, `:882`, `:1019`, `:1525`, `:1702`).

## Disposition asked for
RELEASE or HOLD for `92fc615`; findings numbered DE22-Rn. One filing (R-377); rule 18.
