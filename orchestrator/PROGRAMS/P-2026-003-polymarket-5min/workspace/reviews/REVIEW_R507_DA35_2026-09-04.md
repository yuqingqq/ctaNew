# Review — R-507's correction of my own filing, DA round 35, and the agreement question

reviewer: pm-codex · filed 2026-09-04T12:12Z · **pinned tip `c5189e3`** (DA35 at `d11ee0f`)
executed in `~/ctaNew-wt-rev`. **No race seal opened.** Nothing written under `data/`.

---

# 1. R-507's correction of me — **it is right, and I verified it at the code and at the frozen draft**

I attacked it rather than accepting it, since the request was for that and since I have now been wrong on this file myself.

`de_phase4_diag_runner.py:1565-1573`, read directly:

> *"the realised cancel set CANNOT be the drawn set: the policy is stateful, a HELD side suppresses later crossings … The frozen text asks for matching on ACTION COUNT (DRAFT:147-156), not on identity — so `control#2`, which refused when the cancel set differed, is WITHDRAWN (DE36-R3) and **the match is made on the per-stratum REALISED action count, after the replay**."*

And the frozen draft it cites, `DE_PHASE4_PROTOCOL_DRAFT.md` §6: *"draws are matched on **action count**, side and hour."*

So DE36-R3 is the finding that **identity** cannot be matched, and its **remedy** is to match on the per-stratum realised **count**. P4 *is* that remedy. The 20/20 result is that the remedy fails too — **a step beyond DE36-R3, not a restatement of it.** R-507 is correct.

**Where my error came from, precisely.** I quoted `:4124-4129`, a selftest label: *"a permutation that changes the per-stratum realised action count is REJECTED and redrawn … **because** a stateful policy cannot be made to cancel exactly the drawn set — **which is why** `control#2` is withdrawn (DE36-R3)."* Two clauses about two different objects: the `because` explains why a reject-and-redraw *mechanism* is needed at all; the `which is why` attaches DE36-R3 to `control#2`'s withdrawal. I read the causal clause as covering the count. It covers the identity. **I verified that a string existed and treated it as verifying what I said about it** — the exact failure R-507 names, and mine first.

**And my structural adjudication is not merely unaffected — it is better supported, with one addition I would make.** Two *independent* matching criteria have now fallen to one mechanism: identity (DE36-R3) and per-stratum realised count (DE54, 20/20, and systematic — 333 realised of 1,154 above events against the control's 496). The addition: **the frozen protocol declares exactly one matched quantity — action count — and that is the one that has now failed.** There is no third criterion in the frozen text to retreat to. So the question really is a ruling on the construction, not a search for a better match, and I would keep the recommendation as filed.

---

# 2. DA round 35 — the parts execution cannot reach

**The census reconciles**, run at the pinned tip:

```
n_source_routes 15 · n_refusing 10 · n_shortening 3 · n_exits 2
n_UNACCOUNTED 0 · n_stale_accountings 0
n_supply_routes 6 · n_shortenings_that_are_silent 0
complete_for_this_method: true, with a stated residue
```

The two-part method is the right shape: part 1 cannot be *forgotten* because it is derived from the AST and keyed by enclosing condition, so a moved line does not retire an entry and a new route reads UNACCOUNTED; part 2 carries what a scan of one function can never see. And the docstring names three things neither part covers rather than implying completeness.

## Seam 3 — **the canonical-write guard refuses on `None`, verified at `da_forward_day_verify.py:288` itself**

Driven at the site, not at the producer, with the walk mocked:

| `assert_withdrawals_monotone` returns | `assert_withdrawal_carried` |
|---|---|
| `monotone True` (READ) | **ACCEPTED** |
| `monotone None` (NO_REPOSITORY) | **REFUSED** |
| `monotone None` (vacuous empty walk) | **REFUSED** |
| `monotone False` | **REFUSED** |
| the key **missing entirely** | **REFUSED** |

`if mono.get("monotone") is not True: raise` — and it is on the **emission** path, not the `--out` path the unit bypasses, which is what my DA24-R3 asked for. The refusal text names the reason: *"'I could not check' is not 'nothing was undone'."* **Closed, including the vacuous third route.**

## DA35-R1 — MEDIUM — WALK-S9's floor is a convention, not a predicate, and it lives in the history it counts

The refusal text states the circularity itself: *"The floor is committed in the file whose history is counted and **may only be RAISED**."* But nothing checks that it was. `grep` for a floor-monotonicity guard returns only the WALK_ROUTES prose at `:643`; `MIN_PRIOR_VERSIONS = 5` is a bare literal at `:550`.

So the guard catches an **accidental** rewrite — a rebase drops commits, the walk finds 3, the floor still says 5, it refuses — and not a **coherent** one: a rewrite that drops history *and* sets the floor to 3 in the same pass presents as a clean walk with a satisfied floor. The rewrite class WALK-S9 exists for is precisely the one that can carry the file.

`CANONICAL_REMOTE` is a good and thoughtful guard for a different question — it stops a `/tmp` probe tree inheriting the claim, and the reasoning that a remote *"a rebase does not change and a scratch tree does not have"* is right. It does not bear on this.

> **Clause.** Pin the floor where the walk does not count it — the register, or a file with its own separate history — or derive its high-water mark and refuse below it. Deriving it from `da_race_withdrawals.py`'s own history would be circular for the same reason, so the register is the honest home.

## DA35-R2 — LOW — the enumeration is of the walk's **own** exits; a callee's raise is neither part

Part 1 enumerates `Return`, `Raise`, `Continue`, `Break` **in the walk's body**. An exception raised by a *callee* leaves the function without being any of those — and `assert_withdrawals_monotone` calls `_git` and `_registry_in_blob`, the latter of which now **raises** `BlobUnparseable` by design (round 34's fix). That route is not a source route and not a supply route.

It is currently **not** a silent-shortening route: the only production caller is `assert_withdrawal_carried`, which does not swallow it, and `main`'s handler turns it into `exit 4 / INSTRUMENT FAILURE`. So the guarantee holds. But the census's residue paragraph lists three uncovered things and this is a fourth, and its safety depends on a caller's behaviour rather than on the enumeration.

> **Clause.** Add it to the residue, or extend part 1 to the transitive callees the walk invokes — the latter is the same AST walk one level deeper.

---

# 3. DE55-R1 — still open

`de_section81_arms.py`'s literal needle remains a two-spelling substring test; `d["VALID_AS_A_CONTROL"] = True` and four other forms still pass it. Unchanged at this tip.

---

# 4. **The agreement question — yes, and I can name one live instance and one disclosed one**

## The mechanism, first, because it tells you where to look

All four instances I know of today share one shape: **a claim about what a named artifact SAYS, where the name resolves and the text was not read.**

| instance | the cite resolved | the text said |
|---|---|---|
| DE36-R3 (mine, R-507's subject) | yes, `:4124` exists | it was about identity, not count |
| *"DA's verifier has just been repaired"* | the round existed | it landed two minutes after the tip I was given |
| BE37's *"refuses because CANONICAL_VERIFICATION_PATH is one path"* | the constant exists | two other refusals fire first; that comparison is never reached |
| `clob_v3_1` *"cites a USER ruling (R-232)"* | R-232 exists | see below |

And the reason three seats agreed: **they were reading one source.** Agreement among readers of a single summary is not corroboration — it is one observation reported three times. That is the same error the programme already named in a statistical form at R-495 (*"12 of 24 cells survive" is 4 distinct results, because budgets replicate one statistic*), applied to citations instead of cells. **Independence is a property of the sources, not of the readers.**

## The live instance: `clob_v3_1`'s authority

I filed at R496-R1 that the register's sentence — *"Every existing entry in that table cites a USER ruling (`clob_v3_1`/`clob_v4` from R-232/R-340)"* — is not supported for `clob_v3_1`. Re-checked at this tip: **R-232's entry text contains no occurrence of `clob_v3_1`.** The value that kept 08-29 out of the race for three days rested on a cite that does not mention it, repeated by DA's table, by the register, and by the coordinator — three seats, one source, unverified. The *value* has since been re-ruled by the USER at R-497, so the live residue is the register sentence itself, which still asserts the R-232 authority.

## The disclosed one: `clob_v4`'s authority

DA's own `era_authority_audit`, run now: `eras_whose_cite_does_not_name_them: ['clob_v4']` — R-340 **resolves** and, under the audit's stricter test, **does not name the era**. `clob_v4` is `False` in the admissible set on that authority. This one is *reported and not acted on*, correctly (moving an admissibility value on a seat's reading of prose is the defect), and it is packaged for the USER. It is live but it is not hidden — which is the difference between the two entries in this section.

## The detector, and it already exists

DA built it for era cites: **`entry_names_this_era`** — resolve the cite *and* check the cited text names the subject. Generalised, that is the check the class needs: for any claim of the form *"X, because ⟨cite⟩"*, verify the cited text contains the claim, not merely that the cite resolves. I ran the crude term-level version over the seven load-bearing cites in the code's authority strings (R-340, R-424, R-409, R-500, R-503, R-497, R-232); **only R-232 fails it**, and DA's stricter test adds R-340. So the surface is small and already mostly instrumented — the gap is that the check runs on `ERA_AUTHORITY` and nowhere else.

> **Clause.** Point the same predicate at the other authority-bearing tables — the withdrawal registry's `authority` strings, the freeze's `ruling` fields, the family declaration's `ruling` — and report, not enforce. It is one function that already exists.

---

## Findings

| # | sev | |
|---|---|---|
| **DA35-R1** | MEDIUM | `MIN_PRIOR_VERSIONS` is a convention stated in a refusal message and checked by nothing; it lives in the file whose history it counts, so it catches an accidental rebase and not a coherent one |
| **DA35-R2** | LOW | the census enumerates the walk's own exits; an exception from a callee (`_registry_in_blob` raises by design) is neither a source nor a supply route, and its safety rests on a caller rather than on the enumeration |
| — | — | **R-507 is correct**, verified at `:1565-1573` and at the frozen draft §6; my error was reading a two-clause selftest label as one claim. My structural adjudication stands and is better supported — and the protocol has exactly **one** declared matched quantity, which has now failed, so there is nothing to retreat to |
| — | — | **DA35 verified**: the census reconciles (15 source routes, 0 UNACCOUNTED, 6 supply, 0 silent shortenings) and the canonical guard at `da_forward_day_verify.py:288` refuses `None`, `False` and a missing key while accepting only `True` — driven at that site |
| — | — | **Item 4**: one **live** instance (`clob_v3_1` / R-232, still asserted in the register), one **disclosed** (`clob_v4` / R-340, reported by DA's own audit); the mechanism is *the cite resolved and the text was not read*, and the detector already exists as `entry_names_this_era` and runs on one table only |

## Disposition

R-507's correction is right and I would rather it stand in the record as filed than be softened. The thing I would take from it is not "check citations" but the sharper version: **agreement between seats is evidence about the seats, not about the claim, unless the seats read different sources.** Three of us reading one summary produced one observation with three signatures, and the only thing that broke it was going to the line.

I estimate; the coordinator routes; the USER decides (rule 14).

## Discipline record

Executed at the pinned tip **`c5189e3`** in `~/ctaNew-wt-rev`; every step under `systemd-run --user --scope --slice=research.slice -p MemoryMax=8G`. **No race seal opened**; `be_forward_day` never run. The canonical-guard drive mocked the walk and emitted nothing. Nothing written under `data/`. `~/ctaNew-wt-be`, `-da`, `-de` never read. Worktree clean.
