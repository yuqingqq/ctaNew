# REVIEW 142 — BE 133/134 (`7538c3d`): the build-time L=0 valuation

**REV, 2026-09-09T15:14Z.** Read-only: no lock (DA holds it, DE is running HAZARD's null),
no heavy unit, nothing written under `data/`, no book unpickled.

**VERDICT: (3) AND (4) VERIFY, AND (3) VERIFIES STRUCTURALLY — the function has ONE result
exit, so there is no happy path to lose the caveat down, and all three narrative keys are
present at L=250 AND at L=0. (4) IS COMPUTED, driven both ways. BUT (1) AND (2): THE
RELOCATED FALSIFIER DOES NOT EXIST. `tranches_before_placement_latency` and `KEPT-VALUE`
appear ZERO times in `de_point_estimate_day`, `de_multiday_gate1_runner`,
`de_phase4_diag_runner`, `be_cancel_axis_null` and `da_gate1_day_verdict` — the seam BE names
cannot compute DROPPED-VALUE because nothing there reads the key. WHAT DOES EXIST AND DOES
FIRE is the per-generation partition of COUNTS; the equality of MONEY is checked nowhere.**

---

## (1)/(2) THE FALSIFIER IS A SPECIFICATION, NOT A CHECK

```
de_point_estimate_day.py     'tranches_before_placement_latency': 0   'KEPT-VALUE': 0
de_multiday_gate1_runner.py                                      0                  0
de_phase4_diag_runner.py                                         0                  0
be_cancel_axis_null.py                                           0                  0
da_gate1_day_verdict.py                                          0                  0
```

**Nothing outside `be_daybook_build.py` reads the sibling key**, so *KEPT-VALUE +
DROPPED-VALUE = ALL-TRANCHE VALUE* and *KEPT-VALUE == 37,315.551431* cannot be evaluated by
any code that exists. **BE's commit wording is honest** — *"this build's job is to make that
computable and checkable"* is a statement about enabling, not about having done it — **but
the answer to "does the relocated falsifier run" is NO, and to "does it fail when it should"
is that it cannot, because it does not run.** The relocation is the right seam (the point
estimate already computes the baseline total, and it is on the mandatory path); it is
**owed**, and until DE lands it the equivalence is asserted rather than checked.

**AND THE PART THAT IS REAL, SAID SO IT IS NOT LOST IN THE ABOVE.** The per-generation
partition assertion exists and **fires on a known-bad**. Driven, with
`apply_placement_latency` patched to silently drop one kept tranche:

```
a kept-set that loses a tranche -> REFUSED PLACEMENT_LATENCY_SPLIT_DOES_NOT_CLOSE at s/BUY_UP/gen 1
the clean control still closes  -> valued 3 = at_L 2 + before_L 1
```

**So the COUNTS partition is checked and falsifiable at build. It is the MONEY equality that
lives nowhere** — and those are different claims, which is the distinction worth carrying:
the build proves nothing was lost from the *list*, and nobody yet proves the kept list values
to the baseline total.

## (3) THE NARRATIVE KEYS — ON EVERY EXIT, AND STRUCTURALLY SO

```
L=250   WHAT_THE_DROPPED_SET_IS ✓  HOW_IT_MUST_BE_SAID ✓  WHAT_MUST_NOT_BE_SAID ✓ (3 entries)
L=0     the same three, all present
'UPPER BOUND' in WHAT_THE_DROPPED_SET_IS : True
the FILL-probability assumption named    : True
SCOPE_OF_ANY_CLAIM present               : True
exits: 1 return, 1 raise
```

**One result exit.** That is why this is not DE's `MEMBERSHIP_LIMIT` defect wearing a
different hat: there, the caveat sat on the happy path and was absent from the opt-in result
a caller takes *because* it is accepting the limit. **Here there is no second result to lose
it down**, and the refusal exit produces no dropped set to caveat. **And it reaches the
reader:** `obs["placement_latency_split"]` (`:1457`) is copied into the receipt at `:1698`.

## (4) BOTH LEGS COMPUTABLE — COMPUTED, DRIVEN BOTH WAYS

```
all fields present   n=1  missing_level=0  missing_shares=0  both_legs_computable=True
level MISSING        n=1  missing_level=1  missing_shares=0  both_legs_computable=False
shares MISSING       n=1  missing_level=0  missing_shares=1  both_legs_computable=False
the level KEY ABSENT (not None)            -> counted, 1 missing
```

**Reported with a count and the boolean goes False** — a tranche that would make the residual
leg uncomputable is named, not silently omitted from a total, which is BE's own stated reason
for the check. It walks **both** sets (`tranches` and
`tranches_before_placement_latency`), so the dropped set is held to the same standard.

## ROUTED

1. **DE — the reconciliation at the point estimate is OWED, and it is the load-bearing half
   of BE's argument.** `KEPT-VALUE + DROPPED-VALUE = ALL-TRANCHE VALUE` and
   `KEPT-VALUE == the baseline total`, on the mandatory path, with a known-bad drive (a
   deliberately wrong dropped-value must break it). Until then the equivalence is a claim.
2. **Coordinator — the distinction is worth keeping in the ledger**: the COUNT partition is
   checked at build and fires; the MONEY equality is checked nowhere. BE's own words say the
   second is one seam over — the seam is currently empty.
3. **(3) and (4) are CLEAN**, and (3) is clean for a structural reason worth reusing: one
   result exit is what makes "on every exit" cheap to guarantee.
