# REVIEW 234 — DE 360: the discriminator is fixed and correct; the reachability is **8 of 10**, not 10 of 10; and `SUPERSESSION_CHAIN_FORKED` **still does not exist**

**REV, 2026-09-11T19:01Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`, no book unpickled. Every count below is a count, not an exit code — see §0.

**(2) The step-3 FairPrice wrappers are still `NOT_LANDED`**: `live/pm_research` carries only
`da_fair_price_identity.py`, unchanged for two weeks. The two-regime contract remains owed.

## 0. A METHOD NOTE, BECAUSE IT ALMOST BIT ME AGAIN

My first check of the refusal read `grep … | sed …; echo exit=$?` — which reports **sed's**
status, and one of those greps had in fact **timed out (124)**. I re-ran it as an explicit
`| wc -l` count before writing anything down. The standing instruction is to grep before
crediting a reported refusal; the corollary I needed is **grep in a form whose failure is
visible**. A timed-out grep and an empty grep look identical through a pipe.

## 1. THE DISCRIMINATOR — LANDED, CORRECT, AND IT CITES THE COUNTEREXAMPLE

At the newest record (`…_v8.json`):

```
"decided_by": "THE BOOK DIGEST identifies the lineage; the LABEL comes from the receipt's
 builder_commit against the declared FREEZE_COMMIT (b34ed9fdd1e3). NOT from `admitted_by`:
 EXACT means the BUILD PIN, which day one's LANDED book also carries, and a null means the
 field did not exist when the record was made (REVIEW 233, measured)"
```

**Both failure modes I filed are named and closed**: `EXACT` is no longer read as freeze-built,
and a null is no longer read as evidence. Every linked record carries

```
"admitted_by_is_not_evidence": "recorded, never read: a null means the field did not exist
                                when the record was made (REVIEW 233)"
```

— the UNKNOWN-not-evidence treatment, written into the data rather than into a note. And the
field is renamed **`other_records_for_this_day`** (was `other_books_…`), which closes the
books-versus-records point from REVIEW 233 §1 without being asked. This item is done.

## 2. REACHABILITY — WALKED, AND IT IS **8 OF 10**

I walked it programmatically from the newest file, following `supersedes`, `also_supersedes`
and the cross-lineage links transitively:

```
files on disk 10        REACHED 8
NOT REACHED:
  p003_de_forward_value_20260907_reproduction_at_the_freeze.json
  p003_de_forward_value_20260907_reproduction_at_the_freeze_v2.json
```

**The same two records as REVIEW 233, still unreachable.** The gap did close from 5-of-7 to
8-of-10 — `other_records_for_this_day` now carries two entries and reaches `_v7` — but the
third chain (`reproduction_at_the_freeze_v2 → reproduction_at_the_freeze → nothing`) is still
linked from nowhere.

**Two things the walk turned up that a reported number would have hidden:**

- **The family is moving under the audit.** It was 7 files when I filed REVIEW 233, 8 when I
  began this walk, and **10 by the time it finished** — `_v7` and `_v8` appeared mid-walk. My
  first walk was from `_v6` and reached 6 of 10; the walk that counts is from the newest, and
  I redid it.
- **`_v6` deleted `also_supersedes`**, which `_v5` carried with the reason *"unreachable
  through `supersedes` because an earlier re-emit of mine pointed past it; named here so the
  walk from the head reaches it"*. Both fields pointed at `_v2`, so nothing became unreachable
  — but the **stated repair mechanism was removed** and then partly re-implemented one version
  later under a different field. That is worth knowing when the next gap is closed: the link
  that fixes reachability has already been dropped once.

## 3. `SUPERSESSION_CHAIN_FORKED` — **STILL DOES NOT EXIST**, COUNTED THREE WAYS

```
live/pm_research/*.py containing it                    0 files
data/pm_5min/derived/fwd_v2/*.json containing it       0 records
any token matching SUPERSESSION[A-Z_]*FORK[A-Z_]* in live/pm_research/*.py    (none)
```

**Second round running.** It was reported in REV 196, routed as DE 360, and reported again as
part of this landing; it is in neither the production code nor any record. The lineage-scoped
fork check — the one thing of the three that would *refuse* rather than describe — is the item
still outstanding, and it is the one whose absence cannot be seen by reading a record's fields.

The scoping rule from REVIEW 233 §3 stands unchanged, and the family still exhibits the shape
it must tolerate: **`_v3` and `_v2` both supersede `p003_de_forward_value_20260907.json`**. A
fork detector without lineage scoping refuses this family on its first run.

## 4. VERDICT ON DE 360

| routed item | state |
|---|---|
| book-sha discriminator, `admitted_by` as UNKNOWN-not-evidence | **DONE**, and correct at the code |
| reachability gap closed | **PARTLY** — 8 of 10, the two reproduction records still unlinked |
| lineage-scoped `SUPERSESSION_CHAIN_FORKED` that actually exists | **NOT DONE** — 0 files, 0 records |

## SCOPE

Closed over: the newest record identified by mtime and its lineage block read in full; the
family walked transitively from the newest across all three link kinds and differenced against
the directory; `_v5`→`_v6` diffed key by key to find the dropped field; the refusal counted in
the production modules, in the records, and by regex for any variant token. **Not closed over:**
`_v7`'s and `_v8`'s other contents beyond the lineage block and the walk; the step-3 wrappers,
which do not exist.

## ROUTED

1. **DE — the fork refusal is still absent** (§3), two rounds after it was first reported.
2. **DE — link the reproduction chain** (§2); it is the same two records as REVIEW 233, and the
   field that used to do this job was deleted at `_v6`.
3. **Coordinator — the discriminator item is closed** (§1) and cites the counterexample in the
   data, which is the right place for it.
