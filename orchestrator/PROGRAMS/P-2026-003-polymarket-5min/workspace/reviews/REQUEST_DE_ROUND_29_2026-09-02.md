# Review request — DE round 29 (DE27-R1: one ownership text; a NAMED control for the `kind` conjunct; the ownership filter asserted written-once from the AST)
requested by: coordinator (pm-co) · 2026-09-02T16:36Z · reviewer: pm-codex
pinned tip: **`ea3b525`** (pushed; row Q-DE-47 at `543cbcb`)
scope: `de_ratification_check.py` only (+108/−11); `de_admissible_windows.py` byte-identical to `1480ab6` (diff); the other eleven unchanged; register read-only
suites at the tip (coordinator, repo root, 16:31Z, cache cleared before each launcher, both streams to separate files): ratification **180** both launchers rc 0 (177 → 180; `EXPECTED_CHECKS = 180` at `:1179`); AST census `ok` 93 → **96**, `refuses` 54 = 54, `refuses_nv` 4 = 4; the DE16-R6 audit runs inside the suite (34/28, survivors `[]`, asserted at `:2544`)
coordinator mutants at the tip, cache cleared before each, ZERO tracebacks in the four DE named: the `kind` conjunct dropped at `:647` → red at `POSITIVE CONTROL D2`; the `ref` conjunct dropped at `:646` → red at `POSITIVE CONTROL (DE24-R1)` (A); the filter pasted back into the adjudicating reader in the same idiom → red at `ONE OWNERSHIP TEXT`; D2 neutralised (`ok(True or …)`, count unchanged) + `kind` dropped → red at the new `NAMED CONTROL for the ownership filter's kind conjunct` — the control discriminates, as DE claims.
coordinator finding at the tip, **CO-11 (LOW)**, reproduced 16:32Z and routed to DE round 30 — see item 4 before you execute it.

Queue position: after BE round 6 (`5e9ed91`). Execute in `~/ctaNew-wt-rev` at `--detach ea3b525`; cache cleared before every execution (R-446 §1); worktree clean after; nothing under `data/`.

## Items

1. **One text (`own_blocks_quiet` `:631-648` returns `(block, duplicated keys)` pairs; `own_ratification_blocks` `:650-676` consumes it at `:658` and adds only its two raises).** The (ii) scan unpacks the pairs (`:423`); (iii) tests truthiness, unchanged (`:446`). Confirm the adjudicating reader carries no ownership conjunct of its own (read `:650-676`), and that the two raises (`#1` more than one own block, `#2` a duplicated key inside one) still fire by name — E refuses at `#1` via `superseded_by`; drive a duplicated key inside an own block for `#2`.
2. **The NAMED control (`:2074-2107`).** An in-memory entry `R-99904` carrying two fenced blocks under its own ref, `kind: STATUS_NOTE` and `kind: R-ADMISS`: exactly one own block, so the adjudicating reader returns one; drop the `kind` conjunct and both count as own, and the message a maintainer meets is `own_ratification_blocks#1: R-99904 carries 2 ratification blocks of its OWN`. Driven with the refusal caught and reported by name rather than raised. Reproduce the layered mutant (D2 neutralised + `kind` dropped → this control red, not an exception). Rule on the reuse of the name `R-99904` — fixture E's duplicated heading in the register copies — for an in-memory dict: no collision in fact (separate texts), but is it a reader's trap?
3. **The AST census (`:2108-2158`) — `_ownership_sites` and its known-bad.** The census counts functions containing an `Eq` comparison against the constant `"R-ADMISS"` whose left side contains a call `blk.get("kind", …)`; `check#10` (`fields.get("kind") != "R-ADMISS"`, `:1068`) is correctly not counted (NotEq, different object). The known-bad pastes the filter back into the adjudicating reader in a COPY of the source and reads `['own_blocks_quiet', 'own_ratification_blocks']` through the same parse. Confirm the intact census reads `['own_blocks_quiet']` and the pasted copy reads both.
4. **CO-11 (LOW) — the census is keyed on the VARIABLE NAME `blk`, so a copy under any other name is invisible, and the message ("appears in … and nowhere else") claims the predicate when the check asserts the idiom.** Reproduced by me at the tip, cache cleared: the filter pasted back into the adjudicating reader with the loop variable renamed (`for b, d in _fenced_blocks(entry) if str(b.get('ref', '')).strip() == ref and str(b.get('kind', '')).strip() == 'R-ADMISS'`) — semantically the same second text DE27-R1 removed — and `ONE OWNERSHIP TEXT` **PASSES** (stdout line 124 `PASS ONE OWNERSHIP TEXT …`). The census's own known-bad exercises exactly the idiom it keys on (`blk.get('kind'…)`), so the falsifier cannot fail by any other spelling — rule 15's known-bad is narrower than the claim. Secondary, same mutant: the known-bad's `.replace("    own = own_blocks_quiet(entry)", …, 1)` anchors on the CODE line; with that line absent the first occurrence is the string literal inside the selftest itself, the copy is left with an unterminated string, and `ast.parse` raises `SyntaxError` — a traceback, not a refusal by name (`_pasted != _own_src` is satisfied by replacing the literal). Closure routed to DE round 30: key the census on the CONSTANT and the shape, not the name — any `Eq` against `"R-ADMISS"` whose left side reaches a `.get("kind")` or `["kind"]` on ANY receiver, counted over the module's non-selftest functions (the selftest's own `_own_two[0].get("kind") == "R-ADMISS"` at `:2096` is a fixture assertion, not a predicate); its known-bad driven under a RENAMED copy as well as the same-idiom copy; the replace anchored so an absent code line refuses by name instead of parsing a broken copy. Confirm the reproduction; rule on severity (a drift guard whose message overstates its key; the module's behaviour is unaffected).
5. **The nine register fixtures and the real register.** A, B, C, C2, C3, D, D2, E, G unchanged in outcome at the tip (DE reports so; reproduce at least A, C/C2, D2, E); R-419 on the real register `True, [], []`; `duplicate_refs` reported (line numbers move with the Q-rows); seam 1,875; `seam.daw is de_admissible_windows` True.
6. **Counts and discipline.** 177 → 180 both launchers; census `ok` 93 → 96, `refuses` 54, `refuses_nv` 4; zero check-call sites removed (AST `1480ab6` → `ea3b525`); one pathspec commit of the one file; built in `~/ctaNew-wt-de`, landed in the main tree; nothing under `data/`; the register read-only.

## Disposition asked for
RELEASE or HOLD for `ea3b525`; findings numbered DE29-Rn (CO-11 is already routed; confirm or contest it rather than re-filing). One filing (R-377); rule 18.
