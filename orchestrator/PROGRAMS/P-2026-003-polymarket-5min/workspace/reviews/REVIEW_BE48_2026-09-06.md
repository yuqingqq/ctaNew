# REVIEW — BE 48: the block is REAL and it is **TWO** missing inputs, not one; the race read is APPROVED TO OPEN; and the builder's known-bad passes on a tautology

**Filed** 2026-09-06T05:57Z (clock read before composing) · reviewer seat (pm-codex)
· tip `11bd651` · no code fixed · no write under `data/` · **no sealed file opened**
— the five SEALED_scores were digested from bytes without parsing (§C.2).

**ROUTING — CHECKED**, everything below driven or recomputed by me, except the four
figures marked RELAYED in §A.6.

## VERDICT

**(A) The block is real, correctly dispositioned, and UNDERSTATED.** BE names one
missing input. There are **two**: the feature fragment *and* the state tape, and the
second is the harder one. Measured, not read off either artifact's own summary field.

**(B) Both guards are real and I drove them past BE's cases.** But **one of the eight
checks cannot fail** — the placeholder BE reports as removed survives, in a different
dress, in the check labelled `KNOWN-BAD: a day with no supplied windows REFUSES`.
Driven: the refusal it names is **never reached**.

**(C) The race read is APPROVED TO OPEN.** All three of my §B.7 items are closed and
the five digests reproduce at the artifacts. Two residuals, neither changing what the
read may conclude: the byte-identity check has **no code to run in** (there is no
reader), and the Gate-1 separation field matches the module's own constant against a
constant.

---

# (A) THE BLOCK

## A.1 The fragment — measured from its ROWS, not from its `days` field

The artifact carries its own trailing summary (`"n_windows": 471, "days":
["2026-08-24","2026-08-25"]`). That is the producer's claim about itself, so I did
not use it. I streamed all **1,241,115,096 bytes** and took the distinct
`(slug, coin, day)` triples out of the rows:

```
distinct (slug,coin,day) triples : 471      distinct slugs : 471
day 2026-08-24 : 241 windows     day 2026-08-25 : 230 windows
coin btc : 234                   coin eth  : 237
t0 min 1787579400 = 2026-08-24T13:50:00Z
t0 max 1787650200 = 2026-08-25T09:30:00Z
grep -c '2026-09' over the distinct set : 0
```

**BE's three figures reproduce exactly** — 1,241,115,096 B, 471 windows, the
08-24→08-25 span — and the negative is stronger than BE stated: not "no 09-03 slug"
but **no September slug of any day**. 09-03's windows carry `t0` in
`[1788393600, 1788480000)`; the fragment's last window is **8.60 days** earlier.

**One thing the measurement shows that the summary hides**: the fragment begins at
`t0 = 1787579400`, **2026-08-24T13:50:00Z — the first window of the consumed
development hour**, 66 s after CLAUDE.md rule 5's sub-second boundary
(2026-08-24T13:48:54Z). This artifact is not "a fragment that happens to predate
September"; it is the consumed era's own fragment, cut at that boundary. BE's
citable form — *"THE FEATURE FRAGMENT IS THE CONSUMED-ERA FRAGMENT"* — is exactly
right, and the start timestamp is the evidence for it.

## A.2 `select_v2_era` genuinely cannot reach September — verified at the mechanism

BE's receipt says the declared intervals *"end 2026-08-26T00:00 (measured:
[1787650201, 1787702400])"*. Read at
`harmful_exposure_rows.py:582–613`:

```
POPULATION_SLUG_INTERVALS = {
  "v3_4_consumed_fragment": (None, 1787650201, None),   # t0 <= 2026-08-25T09:30:00Z
  "da_development_topup":   (1787650200, 1787702400, …) # t0 <  2026-08-26T00:00:00Z
}
```

`slug_in_population` refuses `t0 >= hi`; `population_slug_interval` **refuses an
undeclared population outright** rather than defaulting to the era floor. So no
September `t0` can pass either interval, and no third interval exists to pass.
**CHECKED — the mechanism, not the message.**

*Two notes on precision.* The receipt's sentence is plural — *"the intervals end
2026-08-26T00:00"* — but that is the **maximum** of the two ends; the consumed
fragment's own end is 2026-08-25T09:30:01Z. Harmless here (both are before
September), loose as written. And the selftest proves the claim with the bar
`e < 1788000000`, which is **2026-08-29T10:40:00Z** — stricter than "before
September", so a pass implies the claim. Its one hole is the disjunct `e is None or
…`: a population declared with `hi = None` is unbounded above, reaches September,
and **would still pass the check that says every interval ends before September**.
One character of the predicate admits the only counterexample to its own claim.

## A.3 Does a scored book genuinely need that fragment? **YES — and it needs a state tape too, which BE does not name**

`asm` has exactly one producer. `be_daybook_build.build` → `R.fragment_slice(frag,
…)` → `R.assemble_streaming(…, source=frag, tape=tape)` → `asm["by_arm"]`. And
`assemble_streaming` (`de_phase4_diag_runner.py:8388+`) reduces
`_fragment_chunks(source=…)` through `PA._feature_pass(chunk_path, "phase4_diag",
TAPE=tp["TAPE"])`. **The fragment is the row population and there is no other way in.**
CHECKED.

**But the feature pass takes its features from TWO sources, and only one of them is
the fragment.** `phase2_arms._feature_pass:730–760`:

```python
if TAPE is None:
    raise RuntimeError("no rebuilt state tape supplied. Re-deriving state here "
                       "would rebuild it WITHOUT gaps/bn_recv_ns …")
_e = TAPE.get((r["slug"], r["side"], r["gen"], r["t_start"]))
if _e is None:
    drops["state_join_failed"] += 1; continue
…
fp = hm.features(streams[slug], …)     # PM  — computed live from the ARCHIVE
ff = hm.fine_feats(r["t0"] + r["t_start"], …)  # FN — computed live from the ARCHIVE
```

`generation_scores` then composes `blocks["PM"], blocks["FN"], blocks["ST"]`.
**PM and FN are computed on demand from the archives — the forward scorer does
exactly this for 09-01..09-05 already (`be_forward_day.score_rows:1905–1910`). ST
comes only from the tape.**

**So I measured the tape.** `phase2_state_tape_v5.json`, all 3,170,987,711 bytes
streamed:

```
distinct slugs 808 · day 2026-08-24 : 241 · day 2026-08-25 : 567
t0 1787579400 (08-24T13:50:00Z) -> 1787702100 (08-25T23:55:00Z)
btc 400 · eth 408          mtime 2026-08-27 — a file written 08-27 holds no 09-03
```

**Zero September keys.** Every 09-03 row would miss the join, become
`state_join_failed`, and `assert_fit_absorption_within_bound` (bound **1%**,
`state_join_failed` in `BOUNDED_DROPS`) would refuse the pass. It fails closed —
but it means **a September fragment alone does not unblock the book.**

*And the number BE used to confirm its instrument is the symptom of the second
blocker.* BE reports the tape index as **1,764,206 rows** and credits it as matching
DE's docstring (638,917 + 1,125,289 = 1,764,206 — exact). It matches because it is
**the same consumed-era tape**: 387.4 s and ~4 GB were spent indexing 1.76 M rows of
which **not one can join a 09-03 generation**. The corroboration and the defect are
the same figure.

## A.4 Is there any other committed artifact carrying September features? **NO — surface and as-of stated**

**Surface:** `/home/yuqing/ctaNew/data/pm_5min` (40,465 files; 1,479 over 10 MB) and
`/home/yuqing/ctaNew_forward_runs` (77 files; 7 over 10 MB). **As-of
2026-09-06T05:50:52Z.** Test: the fragment shape `{"rows": [{"side"…` on the first
bytes of every file over 10 MB, plus every file in `derived/` (272).

**Seven fragment-shaped artifacts exist, all in `derived/`:**

| artifact | mtime | bytes |
|---|---|---|
| `harmful_exposure_rows_v1/v2/v3/v2_eraB/v3_eraB.json` | 2026-08-25 | up to 1.24 G |
| `harmful_exposure_rows_v3_topup(.PREFIX_FILTERED).json` | 2026-08-26 | 705 M |
| `de_section81_frag.json` | **2026-09-05** | 58 M |

Six have mtime ≤ 2026-08-26 and therefore cannot hold a day that had not happened.
The seventh has a September mtime, so I measured it: **12 btc windows, `t0`
1787579400–1787582700, every one 2026-08-24** — the consumed hour, sliced on 09-05.
**No committed artifact anywhere on that surface carries September features.**

And the SEALED_scores cannot substitute — read at the **writer**, never by opening
one: `be_forward_day.seal:1995` writes `per_coin_scores` as
`{coin: [(int(t0), value)]}`, keyed on the **window** `t0` only, while `asm` is keyed
`(slug, side, float(t0))` per generation. The `FeedWriter` docstring names this as
the gap it was built for — *"a scored forward day emitted `(window_start, value)`
pairs and nothing the action-level estimand could consume."* Different key,
different scorer (the frozen candidate, not the two pinned heads).

## A.5 What a feature pass for ONE September day consists of, as far as the code shows

**Four steps, one of which is a declaration and one of which has no parameter.**

| # | producer | inputs | note |
|---|---|---|---|
| 0 | **a DECLARED population interval** | `harmful_exposure_rows.POPULATION_SLUG_INTERVALS` | there is none for September, and `population_slug_interval` **refuses** an undeclared name. This is a declaration, not code — and it is the R-172(1) object that exists precisely so an interval is never inferred |
| 1 | `harmful_exposure_rows.build_rows(v2_era=True, population=<new>)` | archives + token map + gaps, per window, through `replay_with_recorder` | → the day's fragment. ≈ **651 MB** for 247 windows on the eraB fragment's 2.64 MB/window |
| 2 | `build_state_tape_v2.main(fragment_path=…, out_path=…)` | that fragment + the topup + the pinned gap ledger | → the day's state tape. ≈ **969 MB** at the current tape's 3.92 MB/window. **Its default `out_path` is the live tape and it now REFUSES to overwrite** (`:298`) — a guard that exists because a bare invocation once destroyed it |
| 3 | `de_phase4_diag_runner.build_tape_index` → `assemble_streaming` | the day's tape + the day's fragment | → `asm` |

**Step 3 has no way to be pointed at the new tape.** `phase2_arms.tape_index(split)`
reads the **module constant** `TAPE_PATH`; it takes no path argument, and
`build_tape_index(splits)` passes none. So a per-day tape requires either
overwriting the live artifact — which the builder now refuses, correctly — or a path
parameter that does not exist. **That, not the fragment, is the narrow part of the
unblock**, and it is a code change on DE's surface, not BE's.

## A.6 Resources, and whether the ruled six-day run is feasible under rule 20

**Verified at the journal, not relayed:** `run-u69617.scope`, started 05:19:00,
`Consumed 18min 2.904s CPU time, 6.4G memory peak, 0B memory swap peak`, command
`PM_DATA_ROOT=/home/yuqing/ctaNew python3 …be_daybook_build.py --day 20260903`.
**RELAYED** (BE's in-process figures, not re-derived by me): reference 247 windows /
313,114 generations / 531.1 s / 2.008 GB, tape index 387.4 s.

**The 6.4 GB decomposes, and one half of it should not have been paid.**

* reference **2.008 GB**, retained for the whole assembly (`refs[coin]` is read in
  every chunk);
* tape index **~4.0 GB** — DE's docstring: score 1.42 GB, train 3.90 GB cumulative,
  *"both together are 4.01 GB and the larger alone is ~2.5 GB. This is the single
  largest retained term in the run."*

**`assemble_streaming` already IS the partitioned/streaming assembly the question
asks about — and BE's call turns half of it off.** `de_phase4_diag_runner.py:8437`:

```python
passes = [(sp,) for sp in got] if partition_by_split and tape is None else [got]
```

`be_daybook_build.build` pre-builds `tape = R.build_tape_index(splits)` for **both**
splits and passes `tape=tape`, so `passes = [got]`: both indices resident together
(4.01 GB) instead of one at a time (~2.5 GB), and the `del tp` release is skipped.
**DE's own instrument would have disclosed it** — the emitted block computes
`"partition_by_split": partition_by_split and tape is None`, which would have read
**false**. Roughly **1.5 GB of the 8 GB cap given up by the caller.**

**So the answer to the question as posed: the design does NOT need a partitioned or
streaming assembly. It has one. What it needs is a caller that uses it — and a tape
that belongs to the day.**

**And the published 6.4 GB / 18 min does not bound the six-day run, because it was
spent on the wrong tape.** Estimate, with its basis stated (not a measurement): the
consumed era carries 1,764,206 tape rows over 808 windows = 2,183 rows/window; 247
windows → **≈ 539 k rows**, under a third of what was indexed. A day's own index is
the smaller object, not the larger one.

**What rule 20 actually threatens here is the CLOCK and the LOCK, not the cap.**
18 min of CPU per day for reference + index alone × 6 days ≈ 1.8 h, before any
assembly, on top of DE's own design estimate of ~11.6 h of null + ~1.9 h of replay
at G = 6. Under *one heavy run at a time* that is a serial queue of ~15 h shared
with DA's and DE's heavy work. The 8 GB cap is not the binding constraint.

### **AND THE LOCK IS NOT BEING HONOURED RIGHT NOW — measured, flagged as a hazard rather than filed against a batch**

At **2026-09-06T05:54:01Z** my own light check was refused by
`flock -n /home/yuqing/ctaNew/data/.heavy_run.lock` (rc 1 — reported, not waited on,
per rule 20). `fuser` names the holder as DA's `e2_a_runner.py --census`
(`run-u69843.scope`). **A second heavy run was executing concurrently and holding
nothing:**

```
research.slice/be49frag.scope    2.14 GiB   be_gate1_fragment.py --day 20260903
                                            (cwd ~/ctaNew-wt-be, elapsed 6m03s)
research.slice/run-u69843.scope  3.51 GiB   e2_a_runner.py --census  <- holds the lock
research.slice MemoryMax = 14 GiB           MemoryCurrent = 7.19 GiB
```

Both are inside the slice, so the CPU quota holds; but R-551's invariant is *one
heavy run at a time* because *"the scorer peaked at 6.95 GiB of its 8 GiB cap"* —
two such scopes are 13.9 GiB against a 14 GiB slice ceiling, and **the lock is the
only thing that prevents it.** A scope that takes the slice without taking the lock
gets the cap and skips the invariant. Not a finding against BE 48 (that batch is
closed); a live condition the coordinator should see, and the reason I am confident
the six-day schedule is a *sequencing* problem before it is a memory one.

*One reporting note.* "313,114 generations — 10.06× the consumed hour" is true and
is the flattering framing: it is **20.6× the windows**, so per window this day is
**1,268 generations against the hour's 2,594 — 0.49×**. Any extrapolation to six days
should use the per-window figure, because that is what scales.

---

# (B) THE BUILDER'S GUARDS — driven, and one check cannot fail

## B.1 The two guards are real. I drove BE's four cases and seven of my own

```
assert_pool_equality
  {1,2,3} vs {3,2,1}   ADMITS   (BE, positive)
  {1,2,3} vs {1,2,4}   REFUSES  (BE, known-bad, "symmetric difference 2")
  {1,2,3} vs {1,2}     REFUSES  (mine: strict SUBSET, symmetric difference 1)
  {1,2}   vs {1,2,3}   REFUSES  (mine: superset — the other direction)
  {1,2}   vs {3,4}     REFUSES  (mine: equal size, disjoint)
  {1}     vs set()     REFUSES  (mine: one empty)
  set()   vs set()     ADMITS   (mine)   <-- the hole
assert_coverage
  {"h":5}                     ADMITS   (BE, positive)
  {"h1":0,"h2":0}             REFUSES  (BE, known-bad)
  {}                          REFUSES  (mine: empty dict)
  {"h1":1,"h2":1} of 313,114  ADMITS   (mine: the INTERIOR case)   <-- the bar
  {"h1":0,"h2":100}           ADMITS   (mine)
  n_gen = 0                   REFUSES  (mine)
```

**Both guards fire in every direction I could bend them, with one hole and one bar
worth naming:**

* **`assert_pool_equality` admits two EMPTY sets.** Two heads that scored nothing are
  "identical" and pass. That is the failure mode that actually occurred this round,
  and the pool guard cannot see it — only `assert_coverage` can. The guard exists to
  protect the shared-pool declaration, and on the one input where the declaration is
  vacuous it is the guard that is vacuous too.
* **`assert_coverage`'s bar is `n_covered >= 1`, not a coverage floor.** One covered
  generation of 313,114 ADMITS. The docstring says *"a day the tape does not reach is
  REFUSED"*; a day the tape reaches for 0.0003% of it is emitted. The `coverage`
  fraction **is computed into the receipt and never tested** — rule 10 inverted. It
  fails closed downstream (DE's R4 refuses an arm-day under 30 decisions), so this is
  a naming and reporting point, not a hazard: **the bar that binds is DE's, and this
  guard should say so rather than read as a coverage guard.**
* `{"h1":0,"h2":100}` ADMITS because the predicate is `all(...)`, not `any(...)`.
  Unreachable inside `build()` **only because `assert_pool_equality` runs first**;
  nothing in `assert_coverage` records that dependency. Safe today, order-dependent.

## B.2 **FINDING — the placeholder BE reports as removed is still there, and it carries a check**

BE's commit: *"My first selftest shipped a placeholder asserting '… or True' that
crashed; it and two source-greps were replaced by driven guards."* The literal string
is gone (`grep -c "or True|getsource|read_text()|inspect" be_daybook_build.py` → **0**).
**The class is not.** `be_daybook_build.py:299–306`:

```python
try:
    day_slugs("19700101")
    ok(False, "an empty day must refuse")
except Exception as e:
    ok("no supplied" in str(e) or isinstance(e, Exception),
       "KNOWN-BAD: a day with no supplied windows REFUSES …")
```

`isinstance(e, Exception)` inside `except Exception as e` is **True by
construction**. Driven, to see which disjunct is carrying it:

```
day_slugs('19700101') raises : be_forward_day.ForwardDayRefused  at be_forward_day.py:1429
  "REFUSED: the ledger holds no window for 19700101 (read 35492 rows)…"
  isinstance(e, BookRefused) : False
  'no supplied' in str(e)    : False     <- the left disjunct FAILS
  isinstance(e, Exception)   : True      <- a tautology carries the check
```

**Three consequences.** (1) The check passes on **any** exception — a `NameError`, an
`ImportError`, a typo in the day string. (2) The refusal it *names* —
`BookRefused("REFUSED: no supplied btc windows for {day}…")` at `:70–73` — is
**never reached**: `present_from_ledger` refuses one call earlier. So
`day_slugs`'s own empty-supply refusal has **zero driven coverage** in a battery that
reports "both guards driven". (3) The label states a property of `be_daybook_build`
that the check establishes about `be_forward_day`.

SEAT_PROTOCOL rule 16, and the fourth instance of MEM's harvested shape: **the
probe's input was authored by the seat rather than read from the artifact.** The fix
is one line — `isinstance(e, BookRefused) and "no supplied" in str(e)`, with a day
that HAS a ledger entry and no supplied btc windows, or the refusal at `:70` moved
ahead of `present_from_ledger`.

**Everything else in the battery is genuine:** 8/8 reproduced under my own run at the
ledger tree (2.61 s, 354 MB), and the day-supply count is corroborated at a **second
producer's** artifact — `ctaNew_forward_runs/20260903_be45/be_forward_day_receipt_20260903.json`
carries `population.counts_per_coin.btc.n_supplied = 247` (with
`masking.n_masked_at_supply.btc = 40`, so 247 is post-mask). Two surfaces, one number.

## B.3 The digest discipline is right, and I confirmed the shape

`buf = pickle.dumps(book)` → `digest = sha256(buf)` → `dst.write_bytes(buf)` →
`back = sha256(dst.read_bytes())`, with `readback_matches` reported **beside** the
digest and not instead of it, and `digest_is_of_the_buffer_as_written: true`. That is
the B-1 discipline applied to the write side, and the read-back is a genuinely second
statement. ✓ (Not exercised on real data this round — no book was written.)

## B.4 **The builder has TWO roots, and the audit that exists to catch that cannot see it**

Driven from my worktree with `PM_DATA_ROOT=/home/yuqing/ctaNew`:

```
be_daybook_build.LEDGER_DERIVED  = /home/yuqing/ctaNew/data/pm_5min/derived        (resolver)
be_daybook_build.OUT_DERIVED     = /home/yuqing/ctaNew-wt-rev/data/pm_5min/derived (HERE.parents[1])
same tree? False
```

The **book** goes through `_BDR.derived()`; the **scratch fragment** (`frag`, the
input `assemble_streaming` reads) and the **receipt** written by `main()` go to
`ROOT = HERE.parents[1]`. From any worktree those are different trees: the receipt
naming the book lands where the book is not, and a multi-hundred-MB scratch file is
written into the worktree's `data/` — the same act that manufactures the shells this
seat has spent three rounds removing. `be_daybook_build` also never calls
`_BDR.require_ledger`, so no result-bearing refusal guards either path.

**And `be_data_root.audit_literals()` cannot fire on it.** It greps `be_*.py` for the
literal `/home/yuqing/ctaNew`; `HERE.parents[1]` contains no literal. Its own
docstring names the defect it misses: *"BE modules each computed `parents[1]` or
`parents[2]` on their own, so a BE receipt could not say WHICH tree it read."*
Driven, and the surface reported in full rather than truncated:

```
audit_literals()  -> clean: false, n_offending_lines 4
   all four in be_race_read_declaration.py:49/51/53/55 — the ctaNew_forward_runs paths
grep -n "parents\[[0-9]\]" be_*.py  -> 27 lines across 14 files
   only be_read_cells.py routes one through _BDR.resolve(); the rest compute a root
```

**Rule 16 again, on the instrument this time: the audit passes a module whose root is
wrong, because it looks for the spelling and not for the act.** One line — an AST or
regex check for `parents[` in a root assignment outside `be_data_root.py` — would
make it fire on all 27.

## B.5 The selftest cannot be driven from a worktree

`be_daybook_build --selftest` from `~/ctaNew-wt-rev` reaches check 1 and then
refuses: `de_admissible_windows.py:64` is `ROOT = Path(__file__).resolve().parents[2]`
with no resolver, so it looks for
`~/ctaNew-wt-rev/data/pm_5min/derived/da_blackout_mask_20260903.json`, which is
present at the ledger and **not tracked** (2 masks are tracked; this is not one).
It refuses closed and names the expected path — the right direction — but the R-397
worktrees exist so a reviewer can verify in isolation, and this battery cannot be
verified there. Same class as the `--fixture-run` finding I filed against DE two
rounds ago; I ran it at the ledger tree instead, after confirming
`be_daybook_build.py`, `de_admissible_windows.py` and `be_forward_day.py` are
byte-identical there to `11bd651`.

---

# (C) THE RACE-READ DECLARATION v2 — **APPROVED TO OPEN**

`be_race_read_declaration_v2.json`, `as_of 2026-09-06T05:21:16Z`, 231 leaves,
selftest **14/14** under my own run. v1 is **untouched and it is proven**: exactly one
commit touches it (`1c10075`, BE round 46), and the blob at HEAD equals the file on
disk equals BE's claimed `27e2fa5d09ecf275…`. Rule 13 satisfied by evidence.

## C.1 Item 1 — the floor at both readings, conservative in the resolved field: **CLOSED**

```
OPTIMISTIC_all_five_days_fresh          G=5  2^-5 = 0.03125  x m2 -> 0.0625
PESSIMISTIC_only_three_first_openings   G=3  2^-3 = 0.125    x m2 -> 0.25
permutation_floor.best_possible_adjusted_p = 0.25   <- the CONSERVATIVE one
WHICH_ONE_IS_THIS_FIELD = "the CONSERVATIVE (pessimistic) reading…"
smallest_G_that_clears_at_m2 = 6        neither_clears_0_05 = true
```

**Both arithmetics recomputed and correct**, and the worse number is the one a reader
resolves — which is exactly what I asked for and the opposite of the usual direction.
`who_weighs_the_readings` refuses to resolve the weight and hands it to the USER and
the coordinator, which is right.

*One consumer note, not a defect:* `G` and `cluster_disclosure.G_complete_days` both
read **5** while the resolved floor is the **G = 3** number. The pairing is
conservative, so nothing can be over-claimed from it — but a consumer recomputing
`2^-G · m` from the two fields gets 0.0625, not the 0.25 beside them. A
`floor_is_computed_at_G` field naming 3 would close the gap between the two resolved
values.

## C.2 Item 2 — what the read writes, and the digests: **CLOSED, and all five reproduce**

I recomputed every pin at its artifact, reading bytes and hashing them — **no
`json.load`, nothing parsed, no seal consumed**:

```
20260901  MATCH  aca22317ab06adbf  54,213,086 B   (~/.local/state/pm-co/race_record_20260901_fwd5/)
20260902  MATCH  7522786db5423574  52,911,716 B   (ctaNew_forward_runs/20260902_be13/)
20260903  MATCH  c466a3fe590f2d59  58,851,778 B   (ctaNew_forward_runs/20260903_be45/)
20260904  MATCH  a610b64ab7e20476  61,595,588 B   (ctaNew_forward_runs/20260904_be45/)
20260905  MATCH  1370598870c5fa75  42,663,031 B   (ctaNew_forward_runs/20260905_be44/)
all five reproduce: True
```

`what_the_read_writes.WRITES` names one artifact
(`data/pm_5min/derived/be_race_read_result_v1.json`), states that no per-day file and
nothing under the run dirs is written, and `REQUIRED_AFTER_THE_READ` makes the
post-read byte-identity check **required**, with `on_mismatch: the read is VOID`.
`digested_never_opened` states the principle correctly.

**RESIDUAL — the check has no code to live in.** `be_race_read_result_v1` appears in
exactly **two** places on the whole `live/` surface: the string literal at
`be_race_read_declaration.py:204` and the emitted JSON. **There is no reader.**
`REQUIRED_AFTER_THE_READ.who_runs_it` is *"whoever opens"* — so the declaration's
strongest safeguard is an instruction to a person, and the person is the coordinator
or the USER. That is the same shape as DE's R5 seal before it became a code path, one
step weaker because the consumer does not exist at all. **Not a blocker on opening**
— but whoever opens must run it, and it should be stated in the dispatch that opens
the read, not only in the declaration.

*Also worth one line:* day 09-01's sealed scores sit in
`~/.local/state/pm-co/race_record_20260901_fwd5/` while the other four sit in
`ctaNew_forward_runs/`. Real and pinned, and the asymmetry is not disclosed anywhere
in the declaration; the four `ctaNew_forward_runs` literals are also what makes
`audit_literals()` report this module **not clean** (§B.4).

## C.3 Item 3 — the Gate-1 separation field: **present, and it matches a constant against a constant**

```python
opened = [SEALED_SCORES[d] for d in RACE_DAYS]          # a module constant
hits = sorted({pat for pat in GATE1_ARTIFACT_PATTERNS   # a module constant
               for p in opened if pat in p})
```

**Both the needle and the haystack are authored in this file**, and the haystack is
the five **path strings**, not what any run opens. The field therefore establishes:
*none of seven substrings appears in five filenames* — which no run could make false.

**I drove the positive control the battery does not have:**

```
as shipped                                        -> hits []            assertion True
with a Gate-1 path planted into SEALED_SCORES     -> hits ['be_daybook_'] assertion False
restored                                          -> hits []
```

So it is **not vacuous as a function** — it can fire — but it is **vacuous as a
check**: it fires only when its own constant is edited, and the selftest
(`:458–463`) asserts `matches_found == []`, i.e. it asserts the empty outcome and
never shows the instrument can produce a non-empty one.

**The fix is the discipline BE already applied one field away.** `what_is_opened`
carries `source_of_this_field_list: be_forward_day.seal(), read at the WRITER` —
derived from the producer rather than typed. `gate1_separation` should derive
`opened` from the **reader** the same way, and ship the planted-path case as a
positive control so the field can be seen to fire.

## C.4 The rest, checked rather than accepted

`R_529_A_UP_FRONT` is the first substantive field; `statistic` is inherited with each
element citing its authority; `m = 2` is **read** from `be_freeze_audit`, not typed,
and the selftest names the earlier version's bug at that exact site; `rule_11_in_force`
still closes the sixth-day escape in advance; `what_is_opened` names the fields from
the writer and `STAYS_SEALED` keeps the feed shut on rule 11 grounds; `opens_nothing`
and `selects_nothing` hold — filing the declaration wrote nothing.

## C.5 Verdict on the race read

**APPROVED TO OPEN.** None of the three residuals changes what the read may
conclude, and the substantive protections are real and reproduce. Carry into the act
that opens it: **(i)** the byte-identity recheck is required and no code will do it;
**(ii)** the resolved floor is 0.25 and the resolved G is 5 — quote them together;
**(iii)** the Gate-1 separation is true, and I have checked it independently, but the
field is not the evidence for it.

---

# WHAT THE GATE-1 LINE NEEDS BEFORE ANY BOOK EXISTS

Ordered by what blocks what. Nothing here is a fix I am applying; all of it routes.

1. **A DECLARED population interval for the ruled day set** —
   `POPULATION_SLUG_INTERVALS` has none for September and refuses undeclared names.
   This is a declaration and it is the first thing, because steps 2–4 cannot start
   without it. It is also a rule-11 object: the interval must be declared before any
   September row is materialised, not fitted to what the pass produces.
2. **A path parameter for the state tape.** `phase2_arms.tape_index` reads the module
   constant `TAPE_PATH`; the only alternatives today are overwriting the live tape
   (correctly refused) or not running. **DE's surface, and it gates step 4.**
3. **The two feature passes, in order** — fragment (`harmful_exposure_rows.build_rows`,
   ≈651 MB) then state tape (`build_state_tape_v2.main`, ≈969 MB) — per day, six days.
   **This is the "much larger job" BE names, and it is twice the size BE names.**
4. **The builder stops disabling the split partition** — pass the day's own tape (or
   `tape=None`) so `assemble_streaming` holds ~2.5 GB rather than 4.01 GB, and check
   the emitted `assembly.partition_by_split` reads **true**.
5. **`be_daybook_build`'s two roots collapsed to one** (§B.4), with
   `_BDR.require_ledger` on the result-bearing emission — before a book is written,
   not after.
6. **The tautological known-bad replaced** (§B.2), and `day_slugs`'s own refusal given
   a case that reaches it.
7. **`audit_literals` extended to `parents[N]` root assignments** (§B.4) — 27 lines,
   14 files, so that this class stops recurring one module at a time.
8. **The rule-20 sequencing settled** (§A.6): the six-day schedule is a serial-queue
   problem before it is a memory problem, and the lock is currently being bypassed by
   a scope that took the slice without taking it.

---

## CONTEXT

Approximately 55%. Below the 80% reset threshold; I will report the crossing when it
happens.
