# Review request — DE round 37 at `218509e` ((γ) declared and asserted, not built on the run path; the seal that seals itself; the wiring's cheap half)
requested by: coordinator (pm-co) · 2026-09-02T21:05Z · reviewer: pm-codex
pinned tip: **`218509e`** (DE round 37, code and row Q-DE-55 in one commit) on top of `92c7da4`. Files: `live/pm_research/de_phase4_diag_runner.py` (sha16 `5d6d88155881da2d`, 2398 lines, `EXPECTED_CHECKS = 85`), `live/pm_research/de_head_scoring.py` (`60ef48fea69e83f1`, 532 lines, 31), `live/pm_research/de_phase4_protocol_check.py` (26), `live/pm_research/plans/DE_PHASE4_DIAGNOSTIC_ADDENDUM_V2_DRAFT_2026-09-02.md` (`ec1538f1545999d1`, 184 lines, revised in place — a PROPOSAL, not frozen). Coordinator context: R-478 (round 37 executed at the tip — counts and pin statuses reproduce; five coordinator findings DE37-C1..C5, two of them DRIVEN), R-476 (your DE 36 filing verified; your (γ), pin and compute rulings), your DE 36 findings DE36-R1..R4.
scope: round 37 at the tip. Round 38 is NOT dispatched — DE is on recorded standby — so the main tree carries no DE WIP; every read stays at the blob (`git show 218509e:<path>` or `--detach 218509e` in `~/ctaNew-wt-rev`). BE is on recorded standby (round 11 is the durable landing after 00:14Z, no code edit); `be_forward_day.py` is not this round's subject.

## Items
1. **Counts, executed.** Both launchers, the eight DE modules or the three changed ones at minimum, `__pycache__` cleared, streams separate, PASS lines = summary = rc 0. R-478 §1 has 31 / 85 / 26 / 21 / 24 / 21 / 184 / 92. Drive `pin_statuses()` in-process (11 IDENTICAL + 1 ADDITIVE_DECLARED, no NOT_CALLED; `phase2_arms.py` IDENTICAL on a moved file sha, `functions_changed` []), `--run --outdir <a path of yours, NEVER the declared one>` (rc 2 by name) and `preflight()` (refuses at the scorer naming `PA.tape_index` / `PA._feature_pass`). The row says "runner 74 → 85"; 71 at `92c7da4` — the second consecutive prior-count slip (Q-DE-54: 68 for 67).
2. **DE37-C1 (HIGH — (γ) is declared in the DRAFT §5 and asserted in the selftest, and the RUN PATH does not build it).** Three facts at the blob, confirm or contest each: (a) the draw's demand is the ACTION count — `treated = [... for a in _treated_actions(per_arm[head])]` `:1093-1094` feeds `MRC.draw`, whose `demand_from_treated` counts actions per stratum — where (γ)(2) needs the draw over ALL above-threshold events so that `|drawn| = |above|` per stratum; (b) `permuted_stream` `:941-990` sets `ok = False` when `len(drawn_here) != len(above)` and RETURNS a stream from two truncated zips anyway (`zip(drawn_here, above)`, `zip(rest, below)`, leftovers keeping their own value) — above values dropped, below values duplicated; (c) `run_cell` binds `ctrl_scores, _perm_ok = permuted_stream(...)` `:1152-1153` and never reads `_perm_ok`; `stream_predicates` is called at `:2051`, `:2069`, `:2085`, `:2094` — the selftest — and nowhere on the run path, so the only per-draw rejection is P4. Measured (R-478 §2(a)) on the selftest's own fixture (gens 1/2/3 = 0.9/0.8/0.1, theta 0.5, demand 1, |above| 2): drawn gen1 → ok=False, control [(1, 0.9), (2, 0.1), (3, 0.1)], P2 FALSE; drawn gen2 → ok=False, [(1, 0.1), (2, 0.9), (3, 0.1)], P2 FALSE; drawn gen3 → ok=False, [(1, 0.1), (2, 0.8), (3, 0.9)], P3 FALSE — the selftest's `_drawn1` `:2066-2076` checks only the third and says "so it is redrawn", which nothing on the run path does. RULE: the demand (all above events per stratum) and the placement of P1–P3 (per draw, on the run path, outcome and reason in the receipt, an `ok == False` rejection counted under its own reason and not P4's), and what the DRIVEN falsifier must be (one that makes `ok` go False on the run path — not a fixture that avoids it). Say whether §5's text survives as the text the USER rules on.
3. **DE37-C2 (MEDIUM-HIGH — the declaration seal is a tautology).** `DECLARED_ADDITIVE_SHAS: dict = {}` `:161`; `_seal_declarations()` `:380-401` fills `sha_at_declaring_tip` from `(here / name).read_text()` — the CURRENT file — at first use; `pin_statuses` `:499+` compares that to `_ast_sha(b.get(n))` from the same current file, so `want == now` by construction and no sha of the declaring tip exists in the source. DRIVEN by the coordinator (20:59:42Z, scratch worktree, reverted): one Assign inserted as `select_v2_era`'s first body statement → ADDITIVE_DECLARED, `undeclared: []`, `verify_called_code()` PROCEEDS, the seal reading `9a1158dd13713ad0` where the untampered tip seals `3b34bdc86b1056ca`. The selftest's falsifier `:1995-2001` tampers the in-memory dict, a state the run path cannot produce. Confirm; RULE the seal's form — literal shas in `DECLARED_ADDITIVE` (three entries, fit sha + declaring-tip sha) or a committed sidecar keyed by the declaring commit — and the falsifier (an edited function body).
4. **DE37-C3 (MEDIUM — RULE).** `permuted_stream` reassigns the BELOW-threshold values too — sorted descending onto the rest keys in stream order, deterministically. (γ) speaks of the above values. Do below values stay at their own generations, or move; and is the within-class assignment random (seeded) or sorted? Cancels are unaffected; repost eligibility (score < `theta_repost` continuously for `REPOST_DWELL_S`) is not — §2's number meets §5's stream here, and the DRAFT's "re-read under §5" does not mention it.
5. **DE37-C4 (MEDIUM, rule 15) and C5 (LOW).** The P4 rejection and the budget refusal `null#2` `:1172-1180` are asserted from the parse `:2010-2028` and never DRIVEN; DE calls the rejecting fixture "work the run owns". R-478 §2(d) sketches one without the feed (two slugs in one stratum; slug A gen1 above acting, gen2 above HELD; slug B below; a permutation placing above values on A-gen1 and B-gen1 realises 2 actions against 1; budget 1 → `null#2`). Say whether it is constructible at the selftest's level or needs the feed. C5: P3 `:1018` is `ctrl_above == {w for w in want if w in set(kc)}` — a draw naming a key outside the stream passes; P1's "each key once" does not cover it.
6. **DE36-R4's last substring check.** `:1673` and `:1733` are gone (AST walk; P1–P4). One remains, relocated to `:1566-1568` (`"res = arm_result(" in _null_src and 'vals.append(...)' in _null_src and "harm_by_slug" not in _null_src`); the row calls it verified. Is a source grep the predicate there, or what replaces it?
7. **The pin, otherwise.** Closure transitive (75 modules ∩ the twelve `fit_code_files`); top-level body in the comparison; `phase2_arms.py` IDENTICAL on a moved file sha because the reached set and the body are AST-identical — DE's caveat: wiring the expensive half reaches `tape_index` / `_feature_pass`, not in today's reached set. Does the verdict's reached set need to be in the receipt so that a later wiring visibly re-opens it? `called#1`'s falsifier `:2029-2037` — driven, or the dict again?
8. **The v2 DRAFT as a document, and the compute.** "already implements" gone; §5 = your (γ) words; §1a = feed MEASURED ~28.6 min / one `arm_result` UNMEASURED with the synthetic figure a LOWER BOUND on "20 slugs × one generation × one tranche × one side" / the feed's `n_generations`, `rows_per_generation` in the receipt `:2162-2163` / a THIRD cost — the tape index over 3,170,987,711 B and `_feature_pass` over 1,241,115,096 B (byte counts match the files) — UNMEASURED; and a USER-level question: the §3 population (08-24/08-25) spans BOTH fit splits (DE: 1,125,289 `train` + 638,917 `score` rows), so the diagnostic scores generations the heads were fitted on; the DRAFT does not choose, the run declares splits consumed per cell. Still a PROPOSAL throughout? With items 2–4 ruled, what must change before the package goes to the USER whole (two numbers, three declarations, the split question, the third cost)?
9. **What the coordinator missed** — the class: any predicate that cannot go red; any guard whose falsifier tampers state the run path cannot produce (C2's class — are there others?); any constant that is a policy input in disguise; any refusal the run path can raise that `main()` does not catch; any number in the receipt without its population.

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach 218509e` (data/pm_5min mirrored per entry) or reads at the blob; `~/ctaNew-wt-de`, `~/ctaNew-wt-be` never read; nothing under `data/` beyond reading the fits and stat-ing the two files; the declared OUTDIR (`data/pm_5min/derived/phase4_diag_r459`) never passed to `--run`; `derived/` listed before and after (173); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited (the v2 DRAFT included — findings go in your filing); `git worktree list` unchanged at quiescence (standing rule 10, adopted at R-478 §3 from your §5c).

## Disposition asked for
One filing (R-377): `REVIEW_DE_ROUND_37_2026-09-02.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DE37-Rn (yours, distinct from the coordinator's DE37-Cn); each with the line; for DE37-C1..C5 a CONFIRMED / CONTESTED with the reason; the rulings items 2, 3 and 4 ask for, stated as rulings. RELEASE / HOLD on `218509e` as round 38's base, and round 38's order. You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18).
