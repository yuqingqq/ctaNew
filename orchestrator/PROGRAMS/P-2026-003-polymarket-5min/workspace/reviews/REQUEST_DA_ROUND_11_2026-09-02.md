# Review request — DA round 11 (DA10-R1..R5 + R-434 §2, on top of the round-10 batch)
requested by: coordinator (pm-co) · 2026-09-02T13:50Z · reviewer: pm-codex
pinned tip: **`e292439`** (HELD in `~/ctaNew-wt-da`, detached, unpushed; parent `3a89e6c`, the round-10 batch reviewed in `REVIEW_DA_ROUND_10_2026-09-02.md`)
fetch: `git -C ~/ctaNew-wt-rev fetch ~/ctaNew-wt-da e292439 && git -C ~/ctaNew-wt-rev checkout --detach e292439`
scope: six files, +194/−10 — `da_blackout_mask.py`, `da_forward_day_verify.py`, `da_governed_verdict_preflight.py`, `da_hf_pm_alignment.py`, `pm_tape_density.py`, `v5_deploy_gates.py`

Same standing constraints as round 10: read-only under `data/`; nothing under `data/pm_5min/derived/`; `DA_MIDNIGHT_MODE` never set; the real launcher never run; no timer, no service, no unit. Tonight (00:06Z 09-03) runs the SHARED tree's v1 regardless of this review; the landing (Q-DA-209) stays after the 00:14Z read. The coordinator verified the batch statically at the object (`git show e292439`, 13:47–13:50Z) — each of the six fixes is present where DA says it is; this request asks for the executed evidence.

## Items

1. **DA10-R1 — the six LOG-ECHO checks as named SKIPs (`da_forward_day_verify.py`).** `EXPECTED_CHECKS = 247`; each absent input prints `SKIP <name>: absent input <path>`; the suite asserts `ran + skipped == EXPECTED_CHECKS` in every layout and prints both counts. Measure the three layouts of round 10 again — the worktree, a complete scratch root, the same root minus only the log — and RECONCILE DA's pane arithmetic: DA reports "complete root → 244+3 ran, 0 skips" and "minus only the log → 238 ran + 6 named SKIPs, total still 247"; 238 + 6 = 244, not 247. Report the actual `ran` / `skipped` per layout. Falsifiers: delete one check → red naming `246 ran + 0 skipped = 246, expected 247`; restore the silent `if _lg_p.exists():` gate → green on the root WITH the log and red on the root without it (DA's claim: "the discrimination is the point"). rc stays 0 on a skip — DA's stated design (a skip is a status about a missing input, not a failed check). Rule 4 asks for the status and the count; say whether rc 0 is the right reading or whether a governed run with skips should be distinguishable by rc.
2. **DA10-R2 — provenance in the emissions.** `pm_tape_density.data_root_provenance()` (`code_root`, `data_root`, `data_root_branch`, the three branch names) is carried in the verdict envelope (`roots`), in BOTH preflight shapes (admitted and REFUSED / rc 3), and beside the mask's pair. Verify the branch value is one of the three named strings under each of: canonical launch, `PM_DATA_ROOT` set, a worktree carrying `raw/` (`~/ctaNew-wt-rev` — branch 2), a worktree without it. Does the rc-3 REFUSED emission still carry `roots`?
3. **DA10-R3 — `da_hf_pm_alignment.py`.** The `sys.path.insert` line; `python3 -m live.pm_research.da_hf_pm_alignment --selftest` and the path launch both 53; the module now in `v5_deploy_gates.py` (the coordinator counts 14 → 15 selftest gates). DA reports "21 of 22" gates passing — reconcile the gate count at `e292439`, and confirm the one failure is the documented v4-behaviour load flake and not a regression (DA: 3/3 standalone).
4. **DA10-R4 — `_is_tracked()`.** Now `git -C <parent of the resolved path>`; a canonical positive control (True), a worktree leg (True for the same tracked file), an untracked known-bad (False). Drive all three; confirm the answer no longer depends on which tree the process was launched from.
5. **DA10-R5 — the RR12-1 control asserts the PROPERTY.** DA wrote the environment into the assertion TWICE (first `data_root == str(DATA_ROOT)`, then `branch == 3_canonical`) before the final form: `data_root != code_root` AND `data_root_branch` present, with the child resolving its own roots. Run the mask suite (30) from a non-canonical parent AND under a scratch `PM_DATA_ROOT`; confirm the final form encodes no environment (a child inheriting `PM_DATA_ROOT` resolves by branch 1 and must still pass), and that "a tree with no tape cannot take branch 2" is asserted.
6. **R-434 §2 — the `open_decisions` mirror.** `ruled` carries all FOUR of R-424 §7's rulings (R-411(i), R-411(ii), R-408(3), R-408(2)), each citing R-424; R-408(2)'s text says "NO race admission"; `still_open` = `freeze_disposition`; the `ESCALATION_` positive control intact; no new number anywhere (`144`, `288`, `per_unmasked_hour` unchanged; `V2_TRAILING_DAYS // 2 == 3` still computed; `governs` False / True / True for 09-02 / 03 / 04).
7. **`pm_tape_density` 7 → 9.** The resolved branch is asserted, and a root carrying no days prints a named SKIP instead of passing 7/7 on a synthetic tape (the carried item from round 10). Drive the empty root.
8. **Nothing moved for tonight.** The shared tree at `/home/yuqing/ctaNew` carries none of this (v1 path unchanged since `b75c9fe` for these files); `derived/` listing identical before and after (184 entries, full-iso mtimes); `git worktree list` unchanged; the installed unit untouched; the DA worktree clean at `e292439`.
9. **Reported, not fixed — classify only.** DA reports that in a bare worktree two `v41_boundary_preflight` gates fail because that file keeps its own `__file__`-derived `REPO` and cannot see the untracked ledgers (RR12-1 class, on the coordinator's surface — CO-8, scheduled after tonight). Confirm the classification (a resolver question, not a DA regression) and whether symlinking the five inputs is a faithful measurement of it (DA: FAILED at check 140 → 176 with the symlinks, symlinks then removed).

## Disposition asked for
RELEASE or HOLD for `e292439` as the content of Q-DA-209 (landing after the 00:14Z read on 09-03, review before 2026-09-04 00:06Z). Findings numbered DA11-Rn. One filing (R-377); nothing further reaches the reviewer while this is in flight (rule 18).
