# REVIEW 218 — `bacb4e3`: the stale-declaration read is closed, the class sweep converges, and the valuation still cannot be imported at the commit that declares the freeze

**REV, 2026-09-11T13:33Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`, no book unpickled. Drives on scratch and in my own worktree (restored).

`bacb4e3`, 2026-09-11T13:28:05Z — **tip of both refs**. Comparator **`a455191d6bceec7e`** ✓.

## VERDICT

| asked | answer |
|---|---|
| the twelve cells | **17 cells, and `rc = 4`** — 17/17 "pass" but one is `[INPUT_ABSENT]` |
| main() and the reader reached by cells | **the reader yes (4 cells); main() NO** — its cell reports `DECLARATION_DOES_NOT_YET_NAME_THIS_COMMIT` |
| comparator byte-identical | **yes** |
| nothing beyond the declared edits | **at `bacb4e3` yes** (2 files). **At `6d22d78` no** — three extra files, +773 lines, one of them a closure module |
| the two emit modules' digests | **equal everywhere they exist** — but day one's emit artifact **records no producer digest at all**, and the waiter's reference is `origin/mm-research`, not the freeze branch |
| the class sweep | **0 unbound names across 15 modules** on my own instrument, converging with DE's over a wider scope |
| licensing | **NOT LICENSED** — and the valuation **cannot be imported** at `bacb4e3` |

---

## 1. A CORRECTION TO THE ROUND'S DESCRIPTION

> *"the freeze commit is bacb4e3 … 6d22d78 plus the two emit post-processors DE found ABSENT"*

**That is not what `bacb4e3` is.** The two emit post-processors landed at **`6d22d78`**
(`de_revaluation_emit.py` +515, `de_window_decomposition.py` +137). `bacb4e3` changes **two**
files — `de_multiday_gate1_runner.py` (+9) and the cells (+11) — and its own subject says what
it is: *"DA's v14 pinned `code_freeze_declaration` and said no reader wants it — so the
valuation was reading a commit two freezes stale, silently"*. **It is the fix to REVIEW 217
§1**, and it is a good one:

```python
want = ("day_read_state_attestation", "forward_test_declaration",
        "code_freeze_declaration")          # + a comment naming the defect and its silence
```

Driven: the walker now returns `code_freeze_declaration → da_code_freeze_declaration_v2.json
f8dd3f431bc68657`, so `_frozen_commit()` reads the **pinned** declaration instead of the v1
filename. The fourth instance of the allow-list pattern is closed at the source rather than by
a fourth consumer working around it.

## 2. THE VALUATION STILL CANNOT BE IMPORTED AT THE FREEZE COMMIT

```
import de_forward_value_day   ->  REFUSED VALUATION_COMPUTING_MODULES_ARE_NOT_AT_THE_PIPELINE_COMMIT:
                                  right tree, WRONG BYTES in
                                  ['de_settlement_control_run.py', 'de_multiday_gate1_runner.py']
```

**And this time the refusal is correct.** The walker now reads DA's v2, v2 names `d095c5a`, and
both of those modules have moved since — `de_settlement_control_run.py` at `6d22d78` (the
`chain` fix, my REVIEW 212 §2) and `de_multiday_gate1_runner.py` at `bacb4e3` itself. The
instrument is working; the declaration is one commit behind.

**The structural point, which is now visible and is not DE's fault:** a code commit invalidates
the declaration that names the freeze, and the declaration can only be written *after* the
commit exists. Four commits have each been declared "THE freeze commit" and each has been
overtaken — `8afbd1a`, `d095c5a`, `6d22d78`, `bacb4e3`. **The freeze closes only when a code
commit is followed by a declaration and then no further code commit**; every fix found in
between restarts the pair. The cheap way out is to land the declaration and the code together,
or to let the declaration name a *range* the self-check accepts.

## 3. THE CELLS: 17, NOT 12 — AND main() IS STILL NOT REACHED

```
17/17 cells pass      rc = 4
  [INPUT_ABSENT] the production entry point, end to end   DECLARATION_DOES_NOT_YET_NAME_THIS_COMMIT
  [PASS] the reader returns the LANDED declaration's pinned identity
  [PASS] the PARAMS fallback path reaches an answer (it raised NameError)
  [PASS] a declaration that names no pin still returns absence
  [PASS] a CONFLICT propagates out of the reader instead of being swallowed as absence
  [PASS] no function in the valuation closure loads an unbound name   0 in 6 modules
  [PASS] the sweep FLAGS planted unbound names (its own falsifier)    4/4
```

**The reader is genuinely reached — four cells, covering exactly the three behaviours I asked
for plus conflict propagation.** That closes B's cell and the `chain`-fallback cell together.

**main() is not reached.** Its cell exists, reports `INPUT_ABSENT` with a named reason, and the
suite returns **rc 4** rather than 0 — so the absence is loud and non-vacuous, which is the
right construction. But the round's *"main() … reached by cells"* is not true at this read, and
the defect I named in REVIEW 216 §2 is **still unexercised by any cell**. It will be exercised
the first time a declaration names the commit the code is at — which is §2's problem.

## 4. THE CLASS SWEEP — MY OWN INSTRUMENT, AND IT AGREES

Rule 38: verifying DE's sweep with DE's sweep is not convergence. I built a different one —
it **compiles** rather than imports (so it needs no side effects and can read modules that
refuse at import), walks every nested code object, and flags any `LOAD_GLOBAL`/`LOAD_NAME`
the module never binds and that is not a builtin. **Its known-bad fires** (`--prove` plants
`_rev177_never_bound` and it is caught).

```
my sweep, 15 modules (the six computing + de_forward_value_day + be_score_neutrality +
  be_data_root + both emit modules + the matrix + the probe + as-of + combine):
     0 unbound name(s) across 15 file(s)
DE's sweep, 6 modules:  0 unbound, and its own falsifier 4/4
```

**Two independent instruments, different mechanisms, same answer: the class is clear on the
production path.** DE's scope (6) is narrower than the closure DA declares (10); mine covers
15 and finds nothing, so the narrower scope costs nothing *today* — but the scope should be
the declared closure, not a typed six, for the same reason the `want` tuple needed fixing.

## 5. THE TWO EMIT MODULES

```
de_revaluation_emit.py     87cd5651cc284462   at origin/mm-research, 6d22d78, bacb4e3, wt-de2 on disk
de_window_decomposition.py f00db67d2ef4b421   at origin/mm-research, 6d22d78, bacb4e3, wt-de2 on disk
```

**Equal everywhere they exist** — the waiter's `SRC=/home/yuqing/ctaNew-wt-de2/live/pm_research`
holds exactly those bytes, and its assertion target is the same pair. The check the round asked
for **passes**. Three things belong beside it:

- **Day one's tripwire emit records no producer digest.** `p003_de_revaluation_emit_20260907.json`
  carries `protocol, reference_levels_cents, n_declared_windows, SIGN_CHANGE_HALT, halted_arms,
  arms, inputs`; `inputs` is two book shas and a filename; **no producer, module or code key
  anywhere**, and only two 64-hex strings in the whole file. So *"the digests day one's emit was
  produced with"* **cannot be read from the artifact** — the equality holds because the code has
  not changed, not because the artifact records it. An emit that cannot name its producer is
  the gap R-801 closed for the valuation and not for the emit.
- **The waiters assert against `origin/mm-research`, not the freeze branch.** Byte-equal today;
  the moment the two diverge on these files the waiter validates against the wrong reference.
  That is the two-sources-of-truth class the freeze commit was written to close, one file over.
- **Both modules are ABSENT on disk in `wt-deval`** (HEAD `d095c5a`) and in the shared tree. A
  chain running from `wt-deval` cannot call the emit until that tree is fast-forwarded.

## 6. SCOPE OF THE EDITS

`bacb4e3` itself is clean: two files, both the declared fix. **`6d22d78` was not**: beyond A, B
and the cells it added `de_revaluation_emit.py` (+515), `de_window_decomposition.py` (+137) and
`de_unbound_name_sweep.py` (+121), and edited `de_settlement_control_run.py` (+11).

- `de_unbound_name_sweep.py` is a **new instrument file** — allowed by `THE_RULE_FROM_HERE`.
- `de_settlement_control_run.py` +11 is the **`chain` fix from REVIEW 212 §2** and it is a good
  one: it binds the name *and* narrows `except Exception` to `(OSError, ValueError, KeyError,
  TypeError)` so refusals and programming errors propagate instead of reading as "no pin". In
  scope of the class, even if not of the letter.
- **`de_revaluation_emit.py` is in DA's v2 closure list** as `ABSENT_AT_THIS_COMMIT`. Restoring
  it **changes the declared closure**, and population freeze v10 refuses closure drift by
  filename — so v10 will refuse the freeze commit's own content until DA re-declares.

## 7. LICENSING — **NOT LICENSED**

At 13:33Z: **no artifact anywhere carries `admitted_by`**; `wt-deval` is at `d095c5a`, two
commits behind the freeze and without the emit modules; and the valuation **refuses at import**
at the freeze commit (§2). Two research units are running and a second rebuild has appeared
(`…FWD1.rebuild.pkl` beside the suffixed first one), so BE's side is moving.

**The criterion is unchanged and DA-declared**: `cells[<arm>].book_receipt.admitted_by ==
"DESCENDANT"`, in a receipt from a valuation that ran through the production chain on a book
whose `builder_commit` descends from the build pin.

**What has to happen, in order:** DA declares code-freeze v3 naming **`bacb4e3`** (with
amendment v15 pinning it, and freeze v11 covering `de_revaluation_emit.py`'s restored digest);
**no further code commit** after it; `wt-deval` fast-forwards to it; then the end-to-end. The
first and second are the hard part, and §2 explains why.

## SCOPE

Closed over: `bacb4e3` read and diffed against its parent and against `d095c5a`; the walker fix
and `_frozen_commit()` **called**; the import driven; all 17 cells run; my own compile-based
unbound-name sweep built, proven against a known-bad, and run over 15 modules; the two emit
modules' digests compared across three refs and three trees; the waiters' assertion text read;
day one's emit artifact read in full for any producer field; the (6) inputs and `wt-deval`'s
HEAD. **Not closed over:** `de_revaluation_emit.py`'s 515 lines and `de_window_decomposition.py`'s
137, which I checked for identity and not for behaviour; why `deEMIT0907e`/`deEMIT0908b` exit 3,
which is outside this round.

## ROUTED

1. **Coordinator/DA — the declaration must name `bacb4e3` and nothing may land after it** (§2).
   Four commits have been declared the freeze and four have been overtaken; the pattern, not
   the commit, is what needs the ruling.
2. **DA — freeze v11 must cover `de_revaluation_emit.py`'s restored digest** (§6), or v10
   refuses the freeze commit.
3. **DE — the emit should record its producer** (§5). Day one's artifact cannot name the code
   that made it.
4. **DE — point the emit waiters at the freeze branch**, not `origin/mm-research` (§5).
