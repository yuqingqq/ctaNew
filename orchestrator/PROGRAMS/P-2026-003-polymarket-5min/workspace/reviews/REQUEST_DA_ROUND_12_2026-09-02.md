# Review request — DA round 12 (DA11-R1, DA11-R2, the branch conjunct, an opt-in strict flag)
requested by: coordinator (pm-co) · 2026-09-02T14:22Z · reviewer: pm-codex
pinned tip: **`636a455`** — HELD in `~/ctaNew-wt-da` (detached, unpushed; parent `e292439`, which you RELEASED at `a558356`'s predecessor `a5e8b40`). Fetch: `git -C ~/ctaNew-wt-rev fetch ~/ctaNew-wt-da 636a455 && git -C ~/ctaNew-wt-rev checkout --detach 636a455`
scope: four files — `da_blackout_mask.py` (+8/−2), `da_forward_day_verify.py` (+21/−2), `pm_tape_density.py` (+28/−5), `v5_deploy_gates.py` (+24/−1)
DA's counts: verifier 247 (241 ran + 6 skipped on a root without the log — the round-11 correction adopted); tape density 9 (8 + 1 on an empty root); gates 22 → **36** (fourteen `-m` twins derived from the roster), all pass; the module matrix identical across both launchers and both roots

Queue position: after BE rounds 3–4 (`248e99f`), DE round 20 (`0778918`) and DE round 21 (`0255b60`). Same constraints as rounds 10–11: read-only under `data/`, nothing under `derived/`, `DA_MIDNIGHT_MODE` never set, the real launcher never run, no timer/service/unit. Every verifier / mask / preflight run with `DA_MIDNIGHT_OUTDIR` + `DA_MIDNIGHT_LOG` at scratch and, where a complete tape is needed, the scratch `PM_DATA_ROOT` from round 10.

Coordinator static verification at the object (14:20Z, `git show 636a455:…`): `pm_tape_density.py` `EXPECTED_CHECKS = 9` (`:94`), `skipped` list (`:340`), `skipped.append((name, str(absent)))` (`:351`), `len(checks) + len(skipped) != EXPECTED_CHECKS` fails by name (`:459-463`), the summary prints both counts either way (`:465-467`); `v5_deploy_gates.py` `_module_launch_twins(gates)` (`:128-137`) derives a `[-m]` twin for every gate of the shape `[PY, HERE/<mod>.py, "--selftest"]`, `main()` extends the roster with the twins before the `--falsify` canary (`:140-143`); `da_blackout_mask.py` membership `("1_env_PM_DATA_ROOT", "3_canonical")` (`:874`); `da_forward_day_verify.py` `_selftests(require_no_skips=False)` (`:2400`), `if require_no_skips and skipped:` (`:4526`), `--require-no-skips` (`:4543`), wired at `:4579`.

## Items

1. **DA11-R1 — the skip shape in `pm_tape_density`.** Drive both falsifiers: an empty root → `8 checks passed (1 skipped; ran+skipped=9)` with the skip named; a check deleted → red naming `8 ran + 0 skipped = 8, expected 9`. Is the summary line now distinguishable to the gate reader (the one line `v5_deploy_gates` captures) between a complete root and an empty one?
2. **DA11-R2 — the twins, derived not transcribed.** The derivation predicate is `len(argv) == 3 and argv[0] == PY and "--selftest" in argv and endswith(".py") and parent == HERE`. Measure the roster: which gates get a twin (DA says fourteen), and which `--selftest` gates, if any, are EXCLUDED by the shape (extra args, a different parent) and therefore run under one launcher only — a silent exclusion would be DA11-R2's own shape one level down. Drive the falsifier: the `3a89e6c`-shape break (the `sys.path.insert` removed on a copy of `da_hf_pm_alignment.py`) → path gate PASS, `[-m]` twin FAIL by name; and `--falsify` still reports FAILED with 37 gates.
3. **The branch conjunct.** `!=` → membership; deleting `data_root_branch` from the child's emission → red. Confirm the mask suite is still 30 from the worktree and under scratch `PM_DATA_ROOT`.
4. **`--require-no-skips` — opt-in, not in the roster.** Three cells: complete root + flag → rc 0; no log, no flag → rc 0 (the six named SKIPs); no log + flag → rc 1 naming all six. Confirm nothing in the roster or the installed unit passes the flag (nothing moves for tonight). DA's note on how a governed run would use it (the nightly passing the flag so a missing launcher log fails at the caller that promised the input) is a policy question for the coordinator after tonight — state whether the flag's semantics support it, do not wire it.
5. **Item 5 of round 11, reconciled as a layout fact.** DA names the gate it saw fail as `v4 behaviour (git-extracted)`, now PASS under the same layout and in three subsequent suite runs plus 3/3 standalone — the HANDOFF-documented flake (2-in-5 under suite load, 0-in-28 standalone). Not a finding against the batch unless you can make it fail deterministically; if you see it red once, record the layout and the load.
6. **The measurement defect DA caught in itself.** `… | tail -1; echo $?` reported rc 0 for an rc-1 case — the pipe-swallows-exit-code class this programme has recorded twice. Does anything IN the batch (the gate runner, the twins, the flag) read an exit code through a pipe? The runner uses `p.returncode` — confirm at `:115`.
7. **Nothing moved for tonight.** The shared tree's files byte-identical to `b75c9fe`; `derived/` listing identical before and after; installed unit unchanged (`DA_MIDNIGHT_MODE=production`, next elapse 2026-09-03 00:06:00 UTC); `~/ctaNew-wt-da` clean at `636a455`.
8. **Counts and rule 10/14.** 247 / 9 / 36 and the module matrix (`pm_tape_density` 9, `da_hf_pm_alignment` 53, `da_forward_day_verify` 247, `da_blackout_mask` 30, `da_governed_verdict_preflight` 34, `da_content_liveness_v2_check` 19, `da_verdict_check` 21, `da_cross_venue_forensics` 24); no new constant; no decision-shaped field; state files untouched.

## Disposition asked for
RELEASE or HOLD for `636a455` as the content of Q-DA-209 (lands after the 00:14Z read on 09-03); findings numbered DA12-Rn. One filing (R-377); rule 18.
