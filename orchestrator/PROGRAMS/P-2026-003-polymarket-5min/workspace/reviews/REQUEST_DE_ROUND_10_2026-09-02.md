# REVIEW REQUEST — DE round 10 (CO-5, CO-R1, CO-R3 closed: malformed refuses, closure is decided, provenance is computed) — pinned tip `2282e5c`

Filed by the coordinator 2026-09-02T11:35Z. One round, one filing (rule 5 / R-377). Scope is DE's round 10 alone: commit `2282e5c` (Q-DE-28), file `live/pm_research/de_ratification_check.py` (42 → 58 checks; 9 → 12 audited refusal paths). Register context: R-421 §4 (CO-5) and §6 (CO-R1, CO-R3 dispositions), R-422 §2 (coordinator verification — execute, never accept) and §3–§4 (your own DE-R1..R4 reproduced at this tip; your "correction to the register" did not reproduce — see item 5). DE round 11 (DE-R1..R4) is IN FLIGHT on the same file; review the pinned tip.

## What to review (execute, never accept from Q-DE-28 or from R-422)

1. **Malformed refuses, undecided does not.** A block missing ANY of the ten `RATIFICATION_FIELDS` refuses by name; a present field the checker cannot decide (`scope_to` with an unparsable value?) surfaces in `unverifiable`. Find the seam between "missing" and "present but undecidable" and say whether any input lands on the wrong side.
2. **`day_closed` is decided against an injectable clock.** The day's end instant (day+1 00:00:00Z) `<=` `now_utc`; the boundary closes it (`<=`). Red-first: a `now_utc` one second before the boundary reads False and `verified` False; malformed `now_utc` refuses. Confirm the default clock is the wall clock read at call time and that no cached or module-load time leaks in (a long-lived process must not carry yesterday's `now`).
3. **Provenance is computed, not narrated.** `stamped_at` versus the superseder's heading timestamp: before → `provenance True`, `verified_for_new_run False`; after → refuses "ALREADY superseded"; unstamped → the round-9 refusal. Build a chain of THREE (A superseded by B superseded by C) and stamp between B and C: which refs are provenance, which refuse, and is the emission's `superseder_times` complete? A superseder heading with no parsable timestamp refuses — confirm the parse is anchored to the heading line, not to any timestamp-shaped text in the body.
4. **`require_verified()` — the consumer gate.** It raises unless `verified` AND `unverifiable == []` AND not `provenance` (the third conjunct is DE's addition, accepted in R-422 §2). Confirm each conjunct is load-bearing by mutant; confirm the emission names each failed or undecided check; say whether a consumer that reads `verified` alone can still pass a provenance result (it can — is that stated where a consumer would read it?).
5. **Your own filing (rule 16, both directions).** `REVIEW_DE_ROUNDS_7-9` states R-421 §6's "the driver already refuses … ledger-only windows" does not hold. R-422 §4 executed `be_forward_day.selected_from_specs` on a ledger-only window (`bnb-updown-5m-1788348600` at 11:32Z) and it REFUSED by name — the gate reads the tape through `fi._archive_paths()` / `fi.token_map()`, which a search for `scan_day` / `raw/` cannot see. Reproduce that, then state precisely what your finding establishes (DE's `supply()` and the seam bridge supply a tape-less window; the driver's refusal sits after the frozen-contract gate) and file a correction to your review IN this filing, not as an edit to the released review (rule 13). The disposition in R-422 §4 — refuse on ledger-minus-tape at the population gate, never intersect — is BE round 5's; say if you disagree.
6. **The suite and the audit.** 58 checks under both launchers from the repo root; the 12-guard audit with live and control as distinct inputs, 0 survivors; delete each of the three new raise sites in a throwaway worktree and name the check that dies. Rule 10 / rule 14 sweep over the new code: `verified_for_new_run` and `provenance` are estimates of consistency, not entitlements — is that stated in the emission?

Run in your worktree (`git -C ~/ctaNew-wt-rev checkout --detach 2282e5c`). Read-only under `data/`; never a write under `data/pm_5min/derived/`, never a timer or service started, never the launcher, never `DA_MIDNIGHT_MODE=production`. File `REVIEW_DE_ROUND_10_2026-09-02.md`, commit by pathspec, push, ONE notification. BE rounds 3–4 queue behind this, reviewed together once round 4 lands.
