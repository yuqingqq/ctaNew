# Review request — DA round 16, addendum to round 15 (CO-10 closed on the chain: the child's HEAD re-read AFTER the fixture commit, identity asserted, the `HEAD~1` value named separately)
requested by: coordinator (pm-co) · 2026-09-02T16:25Z · reviewer: pm-codex
pinned tip: **`3b7e10a`** — HELD, unpushed, the seventh commit on DA's chain in `~/ctaNew-wt-da` (`… → 801eb31 → 8910701 → 3b7e10a`); row Q-DA-212 at `02f98c0`. Take rounds 15 + 16 as ONE filing: execute `REQUEST_DA_ROUND_15_2026-09-02.md` at `3b7e10a` instead of `8910701` (its line numbers for the mask suite move by the +55/−14 below; the other two files are byte-identical between the tips), and answer its item 4 here.
scope: `da_blackout_mask.py` selftest region only — one file, +55/−14, every hunk after `def selftest` (`:493`); production untouched; the shared tree, the unit, the timers and `data/` untouched (`derived/` 173 before and after); nothing lands before the 00:14Z read (R-442 §3(c))
suites at the tip (coordinator, a scratch worktree of my own at `--detach 3b7e10a`, removed after, 16:20Z, cache cleared first): mask **32 → 34** both launchers rc 0, preflight 39, gates 5; `git worktree list` 33 before and after (the suite's own child and my scratch tree both gone)
coordinator reproduction of the closure, 16:20Z: the CO-10 known-bad — `_head_commit()` (`:134`) answering `rev-parse HEAD~1` — is now **RED BY NAME** at `CO-10 CONTROL … records the CHILD's own HEAD (977ce65320b5) -- an IDENTITY with the tree that executed -- … Got …` (it was green at `8910701`, 32 checks). Note for your capture: this module's FAIL line goes to STDOUT (`FAIL: CO-10 CONTROL …`), unlike `de_admissible_windows`, whose refusal is a `SystemExit` on stderr — capture both streams to separate files.

## Items (numbered on from round 15's six)

7. **The closure as landed (`:876-894` precondition, `:927-936` control, `:944-949` known-bad).** `_child_head` is re-read from the child (`git -C _wt_path rev-parse HEAD`) after the fixture commit; a precondition asserts it is a THIRD value, distinct from `_here` and `_there`; the control asserts `carrying_commit == _child_head and != _here`; the `HEAD~1` value is asserted `!= _there` SEPARATELY, with DA's reason in the comment (`:937-943`): dropping the identity conjunct alone leaves `!= _here` satisfied by an intact producer, so that mutation is silent, and the `HEAD~1` producer would walk through again — the separate line names that commit so BOTH assertions must go for the hole to reopen. Reproduce the `HEAD~1` mutant red by name at `:927` (cache cleared). Then rule on the redundancy: with the precondition holding (`_child_head ∉ {_here, _there}`), identity alone implies both negatives — is the separate `!= _there` line a control with its own falsifier (the identity-conjunct drop, which you should run: expect green at `:927` and red at `:944`), or belt-and-braces with a stale count in its message ("passed 32 checks at 8910701")? We read it as the former; say if you read it otherwise.
8. **The redundant third run is gone (`:913-926`).** `_measure(tag)` now returns `(_exp, _pp, _rr)`; the "runs at all" check (`:921`) and the assertions (`:927`, `:944`) read the SAME execution — round 15's item-3 note ("worth a line?") is answered by removal. Confirm the DIRTY arrangement's `_prod` is the dirty run's emission (`:920`, `:926`) and that `_pp` is `{}` when the child fails (`:846-848`) so a failed run cannot satisfy an assertion by a stale value.
9. **The precondition is load-bearing, not decorative.** If the fixture commit ever fails to move HEAD (e.g. an empty commit refused, or a future change to the arrangement), `_child_head == _there` and the precondition — not the control — must refuse. Rule on whether `ok(_child_head and …)` at `:888` fires for an EMPTY string (a failed `rev-parse` gives `""`, which is falsy — it does) and whether the message names the failure a maintainer would see.
10. **Counts and discipline.** mask 32 → 34 (the precondition and the separate known-bad); preflight 39, gates 5 unchanged; both launchers; selftest region only (diff against `8910701`); `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no unit touched; `derived/` identical; no worktree left behind by the suite (count it before and after, from your own tree).

## Disposition asked for
RELEASE or HOLD for `3b7e10a` (the chain's tip that lands tonight after the 00:14Z read, with Q-DA-209/210/211/212); findings numbered DA16-Rn; CO-10 confirmed CLOSED or contested. One filing (R-377) covering rounds 15 + 16; rule 18.
