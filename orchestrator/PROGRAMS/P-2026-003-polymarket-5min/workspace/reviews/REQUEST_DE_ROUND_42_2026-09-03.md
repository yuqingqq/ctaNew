# Review request — DE round 42 at `5658f24` (DE41-R1 and DE41-R2 closed, one line each; the package untouched in the USER's hands)
requested by: coordinator (pm-co) · 2026-09-03T01:11Z · reviewer: pm-codex
pinned tip: **`5658f24`** (DE round 42, code and row Q-DE-60 in one commit, committed 01:05:33Z, rebased onto MEM's `8a31112`; the row's heading names `0218ec3`, the tip DE built on — index by `5658f24`) on base **`8479b67`** (your RELEASE at `fcdbb15`). Files: `live/pm_research/de_phase4_diag_runner.py` (sha16 `bc7c010c41933b85`, 3,468 lines, `EXPECTED_CHECKS = 124`, +19 / −6) — the ONLY file changed; the v2 DRAFT (`cb693000880c3d94`, 307 lines) NOT edited; score-stream untouched. Coordinator context: R-490 (your DE 41 filing verified; DE41-R1 reproduced), R-491 (round 42 executed at the tip: 124 / 124 both launchers, stderr empty; the guard-only mutant and the 22nd-field mutant re-driven, both red BY NAME, zero tracebacks; `--run --outdir <scratch>` rc 2, nothing created).
scope: round 42 at the tip, BEHIND the package: the USER holds the DRAFT and its five asks; nothing here re-opens the forwarding or the ask-(5) mechanics. DE is on recorded standby (no DE WIP in the main tree); every read at the blob (`git show 5658f24:<path>` or `--detach 5658f24` in `~/ctaNew-wt-rev`). BE round 12 and DA round 20 are OPEN in their own worktrees (DA 20 HELD) — `~/ctaNew-wt-be` and `~/ctaNew-wt-da` never read; `be_forward_day.py` never run (standing rule 9).

## Items
1. **Counts, executed.** Both launchers, the eight DE modules, `__pycache__` cleared, PASS lines = summary = rc 0: DE reports 124 / 26 / 31 / 26 / 21 / 21 / 184 / 92 (the 92 measured in DE's own worktree this round — DE attributes last round's refusal to the missing `raw/` at that moment, `derived/` 173 → 178 since; say whether that attribution is right). The coordinator measured the runner only (124 / 124); the other seven modules are byte-identical to `8479b67`.
2. **DE41-R2, the line reports what differs.** `:1987-1994`: `_diff_fields` is computed AFTER the block comparison as the sorted keys of the union whose values differ, with an `object()` sentinel so a key present on ONE side only is named; the FAIL text `:2010-2016` now reads "leaves N of the M `null_population` fields DIFFERENT ([names]) outside the one excluded BY NAME, …, and the predicate row differs / identical". Coordinator's re-drive of the 22nd order-dependent field (`_np_free["probe_22nd_field"] = 1` / `_snp["probe_22nd_field"] = 2`): red, "leaves 1 of the 22 … DIFFERENT (['probe_22nd_field'])". Is this the form you asked for; is a key-set difference (a field added to one side only) named rather than absorbed; is the must-differ clause on `n_accepted_stream_differs` unchanged.
3. **DE41-R1, `.get()` below the guard.** `:813-824`: `pred#1` unchanged; `if not c["n_draws_requested"]:` → `if not c.get("n_draws_requested"):` with the comment naming why. Coordinator's re-drive of the guard alone (`if False:`): rc 1 after 37 PASS, `FAIL (no refusal): KNOWN-BAD: a cell with no 'n_draws_requested' REFUSES at 'pred#1' …`, zero tracebacks. DE reports the round-40 derivation restored dies at the same line. Does `.get()` move any state for a PRESENT field (`0`, `None`, a string) relative to the indexed read; is the guard still the only thing standing between an absent field and `NO_NULL_REQUESTED`.
4. **The class.** Any OTHER failure line in the suite whose count or label is computed BEFORE its comparison (the DE41-R2 shape); any OTHER refusal site whose body indexes the field its own guard protects (the DE41-R1 shape) — name them or say none, with the method.
5. **Round 43 / resting tip.** RELEASE / HOLD `5658f24` as the runner's resting tip while the package waits on the USER. Whether your DE-40 item-6 answer on ask (5) — held line for line at `8479b67`, one clause cheaper — still holds at `5658f24`, or what moved.

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach 5658f24` (data/pm_5min mirrored per entry — the 92 needs `raw/`) or reads at the blob; `~/ctaNew-wt-da`, `~/ctaNew-wt-be`, `~/ctaNew-wt-de` never read; `be_forward_day.py` never run; nothing under `data/` beyond reading; the declared Phase-4 OUTDIR `data/pm_5min/derived/phase4_diag_r459` never passed to `--run`; `derived/` listed before and after (178 on the main tree at 01:09Z); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited (the v2 DRAFT is the USER's); `git worktree list` unchanged at quiescence (34; standing rule 10); any mutant applied to your own worktree copy and restored byte-identical.

## Disposition asked for
One filing (R-377): `REVIEW_DE_ROUND_42_2026-09-03.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DE42-Rn; each with the line; items 2–3 as CLOSED / what is left; item 5 as RELEASE / HOLD. You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18).
