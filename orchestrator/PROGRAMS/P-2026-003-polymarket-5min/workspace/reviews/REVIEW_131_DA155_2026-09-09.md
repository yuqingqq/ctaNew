# REVIEW 131 — DA 155's two fixes to DA's own instruments

**REV, 2026-09-09T09:58Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`. Commits `4c88604`, `7363470`, `5e49ad9`; tip `47bc449`.

**VERDICT: (1) THE HORIZON CELL VERIFIES — it still goes red for a schedule regression and
for an unmeasured stage — BUT THE CLAUSES ITS COMMIT BILLS AS "THE ARITHMETIC, WHICH NO
CLOCK CAN MOVE" CANNOT FAIL, and the pinned anchor costs it 4.7× of sensitivity. (2) THE
BRIEF'S PREMISE ABOUT THE FLAKE IS REFUTED AT THE CODE — the field is PATH-SCOPED and no
other seat's dirty file moves it — AND DA 155's OWN NEW CELL IS RED FROM EVERY WORKTREE:
same bytes, `SELFTEST OK 23/0 rc 0` from the ledger tree and `SELFTEST FAILED 23 checks, 1
failure, rc 1` from mine. A verdict that is a function of the launch directory is the class
the round was fixing.**

---

## (1) `da_accrual_report` — the horizon cell

**PASS on the real thing.** Battery at the tip: **20 checks, 0 failures**. Anchored at
`T = 2026-09-06T09:00Z` (a literal, not a captured `now`): 09-08's receipt projected
`2026-09-09T02:33:00Z`, **+9.45 h** inside the horizon, **−2.45 h** against the bar.

**THE CLOCK DEPENDENCE IS GONE, AND I MADE THE CONTROL MOVE.** `now_utc` monkeypatched to
three wall clocks, the asserted reading and the live reading taken side by side:

```
wall 2026-09-09T00:00Z   ASSERTED proj 02:33:00Z bar −2.45h   LIVE proj 09:34:42Z bar   −9.48h
wall 2026-09-09T09:50Z   ASSERTED proj 02:33:00Z bar −2.45h   LIVE proj 19:24:42Z bar  −19.31h
wall 2026-09-12T23:00Z   ASSERTED proj 02:33:00Z bar −2.45h   LIVE proj 08:34:42Z bar −104.48h
```

**Asserted reading identical across all three; live reading different in all three.** The
control could have failed and did move.

**AND IT STILL GOES RED FOR WHAT IT WAS BUILT TO CATCH — twice.** Driven by scaling the
measured stage costs at the fixed anchor:

```
costs ×1.5  margin +8.18 h   all_six True    cell PASSES
costs ×3.0  margin +4.35 h   all_six True    cell PASSES
costs ×5.0  margin −0.75 h   all_six False   cell FAILS
smoke stage UNMEASURED       stage_costs_s smoke=None        cell FAILS
```

So it was **not** made deterministic by dropping what it asserts. Two residuals:

**(1a) THE ADDED CLAUSES CANNOT FAIL.** The commit presents them as *"THE ARITHMETIC, which
no clock can move … That is what this cell is FOR"*. But in `serial_schedule`, on two
adjacent lines:

```python
row["before_the_seal_open_bar"] = f <= t_bar
row["margin_to_the_bar_h"] = round((t_bar - f).total_seconds() / 3600.0, 2)
```

`margin >= 0` **is** `f <= t_bar`. They are the same comparison, so
`(margin >= 0) == before` is true by construction. **Measured: the added clauses were TRUE
in all six drives above — including both drives where the pinned clauses went FALSE.** The
only reachable disagreement is the `round(…, 2)` boundary: sweeping `f` across the bar
second by second gives **17 disagreements in a ±40 s band, all at `f = bar + 1…17 s`**,
where the true margin rounds to `−0.00`. The clause fires on a display-rounding artifact,
never on a schedule fact. **What restores determinism is the pinned anchor; the arithmetic
is decoration, and the commit message credits the wrong half.**

**(1b) THE PINNED ANCHOR COSTS SENSITIVITY, AND THE NUMBER IS 4.7×.** Bisected: the cell
flips at a cost factor of **4.719×** — 43,320 s/day against the measured 9,180.5 s. **A
schedule regression smaller than 372 % is invisible to it.** That is a real weakening, and
it is not recorded anywhere. It is cheap to recover: assert the anchored margin **within a
band** (e.g. `9.0 ≤ margin_to_the_horizon_h ≤ 10.0`) instead of only its sign, which fails
on a few per cent rather than on a factor of five.

## (2) `da_process_budget_audit` — the flake

**THE BRIEF'S PREMISE IS FALSE, AND I DROVE IT.** `committed_state` runs
`git status --porcelain -- <path>`, which is **path-scoped**. On a controlled repo with
`AUDIT_ROOT` pointed at it:

```
clean                    a=COMMITTED_IN_THIS_TREE   b=COMMITTED_IN_THIS_TREE   outside=NOT_IN_THIS_TREE
b.py MODIFIED            a=COMMITTED_IN_THIS_TREE   b=PRESENT_BUT_NOT_COMMITTED
b.py STAGED              a=COMMITTED_IN_THIS_TREE   b=PRESENT_BUT_NOT_COMMITTED
a.py MODIFIED TOO        a=PRESENT_BUT_NOT_COMMITTED
```

**Another seat's file, modified and then staged, does not move the audited path's verdict.**
It moves only when that path itself is dirty. So "the verdict is a function of other seats'
timing" does not hold — which agrees with DA's own in-band correction at `47bc449`, and the
brief carries the superseded diagnosis.

**AND THE FIX LOOSENS NOTHING.** Dirtiness is now *named* (`PRESENT_BUT_NOT_COMMITTED`)
where the bool collapsed **absence** (rc 128, outside the tree) into "uncommitted". Both the
bool and the state are emitted. `committed_in_that_tree` was never gating in this module —
reported at `:1376`, asserted nowhere — which the module states as design (*"the report …
decides nothing"*), and the sibling field in `da_gate1_day_verdict` carries
`why_committed_bytes_is_reported_not_gating` in prose. **Nothing that was caught before is
uncaught now.**

### THE FINDING: DA 155's NEW CELL IS A FUNCTION OF THE LAUNCH DIRECTORY

Its positive control is

```python
committed_state(Path(__file__).resolve()) in (IN_TREE_CLEAN, IN_TREE_DIRTY)
```

and `__file__` is inside `AUDIT_ROOT` **only when the battery is launched from the ledger
tree**. Same commit bytes, two trees:

```
from /home/yuqing/ctaNew          SELFTEST OK      — 23 checks, 0 failure(s)   rc 0
from /home/yuqing/ctaNew-wt-rev   SELFTEST FAILED  — 23 checks, 1 failure(s)   rc 1
   the failing cell is DA 155's own, and it prints its own cause:
   "this auditor's own file -> NOT_IN_THIS_TREE"
```

and at the function:

```
committed_state(/home/yuqing/ctaNew/…/da_process_budget_audit.py)        = COMMITTED_IN_THIS_TREE
committed_state(/home/yuqing/ctaNew-wt-rev/…/da_process_budget_audit.py) = NOT_IN_THIS_TREE
committed_state(/home/yuqing/ctaNew-wt-be/…)                             = NOT_IN_THIS_TREE
committed_state(/home/yuqing/ctaNew-wt-de/…)                             = NOT_IN_THIS_TREE
```

**Every seat runs from its own worktree by protocol** — `_resolve_root`'s own docstring says
so. So the cell is red for every seat except one launching from the ledger tree, and **a
genuine `committed_state` defect and "you ran from a worktree" produce the identical red.**
That is the same shape as the clock flake this round fixed: *a verdict that moves with
something other than the code under test.* DA's "23 checks, 0 failures" is true, and true
only from the ledger tree (rule 29's shape).

**Repair, one line:** the control must name a path that is in `AUDIT_ROOT` by construction —
`AUDIT_ROOT / "live/pm_research/da_process_budget_audit.py"` — so it tests the **function**,
not the launch directory.

## MY OWN PROBE FAILED FIRST, AND I AM RECORDING IT

My first dirty-tree drive reused a fixture repo left dirty by the previous run: its "clean
tree" row read `PRESENT_BUT_NOT_COMMITTED` for both files, which would have read as *"the
field is always dirty"* — the opposite of the truth. **The control row is what caught it**
(`a.py's verdict UNCHANGED while b.py was modified: False` on a run where nothing had been
modified yet). Re-driven from a fresh repo; the numbers above are that run.

## ROUTED

1. **DA — the launch-directory cell.** One line; and re-run the battery from a worktree
   before calling it green.
2. **DA — the horizon cell's added clauses are decoration** (true by construction, reachable
   only in an 18-second rounding band). Keep them or drop them, but the commit's account of
   *why* the cell is now deterministic should name the pinned anchor.
3. **DA / coordinator — record the 4.719× blind spot**, or assert the anchored margin within
   a band so a small regression is visible.
4. **Coordinator — the brief's flake diagnosis is superseded** by DA's own `47bc449`; the
   dirtiness mechanism is not reachable, the field is path-scoped.
