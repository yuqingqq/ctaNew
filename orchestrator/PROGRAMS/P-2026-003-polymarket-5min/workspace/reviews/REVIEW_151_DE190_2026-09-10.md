# REVIEW 151 — DE 190: the reconciliation status as a function of the mode

**REV, 2026-09-10T01:26Z.** Read-only: no lock (DE has it for two point estimates, BE has
09-03 behind it), no heavy unit, nothing written under `data/`.

**VERDICT: IT HOLDS, AND IT CLOSES MY REV 147 FINDING MORE STRONGLY THAN THE FINDING
REQUIRED. The status is computed from the mode, there is no second literal one branch over,
both paths are driven, and the full-null path now carries a NAMED status plus a
`what_must_not_be_read_from_this` clause. THE ONE THING I WENT LOOKING FOR — a third mode
where the field leaks — IS NOT REACHABLE: `_win801 is not None` implies admissible by
construction, so there is no admissible-but-unbuilt path. One small residual, named so nobody
rediscovers it: on a NON-valued arm-day the field is still absent, and the explanation lives
in a sibling status.**

---

## (1) IT IS COMPUTED — and I grepped for what REPLACED it, not for what was removed

```python
PLR_PENDING       = "PENDING__INPUTS_HANDED_TO_THE_POINT_ESTIMATE_DRIVER"
PLR_NOT_PERFORMED = "NOT_PERFORMED__NO_RECONCILIATION_ON_THIS_PATH"

"status": (PLR_PENDING if point_estimate else PLR_NOT_PERFORMED),   # :8977
"run_mode": ("POINT_ESTIMATE" if point_estimate else "FULL"),
```

**Every occurrence of the old literal, tree-wide:** one **comment** at `:6441` explaining the
defect, and `RECONCILED_BY_POINT_ESTIMATE_DRIVER` at `de_point_estimate_day.py:385` — **a
different string, and the driver's own word, written after it has run.** The only writers of
the field are `:8975` (the mode-computed block) and `:9196` (the assignment). **No second
literal.**

**And DE went past my finding.** I asked for the claim to be mode-gated; DE removed the claim
from the runner altogether: *"NEITHER MODE ASSERTS WHAT THE DRIVER DID. The runner says what
THE RUNNER DID: it handed the inputs over, or it did not."* **A consequence worth naming: a
`PENDING` surviving into a published point estimate is now DIAGNOSTIC — it means the driver did
not finish — where `PERFORMED` would have read as done.**

## (2) BOTH PATHS DRIVEN, AND THE CELL CAN FAIL

**The point-estimate path, on the real landed artifact**
(`p003_de_point_estimate_day_20260904_L250ms__20260909T175938Z.json`):

```
arm=CONDVALUE_X_SKEW        status=OK_POINT_ESTIMATE   plr=RECONCILED_BY_POINT_ESTIMATE_DRIVER
arm=HAZARD_OVER_SKEWED_REF  status=OK_POINT_ESTIMATE   plr=RECONCILED_BY_POINT_ESTIMATE_DRIVER
```

**Truthful, and written by the driver after it ran** — it overwrites the runner's `PENDING`.

**The cell runs and passes in the real selftest** — `PASS R-828 / DE 148` at line 209, 0
failures — and it asserts **both** populations. **And it can fail: I drove its own predicate on
forged populations:**

```
correct populations                       -> True
full-null carrying THE OLD LITERAL        -> False
full-null status right, PROSE wrong       -> False
full-null LEAKING the private key         -> False
EMPTY populations (the vacuity trap)      -> False
```

**The last line matters:** DE asserts `len(...) >= 1` on both populations *before* any `all()`,
having noted that `all()` over an empty list is True. **That is the vacuity trap closed
deliberately, and it is why the cell's green means something.** The cell also asserts
`"PERFORMED_BY" not in json.dumps(block)` across **both** modes — the word is the driver's to
write, and the runner may never write it.

## (3) WHAT THE FULL-NULL PATH SAYS INSTEAD — a named status, in rule 35's full shape

```
status    = NOT_PERFORMED__NO_RECONCILIATION_ON_THIS_PATH
run_mode  = FULL
what_this_run_did = "NOTHING. This is a full-null `--day` run: it does not call
    `de_point_estimate_day`, it hands over no inputs, and NO PLACEMENT-LATENCY RECONCILIATION
    WAS PERFORMED FOR THIS ARM-DAY. The kept tranches were not compared against the ledger's
    baseline and the dropped leg was not valued"
what_must_not_be_read_from_this = "that the reconciliation passed, that it failed, or that the
    latency effect is zero here. It was not run. To get it, run the day through
    `de_point_estimate_day`, which is where the check lives (USER ruling, DE 181)"
```

**A status a reader resolves, the mode beside it, what was done, and what must not be
inferred.** Not a boolean and not an absence. **This is the sixth instance of the shape and it
is the first one answered with the negative clause included** — `what_must_not_be_read_from_this`
is the thing the earlier five all lacked.

## (4) THE THIRD MODE — I LOOKED FOR THE LEAK AND IT IS NOT REACHABLE

The mode gate is a boolean, so two branches; the exposure would be the **enclosing** guard.
Both the build (`:8975`) and the assignment (`:9196`) sit under `if _win801 is not None:`.

**But that cannot leave an admissible arm-day unstated:** `_win801, _base801, _sbase801 = None,
None, None` is initialised at the top and `_win801` is set **only inside**
`if _adm801["admissible"]:`. **So `_win801 is not None` implies admissible, which implies the
block was built — there is no admissible-but-unbuilt path, and no `NameError` waiting.**

**RESIDUAL, small, and named so nobody rediscovers it as a bug:** when no winner source
resolves — a day that is not settlement-admissible — the field is **absent**, and the
explanation lives in a **sibling**: `status: NOT_VALUED_DAY_NOT_ADMISSIBLE` with
`settlement_not_valued_reason(...)` as its `why`. Since nothing was valued there is no
reconciliation question to answer, so **I do not call this a defect** — and DE names the class
itself at `:9191` (*"an absent field is silence, not a statement (rule 28) … The field is
present on every valued arm-day and its STATUS carries the mode"*). **It is the one place a
reader meets silence rather than a status, and the sibling covers it.**

## WHAT THIS MEANS FOR THE NULL

The full-null artifact will now carry `NOT_PERFORMED__NO_RECONCILIATION_ON_THIS_PATH` with its
two prose clauses. **There is no driver-side `RECONCILE_INPUTS_ABSENT` on that path, as you
said — and there no longer needs to be, because the runner is no longer making a claim that
would need catching.** The field states a fact about the runner, which the runner is in a
position to know.

## ROUTED

1. **Coordinator — my REV 147 finding is CLOSED**, and closed by removing the claim rather
   than gating it. The null may print this field.
2. **Nothing to DE.** The residual in §4 is named as a non-defect.
