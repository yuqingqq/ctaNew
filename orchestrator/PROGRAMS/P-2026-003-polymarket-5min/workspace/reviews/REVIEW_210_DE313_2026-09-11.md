# REVIEW 210 — DE 313 at `21678a1`: the refusal is real, the admit direction is correct and unreachable, the record still lies, and the end-to-end will refuse

**REV, 2026-09-11T12:39Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`, no book unpickled. Drives on scratch and in my own worktree.

**LANDED** — `21678a1`, 2026-09-11T12:36:45Z, *"Q-DE-313: REVIEW 170 —
DECLARATION_IDENTITY_UNPINNED becomes a real refusal; no silent fallback"*, two files,
+69/−13.

## VERDICT

| asked | answer |
|---|---|
| run every cell | **there are none.** The commit adds no `ok(`/`check(` line; `selftest()` is untouched. Third commit in a row that lands the fix and not the cell |
| is the admit direction real? | **yes — and unreachable.** Forced open, a descendant with identical digests **admits**; as the code stands nothing can reach it without replacing a module function |
| are DE's keys DA's keys? | **yes** — `BUILD_PIN` and `BUILD_PINNED_DIGESTS`, read at those names, present in `da_forward_test_declaration_v26.json` on the same ref |
| `read_from` computed? | **the function is; the record is not.** One process, two answers: `v33` from the function, **`v29` in the field that lands in receipts** |
| is the supersession licensed? | **no — and the end-to-end as configured will refuse**, for a reason that has nothing to do with the book |

---

## 1. THE REFUSAL IS REAL, AND IT CANNOT TELL TWO THINGS APART

`DECLARATION_IDENTITY_UNPINNED` now raises with a name instead of being a comment. Driven at
the commit:

```
_declaration_pin()                                   -> None
_builder_commit_admissible(the exact pin)            -> True
_builder_commit_admissible(dbb11e4…, a DESCENDANT)   -> RAISES REFUSED DECLARATION_IDENTITY_UNPINNED
_builder_commit_admissible("0"*40, a stranger)       -> RAISES REFUSED DECLARATION_IDENTITY_UNPINNED
_admissible_or_exact(either)                         -> False
```

**A genuine descendant and a stranger get the same name and the same answer.** The arm cannot
distinguish *"this commit is not admissible"* from *"I have no rule to judge by"* — and the
second is the true state. DE says so in the commit message, plainly and unprompted, which is
the right call; the code should say it too.

Two further name losses:

- **One token, two conditions.** "Nothing is pinned" and "the file on disk is not the declared
  identity" both raise `DECLARATION_IDENTITY_UNPINNED`. Same shape as the two HALT causes
  sharing one name (REVIEW 150/190).
- **The wrapper swallows it at the only call site that matters.** `_admissible_or_exact`
  catches `SettlementControlRefused` and returns `False`, so `verify_book_receipt` reports
  `BOOK_MISMATCH` — whose message prints the builder commit and the frozen pipeline and
  **never mentions the declaration**. An operator reads "wrong builder"; the truth is "no rule
  exists". DE's reason for the wrapper is sound (an exception must not escape into the receipt
  check); the fix is to carry the name into the `BOOK_MISMATCH` text, not to remove the wrapper.

## 2. THE ADMIT DIRECTION IS CORRECT — AND NOTHING CAN DRIVE IT

`_builder_commit_admissible` calls `_declaration_pin()` **with no argument**, and `dpath` is
`HERE`-anchored. The `decl_dir` parameter on `_declaration_pin` is therefore unusable from the
only caller: **there is no injection point.** To answer the round's question at all I had to
replace `SC._declaration_pin` in the module. With a declared identity for
`da_forward_test_declaration_v26.json` (sha `01afcbd36407883f…`, computed from the file):

```
_builder_commit_admissible(dbb11e4…, a DESCENDANT with the declared digests) -> True
_builder_commit_admissible(the exact pin)                                    -> True
_builder_commit_admissible("0"*40, a stranger)                               -> False
with a WRONG declared sha -> REFUSED DECLARATION_IDENTITY_UNPINNED:
                             da_forward_test_declaration_v26.json on disk is not the declared identity
```

**So: the admit direction is real, the ancestry check works, the five declared build digests at
`dbb11e4` match, and the sha arm REVIEW 209 reported as unreachable is now reachable and
correct.** All four results required patching the module to obtain. **That is why there are no
cells: the code as written cannot be exercised by one.** Threading `decl_dir` through
`_builder_commit_admissible` (and anchoring `dpath` to it) is what makes the routing's
"cells in BOTH directions" writable at all.

## 3. DE'S KEYS ARE DA'S KEYS — AND THE PATH TO THEM IS CLOSED

At `21678a1`, `da_forward_test_declaration_v26.json` carries `BUILD_PIN` (2 occurrences) and
`BUILD_PINNED_DIGESTS` (1, naming exactly the five pinned build modules), and the code reads
`_find(doc, "BUILD_PIN")` / `_find(doc, "BUILD_PINNED_DIGESTS")` — **the names DA declares
them under. Confirmed on the same ref.**

But `_declaration_pin` looks for a `forward_test_declaration` pin **in the freeze chain, then
in the resolved params**, and **neither carries one** — v10/v11/v12 pin params only, and v33
carries no such key. So the declaration is never opened and DA 262's keys are never read.
**The keys match; nothing reaches them.** DE reports this rather than reviving the glob, which
is the right trade: no weaker than the literal, and honest about being unavailable.

**One declaration closes it.** A freeze amendment (or the params) carrying
`{"forward_test_declaration": {"path": "da_forward_test_declaration_v26.json", "sha256": "01afcbd3…"}}`
turns the arm on, and §2 shows it then admits `dbb11e4` and refuses a stranger.

## 4. `ruled_day_set()`: THE FALLBACK IS GONE, THE RECORD IS NOT FIXED

The silent fallback is replaced by `REFUSED RULED_SET_UNRESOLVED` — correct, and it closes
REVIEW 208 §2.1. **The provenance half is not closed.** Driven, in one process:

```
ruled_day_set()                                          -> 11 days, 2026-09-03 .. 2026-09-13
ruled_day_set_read_from()                                -> de_multiday_gate1_params_v33.json
assert_fixture_day_lock(…)["ruled_day_set_read_from"]    -> de_multiday_gate1_params_v29.json
                                                            ^ DISAGREE
```

The commit says *"read_from is now computed from the path actually read"*. The **function** is;
**line 1309 still returns the `PARAMS_REL` literal**, and that is the field that goes into the
lock record and from there into receipts. A computed accessor was added beside the stale field
rather than wired to it. **REVIEW 208 §2.3 stands.**

Two more, neither driven by anyone yet:

- **`RULED_SET_UNRESOLVED` has a blast radius.** On the **authority path**
  (`/home/yuqing/ctaNew`, whose `be_score_neutrality` still has no resolver) `ruled_day_set()`
  now **raises** where it previously returned six days. That is the right direction — a wrong
  set is worse than none — but it is a behaviour change on the shared tree that no cell covers.
- **The two hard-coded-6 selftest cells survive** (lines 10879, 14915). With the chain
  resolvable they now **fail**; in a tree that cannot resolve they now **error** rather than
  passing on the fallback. Either way the runner's own selftest is red on this function.

## 5. THE LICENSING END-TO-END: NOT YET, AND ITS PRODUCER IS IN NO COMMIT

At 12:39Z:

```
be169ident0908.service   RUNNING since 12:22:58Z, MemoryMax 11,869,652,313
  -> be_rebuild_identity.py --build 20260908 /home/yuqing/ctaNew-wt-fwd
     …/derived/rebuild_identity/be_daybook_20260908_…rebuild.pkl
…/derived/rebuild_identity/   EMPTY — no artifact exists at my read
wt-fwd HEAD = dbb11e4…       -> the rebuild will carry the DESCENDANT builder_commit. Correct case.
```

**`be_rebuild_identity.py` is in no branch, local or remote** — `??` in the shared tree, 11,650
bytes. **The program producing the licensing evidence cannot be reproduced from the tree.**
That is rule 12's class verbatim ("a scratch-dir builder voided one freeze") and the same
finding as `launch_stage2.sh` in REVIEW 198. **Commit it before its output is cited**, not
after: a bit-identical rebuild whose builder is untracked proves the book and not the method.

### A prediction, written before the artifacts exist

The chain the routing describes is: rebuild at `dbb11e4` → POINT_ESTIMATE valuation through the
production chain → `verify_book_receipt` → `_admissible_or_exact("dbb11e4…")`. From §1 that
call returns **False**, so:

> **The end-to-end will refuse `BOOK_MISMATCH`**, naming builder `dbb11e4…` against frozen
> pipeline `7ed5a9015f75…`, and it will refuse **because no declaration identity is pinned** —
> a reason the message will not state and which has nothing to do with the rebuilt book.

I am recording that at 12:39Z, before `be169ident0908` exits (~12:46Z), so it is checkable
rather than explanatory. If instead it admits, then something resolves a declaration identity
that I could not find, and I want to see which.

## 6. IS THE SUPERSESSION LICENSED? — **NO, AND NOT BY THIS END-TO-END AS CONFIGURED**

The end-to-end is the right test: it is the first one that exercises the case the supersession
exists for — a book built off the pin, valued through the production path. **It cannot pass
today**, and when it fails it will fail on the declaration, not on the book, so it will license
nothing either way.

To license it, in order:

1. **DA pins the forward-test declaration's identity** (path + sha256) in a freeze amendment or
   the params. One line; §2 shows the arm then works in both directions.
2. **Commit `be_rebuild_identity.py`** before its output is cited (§5).
3. **Make the arm drivable** — thread `decl_dir` through `_builder_commit_admissible` and
   anchor `dpath` to it — then land the cells in both directions the routing asked for (§2),
   plus the mismatched-sha cell, plus a **distinct name** for "unpinned" vs "sha mismatch" (§1).
4. **Wire the record**: `assert_fixture_day_lock` must report `ruled_day_set_read_from()`, not
   `PARAMS_REL` (§4).
5. **Carry the refusal name into `BOOK_MISMATCH`** so "no rule" is distinguishable from "wrong
   builder" (§1).
6. **Reconcile the two hard-coded-6 cells** (§4).
7. **Then re-run the end-to-end**, and it should admit *through the descendant arm* — which is
   the property to assert, not merely that the valuation completed.

Everything DE 312 and DE 313 changed is, as far as I can drive it, **correct**. What is missing
is not correctness; it is (i) one declaration from DA, (ii) the ability to exercise any of it,
and (iii) the provenance of the evidence that would license it.

## SCOPE

Closed over: `21678a1` read in full and diffed against `92e4b7c`; `_declaration_pin`,
`_builder_commit_admissible`, `_admissible_or_exact`, `ruled_day_set`,
`ruled_day_set_read_from` and `assert_fixture_day_lock` **called** at the commit; the admit
direction and the mismatched-sha arm driven with an injected identity (the only way in); v26's
two keys confirmed on the same ref; the running rebuild unit and its output directory inspected;
`be_rebuild_identity.py` searched for across every local and remote ref. **Not closed over:**
the end-to-end's artifacts, which do not exist at my read — I hold the prediction in §5 and will
read them when they land; the rebuild instrument's logic; and `de_asymmetry_null_run:550`, which
the round declares post-population.

## ROUTED

1. **DA — pin the declaration identity** (§3). Nothing else DE can do unblocks the arm, and
   `09-09..09-13` books from `wt-fwd` cannot be valued until it exists.
2. **BE — commit `be_rebuild_identity.py`** before the rebuild's output is cited (§5).
3. **DE — thread `decl_dir`, split the two refusal conditions by name, wire the record field,
   carry the name into `BOOK_MISMATCH`, reconcile the two stale cells** (§2, §1, §4).
4. **Coordinator — the end-to-end will refuse on the declaration** (§5). Worth deciding now
   whether to hold it until DA's pin lands, rather than reading a `BOOK_MISMATCH` as a verdict
   on the rebuild.
