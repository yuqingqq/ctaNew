# Review request — DE round 36 at `92c7da4` (the permuted control, the computed pin, the corrected compute; the v2 DRAFT the USER will rule on)
requested by: coordinator (pm-co) · 2026-09-02T19:54Z · reviewer: pm-codex
pinned tip: **`92c7da4`** (DE round 36; row Q-DE-54 `2432aa5`) on top of `27c1ccd`. Files: `live/pm_research/de_phase4_diag_runner.py` (sha16 `2ba96440ff941785`, 1902 lines, `EXPECTED_CHECKS = 71`), `live/pm_research/de_phase4_protocol_check.py` (`f20323d02303baf1`, 333 lines, 26), `live/pm_research/de_head_scoring.py` (`6e503d39b595e24c`, 373 lines, 22), `live/pm_research/plans/DE_PHASE4_DIAGNOSTIC_ADDENDUM_V2_DRAFT_2026-09-02.md` (`a7f92966ed5e2523`, 165 lines, revised in place — a PROPOSAL, not frozen). Coordinator context: R-475 (round 36 executed at the tip — counts and pin statuses reproduce; six coordinator findings DE36-C1..C6), R-473 (your DE 35 filing verified; your rulings on §5's shape and DE34-R7), your DE 35 findings DE35-R1..R5.
scope: round 36 at the tip. Round 37 is NOT dispatched — DE is on recorded standby — so the main tree carries no DE WIP, but every read stays at the blob (`git show 92c7da4:<path>` or `--detach 92c7da4` in `~/ctaNew-wt-rev`). BE round 10's row is in flight: `be_forward_day.py` is never read, run or counted from the main tree (standing_rule 9).

## Items
1. **Counts, executed.** Both launchers, the eight DE modules or the three changed ones at minimum, `__pycache__` cleared, streams separate, PASS lines = summary = rc 0. R-475 §1 has 22 / 71 / 26 / 21 / 24 / 21 / 184 / 92. Drive `pin_statuses()` in-process and confirm the twelve statuses (11 NOT_CALLED incl. `phase2_arms.py`; `harmful_exposure_rows.py` ADDITIVE_DECLARED, three functions, 17 called); drive `--run --outdir <a path of yours, NEVER the declared one>` (rc 2 by name) and `preflight()` (thresholds → pin PROCEEDS → scorer REFUSES: say what refuses first now).
2. **DE36-C1 (HIGH — §5 again).** `run_cell` `:947-971`: `ctrl_scores = list(_below)` keeps every below-threshold event at its own generation, then an above value is APPENDED at each drawn generation's `t0`. R-475 §2(a) says: a drawn generation that was below carries two events at one `t0`, a vacated generation carries none, `zip(sorted(_want_keys), _vals)` drops the treated stream's non-acting above values (the draw is matched on ACTIONS and `|actions| ≤ |_above|`), and the cycling branch `:956-957` is unreachable. Confirm or contest by building a fixture with (i) a drawn generation that was below and (ii) a non-acting above event (a second above on a slug/side already HELD), and counting events per generation in both streams. Then RULE the realisation: a TRUE swap (acting-above key ↔ drawn key within the stratum, everything else in place) preserves multiplicity but lets a non-acting above event ACT in the control (its quote is alive there) so the control's cancel set exceeds the drawn set and `control#2` `:981-987` refuses on real data — so (α) match the draw on ALL above-threshold events (requested cancels, acting or not) and permute totally, or (β) partial swap with the guard comparing per-stratum ACTION COUNTS (the decision variable, rule 7) instead of set identity, the realised control action count in the receipt — or a third shape. Say what §5 must say so that it is TRUE of the stream the runner builds, and what predicate proves it (R-475 §2(b): one event per generation, per-stratum score multisets equal, drawn generations carry the above values).
3. **DE36-C2.** `:1733-1734` `"_above = [e for e in treated_scores" in _ctrl_src and "_below" in _ctrl_src` — DE34-R6's class at the new check. Confirm; the predicate you want is the one from item 2.
4. **DE36-C3 / C4 / C5 — the pin.** `import_closure` `:357-369` parses the runner alone; `harmful_exposure_rows.py:262` imports `ERA_BOUNDARY_NS` from manifest-pinned `harmful_candidate_manifest.py` at module level, and `pin_statuses` `:451-453` reads that file NOT_CALLED (closure test before identity test) — identical today, silent if it changes; `_fn_asts` `:418-426` compares top-level functions only, so module-level constants and imports are outside the comparison. `DECLARED_ADDITIVE` `:155-173` carries reasons but no sha of the AST it declared, so a later edit to `select_v2_era` still reads ADDITIVE_DECLARED. `called#1` `:490-498` (BLOCKING) is reached by nothing in the selftest — `verify_called_code()` only on the PROCEEDS path `:1646`, `:1872`; the coordinator proved in-process that popping the `select_v2_era` declaration makes it fire. Confirm each; say whether the closure must be transitive across modules (and how deep), whether module-level statements enter the comparison, and whether the declaration must be pinned to its AST sha. Are the three declared reasons `:156-172` TRUE (does `_era_or_refuse` change any non-empty selection for this population)?
5. **DE36-C6 and DE35-R2 — the compute the USER schedules on.** DRAFT §1a (`:35-57`): 0.03 s per `arm_result` over "471 synthetic windows" → 0.007 s per replay → the grid ≈ 40 s of replay; the feed ≈ 28.6 min once. The runner's own timed smoke `:1337-1345` projects from a synthetic `ref`. Say whether a synthetic-window figure transfers to the real reference (what per-window generation/tranche counts does the fixture carry vs the population?), what number should reach the USER, and whether your DE35-R2 (4× understated) stands as "4× per draw, ~1000× overstated in total, per DE" or needs a measurement on the real reference first.
6. **Closures at the line, confirm or contest:** DE35-C2 (`:1536`, `:1755`), C3 (protocol `:278-291`), C4 (`:910-913`), C5 (`:974`), DE31-R2 (`:993-1003`), DE35-R3 (head_scoring `thresholds()` range guard + `LightGBMError` → `HeadRefused`), DE34-R2/R3/R6 as parses (`:1356`, `:1572`, `:1671-1725`); DE35-R4 (§3 rule stated or CHOSEN) and DE35-R5 (§2 under §5) as the DRAFT now words them.
7. **The v2 DRAFT as a document, again.** Still a PROPOSAL throughout? Anything decided in it? With item 2's ruling, what must change in §5, §2, §3 before the package goes to the USER whole (R-473 §2)?
8. **What the coordinator missed** — the class: any predicate that cannot go red, any constant that is a policy input in disguise, any refusal the run path can raise that `main()` does not catch, any number in the receipt without its population.

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach 92c7da4` (data/pm_5min mirrored per entry) or reads at the blob; `~/ctaNew-wt-de`, `~/ctaNew-wt-be` never read; `be_forward_day.py` in the main tree never read, run or counted; nothing under `data/` beyond reading the fits; the declared OUTDIR (`data/pm_5min/derived/phase4_diag_r459`) never passed to `--run`; `derived/` listed before and after (173); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited (the v2 DRAFT included — findings go in your filing, not into the draft); `git worktree list` unchanged.

## Disposition asked for
One filing (R-377): `REVIEW_DE_ROUND_36_2026-09-02.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DE36-Rn (yours, distinct from the coordinator's DE36-Cn); each with the line; for DE36-C1..C6 a CONFIRMED / CONTESTED with the reason; the rulings items 2, 4 and 5 ask for, stated as rulings. RELEASE / HOLD on `92c7da4` as round 37's base, and round 37's order (§5 as ruled; C2..C6; the wiring with it or after it). You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18) — BE rounds 9 + 10 come to you as the round after this one, when BE's row lands.
