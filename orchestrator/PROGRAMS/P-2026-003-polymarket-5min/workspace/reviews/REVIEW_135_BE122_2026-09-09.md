# REVIEW 135 — BE 122's reconciliation, driven

**REV, 2026-09-09T10:46Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`.

**VERDICT: TWELVE IS THE DERIVED SET, NOT THE LARGEST NUMBER — and I closed the shared-scope
worry at the directory boundary rather than assuming it. THE 15 IS THE UNION AND IS
RECONCILED. TWO FINDINGS: (3) `SUPERSEDED_NOTICE` IS A SIBLING KEY — `scoring.n` STILL READS
8 WITH NO NOTICE INSIDE IT, AND THAT IS THE EXACT KEY DE'S PREDICATE READS. (4) THE
RECOMMENDATION CELL TESTS FOUR COUNTS AND NEVER A MODULE NAME — it passes on four integers
with no modules at all. And one hand-off hazard for DE 169: the 13/2 split is over the 15,
while DE's predicate is a SCORING predicate, where the split is 10/2.**

---

## (1) IS TWELVE THE DERIVED SET?

```
receipt-free SCORING   12      receipt-scoped SCORING  8  (withdrawn)
receipt-free REFERENCE  6      union 15, overlap 3
scope of the walk: 268 .py files in live/pm_research
```

**Said plainly, because it is the difference between corroboration and independence:** my
re-derivation calls **BE's own `reachable_modules`**, so it corroborates the *invocation*,
not the *operation*. MEM's pure-AST route is the independent one, and two derivations
agreeing is evidence only to the extent their scopes differ.

**So I tested the scope, which is what they could share.** For each of the twelve I parsed
its imports myself and resolved every name against the repository: **no module of the twelve
imports a project `.py` that lives outside `live/pm_research`.** So the package directory is
not truncating this walk the way the receipt's whitelist truncated the other one — the defect
BE found cannot be hiding one level out. **That is the rule-32 answer: membership is
transitive reference from the scoring entry points, the walk is closed under it inside the
scope, and the scope has no project code behind it.**

## (2) 15 AGAINST 12 — RECONCILED, AND THEY COUNT DIFFERENT SETS

```
for_a_SCORING_predicate    n = 12    scoring reachability
for_a_WHOLE_BOOK_predicate n = 15    scoring UNION reference (12 + 6 − 3)
on the newest real receipt: NAMED 13 (refusable) / UNNAMED 2 (reportable)
   the 2 are ['harmful_hazard_model.py', 'phase2_state_schema_freeze.py']
```

**13 + 2 = 15: the split is over the WHOLE-BOOK set, not over the twelve.** Driven for the
scoring set alone it is **10 named / 2 unnamed**. Both numbers are correct and they answer
different questions; the commit puts "SCORING 12" and "13 of the 15" adjacent, which is where
a reader trips.

**HAND-OFF HAZARD FOR DE 169, stated because DE's fix takes its set from here:**
`assert_book_scoring_code` is a **SCORING** predicate. The number it should carry is **10
refusable of 12**, not 13 of 15 — unless it is re-scoped to the whole book, which is a
different claim about what a book's bytes depend on.

## (3) FINDING — THE NOTICE IS A SIBLING KEY, AND THE STALE SET IS STILL AT THE OLD ADDRESS

```
derive(...)['scoring']['n']                     = 8      <- the OLD key, unchanged
a notice INSIDE ['scoring']                     = False
['SUPERSEDED_NOTICE'] present                   = True   <- a SIBLING key
```

**DE's predicate reads `derived_closures.scoring.{modules, n}`** (REVIEW 132 established
that at the line). **It resolves to the withdrawn eight and never encounters the notice**,
because the notice is beside the key rather than in it. A consumer reading the old key gets
the stale set silently — which is what the notice was added to prevent.

**This is last round's shape again**: the correction is published where a careful human
looks and absent where the automated reader resolves — rule 13's own reason for demanding
in-band supersession. **Put the notice inside `scoring` and `reference`** (a `SUPERSEDED_BY`
key beside `n`), or make those keys carry the corrected set with the truncated one renamed.

*Fairly stated:* DE 168's upper-bound fold already recovers the two in-recording modules the
eight missed — I measured `n_checked = 10` at REVIEW 134 — so today's predicate is not
checking only eight. **But its completeness assertion (`_absent`) is still against the
declared eight**, so the stale key is load-bearing for exactly the property REVIEW 129 asked
for.

## (4) FINDING — THE RECOMMENDATION CELL READS COUNTS, NOT MODULES

```
the cell as written, on the real thing              -> True
the SAME cell on four integers and no modules at all -> True
```

It asserts `n == 12`, `n == 15`, `n_named == 13`, `n_unnamed == 2` and **never a module
name**, so composition is invisible to it: a twelve-element set with a wrong member passes,
and at thirteen it fails without saying what changed. **The cells beside it are the model and
BE wrote them in the same round** — they name `flow_intensity.py` and
`harmful_exposure_rows.py` as the missed pair, name the two legitimately absent, and *drive*
the whitelist cause by adding one unrecorded module and recovering three. **Assert the SETS
there too, and let the counts follow.**

## ROUTED

1. **BE — put the supersession IN the superseded keys.** A consumer reading
   `scoring.modules` must not resolve to the withdrawn set silently; today DE's predicate
   does.
2. **BE — the recommendation cell should assert the SETS**, as its neighbours do.
3. **DE 169 — the scoring split is 10 refusable of 12**, not 13 of 15; 13/2 is the whole-book
   number.
4. **Twelve stands**, and the scope behind it is closed at the directory boundary.
