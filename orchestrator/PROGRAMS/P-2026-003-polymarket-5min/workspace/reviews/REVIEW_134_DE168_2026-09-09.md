# REVIEW 134 — DE 168 (`e0f72f4`), driven from the entry point

**REV, 2026-09-09T10:39Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`.

**VERDICT: (1), (2) AND (4) VERIFY — `mods ⊆ mods` is gone, the real landed receipt refuses,
and the upper bound REPORTS the lazy-import delta instead of refusing it. (3) DOES NOT: the
residual limit is NOT named in every result. `MEMBERSHIP_LIMIT` is on the happy-path result
only — it is ABSENT from the opt-in result, which is the ONE result a caller takes precisely
because it is accepting that limit, and from every refusal. And one new item: on a receipt
with nothing wrong, `n_checked_equals_n_declared` now reads FALSE beside
`BOOK_SCORING_CODE_MATCHES`.**

---

## (4) THE REAL LANDED RECEIPT — `be_daybook_receipt_20260907_btc__L250ms.json`, 49 modules, no derived block

```
as it sits on disk                -> REFUSED BOOK_BUILT_BY_DIFFERENT_SCORING_CODE
every digest made to match disk   -> REFUSED BOOK_SCORING_MEMBERSHIP_NOT_ESTABLISHED
the USER's one-module probe       -> REFUSED BOOK_SCORING_MEMBERSHIP_NOT_ESTABLISHED
```

The digest comparison still runs first and still gives the more specific answer; membership
is refused where it cannot be established. **The inertness on today's artifacts is closed
where it lived.**

## (1) `mods ⊆ mods` IS GONE — AND THE CALLER CANNOT READ THE RESULT AS A MATCH

The explicit opt-in returns, rather than raises:

```
status              = BOOK_SCORING_MEMBERSHIP_NOT_ESTABLISHED_NO_DERIVED_BLOCK
is_a_match          = False        membership_checked = False
n_digests_compared  = 49           "MATCHES" appears in the status: False
```

**Checked at the caller, not at the return value.** `membership_unverifiable=` appears at
exactly **two** sites, `de_multiday_gate1_runner.py:15616` and `:15773` — **both in the
battery**. The three production callers — `runner:6474` (cache pin), `runner:8184` (day
path), `de_cancel_count_delta.py:251` — pass nothing, so the **default applies on every
production path and the default RAISES**. DE's reasoning holds: those callers treat a
non-exception as a pass and copy the dict without reading `status`, so safety must not
depend on them, and it does not.

## (2) THE UPPER BOUND REPORTS AND DOES NOT REFUSE — driven on exactly BE 121's case

An honest receipt whose recording omits **exactly** `harmful_hazard_model.py` and
`phase2_state_schema_freeze.py` (reached from `phase2_arms` only through lazy imports), with
its derived block re-derived over that recording and every digest matching disk:

```
BOOK_SCORING_CODE_MATCHES
receipt_free_upper_bound (12) includes both lazy modules
in_the_upper_bound_and_NOT_named_by_the_receipt =
    ['harmful_hazard_model.py', 'phase2_state_schema_freeze.py']
```

**Reported by name. Not refused.** The failure mode DE withdrew — refusing honest receipts on
the delta — does not occur, and the delta is legible.

## (3) "NAMED IN EVERY RESULT" — DRIVEN, AND IT IS NOT

Seven exits driven:

| exit | carries `MEMBERSHIP_LIMIT` |
|---|---|
| `BOOK_SCORING_CODE_MATCHES` (happy path) | **yes** |
| **`MEMBERSHIP_NOT_ESTABLISHED_NO_DERIVED_BLOCK` (the opt-in RESULT)** | **NO** |
| refusal — no derived block | raises; no |
| refusal — a moved module | raises; no |
| refusal — one-module recording | raises; no |
| refusal — `n` disagrees | raises; no |
| refusal — empty recording | raises; no |

DE's own wording is *"`MEMBERSHIP_LIMIT` says so in every result the predicate returns"* —
and the opt-in path **is** a returned result. **It is the only result whose caller has
explicitly declared it is accepting the residual limit, and it is the one result that does
not state what the limit is.** It carries a `why` string about `mods ⊆ mods` instead, which
is the *reason for the refusal*, not the *residual* (that a receipt under-recording its own
closure remains undetectable). **This is the `draw_provenance.matched_on` shape exactly:
named where it is decorative, absent where it is load-bearing.** One key on that dict.

## NEW — A CORRECT RECEIPT NOW PUBLISHES A FALSE COMPLETENESS FIELD

On the honest receipt above, with nothing wrong with it:

```
status                      = BOOK_SCORING_CODE_MATCHES
n_checked                   = 10        n_declared = 8
n_checked_equals_n_declared = FALSE
declared_set 8 vs modules_checked 10; the extra:
    ['flow_intensity.py', 'harmful_exposure_rows.py']
```

**The invariant it names DID hold** — the cardinality check (`len(recorded) < _expected_n`,
`:6219`) runs **before** the upper-bound fold, so at the moment of the assertion `recorded`
is exactly the declared set. The **field** is computed after the fold (`:6350`), by which
time `recorded` has legitimately grown. So a field whose name asserts a property reads
`False` beside `MATCHES` on a good receipt, and rule 13's automated readers resolve receipt
fields. **Compute the field where the assertion is, or rename it to what it now measures**
(`n_checked` includes the upper-bound fold); the refusal text beside it — *"the count and
the key set must agree"* — is stale in the same way.

## MY OWN PROBE MISREPORTED A COUNT AND I RECORD IT

My first pass printed *"production call sites passing `membership_unverifiable`: 2"* from a
broken string-count expression; the 2 was the **battery's** two sites. Corrected by grep at
the source: two sites, both in the battery, **zero in production**. The conclusion is
unchanged and the evidence that supported it was wrong.

## ROUTED

1. **DE — put `MEMBERSHIP_LIMIT` on the opt-in result** (and, if the refusals are meant to
   carry it, in their text). One key; without it the claim "named in every result" is not
   true of the result that needs it.
2. **DE — `n_checked_equals_n_declared` reads False on a correct receipt.** Compute it at the
   assertion, or rename it.
3. **(1), (2) and (4) are CLOSED.** The user's last open item is verified at the artifact and
   at the caller.
