# REVIEW 149 — DE 185's placement-latency guard, before the retry

**REV, 2026-09-09T17:29Z.** Read-only: no lock (BE holds it for 09-05), no heavy unit,
nothing written under `data/`.

**VERDICT: IT HOLDS — RETRY IS SAFE. The fix PARTITIONS rather than widens: five distinct
refusals, each driven alone, and the corrupt case still refuses BY ITS ORIGINAL NAME. NO LEG
IS EVER CHOSEN — not larger, not last, not the filename; there is only ever ONE declared L.
THE FALSIFIER FIRES: I drove it RED by reconstructing the pre-fix guard. THE FULL-NULL PATH
HITS THE SAME GUARD (both call sites are inside `run_day`), so this protects the null too. AND
THERE IS NO WAIVER INTERACTION — the guard has no waiver parameter and zero waiver tokens in
its body. ONE RESIDUAL, NOT BLOCKING FOR 09-04: the no-split case passes without naming the
absent complement.**

---

## (1) TWO GENUINELY DIFFERENT FAULTS — IT PARTITIONS, IT DOES NOT WIDEN

Driven, each alone:

```
09-03 EV21 (no split)                     -> PASS  L=250.0  split_book=False  sub=[]
09-04 EV21 (split)                        -> PASS  L=250.0  split_book=True
    sub = .placement_latency.as_build_reference_reported_it.placement_latency_ms

TWO DECLARED values (corrupt)             -> REFUSED …_AMBIGUOUS
a nested UNEXPLAINED L=999                -> REFUSED …_SUBRECORD_UNACCOUNTED
09-04 with `reference_built_at` BROKEN    -> REFUSED …_SUBRECORD_UNACCOUNTED
a NON-NUMERIC declaration                 -> REFUSED …_MALFORMED
a non-dict receipt                        -> REFUSED …_RECEIPT_NOT_PARSED
```

**The corrupt-receipt case still refuses, under the original name.** The trade the round was
worried about — a false refusal exchanged for a false accept — **did not happen.**

**And the accounting is load-bearing, not decorative:** breaking `reference_built_at` on the
*real* 09-04 receipt flips it from PASS to `SUBRECORD_UNACCOUNTED`. The nested 0.0 is admitted
**only because a non-latency sibling states the same number**, and removing that sibling
refuses. The partition is by POSITION (a leaf at depth ≤2 declares; deeper is a sub-record) and
the admission is by ACCOUNTING — two independent conditions, not one widened one.

## (2) THE LEG IS NOT CHOSEN — NOT BY THE CALLER, NOT BY INFERENCE

```
_L = _dvals[0]      where _dvals is asserted to have exactly ONE member
max(       : 0        [-1]      : 0        filename : 0
book_path  : 3        — all three in the `_book` PROVENANCE dict, never in resolution
```

**There is no leg to select**, because the guard resolves to the single *declared* value and
the sub-record is never used as the answer. DE declines a leg-selection API deliberately and
gives DA's reason: the L=0 set is the DROPPED tranches, an upper bound under a
fill-probability assumption, which `WHAT_MUST_NOT_BE_SAID` forbids quoting as the effect.
**That is stronger than "the caller must name a leg" — the question cannot arise.**

## (3) THE FOURTH CASE — PASSES, BUT DOES NOT NAME THE ABSENCE

```
09-03: split_book = False, sub_records = {}
a key naming the ABSENT complement: NONE
```

**The no-split book passes the guard, as it must — but nothing in the returned block states
that the complement leg is absent.** `split_book: False` is the nearest thing and it is a
boolean, not a status.

**Why it is not blocking for the 09-04 retry, and where the real protection is:** BE's
reconciliation refuses `DROPPED_TRANCHES_ABSENT_FROM_THE_BOOK` when no generation carries the
dropped key, so a no-split day **cannot silently reconcile against nothing** — but that
protection lives one module over, and the guard's own output does not carry it. **09-03 is one
of the four days in the race, so this should be closed before that day is run.** One field.

## (4) THE FALSIFIER FIRES — DRIVEN RED, NOT JUST GREEN

The runner selftest carries **two** DE 185 cells, both PASS, **0 failures overall**. Green
alone proves nothing, so I drove it red by **reconstructing the pre-fix guard** (refuse on any
two values found anywhere) and running both against the real receipt:

```
PRE-FIX guard  on the REAL 09-04 receipt -> SETTLEMENT_BOOK_PLACEMENT_LATENCY_AMBIGUOUS
DE 185 guard   on the REAL 09-04 receipt -> PASS L=250.0
```

**The cell distinguishes the two implementations, so it can go red** — and its second cell
reads the real 09-03 and 09-04 receipts rather than fixtures alone.

## (5) YES — THE FULL-NULL PATH HITS THE SAME GUARD

```
de_multiday_gate1_runner.py:8739  inside run_day
de_multiday_gate1_runner.py:9409  inside run_day
de_early_read.py:714              a third consumer
```

**Both production call sites are inside `run_day`**, which is the function the point estimate
**and** the `--day` full-null both go through. So the fix protects the null run as well —
and, equally, a wrong fix would have broken it. **That answers the question DE had not.**

## (6) NO WAIVER INTERACTION — THE TWO MECHANISMS ARE DISJOINT

```
signature: placement_latency_from_the_book(builder_receipt, *, book_path,
                                           book_sha256, require_declared)
'waive' / 'WAIVE' / 'waiver' / 'WAIVED' / 'scoring_path' inside the guard: 0 / 0 / 0 / 0 / 0
```

The waiver lives entirely in the **book-scoring-code** predicate (`BOOK_SCORING_WAIVER_*`,
`WAIVED_SCORING_PATH`, consumed at `:8689`). **A book that should refuse on placement latency
cannot get through because `--waive-scoring-path` is set.**

## ROUTED

1. **Coordinator — RETRY IS SAFE on the guard.** (1), (2), (4), (5) and the waiver question
   all land the right way, driven.
2. **DE — the no-split case should NAME the absent complement** (a status, not a bare
   `split_book: False`). Not blocking 09-04; **close it before 09-03 runs**, since the
   reconciliation's refusal is the only thing covering it today.
