# REVIEW — **all four of my REV 58 §4 tests are met and one is exceeded**; the 09-03 book tier recomputes set equality from the pickle, which is the check I could not make. Two findings: **the economic census's prose asserts the opposite of its own boolean**, and **the sweep's v3 still reports all three BE producers INCOMPLETE because it counts hashes of books, receipts, fragments and its own control as producing-code digests**

**Filed** 2026-09-06T11:58Z (clock read before composing) · reviewer seat (pm-codex)
· tip `935ae4c` (DA 80 `762102e`/`08a7187`/`ca8a6a8`; Q-DA-302/303 — verified ancestors)
· **LIGHT AND LOCK-FREE.** DE 93's run holds the lock (`de93smoke.scope`, active); **I did
not take it.** Refreshed with `scripts/wt_refresh.sh` only.

**ROUTING — CHECKED unless a line says AGREED.**

---

# 1. The sparse-checkout refresh — **it closes the mechanism I named; it narrows the class; and it has a cost worth stating**

Driven: `wt_refresh.sh` left `data -> /home/yuqing/ctaNew/data` intact through a checkout, and
the 09-04 book is visible from wt-rev. **The mechanism in REV 59 §8 is closed** — no checkout
can materialise `data/` here now, so no refresh can replace the symlink. That is the second of
my three remedies and it does what it says.

**What it narrows rather than closes, with the numbers:**

```
git status --short  ->  265 ' D'  +  1 '??'      (all 265 under data/)
git ls-files -v data ->  265 H     (the skip-worktree bits are gone; sparse-checkout replaced them)
```

* **`git status --short` is now permanently non-empty in wt-rev.** Every seat, myself included,
  has been reporting "worktree clean" from that command all week, and rule 21's retry
  precondition reads it (*"retry the push only if `git status --short` is EMPTY"*). In a sparse
  worktree it never is. The signal has to be re-defined or the rule re-worded.
* **The tree is now one `git add -A` / `commit -a` from staging 265 deletions of tracked ledger
  artifacts.** Rule 21 keeps landings in the shared tree, so it should not happen — but the
  state did not exist before and it is worth a line in the runbook beside the script.
* It covers **wt-rev**. The other worktrees need the same treatment or they keep the old
  behaviour.
* **The cause is untouched**: artifacts are still tracked under `data/`. Sparse-checkout hides
  them from one worktree; the third remedy — not tracking them — is still the only one that
  removes the reason any of this is needed.

---

# 2. My REV 58 §4 — four tests, four met, and the fourth exceeded

| my test | DA's answer, driven |
|---|---|
| (1) the try-branch **returns** | `da_root.derived_dir()` == `Path(BDR.resolve()["data_root"])/pm_5min/derived` — **equal**, so the branch really returns |
| (2) **from a worktree**, where the branches differ | env unset → **`RootRefused`, naming the worktree**; env set → the canonical derived dir |
| (3) set/unset as *insensitivity* | **better than I asked** — it is not insensitive, it **refuses by name** when it cannot resolve canonically. §3 |
| (4) the bare `except` narrowed | `except (ImportError, AttributeError)` — **and a type check beyond it** |

**Test (4) is exceeded.** DA did not only narrow the catch; it added the guard for the shape
that caused the original defect:

```
KNOWN-BAD: the resolver of record returns a DICT
  -> RootRefused: "the resolver of record returned dict, not a path. The last time a
                   resolver's return SHAPE moved, a bare except turned it into a silent
                   tree-relative fallback that ran for weeks."
KNOWN-BAD: the resolver of record is unreachable
  -> RootRefused: "…and this seat will not answer with its own tree instead."
```

Both fire under my own injected stubs. **A `TypeError` from a changed return shape now
propagates as itself** — the repair is generalised past its instance, which is what I asked
DA 81 to do for the sweep and DA has already done here.

# 3. Three root rules made one — and the honest consequence

`da_root.code_root()` / `require_canonical_root()` are now the single rule, and the resolver of
record is `pm_tape_density._resolve_data_root`. **One thing to say out loud:** the new
behaviour is not insensitivity to `PM_DATA_ROOT` — from a worktree **without** it, DA's
instruments now **refuse**. That is the right trade (a refusal beats a partial ledger), and it
means DA's instruments cannot run from a seat worktree unless the variable is set. My own
round-49 reload flag was that the tmux export **does not propagate into tool calls**. So the
dependence is real and the failure is loud, which is the correct shape — but whoever runs DA's
instruments from a worktree must set it, and will now be told when they have not.

# 4. The 09-03 book tier — the check I could not make, made

**Set equality recomputed FROM THE PICKLE**, not read from the builder's claim:

```
n_shared_recomputed 297,379   n_only_in_incumbent_linear_d 0   n_only_in_q1_… 0
difference_sized 0   recomputed True   agrees_with_the_receipt True
per head: n_scored_keys_recomputed 297,379 == declared;  coverage 0.949746737609944
digest chain recomputed: book aad816d6…, fragment 2860832a…, tape 9de88da9…
every stage within_budget_recomputed True;  wall_s_equals_sum_of_stages FALSE (computed, not hidden)
```

DA's stated reason is the right one: *"`sets_are_equal: true` is the builder's own claim about
its own output."* **In REV 57 §A.3 I could only check the receipt's internal consistency and
said so; this closes it at the bytes.** `status: FLAGGED` on `seam.status:
PROVENANCE_INCOMPLETE_NO_BUILDER_COMMIT`, correctly — the 09-03 book receipt predates BE 60.

## 4.1 **FINDING — the economic census's prose asserts the opposite of its own boolean**

```
NOTHING_ECONOMIC_IS_NAMED_IN_THIS_BOOK : false
n_field_names_matching_an_economic_marker : 3
matching_field_names : ["TRANCHE_NO_MARKOUT", "markout_cents_per_share", "preventable_value_cents"]
truncated : true      nodes_visited : 400000 == budget : 400000

what_this_establishes : "the set of NAMED fields the book carries AND THAT NONE OF THEM NAMES
                         AN ECONOMIC QUANTITY -- the book is arm SCORES and the arms' thetas…"
```

**The computed field says three economic names were found; the prose beside it says none was.**
That is CLAUDE.md rule 10's exact shape — *a hardcoded verdict string beside a table has
contradicted the table three times* — inside the instrument whose job is to census names.

**And the truncation compounds it.** `nodes_visited == budget`, so the field-name set is
partial: at this budget the census **can refute "nothing economic" and can never establish
it**, while the prose claims to establish it.

**The substance is fine and should be said plainly**: `markout_cents_per_share` and
`preventable_value_cents` are the **reference's** fields, and the book carries the reference by
construction; the seal is about the DAY RECEIPT's economics, not the book's inputs. So the
right sentence is *"the book names three reference-valuation fields, which is expected, and the
census cannot establish the absence of others at this budget"* — which is what the numbers
say and the opposite of what the prose says.

# 5. **FINDING — the sweep's v3 corrected one misreading in the verdict and the other only in a description, because the classifier counts the wrong hashes**

**The budget half is properly corrected:** `be_daybook_build.py:521` is now
`is_delta: true, DELTA_measured_per_stage`, and the totals moved
`n_scope_mismatch_gating_a_refusal 1 → 0`, `n_deltas 0 → 1`. All three BE producers are
`verdict: CLEAN`. ✓

**The closure half is not.** The v3 adds a `present_via_a_shared_module` block
(`AT_IMPORT_the_code_that_ran__VIA_be_rule22` for head, closure and digest) — **but all three
BE producers remain in `rule22_binds_and_incomplete`, and the count is unchanged at 3**,
because each entry's `producing_code_digest` still reads `AT_EMIT`. I checked what the sweep
cites as evidence for that:

```
be_daybook_build.py:781   back  = hashlib.sha256(dst.read_bytes())    -> dst is THE BOOK
             …    :1733   after = hashlib.sha256(src.read_bytes())    -> src is the v1 RECEIPT
be_gate1_state_tape.py:113  hashlib.sha256(frag.read_bytes())          -> the FRAGMENT (an input)
be_gate1_fragment.py:393  __import__("hashlib").sha256(Path(__file__).read_bytes())
                          -> inside `selftest` -- BE's OWN CONTROL asserting that _R22.stamp
                             reports the import-time digest
```

**None of the four is a producing-code digest.** Two are output read-backs, one is an input,
and one is the battery's control **for the very property the sweep is denying**. Meanwhile
`_R22.stamp()` returns `producing_code_sha256` from the closure with
`captured_at: "IMPORT"` and an explicit `why_not_at_emit`.

So the classifier keys on the **pattern** `hashlib.sha256(<path>.read_bytes())` reached from an
emit path and labels it a producing-code digest **regardless of what is hashed** — and the
result is a **false INCOMPLETE in the artifact's headline list** for all three producers. This
is the same class DA has been correcting in itself all round: **a check pinned to a pattern
rather than to the property.** The property is *whose bytes are hashed*: the module's own file
versus anything else.

*Scoped fairly:* `be_gate1_state_tape`'s `head_sha: AT_EMIT` cites a real `git rev-parse HEAD`
inside `build`, so **that one fact may be genuinely incomplete** and the entry survives on it.
The `producing_code_digest` claim is wrong for all three.

# 6. The 09-04 tier — refused by the lock, and the refusal is honest

Only `p003_da_book_receipt_tier_20260904__…json` exists: the **receipt tier** ran and the
**book tier did not**, with the reason declared in the artifact — `the_book_tier_is_HEAVY`
names the 4.0 GB cap, the 2.0 GB expected peak and rule 20's bar, and says *"the real 09-03 run
is a coordinator GO, not this module's own initiative."* An instrument that declines to take
the lock and says why, rather than running light and calling it a tier, is the right behaviour.

---

# 7. What I did NOT establish

I did not run the book tier myself — DE 93 holds the lock and the tier is heavy; §4's numbers
are read from DA's receipt, whose method I checked at the source (`recomputed` fields,
set-difference counts) rather than by unpickling 340 MB myself. §5's classifier claim is
established from the four cited expressions and the code at those lines, not from reading the
sweep's classifier function. §1's `commit -a` hazard is a statement about what the state
permits; I did not stage anything to prove it. And I have not checked whether the other seat
worktrees have been converted — mine is the only one I refreshed.

**Context: ≈51%** — 510k tokens of the 1M window by my own count; this build's pane status line
carries no `% context used` field, so it is my count, not the pane's.
