# REVIEW 128 — DE 166 (`3b93077`) against the REVIEW 126 baseline

**REV, 2026-09-09T09:35Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`.

**VERDICT: DE 166 VERIFIES on all three specifics and on the load-bearing argument. Three
residuals, none blocking — and one of them is that MY REVIEW 123 finding is still open, which
is a different defect from the user's and was not in scope.**

## THE BASELINE, RE-DRIVEN — same fields, same entry point

| field | REVIEW 126 (before) | now |
|---|---|---|
| `run_day` passes `arm_cancels` | **False** | **True** |
| `run_day` passes `control_set_path` | **False** | **True** |
| `draw_provenance.matched_on` | **ABSENT** | **`CANCELS`** |
| `draw_provenance.draw_source` | `GENERATED_IN_PROCESS` | unchanged — correctly, that is *how* the draws were made, not what they were matched on |
| arm keys naming match/control | NONE | NONE — *residual 3* |

**(1) The artifact now NAMES its matching unit.** `matched_on = CANCELS` is in the emitted
`draw_provenance`. The failure that let four register entries claim ruling B with no receipt
contradicting them is closed at the artifact.

**(2) A null that cannot name its unit REFUSES — it does not fall back.** Driven five ways
through `null_draws_valued`:

```
matched_on omitted / None / 'ROWS'      -> REFUSED NULL_MATCHING_UNIT_NOT_DECLARED
CANCELS with no arm_cancels             -> REFUSED NULL_MATCHING_UNIT_CONTRADICTED_BY_ITS_INPUTS
CANCELS with no control set             -> REFUSED NULL_MATCHING_UNIT_CONTRADICTED_BY_ITS_INPUTS
DECISIONS (the declared alternative)    -> proceeds
```

The unit is **required**, and it is **checked against the inputs that would make it true** —
which is the requirement, not an extra.

**(3) BE's contract is now ENFORCED — the third drive that failed before now passes.**

```
ALL five present -> BOOK_SCORING_CODE_MATCHES  n_checked=5
ONE present      -> REFUSED BOOK_SCORING_CODE_RECEIPT_INCOMPLETE
all but one      -> REFUSED BOOK_SCORING_CODE_RECEIPT_INCOMPLETE
empty            -> REFUSED BOOK_SCORING_CODE_NOT_RECORDED
```

## THE LOAD-BEARING ARGUMENT — TESTED AGAINST EVERY REAL RECEIPT, AND IT HOLDS

DE's argument is that real receipts carry all five, so a completeness refusal costs nothing
legitimate. **Measured across all twelve book receipts on disk:**

- **eight carry all five** (closures of 48 or 49 modules) — they pass;
- **four carry a closure of ZERO** — `20260903_btc.json`, `20260904_btc.json` and its `.v2`
  and `.v3` supersessions — and **those already refused before this change**, under
  `BOOK_SCORING_CODE_NOT_RECORDED`.

**No receipt on disk carries a non-empty PARTIAL closure.** So the new refusal adds no cost to
any legitimate receipt: the argument is true, and it is true for a reason worth recording —
the four that fail are pre-rule-22, which is a rebuild requirement and not a regression this
change introduced.

## THREE RESIDUALS, NONE BLOCKING

1. **`n_expected` is still absent from the PASSING result.** The predicate refuses correctly
   but does not publish how many it expected, so a pass is not legible from the artifact —
   `n_checked=5` beside nothing.
2. **`SCORING_PATH_MODULES` is still the typed FIVE, not BE 119's derived EIGHT.** **The
   user's defect (a subset accepted as the set) is FIXED; my REVIEW 123 defect (the set is a
   hand-typed sample of a recorded 49) is NOT** — the predicate now refuses a partial receipt
   *of a sample*. Different defect, not in this round's scope, and worth keeping separate in
   the ledger rather than counted as closed.
3. **The richer control block is still absent from the arm.** `matched_on` lives only in
   `draw_provenance`; the demand, the strata count and the control-set digest are not in the
   artifact. Sufficient for the requirement as stated; less than the artifact could carry.

## ON THE PHRASE-ASSERTION CLASS — NO SURVIVOR, AND ONE THAT LOOKS LIKE ONE

DE disclosed two assertions that tested a phrase rather than a property. I swept the new
cells. **The one that looks like a third is not one:** the absent-map cell asserts the
property first — the call **refused** (`_m_none is not None` after a `DiagRefused`) — and
then tests four phrases because *"which statuses are the candidates"* **exists only as prose
in the message**. So it is not a phrase standing in for a property; it is a property whose
only representation is prose. **The fix is to carry the candidate statuses as a structured
field on the refusal**, so the cell can assert the SET rather than four substrings.

## ROUTED

1. **DE — `n_expected` beside `n_checked`** so a pass is legible, and the candidate statuses
   as a field rather than prose.
2. **Coordinator — keep REVIEW 123's finding open**: the typed five is still a sample of the
   recorded closure, and BE 119's derived eight is the set the predicate should be reading.
3. **Standing by for the remaining repairs.**
