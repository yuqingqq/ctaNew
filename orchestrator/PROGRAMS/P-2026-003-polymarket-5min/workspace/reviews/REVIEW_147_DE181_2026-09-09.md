# REVIEW 147 — DE 181, the placement-latency reconciliation wired in the driver

**REV, 2026-09-09T16:52Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`, no book unpickled. **DE is running the 09-04 point estimate; I touched nothing.**

**VERDICT: (1), (2), (3) AND (4) HOLD — the call is on the PRODUCTION path (`run()`, which
`main()` calls), the refusal is TERMINAL and unabsorbed, the emit is atomic, and both sweeps
are DEPTH-GENERAL rather than depth-2. BUT (5) DOES NOT HOLD AS ASKED, AND IT IS THE SHAPE YOU
PREDICTED: `PERFORMED_BY_POINT_ESTIMATE_DRIVER` IS NOT MODE-GATED. It is emitted whenever a
winner source resolved, `point_estimate` or not — so a FULL-NULL `--day` arm-day receipt
carries the claim while the driver never runs. The point-estimate artifact is protected; the
full-null artifact asserts a check that did not happen.**

---

## (1) IDENTITY — the call is on the production path, driven by AST, not read

```
:527  reconcile_placement_latency(result)   is enclosed by   run()  (496-591)
main() calls                                                 ['run']
:824  except (R.RunnerRefused, PLR.ReconcileRefused)  is enclosed by  selftest() (602-954)
```

**So the call is in `run`, which `main` invokes, and the only `except` that names
`ReconcileRefused` is inside the selftest — it cannot absorb a production refusal.** The
distinction the round asked about is real and lands the right way.

## (2) BOTH SIDES ON THE LIVE PATH

The production call is `PLR.reconcile(...)` — **the throwing form**, not `reconcile_status` —
with `first["reference"]`, `first["winners"]` and `first["baseline_total_cents"]` taken from
the run itself under the private key, never re-derived. **And the live shape now exists:**
`be_daybook_receipt_20260904_btc__L250ms__EV21.json` carries
`placement_latency_split` with `n_tranches_before_this_L = 31471`, so the reference the driver
hands over **has a real dropped set** and BE's `_both_sets` engages rather than refusing
`DROPPED_TRANCHES_ABSENT_FROM_THE_BOOK`. **Both anchors are reachable on the path DE is
running right now.**

Before the checker is even reached, the driver refuses three ways of its own — inputs absent,
any of the three `None`, or the arms disagreeing on reference / winners / baseline — each by
name.

## (3) THE REFUSAL IS TERMINAL, AND THE EMIT IS ATOMIC

`reconcile_placement_latency` **does not catch**; `run` calls it at `:527`, **before** the
artifact path, the payload and the write. And the write is `tempfile` + **`os.replace`**
(`:327`, `:337`) — atomic, as DE 151 established. **A refused reconciliation leaves no
artifact, not even a half-written one.**

## (4) THE PRIVATE-KEY SWEEP IS DEPTH-GENERAL — driven at eight

```
nested at depth 1 / 2 / 3 / 5 / 8 (dicts AND lists) -> CAUGHT at every depth
through three nested LISTS                          -> CAUGHT
a DIFFERENT _*_DO_NOT_SERIALISE key                 -> CAUGHT (a PATTERN, not a literal)
clean payload                                       -> checked, no hits
```

**Not depth-2, and not keyed to the one name.** The selftest plants it at two depths; the
implementation recurses without limit.

## (6) THE FINITENESS CHECK IS TOTAL — with one hole worth naming

```
top-level nan            -> $.total
nan at depth 4           -> $.a.b.c.d
inf inside a LIST        -> $.a[2]
nan in a list of dicts   -> $.a[0].b[0].c
clean control            -> none
```

**It walks the whole returned block rather than named fields** — so DE's guard is total over
dicts and lists. **The hole: a non-finite inside a SET is not found** (`_finite_scalars` walks
dicts, lists and tuples only), and neither are dict KEYS or a non-`float` scalar. **No
reconciliation output is set-valued today, so this is a limit and not a defect** — but the
guard exists precisely because a publisher should not depend on its checker's arithmetic being
total, and by the same argument it should not depend on its own container coverage being
lucky.

## (5) THE FINDING — THE RUNNER'S CLAIM IS NOT MODE-GATED

Driven by AST over the guard nesting, not read:

```
:8748  _plr177 = {...}                              under  if _adm801['admissible']  (:8677)
                                                    under  if _win801 is not None    (:8720)
:8942  r['placement_latency_reconciliation'] =      under  if _win801 is not None    (:8941)
```

**`point_estimate` appears in neither guard.** Only the *private-key attachment* — the inputs
that let the driver actually reconcile — sits inside `if point_estimate:`, immediately after
`:8942`.

**Consequence:** on the full-null `--day` path, a settled admissible arm-day receipt carries

```
placement_latency_reconciliation.status = "PERFORMED_BY_POINT_ESTIMATE_DRIVER"
```

**while the driver never runs and no reconciliation happens.** That is
`NOT_WIRED_PENDING_BE_138` with the sign flipped: a status asserting a check that did not
occur, in an artifact rule 13's automated readers resolve by field.

**What DOES protect the point-estimate artifact, and is real:** the driver refuses
`RECONCILE_INPUTS_ABSENT` if the private key never arrives, so a point estimate cannot be
emitted with the reconciliation silently skipped. **That protection does not extend to the
full-null path, which emits its own artifact and never involves the driver.**

**The fix is one condition:** gate `_plr177` — or its status string — on `point_estimate`, and
give the full-null path its own honest status (`NOT_PERFORMED_ON_THIS_PATH`, with the reason).
The runner needs no reconciliation call to say that, so it costs no book.

## ROUTED

1. **DE — `PERFORMED_BY_POINT_ESTIMATE_DRIVER` must be mode-gated** (`:8748` / `:8942`).
   Today the full-null path asserts a check that did not happen. One condition; no scoring-path
   behaviour changes.
2. **DE — `_finite_scalars` does not walk sets** (or dict keys). A limit today; name it or
   close it.
3. **(1), (2), (3), (4) are CLEAN**, and the identity question lands the right way: the call
   is in `run`, and the only `ReconcileRefused` handler is in the selftest.
