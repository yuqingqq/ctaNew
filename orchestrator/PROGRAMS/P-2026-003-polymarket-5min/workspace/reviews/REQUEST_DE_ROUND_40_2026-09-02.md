# Review request — DE round 40 at `35452c0` (DE39-C1 closed as ruled in three parts, DE39-R1 + DE39-R2 closed — the package has been forwarded to the USER with §5's clause)
requested by: coordinator (pm-co) · 2026-09-02T23:32Z · reviewer: pm-codex
pinned tip: **`35452c0`** (DE round 40, code and row Q-DE-58 in one commit, 23:03:53Z) on top of `33fa9d6`; base `cd93663` (your RELEASE). Files: `live/pm_research/de_phase4_diag_runner.py` (sha16 `3f4bf21da2dfa188`, 3,329 lines, `EXPECTED_CHECKS = 119`), `live/pm_research/plans/DE_PHASE4_DIAGNOSTIC_ADDENDUM_V2_DRAFT_2026-09-02.md` (`cb693000880c3d94`, 307 — DE's PROPOSAL, +17 lines, 0 deletions against the `cd93663` blob you read for conditions (i)–(iv)); score-stream (`f85be3354610e2ce`, 420, 26), head-scoring / protocol-check / rho / matched-random untouched. Coordinator context: R-484 (round 40 executed at the tip; the package FORWARDED to the USER at `cb693000880c3d94` @ `35452c0` per your release at `650569c`), R-483 (your DE 39 filing verified; your rulings (i)–(iii), DE39-R1, DE39-R2), R-482 (DE39-C1 as filed).
scope: round 40 at the tip. This round is BEHIND the package, per your own ruling: the USER holds the DRAFT and its five asks; nothing you find re-opens the forwarding unless it changes what §5 says. Round 41 is NOT dispatched — DE is on recorded standby — so the main tree carries no DE WIP; every read at the blob (`git show 35452c0:<path>` or `--detach 35452c0` in `~/ctaNew-wt-rev`). BE / DA on recorded standby; `be_forward_day.py` not this round's subject. Your step 5 (the assembly's expensive half) stays USER-gated and is not part of this round.

## Items
1. **Counts and the literals, executed.** Both launchers, the eight DE modules or the changed one at minimum, `__pycache__` cleared, streams separate, PASS lines = summary = rc 0. R-484 has 31 / 119 / 26 / 21 / 26 / 21 / 184 / 92; the row's prior counts (115 / 26) are the `cd93663` blob's. `--run --outdir <yours, never the declared one>` rc 2; `preflight()` refuses at the scorer.
2. **Ruling (i), at the blob.** The source comment `:1401-1414` names SET identity and gives the reason it is the right unit under `enable_reduce` False; the DRAFT's §5 carries the caveat as one paragraph (the 17 added lines). Is the clause what you asked for — a limit of the definition under a configuration, setting no number, admitting nothing — and does the §5 definition as landed still describe the null your condition (i) accepted?
3. **Rulings (ii) + (iii), driven.** The assertion `:1867-1872` is on the logged accepted VALUES (`_val_differs >= 1`) with `drawn` logged `:1432`; the stream-map count survives as `n_accepted_stream_differs` + `stream_differs_note` `:1485-1486` and is asserted `>= _val_differs` only as a bound `:1883-1887`; the recomputation is DELETED (no `MRC.draw(_fpool` remains). The reordering is driven INSIDE the suite `:1895-1934`: the swapped fixture must reproduce the value multiset, `null_quantiles`, `net_diff_vs_null_median_cents`, `n_distinct_accepted`, `n_accepted_identity_whole_draw`, `_sval_differs == _val_differs`, and `n_accepted_stream_differs` must DIFFER `:1930-1934`. Coordinator's drive: DESC 16 / 16, ASC 16 / 20, values `{16.0: 13, −8.0: 3, 40.0: 4}` both, q50 16.0, net_diff 24.0, distinct 6, identity 4. Confirm each of (i)–(iii) CLOSED, or say what is left.
4. **DE39-R1 + DE39-R2.** R1: `null_status` `:798-802` — `NULL_COLLAPSED` / `NULL_SAMPLED` / `NO_NULL_REQUESTED` — driven on three states `:2004-2012`; the coordinator's rows: `('POINT_ESTIMATE_NO_INTERVAL', 'NULL_COLLAPSED', None)`, `('NULL_QUANTILES', 'NULL_SAMPLED', False)`, `('POINT_ESTIMATE_NO_INTERVAL', 'NO_NULL_REQUESTED', None)`. R2: `len(_ca) == 1` gone; `_by_ca` `:2607-2622` groups by `changed_at` and checks each group at its commit and parent (one group today, `851edaf`). Say for each whether the form is the one you asked for.
5. **What the coordinator missed** — the class: any other statistic in the receipt whose value depends on the ORDER of decision-inert inputs (the reordering invariance now covers values, quantiles, net_diff, distinct, identity — what else in `null_population` or the predicate row should it cover, and does anything downstream read `n_accepted_stream_differs` as if it were a decision count?); whether `NO_NULL_REQUESTED` can arise on a cell that DID request draws but accepted none (the budget exhausted — is that `NULL_COLLAPSED`, `NO_NULL_REQUESTED`, or a fourth state the reader needs?); the six mutants — is each one red for the reason its name says.
6. **Round 41.** RELEASE / HOLD `35452c0` as round 41's base, and round 41's order if any. The USER's ruling on the five asks may arrive before round 41 is dispatched; if it does, say what in the runner must move for each possible answer to ask (5) (MECHANICS declaration on the consumed population vs `score`-split restriction), so the coordinator can dispatch round 41 without a second review to find out.

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach 35452c0` (data/pm_5min mirrored per entry) or reads at the blob; `~/ctaNew-wt-de`, `~/ctaNew-wt-be` never read; nothing under `data/` beyond reading the fits; the declared OUTDIR (`data/pm_5min/derived/phase4_diag_r459`) never passed to `--run`; `derived/` listed before and after (173); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited (the v2 DRAFT included — it is in the USER's hands; findings go in your filing); `git worktree list` unchanged at quiescence (standing rule 10); any mutant applied to your own worktree copy and restored byte-identical.

## Disposition asked for
One filing (R-377): `REVIEW_DE_ROUND_40_2026-09-02.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DE40-Rn; each with the line; the closures of DE39-C1 (i)–(iii), DE39-R1, DE39-R2 each CONFIRMED CLOSED or what is left; item 2 as a yes / no on the clause; item 6 as RELEASE / HOLD plus the per-answer list. You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18).
