# REVIEW REQUEST — BE mask-consumer batch, round 2 (RR8 closures + CO-1) — pinned tip `d337b73`

Filed by the coordinator 2026-09-02T10:26Z. One round, one filing (rule 5 / R-377). Scope is BE's complete batch since the released round at `fa87156` (`3a1d475`): commits `791b53b` (RR8-1/RR8-2/RR8-3 + the R-410/R-411/R-412 trigger), `c200262` (Q-BE-225), `d151afa` (CO-1), `d337b73` (Q-BE-226). One file: `live/pm_research/harmful_forward_scorer.py`. Register context: R-412 (rulings 1–3), R-414 (CO-1, coordinator-reproduced).

## What to review (execute, never accept from the report — including from R-414)

1. **RR8-1 — the producer's artifact is the contract.** Load the REAL committed `data/pm_5min/derived/da_blackout_mask_20260901.json` through `load_blackout_mask` and confirm 141 windows with per-coin counts read from the artifact (btc 23, eth 22, sol 23, bnb 22, doge 22, xrp 20, hype 9 — assert against the artifact, not literals). Then drift it: `protocol`/`per_coin` envelope, `n_masked ≠ len(list)`, `total_masked_windows` off by one — each must refuse by name. Note: DA's round 7 (in flight) adds `carrying_commit` + module sha to the mask artifact; the adapter carries `detector` in its place today. Say whether the adapter will accept the extended envelope without a change (additive keys) or refuse it.
2. **The trigger (R-410 as amended by R-411/R-412).** Presence consumes on ANY day (09-01's 141 windows are masked at scoring); governance requires from `EFFECTIVE_FROM_DAY` (imported from the frozen rule — check it is not restated); `CONTENT_LIVENESS_UNRESOLVED` on a governed day refuses with a retry-when-verdict-lands remedy; `CONTENT_LIVENESS_UNJUDGEABLE` refuses on any day, is named permanent, and emits `routed_to` as text without deciding a disposition (rule 14). BE says a control asserts the two statuses stay distinguished — find the mutant that collapses them and confirm it is killed.
3. **RR8-3.** `day_closed_calendar` False → refused by name, both directions.
4. **CO-1 (R-414).** Under `python3 -m live.pm_research.harmful_forward_scorer --selftest` AND the script-dir launch: both rc=0, 60 checks. Simulate the frozen rule being unimportable (both mechanisms) and confirm `GoverningRuleUnreadable` at the point of use, never `governed=False`. BE reports two accepted survivors with proof (either import mechanism alone suffices; both removed → killed): verify the both-removed mutant is actually killed and that the "launch check contributes no checks" guard fires.
5. **BE's two self-found defective controls** (a refusal control that set an attribute on a second module object under `__main__`; a stale control now pointing at 08-27 because 09-01 reads CONTENT_THIN under the landed rule block): can each control now fail? (rule 16).
6. **The live case.** BE pins 09-02 as a real-artifact case: governed, CONTENT_THIN, mask absent on disk → refuses by name. Confirm on the on-disk verdict as it stands when you run (DA's round 7 may restore the 00:06Z bytes and/or emit `da_blackout_mask_20260902.json` while you review — record the as-of and sha of what you read; a changed answer with a changed input is not a finding).

**Do not run `da_midnight_verify.sh`** in this round (R-414 incident; DA's round 7 is changing its guard). Nothing in this round needs it.

Run in your worktree (`~/ctaNew-wt-rev`, refresh with `git -C ~/ctaNew-wt-rev checkout --detach d337b73`), inside the research slice for anything heavy. File `REVIEW_BE_MASK_CONSUMER_R2_2026-09-02.md`, commit, push, ONE notification.
