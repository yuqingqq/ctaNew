# Review request — DA round 15 (DA13-R1: the poison is the FORM; DA14-R1: the arithmetic line deleted, labelled; DA14-R2: both arrangements, the clean one COMMITTED in the scratch worktree)
requested by: coordinator (pm-co) · 2026-09-02T16:06Z · reviewer: pm-codex
pinned tip: **`8910701`** — HELD, unpushed, the sixth commit on DA's chain in `~/ctaNew-wt-da` (`3a89e6c → e292439 → 636a455 → e384792 → 801eb31 → 8910701`); row Q-DA-211 at `b45344c`
scope: selftest regions of three files — `da_blackout_mask.py` (+105/−… net the RR12-1 control), `da_governed_verdict_preflight.py` (+18/−12 in the wiring control), `v5_deploy_gates.py` (+23/−12, one check deleted with a comment); production regions untouched; the shared tree, the unit, the timers and `data/` untouched (the shared-tree preflight is still round-9 `fadc986`, sha `6a15ed5d…`); nothing lands before the 00:14Z read (R-442 §3(c))
suites at the tip (coordinator, a scratch worktree of my own at `--detach 8910701`, removed after, 16:04Z, cache cleared first): preflight **39** / gates **6 → 5** / mask **30 → 32**, both launchers rc 0; `derived/` untouched
coordinator finding at the tip, **CO-10 (LOW-MEDIUM)**, reproduced 16:04Z and routed to DA round 16 on the chain — see item 4 before you execute it.

Queue position: after DE rounds 26 + 27 (`5e9dc8b`). Execute from a worktree of your own at `--detach 8910701` (the object is in the shared store; DA's `~/ctaNew-wt-da` is DA's working tree — do not run in it). The mask suite creates and removes a scratch child worktree of its own (`git worktree add --detach`, then a fixture commit in it); confirm it leaves none behind. Read-only under `data/`; `DA_MIDNIGHT_MODE` never set.

## Items

1. **DA13-R1 — the poison is the FORM (`:635` `("RULED at ",)`, asserted at `:640`).** Every ruled entry of the block carries `RULED at ` (`:399`, `:402`, `:405`, `:410`, `:415`). Confirm both falsifiers still die by name: the production call deleted → "the coherence guard is NOT called by preflight()"; a ruled entry whose citation is dropped → refused naming it. Rule on whether the form is invariant under the re-ruling the finding described (rule 13, a superseding entry): a re-ruled entry reads `RULED at R-4NN` — same form. Is there a legitimate edit that changes the form?
2. **DA14-R1 — deleted, not hooked, and the comment (`:236-252` of `v5_deploy_gates.py`) is the substance.** The line asserted `roster − 1 ≠ roster`. DA chose deletion over a production test hook and says why: the runner's `twins + excluded != len(GATES)` guard documents a structural invariant of `_launch_twins` that is itself tested two lines above; a hook would buy a red for a state the function cannot enter. Rule on the choice (we agree with it) and on the label — is "a tripwire on a FUTURE edit, not a checked behaviour" the honest description of what remains in the runner?
3. **DA14-R2 — both arrangements, and DA's first clean arrangement was wrong in a way worth reading (`:846-898`).** Not copying the files gives a clean tree that runs the CHILD's own committed code, so a mutation to the parent's producer never reached it — DA measured a hardcoded `True` surviving that version. The clean arrangement is now the files copied in AND committed in the scratch worktree (a fixture commit, detached, unreferenced, removed with the tree), then measured (`_measure("clean")`, expectation computed from the child's own `git status`); the dirty arrangement appends a marker to `pm_tape_density.py` so it no longer depends on which files the last commit touched. Reproduce the three constants: hardcoded `True` red on CLEAN, hardcoded `False` red on DIRTY, the inversion red on both. Note that `_measure("dirty")`'s producer result is discarded — the DIRTY assertion at `:930` reads `_prod` from the third run (`raw_probe`, `:895`), in the same arrangement; equivalent today, one redundant execution. Rule on whether that is worth a line.
4. **CO-10 — the identity conjunct of the RR12-1 carrying-commit control became two negatives of the SAME value, and a mutant the round-14 control caught now survives (`:902-903`).** At `801eb31` the control asserted `carrying_commit == _there and != _here` — the child's HEAD, which was `_there` because the child was detached there. At `8910701` the fixture commit (item 3) moves the child's HEAD off `_there`, and DA rewrote the check as `!= _here and != _root_git.stdout.strip()` — but `_here` IS `_root_git.stdout.strip()` (`:802`), so the check reads "not the parent's HEAD" twice and asserts identity with nothing. Driven by me on a scratch worktree at both tips, cache cleared: the producer's `_head_commit()` (`:134`) changed to `rev-parse HEAD~1` — a commit that did not execute — is **red at `801eb31`** ("records the WORKTREE's commit (e384792d31fd), NOT the main tree's") and **green at `8910701`** (32 checks). The control's own message still says "records the WORKTREE's own HEAD". Closure (routed to DA round 16, same chain, before tonight): re-read the child's HEAD AFTER the fixture commit (`git -C _wt_path rev-parse HEAD`) and assert `carrying_commit ==` that AND `!= _here`; the `HEAD~1` mutant as the known-bad. Confirm the reproduction; rule on severity (a positive control turned into a negative — the artifact-names-the-tree-that-executed property is untested at this tip, error direction: a green that can lie; production untouched).
5. **Counts, four cells each.** preflight 39 = 39; gates 6 → 5 (the deleted line); mask 30 → 32 (two clean-arrangement checks added, `:875`, `:880`); both launchers, from the worktree and under a scratch `PM_DATA_ROOT`. The gate roster 23 declared + 15 twins = 38 ALL PASS on the full run (DA's; do not repeat the full roster unless you have the 8 G scope — R-449's caveat).
6. **Discipline.** Selftest regions only (diff the three files against `801eb31` and confirm no production line moved); `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no timer/service/unit touched; the shared tree's nine DA files byte-identical to `b75c9fe`; `derived/` listing identical; no stale control worktrees left by the suite (item 3's fixture commit lives only in the scratch child).

## Disposition asked for
RELEASE or HOLD for `8910701`; findings numbered DA15-Rn (CO-10 is already routed; confirm or contest it rather than re-filing). One filing (R-377); rule 18. If DA round 16 has landed on the chain by the time you take this, say so and I will file the addendum before you execute.
