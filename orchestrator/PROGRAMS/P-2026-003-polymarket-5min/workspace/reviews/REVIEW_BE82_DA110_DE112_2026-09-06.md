# REVIEW — BE 82 drives in six cells and **distinguishes the two faults by name** (`HALF_WRITTEN_LINK` vs `DECLARATION_LINK_CORRUPTED`), including the 32-char **prefix** shape that is exactly what surfaced `phase2_four_arm`. **MARKED is right for a pre-rule family — with one clause it lacks.** DA's retirement of its own branch is the right reading of §3.2 and sharpens it. And **I could not locate DE 112's fixture receipt** under `data/`, `wt-de2` or `/tmp`.

**Filed** 2026-09-06T20:22Z (clock read before composing) · reviewer seat (pm-codex) · tip `6e206fd` (my refresh took me past `51b4939`)
· At the artifacts and in scratch; nothing written under `data/`. **BE 83 landed while I read** (`c1c41fc`, R-725) — included at §6.
· *Landing:* `land_register_row.sh` is register-only; this filing lands by file pathspec under the same post-condition.

---

# 1. BE 82 — driven

| link shape (on `supersedes`) | verdict |
|---|---|
| no `sha256` key | **`HALF_WRITTEN_LINK`**, naming both files |
| `sha256: None` | `HALF_WRITTEN_LINK` |
| 64 chars, **uppercase** hex | `HALF_WRITTEN_LINK` |
| 64 chars, not hex | `HALF_WRITTEN_LINK` |
| **a 32-char prefix** | `HALF_WRITTEN_LINK` |
| target absent, digest **good** | resolves — head v2, **1 orphan** (unfollowable, not refused) |
| target absent **and** digest missing | `HALF_WRITTEN_LINK` — **the half-written check fires ahead of the absent-target handling** |
| **well-formed but WRONG digest** | **`DECLARATION_LINK_CORRUPTED`** — its own name, with the offending version and target |

**The two faults are distinguished and neither is ever followed.** The last row is the cell the brief asked me to confirm: a wrong-but-well-formed digest still refuses **under its own name**, so "half-written" never absorbs "corrupted" — the ordering defect this programme has met four times does not recur here.

**One thing I did not exercise:** the claimed order relative to `MERGE_TARGET_ABSENT`. My cells drive the `supersedes` path; `MERGE_TARGET_ABSENT` belongs to the `also_supersedes` merge path, which I did not construct. What I can say is that on the path I drove, the half-written check precedes the absent-target handling.

---

# 2. `phase2_four_arm` — MARKED is right, and the mark is missing its trigger

**Why a mark and not a v3, when the read artifact and the receipts got reconstructions:** those are read by resolvers — DA's verifier walks the read artifact, the seal-scope selector and the read gate walk the receipts. This family is walked by **nobody** (DA measured it: `who_reads_it_through_the_chain: nobody`) and its two references are literals. **A reconstruction is warranted where a resolver will walk the chain; a mark is enough where none does.** Manufacturing pairs for a family no resolver reads adds an artifact whose only reader is the checker that would then stop complaining.

**And nothing irreversible is being deferred.** The family carries `sha256_prefix` — a *partial* digest — and the files are on disk, so a full digest can be computed and checked against the prefix at any time. The repair stays available and verifiable; only the churn is avoided.

**The clause the mark lacks: the trigger.** `who_reads_it_through_the_chain: nobody` is a fact about today, and DA's self-staling `MARKED_BUT_THE_RESOLVER_NOW_ADMITS_IT` covers the *resolver* changing, not a *reader* appearing. **Make a non-empty `who_reads_it_through_the_chain` a refusal**: the day any code resolves this family through the chain, the mark converts to a repair requirement, automatically. Then the deferral is bounded by the condition that justifies it, rather than by memory.

---

# 3. DA 110 — the right reading, and it sharpens my §3.2

DA retired its own half-link branch because the module now refuses first, so DA's duplicate was unreachable — *"a guard that cannot fire cannot prove it works"* — and kept the cell that asserts **DA's translation**: a `ChainRefused` becomes a named `VerifierRefused` carried as a per-family status, so one unfollowable family does not end the census.

**That is exactly right, and it is a better rule than the one I wrote.** My §3.2 said "keep an independent cell for the properties your own verdicts rest on", which read literally would have DA re-test the module's invariant — producing precisely the unreachable branch DA removed. The distinction DA drew is the one that matters:

> **An importer's independent cell tests the importer's OWN behaviour at the seam — never re-tests the module's invariant.** The module's invariant is covered from the importer's suite by running the module's falsifier as a cell (§3.2's second part, now landed at BE 83), not by a second implementation of the check.

DA's translation *is* DA's own property: nothing but DA's suite can tell whether a refusal from the module becomes a named per-family status or an aborted census. **Keep that cell; retire the duplicate. §3.2 as amended above.**

---

# 4. DE 112 — the receipt I could not find

**`…FIXTURE-DAY-1_FIXTURE__20260906T200348Z.json` is not present under `/home/yuqing/ctaNew/data`, `~/ctaNew-wt-de2` or `/tmp` to depth 4.** DE names it with a digest, so it exists somewhere; the gap is **locatability**. This programme's own standard is that a cited moment must be locatable (R-601), and a rehearsal receipt a reviewer cannot find is one a 09-09 reader cannot find either. **Either it lands in the ledger, or the row states it is scratch-only and names the path** — a digest without a resolvable location is a pin to nothing.

**So my check of its form is from the brief's description, not the artifact**, and on that basis: `resources.scope_memory` MEASURED from the leaf, in-process, with `this_is_the_peak_of_record: true`, and the post-exit property recorded as `NOT_READ_FROM_INSIDE_THE_UNIT` while agreeing when read from outside **while loaded** — that is REV 80 §1.1 implemented exactly, including the part that matters most: the reading that is a measurement is labelled as such and the other is kept verbatim beside it. The receipt naming the **exit-map head** (GAP 1) and carrying `orphan_branches []` beside `n_merge_links 5` / `merged_tips` / `fork_status: MERGED` (GAP 2) is REV 83 §1.2's "consume the field" — a receipt that says the fork was merged rather than leaving a reader to infer it from an empty list.

`rehearse_smoke('2026-09-06')` → `NOT_READY_P2_book_exists_P2_builder_receipt_exists` with every other precondition holding is the right pre-launch state: the two blockers are the book and its receipt, which tonight's build produces.

---

# 5. DE 113 — driven

| cell | outcome |
|---|---|
| `AMBIENT_KINDS` | `('memory', 'the lock', 'the cgroup', 'the clock', 'the journal', 'cwd', 'the worktree')` — **my REV 68 §1.2 list, enforced as an enum** |
| `record_ambient_verdict(..., ambient='not_an_ambient')` | **REFUSED**, naming the value and listing the permitted ambients |
| a hand-made dict as the verdict | **REFUSED** — the sink takes only a real `known_bad_verdict`, not a shape that looks like one |

**What I could not exercise:** "the sink takes only DISARMED" with a *well-formed* FIRED verdict — my synthetic verdicts were rejected on shape first, which is the stricter guard doing its job and which left the DISARMED-only rule untested by me. DE's `--falsify-disarmed` planting a disarmed cell in the **real** battery and producing `NOT CLEAN — 342 checks, n_disarmed 1`, rc 1, in all three suites is the cell that matters, and it is DE's drive rather than mine.

**`n_disarmed` in the summary with a non-zero count raising and naming the cells is REV 84 §2's addition, complete** — the count is not merely reported, it is fatal, which is what stops "N checks passed" from absorbing it.

---

# 6. DA 109 and BE 83

The removed row **restored byte-identical against the coordinator's hash and superseded in the file** — REV 84 §5's form, and the register at HEAD is self-describing again.

**BE 83 landed while I read** (`c1c41fc`): the shared falsifier as a **subprocess** cell in BE's own batteries — §3.2's second part, and the subprocess form is the right one (a module's falsifier invoked in-process could pass on a module already imported and patched by the suite around it; a subprocess runs the module as a reader would).

---

# 7. Not established

- §1's cells are a scratch family in my scratchpad; the `also_supersedes` / `MERGE_TARGET_ABSENT` path was **not** exercised.
- §2 is a ruling; I did not read `phase2_four_arm`'s files or verify the two literal references.
- §3 is a ruling on form; I did not run DA's census this round (its numbers 25 / 324 / 69 / 0 are DA's).
- §4's form check is **from the brief**, because the receipt is not locatable to me; the search covered `data/`, `wt-de2` and `/tmp` at depth 4 only.
- §5's third row was refused on shape, so the DISARMED-only rule is untested by me.
- §6 is read from the register's log; I did not diff the restored row against the coordinator's hash myself.

**Routing:** DA — §2's trigger (a non-empty `who_reads_it_through_the_chain` refuses). DE — §4 (land the rehearsal receipt or state it scratch-only with its path). Coordinator — §3's amended wording for §3.2 into the pattern file, and §4's locatability point.

**Context ≈ 63 %.**
