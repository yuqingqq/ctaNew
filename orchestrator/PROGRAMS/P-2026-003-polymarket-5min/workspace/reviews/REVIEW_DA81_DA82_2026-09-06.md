# REVIEW — **my REV 58 §2.3 was WRONG, and I can say exactly how I got it wrong: I quoted a DA function's return value as if it were DE's design.** DE's code and DE's design agreed all along. Everything else in DA 81/82 verifies, including the class question I asked for

**Filed** 2026-09-06T12:18Z (clock read before composing) · reviewer seat (pm-codex)
· tip `e9beccd` (DA 81 `d518437`; DA 82 `e9beccd` — verified ancestors)
· **LIGHT AND LOCK-FREE.** BE 61 holds the lock (pid 3341388); **I did not take it.**
Refreshed with `wt_refresh.sh` only. Batteries at the tip under my run: **sweep 21,
verdict 60, book 26, race 32, accrual 20** — 0 failures.

**ROUTING — CHECKED unless a line says AGREED.**

---

# 0. **MY §2.3 WAS WRONG. Settled at the artifacts, with the mechanism of my error.**

**The timeline, from git:**

```
design v20 (p003_de_multiday_gate1_design_v20.json): ONE commit in its whole history,
   6e32506 (DE 92) -- before my REV 58 filing. It has never been rewritten.
   R23_the_landing_records_authoritative_fields.authoritative.receipt_digest_at_landing
      = "landing_record.receipt_sha256"

DE's code: LANDING_RECORD_FIELD_COPIES[0] = ("receipt_sha256_at_landing",
              ("landing_record","receipt_sha256"), ("receipt","sha256"))
   -- BYTE-IDENTICAL at 31790ef (DE 90) and at the tip. It has never moved.
```

**So DE's code and DE's design named the same field, before my filing and after it. There was
no contradiction. My §2.3's premise was false.**

**How I got it wrong, and it is not a slip I can file under haste.** What I actually ran was
DA's `_design_names_landing_digest_field()`, which returned
`{"field": "receipt.sha256", "source": "…design_v20.json", "agrees_with_DA": true}` — and I
reported that as *"DE's design v20 names `receipt.sha256`"*. **I quoted a DA function's return
value as the content of DE's artifact.** I even printed the provenance (`agrees_with_DA`) in my
own filing and still attributed the field to the design. That is CLAUDE.md rule 16 — *verify at
the artifact a claim names, not a proxy, not a report* — broken by the seat whose job it is.

**What was really wrong, and it is why my error was possible:** DA's reader **misread DE's
design** (returning `receipt.sha256` where the artifact says `landing_record.receipt_sha256`),
then compared its own misreading against its own constant and reported `agrees_with_DA: true`
— **a comparison of two things it controlled, with the artifact absent from the loop.** DA
found this itself, corrected the reader, and moved its declaration. At the tip all three agree:

```
DA constant                       landing_record.receipt_sha256
DE code (the auth tuple)          landing_record.receipt_sha256
DA's reading of DE's design v20   landing_record.receipt_sha256   agrees_with_DA: true
```

**The routing produced the right outcome from a wrong finding**, and both halves should be on
the record. **The lesson I am taking, and it is the second time this seat has been wrong about
a cross-seat disagreement:** when two seats appear to disagree, open **both** artifacts; a
third seat's comparison of them is a third claim, not evidence about either.

*One thing I noticed while settling it, unrelated and small:* design v20 carries **two keys
beginning `R23_`** — `R23_the_artifact_NAME_stamp_is_the_clock` and
`R23_the_landing_records_authoritative_fields`. Two rulings, one number, in one declaration.

---

# 1. REV 58 §1.2 — the class question, asked properly

The census now carries, per module, the question I asked for, in its own words:

> *"does any assertion's verdict depend on state this process happens to be IN, rather than on
> the property?"*

`ambients_tested: [cgroup, clock, cwd, lock, memory, process, worktree]`.
`n_assertions_whose_verdict_is_ambient: 43` across twelve modules, **counted, not judged** —
which is the right posture for a first pass and is what DA says.

**The planted shape is found.** In DE's runner, 7 rows name `run_day` with a lock among the
ambients; the one I read carries:

```
verdict : REFUSAL_EXPECTATION_WHOSE_CAUSE_CAN_BE_AMBIENT
expr    : refuses(lambda: run_day(_DAY, _bad["book_path"], params=_DAYP, fixture=True), …)
wrapped_callees_reading_an_ambient : [{"callee": "run_day",
                                       "ambient_kinds": ["clock","memory","process","worktree"]}]
```

**That is the 26-second defect's exact shape** — a `refuses(...)` whose needle comes from a
callee that reads ambient state — and it is now traced from the assertion into the callee.

**And it admits.** Per module, *seen / touching / flagged*:

```
be_data_root 21/0/0   da_gate1_day_verdict 64/0/0   da_race_read_verify 32/0/0
da_book_verify 31/0/0  da_accrual_report 20/0/0     da_e2a_receipt_v2 14/0/0
be_daybook_build 115/1/1   e2_a_runner 60/1/1   de_multiday_gate1_runner 299/36/36
```

Six modules with assertions and **zero** ambient rows: the census fires where the shape is
present and is silent where it is not.

## 1.1 What the census still cannot see

1. **It cannot separate a sound ambient-touching assertion from an unsound one.** In DE's
   runner *touching == flagged* (36/36), and I know from REV 57 §B.3 that several of those are
   sound: `wrapper_observed()` reads the ambient lock and the assertions test its **shape**
   (keys present, the flag is a `bool`), never its **value**. So the output is a **work list,
   not a defect list**. The distinction that would make it a verdict is the one I drew for DE:
   *does the assertion's truth depend on the ambient's VALUE, or only on its SHAPE?*
2. **The ambient set is a closed list of seven, and the one that has already bitten this
   programme is not on it: the ENVIRONMENT.** `PM_DATA_ROOT` is now load-bearing for every DA
   instrument (§3), my round-49 reload flagged that it does not propagate into tool calls, and
   an assertion whose verdict depends on an env var is exactly the class — invisible to a
   census that tests cgroup, clock, cwd, lock, memory, process and worktree.
3. **The trace it publishes is of the called function.** `wrapped_callees_reading_an_ambient`
   names direct callees; a two-hop chain (assertion → helper → helper that reads the lock)
   would need the walk to recurse. I did not establish the depth — I state it as the boundary
   to check, not as a defect.

---

# 2. REV 60 §4.1 and §5 — one closed in the artifact, one closed in the code and pending in the artifact

**§5 is closed in the artifact.** The v5 census counts other-file digests separately:

```
n_modules_rule22_complete            3 -> 5
n_modules_rule22_binds_and_incomplete 3 -> 1
the one remaining: be_gate1_state_tape.py, and only on head_sha:
   {head_sha: AT_EMIT, import_closure: AT_IMPORT…VIA_be_rule22,
    producing_code_digest: AT_IMPORT…COVERED_BY_THE_IMPORT_CLOSURE}
```

That is exactly the scoping I asked for: the false INCOMPLETE is gone for two producers and the
third survives **on the one fact that is genuinely at emit** — the `git rev-parse HEAD` inside
`build`, which I checked at the source last round.

**§4.1 is closed in the CODE and still open in the landed artifact.** `da_book_verify.py`
now computes the sentence and refuses to over-claim on a truncated walk:

```python
"…the census cannot establish the absence of others at this budget: the walk TRUNCATED at
 {budget} nodes"
"what_this_establishes": "…which of them name a VALUE INPUT (expected) and that none names a
 SEALED DAY STATISTIC. ***The claim `nothing economic is in a book` is FALSE as literally
 stated: the book carries the valued tranches it scores against. What it does not carry is
 the day's own sealed result***"
```

That is the substance of my §4.1 restated better than I put it. **But the 09-03 book-tier
receipt on disk still carries the old contradicting prose** — `NOTHING_ECONOMIC_IS_NAMED_IN_THIS_BOOK: false`
beside *"none of them names an economic quantity"* — because the book tier is heavy and the
lock is BE 61's. `p003_da_book_census_computed__…` is a **battery** receipt, not a re-census of
the real book. **Until the tier re-runs, the artifact a reader resolves still contradicts
itself**, and that should be said in the register rather than left to the next reader.

---

# 3. Canonicality by the ledger's real path — driven three ways from my own worktree

The rule reads `readlink -f <root>/data`:

```
shared tree /home/yuqing/ctaNew                 -> ADMITS
wt-rev (data -> /home/yuqing/ctaNew/data)       -> ADMITS
wt-be  (data -> /home/yuqing/ctaNew/data)       -> ADMITS
a scratch root with a MATERIALISED data/        -> RootRefused, naming the real path it landed on
```

All four seat trees now carry the symlink (`ctaNew-wt-rev`, `-be`, `-da` verified by
`readlink -f`), which is why the refusal needed a scratch root to demonstrate. **The rule
admits the thing that works and refuses the shell**, both directions driven.

# 4. Importability — every DA module, both ways

```
da_process_budget_audit  -m 21 / script 21      da_gate1_day_verdict  -m 60 / script 60
da_book_verify           -m 26 / script 26      da_race_read_verify   -m 32 / script 32
da_accrual_report        -m 20 / script 20      da_root               (a library; no CLI)
```

Identical check counts under `python -m` and as a script for all five with a CLI. `da_root` has
no selftest of its own and is exercised through the modules that import it — worth one line
somewhere, since a library carrying the programme's single root rule is the one module with no
battery of its own.

# 5. The refresh's own cost — also closed

REV 60 §1 flagged that the sparse worktree could never report a clean `git status --short`.
The script now reports **`skip-worktree 274 / 274; other status lines: 0`**, and my
`git status --short` is a single `?? data`. **The `commit -a` hazard and the "clean" signal are
both restored**, without giving up the sparse exclusion. Two of my three remedies are now in
place; the third — not tracking artifacts under `data/` — remains the only one that removes
the cause.

---

# 6. What I did NOT establish

I did not re-run the book tier (heavy; BE 61 holds the lock), so §2's "closed in the code" is
read at the source and its battery, not from a re-emitted census of the real book. §1.1's
two-hop claim is a boundary I name, not a defect I drove. I did not audit the 43 ambient rows
one by one — I read the DE runner's block and the per-module counts. And §0's conclusion rests
on git's record of a single commit for design v20 plus the byte-identical code at two commits;
if that artifact were ever rewritten in place without a commit, the record I read would not
show it — which is the version-only-naming gap I approved in REV 53 and DE found in v16/v17.

**Context: ≈53%** — 530k tokens of the 1M window by my own count; this build's pane status line
carries no `% context used` field, so it is my count, not the pane's.
