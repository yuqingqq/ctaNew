# Review — DA rounds 23 and 24 (held chain, read by sha)

reviewer: pm-codex · filed 2026-09-03T08:14Z
chain: `04dccc7` → `a111468` → `9b67cb3` → `7b484ae` → `5d20b01` → **`8c99511`**, read from the shared object store and executed in a detach of my own worktree. **`~/ctaNew-wt-da` never read.** No seal opened. `be_forward_day.py` read, never run. The 00:06Z unit never run; `DA_MIDNIGHT_MODE` never set. Nothing written under `data/`.

# ITEM 1: **BINDING.**

**The withdrawal cannot be quietly undone.** I attacked every route the dispatch named, on a real git history I built myself, and each refuses **by name**:

```
at the held tip: status READ, monotone True, vacuous False,
                 2 commits touching the file, 2 prior versions carrying the registry

A) entry REMOVED              REFUSED  WithdrawalRefused — "a recorded race withdrawal has been
                                        REMOVED or RE-CITED, which the ruling that made it forecloses"
B) entry RE-CITED             REFUSED  WithdrawalRefused (both cites printed)
D) day SUBSTITUTED (08-30     REFUSED  WithdrawalRefused — the removal of 08-29 is what fires
   carrying 08-29's entry)
C) non-authority fields edited ACCEPTED (monotone True) — see DA24-R4
E) run from a non-git tree    status NO_REPOSITORY, monotone **None**, vacuous True — see DA24-R3
```

The guard is not vacuous: **two** prior committed versions already carry the registry, so the comparison has something to compare and `vacuous` is False. Adding a day is allowed — the property is one-way, not frozen — which is right.

**And the front door holds, driven in both directions on the real shape**, including the `5d20b01` correction:

```
a REFUSED receipt carrying "sealed": true, no sealed_file, no rows
   -> n_reads 0, n_refusals 1, a_read_exists False    admissible_to_withdraw TRUE
a SCORED receipt with a sealed-file path and a positive count
   -> n_reads 1, a_read_exists True                   REFUSED BY NAME
09-01, on the real artifact (a SCORED receipt)         REFUSED BY NAME
```

That is the property that makes the ruling *prospective* rather than retrospective: a day may be withdrawn only while no forward read of it exists, and 08-29 satisfied that when it was recorded. BE's `"sealed": true` is a protocol flag and is no longer read as evidence of a score — the definition is now a path or a positive count, not a boolean.

**Item 1's second half — the verdict has stopped asserting the false thing.** Computed read-only (`verify_day`, no `--out`, nothing written):

| field | 08-29 now |
|---|---|
| `era_admission.race_admissible_by_era` | **True**, cite `USER RULING 2026-09-03, R-497 (F)(1)` |
| `verdict_split.era_admissible` | **True** |
| `verdict_split.race_accrual_eligible` | **True** |
| `verdict_split.withdrawn_from_race` | **True**, cite `USER RULING 2026-09-03, R-500 (B)` |
| `verdict_split.counts_toward_race` | **False**, with a `_why` naming which of the two it is |

The two facts are separable and separately stated, and the policy is encoded in its own field rather than by falsifying the computation. That is the right shape: making `era_admissible` False to keep the day out would have been the `ERA_ADMISSIBLE` defect in a new coat.

**Two residuals, neither of which lets the day back in, and one of which matters more than DA's framing suggests.** They are DA24-R1 and DA24-R3 below. DA also names the load-bearing open item itself: **the canonical artifact is not rewritten**, so `era_admissible: false` still stands on disk and the corrected answer exists only in code.

---

### DA24-R1 — MEDIUM-HIGH — `counts_toward_race` has no consumer; the field the race is counted by still reads True

The design is correct: `race_accrual_eligible` stays the pure four-conjunct computation and the policy lives in `counts_toward_race`. What is missing is that nothing else has been moved onto the derived field.

```
grep counts_toward_race  -> da_forward_day_verify.py only (definition + suite)
da_verdict_check              knows counts_toward_race: False
da_governed_verdict_preflight knows counts_toward_race: False
da_verdict_check CONJUNCTS = ('day_closed','post_freeze_pass','era_admissible','day_quality_pass')
                              — it RECOMPUTES and validates race_accrual_eligible
```

So 08-29's artifact will carry `race_accrual_eligible: True` and `counts_toward_race: False`, and the **independent verifier validates the one that says the day counts.** A reader — or an instrument — counting G by `race_accrual_eligible`, which is the field's name, the field every prior document counted by, and the field the checker checks, gets **3 of 5**, not 2.

This is the failure mode `assert_withdrawal_carried` already guards against *between the block and `verdict_split`* — *"One artifact, two answers, is how a reader ends up quoting the wrong one"* — applied one field short.

> **AMEND clause.** Teach `da_verdict_check` and `da_governed_verdict_preflight` `counts_toward_race`: recompute it from the four conjuncts AND the registry, and assert the artifact's value equals the recomputation, exactly as they already do for `race_accrual_eligible`. State in `ACCRUAL_RULE` that G is counted by `counts_toward_race`. Until then the withdrawal is binding against edits and not yet binding against counting.

### DA24-R2 — MEDIUM — `flow_intensity` is RED at the held tip, and red precisely in the canonical tree

Both launchers, rc 1:

```
AssertionError: RR12-1 THE OTHER HALF: the register default follows CODE_ROOT
(/home/yuqing/ctaNew-wt-rev), not the data root...
   flow_intensity.py:1434
```

The check is

```python
ok(str(REGISTER_DEFAULT).startswith(str(CODE_ROOT))
   and not str(REGISTER_DEFAULT).startswith(str(DATA_ROOT) + "/orch"), …)
```

and it fails **whenever `CODE_ROOT == DATA_ROOT`**, because then the two prefixes are the same string. Measured here: `CODE_ROOT` = `DATA_ROOT` = `/home/yuqing/ctaNew-wt-rev`, branch **`2_code_tree_carries_the_tape`**. That branch fires whenever `<code tree>/data/pm_5min/raw` is a directory (`pm_tape_density._resolve_data_root`) — which is true of **the canonical tree**, the tree the nightly unit runs in.

The check immediately below it says so in as many words: *"the two roots are ALLOWED to be equal — they are, in the canonical tree"*. The two contradict each other, and Q-DA-219 reports the module green at 54 — green only in a tree where the roots happen to differ.

> **AMEND clause.** `and (CODE_ROOT == DATA_ROOT or not str(REGISTER_DEFAULT).startswith(str(DATA_ROOT) + "/orch"))`, and re-run the module from a tree that carries the tape so the control is exercised where the unit runs.

### DA24-R3 — MEDIUM — the canonical-write refusal on "cannot evidence" is off the path the unit takes

`da_forward_day_verify.py:5876-5895` refuses a canonical write when `monotone is not True` — correct, and it is guarded by `_canon`, which requires `--out` to resolve **into** `CANONICAL_VERDICT_DIR`. The unit does not write that way: `da_midnight_verify.sh:252` runs `verify … --out "$tmp"` and installs the temp file afterwards. So `_canon` is False on every production write and that branch never fires there.

The substantive undo is still caught on the unit path, because `assert_withdrawal_carried` calls `assert_withdrawals_monotone()`, which **raises** on REMOVED/RE_CITED. What is not caught is `monotone is None`: `assert_withdrawal_carried` retrieves the status and never inspects it. Driven:

```
assert_withdrawal_carried(rep, "20260829") with monotone=None  ->  ACCEPTED
   {'carried': True, 'withdrawn': True} | mono {'status': 'NO_REPOSITORY', 'monotone': None}
```

The module comment claims *"a canonical write refuses on it, and does"*. True for a direct canonical `--out`; not true for how the unit actually writes.

> **AMEND clause.** Have `assert_withdrawal_carried` refuse on `monotone is not True` (it already holds the value), so the guarantee travels with the emission rather than with the output path's spelling.

### DA24-R4 — LOW — the monotonicity comparison covers presence and `authority` only

`reason`, `note` and `era_admissible_at_withdrawal` may be rewritten with no violation (driven: `reason="re-admitted after review"` → `monotone: True`). It cannot re-admit the day, so it is not a defeat — but `reason` is the field a reader quotes, and a withdrawal whose stated reason can be rewritten silently is weaker than one whose cite cannot.

> **AMEND clause.** Compare the whole entry, or name in the module which fields are load-bearing and which are prose.

---

## ITEM 2 — DA22-R1 upheld, and the second defect closed with it

The verdict now says what it computes, and the route is reported rather than foreclosed:

| day | verdict | `attribution_reachable` | `prior_attributed_versions` |
|---|---|---|---|
| 08-29 | `NOT_REACHABLE_FROM_THE_ARTIFACT_NOW_ON_DISK` | **True** | 3 versions, **1** gate-1-acceptable — `4e1133c` / blob `79767c…`, attributed, closed, `race_accrual_eligible` true |
| 08-30 | `NOT_REACHABLE_FROM_THE_ARTIFACT_NOW_ON_DISK` | **False** | 1 version, 0 acceptable |
| 09-01 | `ATTRIBUTED_ALREADY` | True | 1 version, 1 acceptable |

**The field discriminates** (08-30 False vs 08-29 True), which is the second defect DA found in its own round and closed: `attribution_reachable` used to be `verdict != "UNREACHABLE_BY_ANY_HONEST_ROUTE"`, so renaming the string would have made every day reachable. It is now computed from the facts.

**No other member of the same category error in that module:** I scanned every upper-case verdict-like constant in `da_forward_day_verify.py` for the generalising vocabulary (`ANY`, `NEVER`, `ALL_`, `IMPOSSIBLE`, `CANNOT`, `UNREACHABLE`, `EVERY`) and none remains. What the function still declines to call a route — deleting or truncating the artifact, forging `DA_WRITE_REASON` — is unchanged and named, and whether restoring a cache from its declared provenance is permitted stays the USER's.

## ITEM 3 — round 23

**RR12-1 is fixed where the resolution happens.** The resolution is a single function, `pm_tape_density._resolve_data_root()`, with three named branches (`1_env_PM_DATA_ROOT` > `2_code_tree_carries_the_tape` > `3_canonical`); `flow_intensity` and the rest consume `_TDROOT.DATA_ROOT` and derive `CODE_ROOT` separately from `__file__`. **The falsifier drives both directions in child processes**: imported from a code tree with no `data/`, the fixed module resolves `DATA_ROOT` elsewhere and finds the tape; the same tree with `REPO = Path(__file__).resolve().parents[2]` restored resolves the tape to the code tree and finds nothing; and a regression control shows a code tree that *does* carry the tape still resolves to itself, which is what the nightly unit does.

**The unclosed class is correctly scoped, and I checked the scope independently rather than accepting it.** DA's claim is "accrual path clean, class NOT closed at 33 declarations across 26 files". Censused per module:

```
da_forward_day_verify   CODE_ROOT=__file__   REPO=_TDROOT.DATA_ROOT
da_verdict_check                             DERIVED=_TDROOT.DATA_ROOT/…
da_governed_verdict_preflight  CODE_ROOT=__file__  DATA_ROOT=_TDROOT.DATA_ROOT
da_race_withdrawals            CODE_ROOT=__file__  DATA_ROOT=_TDROOT.DATA_ROOT
da_blackout_mask               CODE_ROOT=__file__  DATA_ROOT=TD.DATA_ROOT
pm_tape_density                CODE_ROOT=__file__  DATA_ROOT=_resolve_data_root()
flow_intensity                 CODE_ROOT=__file__  DATA_ROOT=_TDROOT.DATA_ROOT
da_dark_interval_scan          CODE_ROOT=__file__  reads TD.RAW / TD.DATA_ROOT
pm_host_load_join              REPO = Path(__file__).resolve().parents[2]   <-- the one member
```

The only `__file__`-derived data root on or near the accrual path is `pm_host_load_join:57`, which is exactly the member DA already names (my DA20-R4, now correctly re-framed as one of 33 rather than a lone nit). **No second instance hidden.**

**The `clob_v4` packaging is a fair statement of both answers, not a steer.** It leads with the decisive computed fact — the era ran 2026-08-30T05:30:02Z → 2026-08-31T22:00:02Z, 40.5 hours, **no complete UTC day lies inside it**, so **0 day verdicts change under any answer** (computed over n = 16 days through `day_era_admission`, with an as-of). All three answers are given the same four headings (runner behaviour / table state / what it asserts / cost). **(c) NO RULING is presented as a legitimate choice**, not a strawman, and (b)'s prospective precedent risk is stated against (b). The recommendation narrows to (a)-or-(b) with its criterion stated — both end the state where a cite exists and names something else — and **explicitly declines to prefer between them**, leaving the merits to whoever weighs R496-R7. I re-drove its load-bearing claim: 08-30 with **both** eras admitted still refuses on `boundaries_inside_day`. Fair.

## ITEM 4 — the dark-interval checker

Neither round touches it: `da_dark_interval_scan` does not appear in the `131b1d7 → 8c99511` diff. Not re-opened; the 105-minute cross-midnight event stands as filed.

---

## Findings

| # | sev | finding |
|---|---|---|
| **DA24-R1** | MED-HIGH | `counts_toward_race` has no consumer; `da_verdict_check` and `da_governed_verdict_preflight` still validate `race_accrual_eligible`, which reads **True** for the withdrawn day. Binding against edits, not yet binding against counting |
| **DA24-R2** | MEDIUM | `flow_intensity` selftest RED at the held tip, both launchers — the check fails whenever `CODE_ROOT == DATA_ROOT`, i.e. in the canonical tree; the row reports it green at 54 |
| **DA24-R3** | MEDIUM | the canonical-write "cannot evidence" refusal is keyed on `--out` into the canonical dir; the unit writes to a temp path, so it never fires there, and `assert_withdrawal_carried` ignores the `monotone: None` it is handed |
| **DA24-R4** | LOW | monotonicity compares presence and `authority` only; `reason` and `note` are silently rewritable |
| — | — | **Item 1 BINDING**; item 2 upheld and closed with its second defect; item 3's fix at the resolver, class correctly scoped, packaging fair; item 4 untouched |

## Disposition

**AMEND.** The one-way property is real and I could not defeat it — the USER's ruling **is** in force against every undo route I drove. But DA24-R1 and DA24-R2 should land before this chain does: the first because a withdrawal that does not subtract the day from the field the race is counted by is only half in force, and the second because a suite that is red in the canonical tree will be red at landing.

**Suites at the held tip, both launchers:** `da_race_withdrawals` **25**, `da_forward_day_verify` **301** (0 skipped), `pm_tape_density` **16**, `da_verdict_check` **22** — rc 0; `flow_intensity` **rc 1** (DA24-R2).

I estimate; the coordinator routes; the USER decides (rule 14).

## Discipline record

Chain read by sha from the shared object store and executed in a detach of `~/ctaNew-wt-rev`; **`~/ctaNew-wt-da` never read**, no git command in any seat's worktree but my own. Every step under `systemd-run --user --scope --slice=research.slice -p MemoryMax=8G`. No seal opened. `be_forward_day.py` and `da_midnight_verify.sh` READ, never run; the unit never run; `DA_MIDNIGHT_MODE` never set. **Nothing written under `data/`** — `verify_day` was called without `--out`, the one-way attacks built throwaway `git init` repositories in temp directories, and the front-door attacks wrote only into a temp dir. My worktree does not carry the untracked 08-29 forward receipt, so the front door's ADMIT direction was driven on a constructed receipt of BE's real shape and its REFUSE direction on the real 09-01 artifact — stated rather than glossed. `git worktree list` **34** at quiescence, worktree clean.
