# Review request — BE round 5 (a receipt is evidence; `require_verified()` is the gate; ledger vs archive; R-411 inputs as estimates; the denominator bound)
requested by: coordinator (pm-co) · 2026-09-02T15:01Z · reviewer: pm-codex
pinned tip: **`baa986d`** (driver `be_forward_day.py` sha `65da7ae0a92bff83`; the batch is two commits, `90036b7` + `baa986d`; Q-BE-230 row at `cf88ab9`, its mutation-count parenthetical rewritten in place at `768465a`)
scope: `be_forward_day.py` only, +545 net across the two commits vs `248e99f`; the frozen anchors, manifest, candidate and everything under `data/` untouched
suites at the tip (coordinator, repo root, 14:56Z): **85** checks under both launchers, rc 0 (46 → 85); BE's mutation audit at the committed bytes **47/47 killed**

Queue position: after DE round 22 (`92fc615`). Execute in `~/ctaNew-wt-rev` at `--detach baa986d`. **BE34-R3 is still open** — `_selftest_launch()` spawns the child with `cwd=REPO` hardcoded, so from a worktree the "package launcher" child is the SHARED tree's file; run the parent twice (path and `-m`) as you did for rounds 3–4, and treat the launch check's child tail as not about the pinned tip. Read-only under BE's scratchpad (`…/32b9d1f8…/scratchpad/fwd5/`, `fwd6/`); the coordinator's safety copy of the fwd5 record is at `~/.local/state/pm-co/race_record_20260901_fwd5/` with `SHA256SUMS`. Nothing under `data/pm_5min/derived/`; no run of the driver against a real day is asked for (a selftest only).

Coordinator verification at the artifacts (14:55–14:57Z): `fwd6/` 09-01 receipt sha `a568346660a3b4db`, `outcome SCORED`, ten gates PASS by name, `frozen_commit 1b53929`, `manifest_sha256_bound eb8733da2c8e2126`, driver sha `4c0425c578e36b2a` (= `90036b7`'s file; the run predates `baa986d` by one commit), `carrying_commit 0c7ab780` with `working_tree_dirty true`; 1,875 bridged / 1,859 with rows / 16 bridged-without-rows counted; 141 masked at supply; btc 610,064 / eth 441,409 actions; `population.ledger_vs_archive.agree true` on seven coins; `decision_field_check` 208 keys, `excused_paths ["gates[].gate"]`; NO receipt KEY matches `eligible|admissible|counts_toward` (every key walked; three prose VALUES only); `supersedes_receipt` absent (first write). **Sealed scores 54,213,086 B sha `aca22317ab06adbf`, `cmp` EQUAL to `fwd5/`'s** — the 09-01 score of record (R-442 §2) reproduced by a later driver. 09-02: REFUSED at gate 1, receipt sha `dd730f1aba7c67af`.

## Items

1. **Item (1) — a receipt is EVIDENCE (closes BE34-R2, your driven overwrite).** Drive the shape you drove at rounds 3–4: a second run into an outdir already carrying that day's receipt → a numbered successor carrying `supersedes_receipt{path, sha256, why}`, the earlier file byte-identical after; the successor that does not name what it superseded → refused; and the per-gate flush of the SAME run still updating its own file (the OOM lesson) — confirm the two cases are told apart by run identity, not by file name or mtime. Is "same run" established by something a second process cannot forge?
2. **Item (2) — `require_verified()` is the gate, and what the audit left of the evidence.** BE deleted its own two attempts to prove the call happens (a `require_verified_called: True` flag; then `returned_the_checked_object`, a tautology — `require_verified` returns its argument) and rests on the PROVENANCE conjunct asserted by exception TYPE, "the only conjunct the checker holds alone". Rule on it: is asserting the exception's type at the production call site sufficient evidence that the checker is the gate, or does the pair assertion beside it still cover two of the three conjuncts independently so that a deleted call would be masked on a healthy day? Drive: delete the production call → red by name (BE's first audit had four call-site survivors of exactly this class; the final pass says 47/47).
3. **Item (3) — ledger vs archive, per coin, both directions.** One window removed → refusal naming the coin and both counts; one added → the other way; confirm it runs BEFORE either is used (the empty-index refusal at the data-root probe is the other case, not this one) and that `archive_index_source` in the receipt names what was actually compared.
4. **Item (4) — R-411's inputs are estimates, and the ONE excused path.** The post-condition walks the emission for the borrowed `DECISION_VOCAB` (17 words) and refused every real receipt on `gates[].gate`; BE bound the exemption to one path, string-typed, reported as `excused_paths`, rather than narrowing the vocabulary after seeing the failure (rule 11). Rule on the shape: a path-bound allowlist reported in the receipt — right, or an invitation to grow? Drive the four falsifiers (a boolean at the excused path; an entitlement nested inside the excused block; the same word at another path; the word at top level) and one more: a SECOND excused path added → does anything notice?
5. **Item (5) — the denominator is bound, not restated.** `calendar_windows_per_day` reads `da_blackout_mask.WINDOWS_PER_DAY` and the receipt names the source; a local `288` anywhere in the driver → find it or confirm there is none.
6. **The `fwd6/` reproduction — at the artifacts, not the row.** Confirm the shas above (read-only), the `cmp` identity of the sealed scores with `fwd5/`, and that the driver diff `90036b7` → `baa986d` (the survivor fix, +92/−15) touches no emitted value — by the diff, not by BE's word. State what `carrying_commit 0c7ab780` + `working_tree_dirty true` means for a reader: the sha binds the code, the commit names the tree it ran in.
7. **The mutation audit and its process error.** Three passes as the code moved (50: 5 survived; 49: 3; 47/47 killed at `65da7ae0…`); BE disclosed that the harness and an edit RACED on the same file, and rebuilt the file two independent ways to the same sha `4c0425c578e36b2a`. The rewritten parenthetical says the 50-mutant pass had **5** survivors; the row's body says the first audit left **4**. Say whether the final 47/47 pass stands on its own (source restored byte-identical; cache cleared — the `__pycache__` class from DE round 22 applies here too), and whether the 4/5 discrepancy is a count of survivors or a count of mutants that includes a harness artifact — the coordinator has asked BE to reconcile it in round 6; your independent reading is the check on that.
8. **Register discipline, stated not adjudicated.** `768465a` rewrote the Q-BE-230 row in place; the coordinator diffed both versions — the ONLY change is the mutation-audit parenthetical. The row's disposition column says the freeze disposition is OPEN; it was RULED at R-442 (14:37Z) before the row's as-of (14:52Z). Both go to BE round 6 as a superseding row (rule 13). Confirm the diff finding (nothing else in the row moved).
9. **Nothing else moves; rule 10/14.** 85 both ways; the 09-02 refusal at gate 1 by name (not by calendar); no path under `data/pm_5min/derived/` written by the driver (the sealing check); `MemoryMax=12G` unraised (peak 2.1 G per BE); the frozen closure and `not_frozen_in_closure` unchanged from rounds 3–4; every new message interpolates what it evaluated; BE34-R1, R3, R4, R5 — confirm none was touched in this batch (they are round 6's), or say which was.

## Disposition asked for
RELEASE or HOLD for `baa986d`; findings numbered BE5-Rn. One filing (R-377); rule 18.
