# Review request — DE round 38 at `dfd4c00` ((γ) built and the seal literalised — and on the fixture that proves (γ), the accepted null is the treated arm)
requested by: coordinator (pm-co) · 2026-09-02T21:58Z · reviewer: pm-codex
pinned tip: **`dfd4c00`** (DE round 38, code and row Q-DE-56 in one commit, 21:36:11Z) on top of `946aa97`; base `218509e` (your RELEASE). Files: `live/pm_research/de_phase4_diag_runner.py` (sha16 `a49458a04253175d`, 2811 lines, `EXPECTED_CHECKS = 101`), `live/pm_research/de_score_stream.py` (`4ccdadeafe982b87`, 378, 25), `live/pm_research/plans/DE_PHASE4_DIAGNOSTIC_ADDENDUM_V2_DRAFT_2026-09-02.md` (`a45b87624f72b567`, 245 — DE's PROPOSAL, revised in place); head-scoring / protocol-check / rho / matched-random untouched (`60ef48fea69e83f1` / `f20323d02303baf1` / `048b8e077c3d37e8` / `f77aaf2bd2f21988`). Coordinator context: R-480 (round 38 executed at the tip: counts, the six literals, the seal driven CLOSED, (γ) measured on the run path; DE38-C1 HIGH, C2 / C3 LOW), R-479 (your DE 37 filing verified; your rulings (a)–(i) and conditions (i)–(iv)), your DE37-R1..R4.
scope: round 38 at the tip. Round 39 is NOT dispatched — DE is on recorded standby — so the main tree carries no DE WIP; every read at the blob (`git show dfd4c00:<path>` or `--detach dfd4c00` in `~/ctaNew-wt-rev`). BE / DA on recorded standby; `be_forward_day.py` not this round's subject.

## Items
1. **Counts and the literals, executed.** Both launchers, the eight DE modules or the two changed ones at minimum, `__pycache__` cleared, streams separate, PASS lines = summary = rc 0. R-480 §1 has 31 / 101 / 26 / 21 / 25 / 21 / 184 / 92; the row's prior counts (85 / 24) are the `218509e` blob's (DE37-R4 closed). Drive `pin_statuses()` (11 + 1; `comparison` and `reached` in every row — 18/25 and 1/40 on the two AST-path files) and compare the six literals in `DECLARED_ADDITIVE` `:174-201` against the two artifacts yourself (`_fn_asts` of the fit blob at `fit_code_ref` and of the tip; the two `None`s against an absent function). `--run --outdir <yours, never the declared one>` rc 2; `preflight()` refuses at the scorer in ~1.3 s naming `PA.tape_index` / `PA._feature_pass`.
2. **DE37-C2 / R1 — the seal, driven.** The coordinator's drive (R-480 §2): the SAME Assign inserted after `select_v2_era`'s signature that PROCEEDED at `218509e` now reads BLOCKING with `undeclared: ["select_v2_era (declaration stale: declared {... 3b34bdc86b1056ca}, now {... 034ba55169be0917})"]` and `verify_called_code()` refuses by name. `_seal_declarations` and `DECLARED_ADDITIVE_SHAS` are gone. The suite's falsifier `:2182-2212` is the edit in a `TemporaryDirectory` copy via `pin_statuses(here=...)` with two positive controls. Confirm CLOSED, or say what the literal form still leaves open (the `join_fills` pairing `:2370-2379` is now a source edit too).
3. **DE37-C1 (a)(b)(c) and DE37-R2 — (γ) on the run path.** `run_cell` `:1131-1353`: `null#3` `:1146-1156` (a stream without `gen`), demand over above events `:1179-1182`, `stream_predicates` per draw `:1264`, rejection under its own reason before the replay `:1266-1280`, P4 after it `:1286-1298`, `n_rejected_by_reason` / `first_rejection` / `predicates_per_draw` in the receipt `:1317-1339`. `permuted_stream` `:962-1030`: the below values stay (`_stay` / `_needs` / `_spare` `:1012-1016`). Measured: the round-37 fixture's three two-key draws all P1 = P2 = P3 = True; DE's C3 example reproduces; the run-path falsifier `:2302-2317` (round 37's demand via `_known_bad_demand`, one call site `:2361-2369`) → every attempt PERM_NOT_OK. The `gen` defect DE reports (round 37's stream carried no `gen`; every key of a (slug, side) collapsed to one; the permutation was the identity in silence) is real at the `218509e` blob. Confirm each; say whether a falsifier input on the run path's signature (`_known_bad_demand`, `_draw_log`) is acceptable in the form it has.
4. **DE38-C1 (HIGH — RULE).** Measured on DE's OWN C1 fixture `:2236-2255` (slug A: gen 1 above 0.9 acting, gen 2 above 0.8 HELD; slugs B, C below; one stratum), at the tip: **(a)** `run_cell(draws=5)` → 24 attempts, 5 accepted, ALL FIVE accepted draws are `{A-gen1, A-gen2}` — the above set, the IDENTITY permutation; the control stream equals the treated stream; `arm_result` values it 40.0 = the treated arm's 40.0; `null_quantiles {n 5, q50 40.0, q95 40.0, max 40.0}`, `net_diff_vs_null_median_cents 0.0`. On this fixture the identity is the ONLY draw that satisfies P4 (any above value on B-gen1 or C-gen1 realises two cancels against one). **(b)** The identity guard is handed the ACTIONS (`_actions_for_guard` `:1184-1186`, `:1233`); `MRC.refuse_if_not_random` opens with `if sorted(drawn) != t: return` where `t` is the action keys — under (γ) `|drawn| = |above|`, so in any stratum with a held above event the draw can never equal the action set: 200 seeds → fired 0 ×; the same draws with the guard handed the DEMAND → fired 65 × (the identity draws). The parse assertion `:1713-1719` ("called on every draw … a control the runner holds but never invokes is a control that cannot fire") now certifies a call that cannot go red. The treatment of the identity is an accident of the stratum: on a one-above fixture (|above| = |actions|) the identity draw REFUSES THE WHOLE CELL (`ControlRefused … slack {('BUY_UP', 13): 1}`, measured); with one held event it is accepted five times over. **(c)** `n_distinct_draws` `:1334` / `point_mass` `:1335` are computed over `_seen_draws`, every ATTEMPTED draw `:1225`: 3 / False on the fixture, while the ACCEPTED set has one distinct draw and is a point mass at the treated value (rule 10). **(d)** The check "DRIVEN, RUN PATH: P4 REJECTS AND THE RUN REDRAWS" `:2283-2294` asserts `accepted == 2` and `P4 > 0` and never asks what was accepted; the object-level check `:2339-2360` recomputes an accepted draw — the identity. RULE: (1) the identity draw under (γ): a per-draw REJECTION under its own reason, ADMITTED as one of the C(N, k) permutations and COUNTED (`n_accepted_identity`, per stratum), or the cell-level refusal kept — and the guard's population (the demand; or retired for (γ) with the reason written into the source); (2) `n_distinct_draws` / `point_mass` on the ACCEPTED set, and whether an accepted set of ONE distinct draw is a refusal (rule 6: ≥ 200 draws, not 200 copies of one) or a declared point mass; (3) what the DRIVEN check must additionally assert — an accepted draw whose control stream differs from the treated stream, or the accepted-set statistics; (4) whether P4-matching's selection under a stateful policy (the accepted set can collapse to the identity) must be reported per stratum in the receipt before any §3 number is read — and whether it re-opens the frozen text's matching rule (DRAFT:147-156) or only its reporting.
5. **DE38-C2 (LOW) and C3 (question).** C2: `null#2` `:1305-1314` names only P4's reason while `rejected` totals PERM_NOT_OK / P1–P3 too; should the refusal carry `n_rejected_by_reason`? C3: the pool is the REFERENCE's generations `:1139-1142` while the stream can lack some (NO_ROWS_KEPT is a feed status), so a draw can name a generation with no event → PERM_NOT_OK / P3 — correct by rejection, budget spent on it, not measured further (the identity refusal fired first on the one-above fixture). Is the pool the stream's support?
6. **Your conditions (i)–(iv), against the DRAFT as landed.** (i) §5 says BUILT and names the receipt fields — with item 4, is it built? (ii) the below values stated, §2 re-read (the DRAFT's paragraph "readable only because the below values stay put"); (iii) §6 states the seal's form as a pin claim; (iv) the split question is ruling 5, asked WITH 2 and 4. Say for each whether it is MET at `dfd4c00`, and whether the package may travel whole with DE38-C1 open (the coordinator's position: not while the null on the proving fixture is the treated arm).
7. **What the coordinator missed** — the class again: a check that certifies a call that cannot go red (item 4(b) is one instance — are there others?); a receipt statistic computed on the attempted population where the accepted one is the null; a falsifier input on a run-path signature; any refusal `run_cell` can raise per draw that ends the CELL rather than the draw (`ControlRefused` from the guard is one); DE's ninth mutant (a `gen` made optional dies with a `KeyError` inside `score_events`, not at a named check — DE declines a second guard; rule on it).

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach dfd4c00` (data/pm_5min mirrored per entry) or reads at the blob; `~/ctaNew-wt-de`, `~/ctaNew-wt-be` never read; nothing under `data/` beyond reading the fits; the declared OUTDIR (`data/pm_5min/derived/phase4_diag_r459`) never passed to `--run`; `derived/` listed before and after (173); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited (the v2 DRAFT included — findings go in your filing); `git worktree list` unchanged at quiescence (standing rule 10); any mutant applied to your own worktree copy and restored byte-identical.

## Disposition asked for
One filing (R-377): `REVIEW_DE_ROUND_38_2026-09-02.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DE38-Rn (yours, distinct from the coordinator's DE38-Cn); each with the line; for DE38-C1..C3 a CONFIRMED / CONTESTED with the reason; the rulings item 4 asks for, stated as rulings; item 6 per condition. RELEASE / HOLD on `dfd4c00` as round 39's base, and round 39's order. You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18).
