# Review request — BE rounds 3–4 (`be_forward_day.py`: the frozen bytes execute; 09-01 scored, sealed)
requested by: coordinator (pm-co) · 2026-09-02T13:54Z · reviewer: pm-codex
pinned tip: **`248e99f`** (pushed; `be_forward_day.py` +667/−16, sha256 `4a2c5e667b9765c0…`); filing row **Q-BE-229** at `0f45921` (as-of 13:51:58Z)
artifacts (BE's scratchpad, read-only): `/tmp/claude-1001/-home-yuqing-ctaNew/32b9d1f8-67be-4de0-b959-330c9677da97/scratchpad/fwd5/` — `be_forward_day_receipt_20260901.json` (14,022 B, sha256 `4000106752f816e4…`), `be_forward_day_SEALED_scores_20260901.json` (54,213,086 B, `aca22317ab06adbf…`), `be_forward_day_receipt_20260902.json` (1,123 B, `0907b0369e14d77b…`), `frozen/` (code materialised; `data` → the repo's `data/` by symlink), `run.sh`. The superseded 12:49 receipt (13,860 B, `68234320…`) is in `fwd4/` and in the coordinator's scratchpad as `be_receipt_20260901_pass2_12h49.json`.

Queue position: after DA round 11 (`e292439`) and DE round 19 (`2f6da2c`). Constraints: read-only under `data/`; nothing under `derived/` (184 entries, latest mtime 10:34:15Z — unchanged by BE's runs); never run `da_midnight_verify.sh`; the driver's own runs need a memory cap (`systemd-run --user --scope -p MemoryMax=12G`; BE's run peaked at 5.8 G in 26 min); do NOT re-run the full 09-01 scoring unless you need to — the selftest (46 checks, both launchers, package launcher spawns the path launcher) and the receipts are the evidence; a re-run goes into a NEW outdir, never `fwd4/` or `fwd5/`.

What the coordinator verified (13:52–13:53Z): the confirming receipt at 13:50Z carries the driver sha `4a2c5e667b9765c0` = `git show 248e99f:live/pm_research/be_forward_day.py`, `carrying_commit` `248e99f…` resolved, `working_tree_dirty: true` (other seats' files); ten gates PASS by name; `population.ratification` = R-419 `verified true, unverifiable []` with `pair_asserted {asserted, ref_seen, unverifiable_seen, verified_seen}`; **68 shared numeric fields vs the 12:49 receipt, 2 differ — `n_archive_slugs` 27,947 → 28,031 (the archive index grew between runs; not a population count) and `wall_seconds`**; every population count identical (1,859 / 2,262,457 / 1,847,824; btc 610,064 / eth 441,409; 1,344 windows without a fit; 141 excluded at supply); 09-02 REFUSED at gate 1 by name (`day_closed_calendar=False`). The scores file sha matches the row.

## Items

1. **Rule 12 — how much of the run is the freeze.** The receipt names 8 anchors sha-checked before import, 7 modules imported from the run dir with `__file__` asserted under it, and `not_frozen_in_closure`: the anchors' import closure is 26 modules, **2 of which execute at HEAD** (`tier1_pipeline.py`, `warning_window.py`, each with its sha at both commits). State precisely what "the frozen bytes execute" covers and what it does not; whether the two HEAD modules are on the scoring path or only imported; and what a reader of R-424 §6's recommendation ("race on frozen bytes at `1b53929`") should understand "frozen bytes" to mean at this driver. This bears on the USER's open freeze-disposition decision — report the fact, do not rule the disposition.
2. **The `__file__` data-root discovery (BE's "three things").** Frozen code derives its data root from `__file__`; materialised into the run dir it read an EMPTY archive index (0 slugs) in silence. Now: the run dir symlinks the repo's `data/` (freeze's code, today's data, both named), a probe REFUSES on an unresolved root or empty index; materialising the DATA anchor shadowed the symlink, so only code is materialised; the eraB fragment (gitignored) is verified by CONTENT with its source named, a CODE anchor absent from the freeze still refuses. Drive each: an empty index → refusal by name; a data anchor materialised → the shadow is detected or impossible; a code anchor missing from `1b53929` → refusal. Is "verified by content" for a gitignored anchor a freeze (rule 12: a freeze is a commit) or a disclosure?
3. **Streaming pass — score identity.** The OOM at exactly 12.0 G (cap NOT raised) moved the run to replay → label → score → drop per window. The selftest must show the streamed scores are IDENTICAL to the held-in-memory path on a fixture (a falsifier: a streamed row perturbed → red by name), and that the STRICT failure condition (the v3 builder's) is preserved — a reconciliation failure in window k after k−1 windows were dropped still fails the DAY.
4. **Receipts flushed after every gate.** The SIGKILL left no receipt last round. Verify the per-gate flush by killing the driver (SIGKILL) mid-run on a fixture and reading the partial receipt: the gates reached are on disk, the outcome is not `SCORED`, nothing under `derived/`. State the guarantee in the receipt's own words.
5. **Outdir-per-run / never overwrite.** The 12:49 receipt was overwritten in `fwd4/` last round by a same-path re-run (R-433/434 seats). Does the driver now refuse an existing outdir, or supersede in-band (a new file, the old one kept)? Drive: a second run into an existing outdir → refusal or a numbered successor, never a silent overwrite.
6. **Two disclosures a receipt-only reader needs** — `n_masked: 0` at the scoring seam while 141 windows were excluded at supply (per coin in the receipt); `scored` ≠ the whole day (btc/eth fits only; 1,344 windows counted, unscored). Are both computed from the run (rule 10), and is the `n_windows_scored`-style labelling honest (the R-433-era mislabel)?
7. **The seven surviving mutants, and their fixes.** The §10(1) control that accepted either answer; the import control catching bare `Exception`; the forgeable `pair_asserted: True`. Verify each fix asserts against an INDEPENDENTLY computed value; re-run the 29 mutants (BE reports 29/29 killed, no skips) and add your own on the streaming path (item 3) and the flush (item 4).
8. **Rule 10/14; register hygiene.** `sealed` — counts and refusals only, no metric/rho/net/sign in either receipt; nothing admits a day or a candidate; no new constant; anchor/manifest/candidate unedited; the 21 deploy gates (`v5_deploy_gates.py`, BE reports 21/21 — reconcile with DA's "21 of 22" at `e292439`, which adds a gate). **Note for the record, not for BE to edit:** the Q-BE-229 row's disposition column says "USER-pending unchanged (R-408(2), R-408(3), R-411(i), R-411(ii))" — those four are RULED at R-424 §7; only the freeze disposition is open. A stale line in a filing row; the coordinator records it in R-436 and BE supersedes it in its next row (rule 13).

## Disposition asked for
RELEASE or HOLD for `248e99f` as the confirming driver of record for the 09-01 score (a governed 09-03 verdict is DA's instrument, not this driver's); findings numbered BE34-Rn. One filing (R-377); rule 18. Item 1 is the one a USER decision depends on — state it in its own section.
