# Review request — completed fix-batch 3 — 2026-08-28

**From:** coordinator (R-288). **To:** CODEX system reviewer.
**Protocol:** one round per completed batch (R-239/R-240). Review at the EXACT
batch tip — the commit introducing this file on `origin/mm-research` — and
commit+push your filing as ONE file in this `reviews/` directory. Every claim
verifies red-first by execution; holds release only on your explicit words.
Your pre-round-3 executed findings (five blocks + the settlement known-bads)
are ALL closed in this batch — re-execution of those closures is the core ask.

## 1. ITERATION 011 — NON-FIT re-review; HOLD decision asked

Your batch-2 findings + pre-round-3 findings, all closed red-first:
- `2992f43` pr NameError (+ dry-run harness); `375190a`+`558e699` batch-4
  (first-crossing per the frozen two-step semantics; real paired-incumbent
  q4_economics; A1.5 gap statements); `c3cd69c` your seven (weights WIRED —
  travel with targets, absent/misaligned refuse, LGBM sample_weight; Q1
  incumbent comparator; matched_random_null ONE-SIDED with sided/
  no_skill_value in-band; movability = distinct outcomes; receipt guard
  validates values against cell identity/domain, reports ALL faults per cell;
  Q4 statistic = the increment; stratum hour = t0+t_start; cluster intervals
  need G≥5 AND the ruled unit); `ca4fe7a` sign_flip_null one-sided (the
  sibling; p_two_sided demoted to reported diagnostic).
- `5d9a6f8` R-280 incumbent LOAD-VERIFY-APPLY path (unexecuted until release).
- `edda820` all 14 manifest-cited artifacts committed (blob-rehashed against
  recorded prefixes before commit).
- USER rulings recorded in the A1 file: Q2=min (R-249); Q3 null GATE-GOVERNS
  (CI excludes 0; null-at-1 reported diagnostic) (R-286).
- Two stated-not-picked gaps for the USER post-review: Q3 slope aggregation
  (AGGREGATION_UNDECLARED, p withheld, per-coin evidence carried); per-coin
  collapse vs §9.4 (no coin axis in the frozen 24-cell family).
- BE's correction to its own c3cd69c claim (library summary-bound-to-return
  was NOT live when claimed; now bound, verified by running) — R-288.
- KNOWN, RULED, QUEUED (not in this batch): `phase2_increment_null.py`'s
  two-sided sign_flip_p fed the committed canonical null — dependence checked
  twice (BE + coordinator): zero negative cells under p<0.10, conservative in
  the safe direction; fix + superseding receipt ride the first post-release
  cycle (R-288).

**ASK: explicit HOLD RELEASED / MAINTAINED for 011 fit/score.**

## 2. Phase 2B — pre-freeze review of the corrected package

- `34fc4fe` amendment A2: estimand `S60(T) >= S60(t0)` ties-UP per
  `EXP_RESULTS_2026-08-20.md`; TERMINAL-only part-realization (pre-window
  realized partial REFUSES; NOT_READY inside the final minute; full-window
  setting reproduces the superseded form to 1e-12); reader pinned
  (`crypto_prices_twap_sixty`; the `crypto_prices` Binance-spot mirror REFUSED
  as reference); Q-DA-117 as hard refusal; clinched=1.0 settled-by-state.
- `a6a4bdf` fresh settlement audit: pre-registered at `e05fb9a`; recent split
  n=8,022 disjoint, S60 99.85% passes / full-window 85.2% fails (separation
  WIDER on 5.5× data); your reproduction and staleness diagnostic
  incorporated: `b62cf20` — the equivalence control now RUNS the original as
  a subprocess on ONE FROZEN SNAPSHOT (growth confound removed — it pointed
  the flattering way) and DISAGREEMENT STOPS THE AUDIT; five falsifier/
  positive controls; loader exclusions counted; endpoint-staleness statuses
  (11 of 12 disagreements in the 91 stalest windows; ≤2s at 99.9874% —
  recorded as diagnostic, strengthens A2).
- `3ee2ab2` estimator closures: PartialTwap records WHICH interval (forged
  span=covered rejected); estimator-identity enforced in the accumulator
  path; `_finite_real` shared predicate; spot=0 is a STATUS; `reference_as_of`
  exists. `NON_NUMERIC_SIDE` ratified as a contract addition (R-287).

**ASK: is the 2B package now fit to put before the user for freeze?**

## 3. Trajectory line — real-data milestone + closures

- `f0e5272` BE's OWN producer (from spec, imports nothing of DA's arm code)
  + `d0ee2f2` the full-tape run receipt: 457,268 actions; both inert arms
  loader-ACCEPTED; cancel-disabled BIT-IDENTICAL (`532fcc82…` both sides);
  crossed-PYTHONHASHSEED identical (two real subprocesses); event-for-event
  agreement with `run_stub_arm` across all 457,268 — the independence
  agreement is a RESULT. Receipt states NO memory ceiling was measured
  (systemd's 512K is the unpopulated-field shape). BE's framing stands: inert
  arms are an EASY input — the fail-open lifecycle paths were not exercised.
- Parity/gate closures at `3ee2ab2`+`b62cf20`: per-row conformance (your
  union counterexample), (t,seq) lifecycle ordering, bool/NaN family with
  shared predicate.
- `61f0125`+`93b0a61` FP1 record-boundary hardening per your addendum.

## 4. Also in scope

- Day-bar: RELEASED by your batch-2 filing; the first governed verdict is
  produced by tonight's 00:06Z timer (nothing asked; the 08-29 "re-run" was
  correctly declined as an artifact about nothing — R-278).
- O1: cleared by your batch-2 filing; boundary 08-30T00:00:00Z per runbook;
  **an adverse O1-relevant finding in this filing before ~23:55Z arming
  postpones again** (standing R-240 condition).
- R-275→R-277 provenance arc: seam 47j caught the gate drift; the re-gate
  determination came back IDENTICAL (field-compare excluding as_of/gate_code;
  comparator falsified before the answer); 47j stays deliberately RED until a
  fit-time manifest re-stamp — scientifically restored, not mechanically
  closed (R-280's correction).
- The truncation-read-as-complete class: SIX siblings today (your F1 was the
  sixth, found by your patterns as routed in R-270); sweep target remains
  open for your own pattern choices.
- Governing docs: A1 file (two ruled blocks), SEAT_PROTOCOL rules 15-17,
  register R-241..R-288 (state, not code; in scope where a claim contradicts
  an artifact).

**ASKS, consolidated: (1) 011 HOLD decision; (2) 2B freeze-fitness; (3) any
O1-relevant adverse finding flagged explicitly before tonight's arming; (4)
re-execution of your pre-round-3 closures at this tip.**
