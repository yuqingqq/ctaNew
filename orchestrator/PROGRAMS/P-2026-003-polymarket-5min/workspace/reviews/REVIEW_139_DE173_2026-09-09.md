# REVIEW 139 — DE 173's point estimate on the corrected book

**REV, 2026-09-09T14:58Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`, no book unpickled. **Arithmetic left to DA, as briefed.**

**VERDICT: (1) THE REFUSAL IS REAL — driven, not read — BUT IT PROTECTS THE `economic` BLOCK
AND NOT THE RULED PRIMARY ENDPOINT. (2) THE RATE'S DENOMINATOR IS RIGHT AND THE INSTRUMENT
FIRES — I supplied the falsifier the script does not ship — but the script that produced the
number IMPORTS FROM A HARDCODED WORKTREE, so its bytes are not the tree the artifact cites.
(3) THE RULED EXEMPTION IS NOT A PREDICATE YET: a THIRD unnamed module gives the IDENTICAL
status, the IDENTICAL `is_a_match: False` and NO refusal — only the count in the flag string
changes. (4) CONFIRMED: the two unnamed are EXACTLY BE 122's lazy pair, re-derived
independently.**

---

## (1) THE REFUSAL — DRIVEN, AND THE BLOCK IT DOES NOT COVER

```
test_statistic_from(receipt)  ->  REFUSED POINT_ESTIMATE_RUN_HAS_NO_TEST_STATISTIC
```

**The prose is backed by a property.** Both arms carry
`economic.Z = 'NULL_NOT_DRAWN_POINT_ESTIMATE_RUN'` and
`economic_settlement.Z = 'NULL_NOT_DRAWN_POINT_ESTIMATE_RUN'`.

**But the function reads one block, hardcoded:**

```python
v = ((a.get("economic") or {}).get(field))
```

`economic_settlement` — **the ruled primary endpoint (R-801)** — is never inspected. On this
artifact the refusal fires anyway, because the `economic` block also carries the status; **a
receipt carrying the status only on the settlement block would return a value instead of
refusing.** That is REVIEW 123's `ECONOMIC_FIELDS` shape exactly: **the guard protects the
diagnostic and not the result.** One line: iterate both blocks.

## (2) THE DOUBLE-CANCEL RATE — DENOMINATOR RIGHT, FALSIFIER SUPPLIED, PROVENANCE WEAK

**The key is the reference id space**, at the line:

```python
k = (c["slug"], c["side"], int(c["ref_gen"]))
```

**and the denominator is reference generations THAT WERE CANCELLED** — which is the right one
for the premise under test (*a cancelled generation yields at most one cancel*). 1 in 3,862
therefore means 3,862 distinct reference generations cancelled, one of them twice.

**`de_corrected_book_measures.py` ships no selftest and no cell** (58 lines), so I drove the
function that produces the number:

```
no double cancel          n_cancels=3  n_ref_gens=3  max=1  >1: 0  rate 0.0   premise_holds True
gen 7 cancelled TWICE     n_cancels=3  n_ref_gens=2  max=2  >1: 1  rate 0.5   premise_holds False
gen 7 THREE times         n_cancels=3  n_ref_gens=1  max=3  >1: 1  rate 1.0   premise_holds False
```

**It fires on the fault it measures and stays quiet on the clean case**, and
`de_matched_cancel_control` does carry its own cells — so the quantity is instrumented even
though the script quoting it is not.

**THE PROVENANCE GAP, and it is on a number the USER ruled on:**

```python
sys.path.insert(0, "/home/yuqing/ctaNew-wt-de2/live/pm_research")
```

**The script imports from a hardcoded worktree**, so 1-in-3,862 was produced by whatever bytes
sat in `wt-de2` at that moment — not by the tree the artifact cites, and not reproducible from
the repository path alone. The measurement is sound; **its provenance is a path literal.**
*(The same hardcoded root appears in the artifact's own
`day_run.be_module_citation.loaded_from`, so this is a property of the run's launch, not of
this script alone.)*

## (3) THE RULED EXEMPTION IS NOT A PREDICATE — DRIVEN

The day path raises a **flag string** and proceeds; there is no subset test
(`issubset`: 0 occurrences; no ruled-pair constant in the predicate). Driven on the real EV21
receipt with modules removed:

| unnamed set | status | `is_a_match` | refusal |
|---|---|---|---|
| the ruled pair (2) | `…_MATCHES_WITH_UNNAMED_MEMBERS` | False | none |
| **+ `de_score_stream.py` (a THIRD)** | **identical** | **False** | **none** |
| **+ three more (5 unnamed)** | **identical** | **False** | **none** |

**Only the count inside the flag text changes.** Today the ruled exemption cannot distinguish
itself from a hole — which is the round's own standard. **DE's code is honest about why** (it
says the policy question "is not decided here", rule 14, written before the ruling existed);
**this is the BASELINE the implementation must be measured against**, in the REVIEW 126 form:
the third-module case must REFUSE BY NAME, and I will re-drive exactly this table.

## (4) THE TWO UNNAMED ARE THOSE TWO — RE-DERIVED, NOT READ

```
BE's receipt-free scoring set : 12
named by the EV21 receipt     : 10
NOT named                     : ['harmful_hazard_model.py', 'phase2_state_schema_freeze.py']
the artifact's own field      : the same two, n_checked 10/12
my re-derivation == artifact  : True
```

**Exactly BE 122's lazy pair, not merely two.**

## ROUTED

1. **DE — `test_statistic_from` must inspect `economic_settlement` too**; today the refusal
   guards the diagnostic block and the ruled endpoint is uncovered.
2. **DE — the ruled exemption needs to become a predicate**, and the third-module case must
   refuse by name. The table above is the before.
3. **DE — a measurement quoted to the user should not import from a hardcoded worktree**; the
   number is right and its provenance is a path literal.
4. **(4) is CLEAN**, and the rate's denominator and instrument are sound.
