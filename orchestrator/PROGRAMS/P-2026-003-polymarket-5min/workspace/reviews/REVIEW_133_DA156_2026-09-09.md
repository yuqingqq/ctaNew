# REVIEW 133 — DA 156 (`132bf4f`): my own red, re-driven from both trees

**REV, 2026-09-09T10:13Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`.

**VERDICT: THE FIX VERIFIES AND IT DID NOT COST THE ASSERTION. 23 checks, 0 failures, rc 0
from BOTH trees — and the cell STILL GOES RED under both defects it exists to catch, driven
by re-introducing each one. DA's diagnosis is right and mine at REVIEW 131 was the symptom:
the question was being asked of the wrong tree. ONE RESIDUAL, non-blocking: the row's BOOL
now answers a different tree from the one its name says.**

---

## THE RED IS CLOSED, FROM BOTH TREES

```
from /home/yuqing/ctaNew         SELFTEST OK — 23 checks, 0 failure(s)   rc 0
from /home/yuqing/ctaNew-wt-rev  SELFTEST OK — 23 checks, 0 failure(s)   rc 0
        auditor's own file -> COMMITTED_IN_THIS_TREE (wt-rev is clean at e9078c9;
        DA saw PRESENT_BUT_NOT_COMMITTED from wt-da, and the cell admits both)
```

## THE RULE-33 QUESTION, DRIVEN: CAN IT STILL GO RED FOR THE RIGHT REASON?

**Yes — under both defects, and I re-introduced each rather than reasoning about it.** Both
runs are from **wt-rev**, the tree where the cell used to fail for the wrong reason:

| `committed_state` replaced by | battery |
|---|---|
| as landed | **OK — 23 checks, 0 failures** |
| **the DA 154 collapse** (rc ≠ 0 read as "not committed") | **FAILED — 1 failure, at DA 155's own cell** |
| **the DA 155 form** (always ask `AUDIT_ROOT`) | **FAILED — 1 failure, at the same cell** |

So the cell discriminates in both directions: it catches **absence read as a negative** (the
defect it was written for) **and** it reproduces **my REVIEW 131 red on demand** (the defect
the fix removed). A fix that had bought determinism by weakening the assertion would have
stayed green under the first of those. It does not.

## AND THE PROPERTY THE COORDINATOR ASKED ABOUT WAS NEVER THIS CELL'S

*"Does it still catch that producing code IS the committed bytes?"* — **that property does
not live here, before or after.** The cell's conjunct admits `IN_TREE_CLEAN` **and**
`IN_TREE_DIRTY`, so it has never asserted cleanliness; it asserts the three-state
**distinction**. The cleanliness flag is `auditor_identity()`'s
`producing_code_is_the_committed_bytes`, which is **reported and not gating by stated
design** — REVIEW 131 established that, and DA 156 does not touch it. Driven from wt-rev:

```
auditor_identity: producing_code_is_the_committed_bytes = True   head e9078c9
committed_state(own file, its OWN tree)    = COMMITTED_IN_THIS_TREE
committed_state(own file, root=AUDIT_ROOT) = NOT_IN_THIS_TREE      <- the two questions
```

and the third conjunct DA added asserts exactly that disagreement, and asserts it holds
**iff** the file is outside `AUDIT_ROOT`. **Dirtiness still registers**: on a controlled
repo, committed → `COMMITTED_IN_THIS_TREE`, modified → `PRESENT_BUT_NOT_COMMITTED`,
`_is_committed` → `False`. **The distinction was added at no cost to any assertion.**

## RESIDUAL — THE ROW'S BOOL NOW ANSWERS A DIFFERENT TREE FROM ITS NAME

`audit_module` emits both fields, and after DA 156 they ask different trees:
`committed_state` is asked of `AUDIT_ROOT` **on purpose** and records
`committed_state_asked_of`; `committed_in_that_tree` goes through `_is_committed`, whose
default root is now **the file's own tree**. Driven on a module in another worktree:

```
committed_in_that_tree     = True                 (wt-be, the file's own tree)
committed_state            = NOT_IN_THIS_TREE     asked_of /home/yuqing/ctaNew
read_from_tree             = /home/yuqing/ctaNew  (hardcoded — and the bytes came from wt-be)
```

**Not reachable on the audited set** — every module under audit is under `AUDIT_ROOT`, and a
scratch path is in no tree so both agree — so this is a naming defect, not a live one. But
`committed_in_that_tree` beside `read_from_tree: AUDIT_ROOT` now reads as a claim about
`AUDIT_ROOT` that the value no longer makes, and `read_from_tree` is hardcoded rather than
measured. **One line each: give the bool the same explicit `root=AUDIT_ROOT` its sibling has,
or rename it.**

## ROUTED

1. **DA — the bool and `read_from_tree`**, so every field in the row names the tree it
   actually answered. Non-blocking; today's audited set cannot reach the discrepancy.
2. **Nothing else. REVIEW 131's finding is closed and my own framing of it is corrected in
   band: the redness was real and the dirtiness story I did not claim, DA did not claim, and
   the brief carried, is not the mechanism** — a spotless worktree fails identically.
