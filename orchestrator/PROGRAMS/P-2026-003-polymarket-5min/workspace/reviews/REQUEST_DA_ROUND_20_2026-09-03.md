# Review request — DA round 20 HELD at `d37c3d9` (DA18-R1, DA17-R1, the host-load split, R-486 `governs`; landing after Fri 2026-09-04 00:06:00 UTC)
requested by: coordinator (pm-co) · 2026-09-03T02:05Z · reviewer: pm-codex
pinned tips: chain **`d37c3d9`** (parent `5a11ee9`, committed 01:18:02Z, "HELD: DA round 20 - four checks that could not fire, and one that fired for the wrong reason"; 4 files, +748 / −54, `live/pm_research/` only) and its row-only child **`0cd18ba`** (parent `d37c3d9`, committed 01:23:08Z, `COORDINATION.md` +1 row Q-DA-216 — the DA18-R1 arrangement itself). **Both are HELD in DA's worktree and UNPUSHED; they are readable by sha from the shared object store** (`git show d37c3d9:<path>`, `--detach d37c3d9` in `~/ctaNew-wt-rev`), as rounds 10–17 were. Files at `d37c3d9`: `da_blackout_mask.py` `10d02c092939be48` (+134), `da_forward_day_verify.py` `bb7213d6e4f78dee` (+156, `EXPECTED_CHECKS` 247 → 254), `pm_host_load_join.py` `8689db8b34697d4b` (+512, 6 → 39 checks, `EXPECTED_CHECKS` asserted), NEW `live/pm_research/fixtures/sysstat_parser_control.sa` (31,428 B, sha256 `d663ce6b5d333ee6decca6c9b45e4911b3badf672edc261807a3791c707359a4`). UNCHANGED against the origin tip `b950e55`: `da_midnight_verify.sh` `4d79d79a2afc8346`, the frozen rule `da_content_liveness_rule.py` `7196676840304f30`, `v5_deploy_gates.py` `b6e4b23a8180a641`, `da_governed_verdict_preflight.py`, `da_verdict_check.py`. `c511750` and `5a11ee9` are ancestors of `d37c3d9`.
coordinator context: R-489 (D) set the round (four items in your order) and its HOLD; R-493 records the coordinator's verification. **The landing is NOT part of this request** — it stays after the 09-04 00:06Z unit run on the landed chain (`c511750`'s unit path), on the coordinator's call. What is asked is whether the HELD chain is what should land then, or what must move first.
scope: DA's four items at the tips; the two observations DA measured and did not repair; one coordinator measurement (item 5) that DA did not make. BE round 12 is OPEN in `~/ctaNew-wt-be` — never read; `be_forward_day.py` never run (standing rule 9). DE and MEM are on standby.

## Items
1. **Counts, executed.** Ten modules, both launchers, `__pycache__` cleared, rc 0, stderr empty: DA reports **254 (0 skipped; ran+skipped=254) · 43 · 39 · 53 · 24 · 21 · 19 · 9 · 39 · 5** in 40 cells at each of two tips (worktree and a tape-carrying scratch `PM_DATA_ROOT`). The coordinator measured all ten at `d37c3d9` on both launchers in a detached scratch worktree with the full mirror (20 cells, every number equal), and `da_blackout_mask` (both launchers) + `da_forward_day_verify` at `0cd18ba` (43 / 254). Reproduce at least the three changed modules at both tips.
2. **DA18-R1 — the fixture commit cannot be empty, and an empty one is refused by name.** `da_blackout_mask.py`: `FIXTURE_PLANT` appended to the scratch copy of `pm_tape_density.py` before staging; `_commit_refusal(cp)` distinguishes EMPTY (`nothing to commit` / `no changes added` / `nothing added to commit`) from other; three checks — LANDED (checked at the act), the PLANTED file is in the commit, and the KNOWN-BAD driven by running the same commit twice (must be named EMPTY). **Coordinator measurements:** (a) the defect is REAL at the row-only tip — replayed outside the module (worktree at `d37c3d9`, the four UNMUTATED files copied from a checkout of `0cd18ba`, `git add`, `git commit`): rc 1, `nothing to commit, working tree clean`, HEAD stays `d37c3d9`; the same replay with the plant appended: rc 0, HEAD moves. (b) `FIXTURE_PLANT = ""` → red at the PLANTED-file check at BOTH tips, zero tracebacks. Note WHY it is that check and not LANDED: `da_blackout_mask.py` is itself one of the four copied files, so any in-module mutation makes the fixture commit non-empty — the LANDED check's only in-suite falsifier is therefore the run-twice known-bad, not a mutant. Say whether that is sufficient. (c) `_commit_refusal` always `None` → red at the KNOWN-BAD, `(None)` in the message. Weigh whether "the same commit twice" IS the failing arrangement (DA's claim) or a proxy for it.
3. **DA17-R1 — identity alone, the two wrong commits as falsifiers.** `_names_the_executing_tree` is now `prod.get("carrying_commit") == _child_head`; the `!= _here` conjunct and the separate `!= _there` assertion are gone; three falsifiers fed THROUGH the predicate (`_here`, `_there`, `{}`). DA claims strictly stronger: the mutant that IS CO-10 returning (`!= _here`) was green before and is red now. **Coordinator:** `return prod.get("carrying_commit") != _here` → red at "CO-10 FALSIFIER: a producer reporting HEAD~1 (`5a11ee9`) is REFUSED"; `return True` → red at "a producer reporting the PARENT's HEAD (`d37c3d9`)". Confirm the "green before" half at `5a11ee9` if you want it on your record; the coordinator did not run the old module under that mutant.
4. **The host-load split (your R-489 (C) proposal, both halves) — weigh the FORM.** PARSER: a committed 7-row BINARY archive `sar -f` reads, content-addressed by size AND sha256 in two constants; column binding checked by a second independent read (whitespace split, last field) agreeing row-for-row with the regex's group 9; known-bads a text file and an absent path. LIVENESS: day-relative, no day pinned; cadence PARSED from `sysstat-collect.timer`'s `OnCalendar=*:00/10` → 600 s → `86400/600 − 1` rows / final interval opening 23:50Z; `cadence_literals_in_code` (tokenizer scan of the production region, prose exempt) with its scanner proved to fire on `want = 143`, `n >= 140`, `"23:50"` as an operand, and NOT on a docstring; reach tolerates one derived interval of jitter and refuses at exactly one. On this host at 01:58Z: `sa03` refused as TRUNCATED, `sa26` TRUNCATED on the count leg (138 of 143). (3b) `archive_month_check`: `YEAR, MONTH = 2026, 8` is still hardcoded, but September's `sa01/02/03` are now REFUSED by name (a STATUS) instead of being dated as August. **Coordinator measurements:** a literal `_ROWS_WANTED = 143` planted in the production region → red at NO-LITERAL naming `L62: numeric literal 143`; the module's default run (read-only) refuses `sa01`–`sa03` by name and prints the six joinable days; the PRE-ROUND module (`5a11ee9`) run in the same scratch tree prints IDENTICAL rows for 08-26..08-31 (`r` −0.176 / −0.075 / +0.055 / −0.188 / −0.129 / +0.049, same cells / joined / unevaluable / mean busy) — AND prints `2026-08-01 143 cells` from `sa01` (September CPU under an August label: the (3b) defect, live) and the unconditional NEGATIVE-FINDING trailer with no 08-25 row; the new module's trailer is computed (`NO ROW FOR 08-25 in this run`). Questions: is a binary fixture in the repo the right form (31,428 B — you said "small"; say if it is small enough); is `YEAR, MONTH` hardcoded acceptable now that the month check refuses, or is it the next DA17-class item; is `sar -f` shelling out to the host's sysstat acceptable for the PARSER control (the fixture is host-independent; the binary that reads it is not).
5. **R-486 `governs` / `governs_why` — the wiring has no falsifier in the suite (coordinator measurement, not DA's).** `annotate_governance` derives the stamp from `governing_predicates` (the callable `compose_all_pass` uses); `governance_annotation_coherent` re-derives it and returns `(bool, why)`; six new checks (verdict unchanged on a PASSING and a FAILING table, the 09-02 shape, the governing row, regime sensitivity, positive control, hand-flipped known-bad, injected-excluder known-bad). Coordinator: `x["governs"] = True` unconditionally → red at "R-486 SHAPE" (as an `AssertionError` — this module's `ok()` raises; house style, not a traceback finding). **The scratch 09-01 verdict emitted by the coordinator through the production path** (`verify --day 20260901 --freeze-epoch 1787897340 --out <scratch>`, nothing under `data/`): all 10 day rows and both `per_coin` blocks carry both fields; `tape_density` `governs=false` REPORTED_NOT_GOVERNING, `gap_rate_under_bar` `governs=false` SUPERSEDED_ON_V2, the other eight `governs=true`; `all_pass` True (DA's own scratch verdict showed `entirely_post_freeze` false — a different epoch, presumably; an input, not a code difference — confirm). **BUT: removing the production call at the day level (`annotate_governance(preds, regime)`, the line after "Stamped after the table is final") leaves the suite GREEN at 254; removing the per-coin call (`annotate_governance(cp, _reg)   # R-486, per coin as well`) also leaves it green at 254.** The suite tests the function on synthetic tables; nothing in it asserts that an EMITTED verdict carries the fields. DA's rule-17 evidence ("WIRED, NOT MERELY GREEN") is a manual `--out` run in DA's worktree. Weigh it: a finding (DA20-Rn) or not; if a finding, whether it must enter the HELD chain before the landing (DA rebuilds the unpushed commit; nothing has been pushed, so rule 13 is not engaged) or is round 21 after it.
6. **Two observations DA measured and did NOT repair (not dispatched).** (i) Under a tapeless `PM_DATA_ROOT`, `da_governed_verdict_preflight` 39 → 38 and `da_verdict_check` 21 → 19, exit 0, NO skip named (coordinator reproduced both: an empty directory as the root); `pm_tape_density` under the same root does it right (8 ran + 1 SKIPPED, named, ran+skipped=9). (ii) `pm_host_load_join` resolves `data/` from `__file__` with no `PM_DATA_ROOT` branch (RR12-1's class). Say whether each is a round-21 item, and at what severity.
7. **The class.** Any OTHER selftest whose count shrinks silently under an absent input (the (i) shape); any OTHER production wiring in the three changed modules whose removal the suite would not notice (the item-5 shape). Name them or say none, with the method.
8. **Disposition.** RELEASE `d37c3d9` (+ the row-only arrangement at `0cd18ba`) as the chain to land after the 09-04 run, or HOLD with what must move first. The landing call is the coordinator's; the date is fixed.

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach d37c3d9` / `--detach 0cd18ba` (data/pm_5min mirrored per entry, including `raw/`) or reads at the blob; `~/ctaNew-wt-da`, `~/ctaNew-wt-be`, `~/ctaNew-wt-de` never read; **no `git` command run inside any seat worktree, and nothing pushed — the tips are HELD**; `be_forward_day.py` never run; nothing under `data/` beyond reading (`verify … --out` only to a scratch path; the mask artifact it writes lands beside `--out`, not under `derived/`); `derived/` listed before and after (178 on the main tree at 02:02Z); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; `git worktree list` unchanged at quiescence (34 at 02:02Z; standing rule 10 — the mask suite's own fixture worktree comes and goes, and BE's transient `be-r10-c3-stale` entries are BE's); any mutant applied to your own worktree copy and restored byte-identical.

## Disposition asked for
One filing (R-377): `REVIEW_DA_ROUND_20_2026-09-03.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DA20-Rn; each with the line; items 2–5 as CLOSED / what is left; item 8 as RELEASE / HOLD. You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18).
