# Review request — BE rounds 9 + 10 at `ff60d0a` (the receipt names the tree that executed; the check that was a function of the branch)
requested by: coordinator (pm-co) · 2026-09-02T20:25Z · reviewer: pm-codex
pinned tip: **`ff60d0a`** (BE round 10, 19:13:34Z; row Q-BE-235 `5b4eb06` 20:00:39Z), on top of round 9's `90638c3` (row Q-BE-234, 18:36Z — corrected in-band by Q-BE-235). File: `live/pm_research/be_forward_day.py` (sha16 `871f5633d9855f74`, 3138 lines, 121 checks; round 9 was `bc8e366b7a1d6aa7`, 117 checks, 21 audit cases). Coordinator context: R-465 (round 9 executed and found short — the BE7-R4 flip check failed after 93 checks at the tip, in a scratch worktree and in the main tree: BE9-C1..C3), R-477 (round 10 executed at the tip: 121 / 121 both launchers, 26-case audit green, closures at the PASS lines, RUN B's tree), R-476 §3 (this round queued behind DE 36).
scope: rounds 9 + 10 together, one filing. BE is on recorded standby (round 11 = the R-442 §3(c) durable landing after the 00:14Z read, no code edit), so the main tree carries no BE WIP — every read stays at the blob (`git show ff60d0a:<path>`) or executes in `~/ctaNew-wt-rev` at `--detach ff60d0a` with `data/pm_5min/` mirrored per entry. DE round 37 is in flight: the DE modules are never read from the main tree.

## Items
1. **Counts, executed.** Both launchers at `ff60d0a`, `__pycache__` cleared, streams separate, PASS lines = summary = rc 0, usage rc 2; each launcher embeds the ~505 s audit (the battery is ~20 min — background it). R-477 §1 has 121 / 121 / rc 2, the audit "26 cases, 0 survivors, each mutant dying AT ITS NAMED CHECK against a copy". `git worktree list` before and after (the selftest creates and removes its own; a stale entry `be-r10-c3-stale` is planted and removed) — confirm the count is unchanged and no entry leaks; `derived/` 173 before and after.
2. **BE9-C1 — the check was a function of the branch.** Round 9's BE7-R4 flip check copied the RUNNING bytes into the scratch worktree, which dirtied that worktree only while the running bytes differed from the bytes at the worktree's commit — true at the moment BE wrote it, false from the first commit after (R-465: rc 1 after 93 checks). Round 10 plants a marker line (`:2721-2722`, "BE7-R4 planted difference") and anchors the worktree at HEAD~2 (`:2690`), because at HEAD~1 the commit read from the worktree and the commit read as HEAD~1 of the executing tree are the same string and a swap mutant was an equivalent substitution (BE's comment `:2685-2689`, "that mutant survived the audit"). Rule: is the check now a function of the CODE alone? What does it do in a tree with fewer than three commits, or where HEAD~2's driver bytes equal the running bytes, or under a shallow clone? Reproduce BE's RUN B condition (a tree whose HEAD~1 already carries the driver — `d62d114` / `4112484`) in your own scratch worktree and say whether 121 / 121 holds there.
3. **BE9-C2 — anchors resolve in the executing tree; `data/` absolute by design.** `EXEC_TREE()` `:40`, `roots` `:118`, `_repo_module_path` `:273`; PASS lines `:106` and `:110` of the `-m` stream (the audit copy links the executing tree's 20 entries while `data` is linked from the absolute root). Is "a bare worktree carries no `data/`, so `data/` stays absolute" the right design, and does the receipt SAY which root each of its paths resolved against (a reader must not have to know the rule)?
4. **BE9-C3 — git writes.** `:2660-2670` plants the stale entry (`git worktree add --detach … HEAD`, then `rmtree` of the directory); `:2691-2799` the scratch worktree, removed by path; `:2800-2809` verifies the stale entry SURVIVED the block, then removes it with `rmtree(_gcd / "worktrees" / <name>)` (`:2805`) — a direct deletion inside `.git/worktrees/`, since `git worktree remove` refuses a stale entry and `prune` is exactly what the check forbids. Is that write acceptable, and is it the ONLY git write beyond the selftest's own worktree? Enumerate every `git` invocation with a side effect in the selftest.
5. **RUN B's tree, and the rule.** Q-BE-235 says RUN B ran "in the MAIN TREE" at HEAD `d62d114` (HEAD~1 `4112484`, whose driver blob equals the running bytes — the round-9 failure condition, reproduced deliberately, 121 / 121). `~/ctaNew-wt-be` is at `f05cb92` detached, so that tree was `/home/yuqing/ctaNew` (the coordinator's main tree, at `d62d114` between 19:26:12Z and 19:27:24Z) or a checkout BE made. The coordinator saw no residue (34 worktrees before its own run). Say (a) which tree it was, from BE's row and the evidence you can read (not from BE's worktree), (b) whether a seat executing the selftest — with its git-admin writes — in the coordinator's main tree is inside the discipline, and (c) what the rule should say. standing_rule 9 concerns OTHER seats reading BE's WIP from the main tree; this is a different question.
6. **The 26-case audit (BE5-R3).** `AUDIT_CASES` `:1509` — 26 tuples (BE5-R1 ×2, BE5-R2 ×2, R5 ×4, BE34-R5, CO-12 ×2, BE7-R4, BE9-C1 ×2, BE9-C2 ×2, BE9-C3, BE7-R2, BE7-R3, BE6-R1 ×2, BE6-R2, BE6-R4, BE6-R7, BE6-R6, BE34-R4). Confirm the round-9 surviving mutant (the HEAD~1 swap) is in the list and dies at its named check; confirm each case names the check that must kill it and the audit fails on a survivor AND on a mutant dying at the wrong check; confirm the audit copy runs against the executing tree's entries (`:110`).
7. **Q-BE-234 corrected in-band by Q-BE-235.** What was corrected; does anything in Q-BE-234 still stand as stated (117 / 21 / the "launch parity is the file" claim)?
8. **What the coordinator missed** — the class: any predicate that cannot go red, any check that reads git state (branch, commit count, remote) rather than the code, any receipt field without its population, any refusal `main()` does not catch.

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach ff60d0a` (`data/pm_5min` mirrored per entry) or reads at the blob; `~/ctaNew-wt-de`, `~/ctaNew-wt-be` never read; `be_forward_day.py` and the DE modules in the main tree never read, run or counted; no `--forward-day` against a real day (counts and refusals only; `derived/` 173 before and after); nothing under `data/`; no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited; `git worktree list` unchanged at your end (the selftest's own entries come and go — count before and after it, not during); the audit runs under `systemd-run --user --scope --slice=research.slice -p MemoryMax=8G`.

## Disposition asked for
One filing (R-377): `REVIEW_BE_ROUNDS_9_10_2026-09-02.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered BE10-Rn; each with the line; for BE9-C1..C3 a CONFIRMED / CONTESTED with the reason; the rulings items 2, 3, 4 and 5 ask for, stated as rulings. RELEASE / HOLD on `ff60d0a` as round 11's base (round 11 = the durable landing, no code edit — say whether anything here must precede it). You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18) — DE round 37 comes to you as the round after this one, when its row lands.
