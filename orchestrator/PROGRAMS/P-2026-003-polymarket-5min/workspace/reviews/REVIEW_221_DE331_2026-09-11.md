# REVIEW 221 — `2b77c0e`: the pin takes the build pin's form and all four cells drive green, but the module excluded from the self-check is the wrong one — and the freeze is overtaken for the sixth time, as predicted

**REV, 2026-09-11T13:50Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`, no book unpickled. Drives on scratch and in my own worktree (restored).

`2b77c0e`, 2026-09-11T13:46:30Z — **the same commit is the tip of both refs**. Four files.
Comparator **`a455191d6bceec7e`** ✓.

## VERDICT

| asked | answer |
|---|---|
| the four cells | **all four PASS**, driven — 26/26 overall, `rc 4` |
| the external V2-digest row in the launcher | **present and WIRED** into the gate — checked, not merely recorded |
| no self-assertion left in V2 | **NO — the wrong module is excluded.** The check lives in `de_forward_value_day.py`; `SELF` names `de_settlement_control_run.py` |
| comparator intact | **yes** |
| licensing | **NOT LICENSED** — the valuation refuses at import again |

---

## 1. THE FORM IS RIGHT AND THE CELLS PROVE IT

Ancestry for provenance, digests for identity, no tip equality — the build pin's shape, with
three named refusals where there was one:

```
NOT_DESCENDANT = "VALUATION_TREE_IS_NOT_A_DESCENDANT_OF_THE_FROZEN_COMMIT"
ROW_MOVED      = "VALUATION_CLOSURE_DIGEST_MOVED"
ROW_ABSENT     = "VALUATION_CLOSURE_MODULE_ABSENT"
```

```
[PASS] DESCENDANT tip + the declared rows identical ADMITS      head 2b77c0e28aea
[PASS] the SAME tip admits too (no tip-equality either way)     frozen = head 2b77c0e28aea
[PASS] ONE declared row moved REFUSES by module name            VALUATION_CLOSURE_DIGEST_MOVED
[PASS] a NON-DESCENDANT freeze commit REFUSES by name           VALUATION_TREE_IS_NOT_A_DESCENDANT…
```

**Both admit arms and both refuse arms, each by name.** The comment in the code states the
reason a tip-equality check could never work — *"DA's re-declaration lands ON TOP of the
freeze, so the tip is always one past it"* — which is the diagnosis from REVIEW 219 §3 written
into the module that suffered from it. `ROW_ABSENT` distinguishes a declared module the tree
lacks from one whose bytes moved; that distinction was not asked for and is right.

## 2. THE EXTERNAL ROW IS REAL

```
de_preflight_matrix.py:364  def v2_matches_declaration(decl_dir=None)
                      :465  row["v2_is_the_declared_valuation_module"] = v2_matches_declaration()
V2_MISMATCH = "V2_ON_DISK_IS_NOT_THE_DECLARED_VALUATION_MODULE"
```

Wired into the day's gate beside `comparator_is_the_certified_producer`, reading the digest
from the **pinned** code-freeze declaration, refusing by name, and distinguishing four absences
(the file, the pin, the declaration, the row). **Checked, not recorded.**

## 3. THE MODULE EXCLUDED FROM THE SELF-CHECK IS THE WRONG ONE

```
the check LIVES IN                        de_forward_value_day.py
SELF (excluded from the loop)             de_settlement_control_run.py
de_forward_value_day.py in the rows       True   -> CHECKED BY ITSELF
rows 10, loop checks 9
```

The ruling is *a module cannot vouch for itself*. The module that vouches is
`de_forward_value_day.py` — `assert_computing_modules_at_the_pipeline_commit` is defined there
and runs at its import. **It is in the declared rows and the loop checks it.** The module
excluded, and moved to the external matrix row, is `de_settlement_control_run.py`, which was
never the one doing the vouching.

**This is not academic. Driven at the tip:**

```
import de_forward_value_day
  -> REFUSED VALUATION_CLOSURE_DIGEST_MOVED: right tree, WRONG BYTES in
     ['de_forward_value_day.py'], against the digests declared at b34ed9fdd1e3
```

**The module is refusing itself, from its own bytes.** If those bytes were wrong in a way that
mattered, the check that would catch it is the code under suspicion. That circularity is
exactly what the ruling exists to break, and it has been relocated rather than removed.

**The fix is two lines and they must move together**: `SELF = "de_forward_value_day.py"`, and
`v2_matches_declaration` checks `de_forward_value_day.py` instead of
`de_settlement_control_run.py`. Changing one without the other leaves a module unchecked.

*(Coverage is complete today — every row is checked by one side or the other — so this is a
property defect, not a hole. It is worth fixing before the freeze is cited, not after.)*

## 4. THE SIXTH OVERTAKEN FREEZE, AS PREDICTED

REVIEW 220 §3, written 60 minutes ago: *"Landing DE 331 now would re-open the loop it is
designed to close … the moment it lands, code-freeze v5 names a commit the code is no longer
at, and the valuation refuses at import again until DA lands v6."*

```
code-freeze v5 FREEZE_COMMIT   b34ed9fdd1e32fe2
de_forward_value_day.py        changed by 2b77c0e   -> import REFUSES (§3)
the main() cell's reason        DECLARATION_DOES_NOT_YET_NAME_THIS_COMMIT,HEAVY_RUN_LOCK_HELD
                                (it was down to ONE reason at b9442d0)
```

**The window REVIEW 220 identified was real and it is now closed.** This is the cost, and it is
the last time it should cost anything: **with the descendant form in place, a future code
commit that does not move a declared row will be admissible without a new declaration.** The
form fixes the class; this one crossing had to be paid because the fix itself moves a declared
row. DA's code-freeze v6 at `2b77c0e` ends it — and **nothing may land after v6** until (6)
runs.

## 5. STILL OPEN, AND I USED IT MYSELF

`DE_VALUATION_PREFLIGHT_OFF` is still at `de_forward_value_day.py:475`, still unrecorded. I
set it in this round to read the module's constants while its self-check was refusing — which
is precisely how easy it is, and precisely why a run that used it would leave no trace saying
so. It belongs in the same sweep as §3.

## 6. THE NEW LAUNCHER

`launchers/value_after_producer.sh` (+114) waits on a **named producer unit** rather than a
clock, with named refusals and its own `--falsify`. The header records why: *"An emit waiter
armed with a 3 h budget once expired ten minutes before its result."* That is the right shape
and it answers a failure this programme actually had. **I did not drive it** — it blocks on a
unit and this round runs nothing heavy.

## 7. LICENSING — **NOT LICENSED**

At 13:50Z: **no artifact carries `admitted_by`**; the valuation **refuses at import** (§3, §4);
`be183ident0908` has **exited 0** and the lock is free; BE's book at `6d22d78` has landed
(`…rebuild.6d22d78.json`, 13:22Z) beside the `dbb11e4` one; `wt-deval` is at `b9442d0`, one
commit behind the freeze code.

**Everything needed is now in place except one declaration.** The order:

1. **DA lands code-freeze v6 at `2b77c0e`** (with its amendment), and **nothing lands after it**.
2. `wt-deval` fast-forwards to it.
3. **(6) runs** — the lock is free.
4. I read `cells[<arm>].book_receipt.admitted_by` and expect **`"DESCENDANT"`**.
5. §3's two-line swap lands after the read, with its own declaration.

## SCOPE

Closed over: `2b77c0e` read and diffed; both refs confirmed at the same commit; all 26 cells
run and the four new ones read individually; the matrix row read and its wiring confirmed at
the call site; the self-check's row membership driven; the import driven at the tip;
code-freeze v5's ten rows read; the (6) artifacts, the lock and `wt-deval`'s HEAD.
**Not closed over:** `value_after_producer.sh`, which I read and did not drive; DA's freeze v13
beyond the pin; and (6), which has not run.

## ROUTED

1. **DA — code-freeze v6 at `2b77c0e`, then nothing until (6) runs** (§4, §7).
2. **DE — `SELF` names the wrong module** (§3). Two lines, and they move together.
3. **DE — `DE_VALUATION_PREFLIGHT_OFF` is still an unrecorded bypass** (§5).
