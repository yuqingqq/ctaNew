# Review request — DE round 35 at `27c1ccd` (the runner after DE34-C1..C4 and EST-R1..R5; the addendum v2 DRAFT the USER will rule on)
requested by: coordinator (pm-co) · 2026-09-02T19:22Z · reviewer: pm-codex
pinned tip: **`27c1ccd`** (DE round 35; row Q-DE-53 `19ddb43`) on top of `47a2ba6`. Files: `live/pm_research/de_phase4_diag_runner.py` (sha16 `81e0bacc2e355859`, 1645 lines, `EXPECTED_CHECKS = 67`), `live/pm_research/de_phase4_protocol_check.py` (`5add6cdfafd1ff15`, 318 lines, 26 checks), `live/pm_research/plans/DE_PHASE4_DIAGNOSTIC_ADDENDUM_V2_DRAFT_2026-09-02.md` (`6edefdfda909a897`, 106 lines, NEW). `de_head_scoring.py` unchanged (`a074c150a1f2155d`). Coordinator context: R-471 (round 35 executed at the tip — counts reproduce; five coordinator findings DE35-C1..C5), R-470 (your DE 33 + 34 filing verified), your own estimand filing (EST-R1..R5) and your DE 33 + 34 findings DE34-R1..R7.
scope: round 35 at the tip. Round 36 is NOT yet dispatched — DE is on standby — so the main tree carries no DE WIP tonight, but every read stays at the blob (`git show 27c1ccd:<path>` or `--detach 27c1ccd` in `~/ctaNew-wt-rev`) as the standing discipline. BE round 10's row is in flight: `be_forward_day.py` is never read, run or counted from the main tree (standing_rule 9).

## Items
1. **Counts, executed.** Both launchers, the eight DE modules or the three changed ones at minimum, `__pycache__` cleared, streams separate, PASS lines = summary = rc 0. R-471 §1 has 21 / 67 / 26 / 21 / 24 / 21 / 184 / 92; confirm or contest. Drive `--run --outdir <a path of yours, NEVER the declared one>` (rc 2 by name, nothing created); drive `preflight()` and `_head_scorer(...)` in-process and say what each refuses on and in what order.
2. **DE35-C1 (HIGH — the one that matters for the USER).** The control's stream `:800-804` carries a below-threshold event at `t0 + REPOST_DWELL_S` per drawn generation; the treated stream is one event per generation at its `t0` (`score_events_for` `:1571-1573`; `de_score_stream.score_events` `:147-166` emits one event per row; `_decision_times` `:631-632` says so). Under `harmful_stateful_policy.py:40-42` (repost after the score has been below `theta_repost` continuously for the dwell, the clock anchored at the score EVENT that went below), does the treated arm ever repost inside the generation it cancelled, and does the control? R-471 §2(a) says: no and yes — so the null carries repost-fill economics the treated arm never incurs, and addendum v2 §5 (`:82-85`, "exactly as the treated arm's stream would") asks the USER to adopt parity in a wording that is not true of the stream the runner builds. Confirm or contest from `replay_policy`'s join semantics (`:719-773`, `:903-910`), and RULE between the two truthful shapes: (i) the control's stream takes the treated stream's shape — one event per generation, no repost event, parity by construction; (ii) both streams re-score after a cancel. Say which is the estimand DRAFT:147-156 describes, and what §5 should say instead.
3. **DE35-C2.** DE34-R1 at `27c1ccd`: `:1339-1341` still greps `open(__file__).read()` for "the frozen protocol names no rate limit" — the needle now matches the corrected comment `:229` describing the defect; the message `:1343` still says the axes carry no rate limit; `:135-136` still cites `:88-99` for the axes against `:229-233`'s `:99-108`. Confirm, and say what the check should assert (that `max_cancels_per_minute` is DECLARED per cell and the identity reported — a predicate over `arm_result`'s output, not over prose?).
4. **DE35-C3.** Protocol check `:275-281`: `ok(_RUN.FILL_HORIZON_S == 1.0, "the runner IMPORTS ... rather than restating it")`. You ruled the binding with no number (R-470 §1). Confirm that a restated constant passes this predicate, and state the predicate you want (`is`-identity against `phase4_generation_tables.FILL_HORIZON_S`, or the `ImportFrom` asserted from the parse).
5. **DE35-C4 / C5.** `_gen_index(reference)` rebuilt per drawn key per seed `:790` (and `_treated_actions` per seed `:773-774`) — estimate the cost against the §3 population (471 windows, 200 draws, 2 null cells) and say whether it is a run-time blocker; `:807` `else 0.5` unreachable given `:725-731` and `:758-761` — confirm dead, and whether the parse-level ban you would want covers "a literal policy constant in a `theta=` keyword".
6. **DE34-R7, ruled.** DE's pin design is stated at `:115-133` and built at `:331-358`: a called file that has moved REFUSES until the run supplies the fit-commit bytes. Yours was: import closure ∩ `fit_code_files`, moved files as named statuses, function-level exemptions computed. As built the run cannot start until someone decides which bytes of `harmful_exposure_rows.py` (`851edaf` 09-01, +79/−8) and `phase2_arms.py` (`2e1204f`, parser-only) the diagnostic runs against. Read `851edaf`'s diff and say what it changed in the FEED (does the reference the diagnostic builds differ from the one the fits saw?); then rule: run against the fit-commit bytes (how — a pinned import path?), run against the tip with the two files' differences declared as statuses, or hold. This is yours to weigh and the coordinator's to route; DE does not narrow it.
7. **Your remaining DE34-Rn at the line:** R2 (`:1297-1298`), R3 (`de_head_scoring:271`), R5 (`:1633-1635`: `LightGBMError` absent — unreachable at this tip because `_head_scorer` refuses first; state what the tuple must carry when the wiring lands, and whether `HSP.InvalidParameter` is reachable from a fit-supplied threshold TODAY via `theta_repost = theta_cancel/2` with `theta_cancel` ≤ 0), R6 (`:1194-1196`), R4 (closed at `:807` modulo C5). DE31-R2: closed on the fixture `:1124-1134`; the run's `null_quantiles` `:826-835` reports no per-stratum freedom — say whether that is a receipt requirement.
8. **The addendum v2 DRAFT as a document.** Read it whole. Is it a PROPOSAL throughout (rule 14)? Is anything in it a decision already taken? §2's tight rung `theta_cancel − ε`: with the control's repost event at score 0.0 and the treated stream's shape from item 2, does the rung measure anything? §3's "smallest dwell still longer than the largest latency rung" is a sentence, not a computation — say whether 0.5 s is justified or merely chosen. Then rule: can §1–§4 go to the USER ahead of §5, or does the package travel whole after §5 is restated?
9. **What the coordinator missed** — the class again: any check whose predicate cannot go red, any constant that is a policy input in disguise, any docstring standing in for code, any refusal `main()` does not catch that the run path can raise, any place the receipt reports a number without its population.

## Discipline
Execution only in `~/ctaNew-wt-rev` at `--detach 27c1ccd` (data/pm_5min mirrored per entry) or reads at the blob; `~/ctaNew-wt-de`, `~/ctaNew-wt-be` never read; `be_forward_day.py` in the main tree never read, run or counted; nothing under `data/` beyond reading the fits; the declared OUTDIR (`data/pm_5min/derived/phase4_diag_r459`) never passed to `--run`; `derived/` listed before and after (173); no unit, timer, scope-with-timer, anchor; `DA_MIDNIGHT_MODE` never set; `da_midnight_verify.sh` never run; no plan file edited (the v2 DRAFT included — findings go in your filing, not into the draft); `git worktree list` unchanged.

## Disposition asked for
One filing (R-377): `REVIEW_DE_ROUND_35_2026-09-02.md` under `workspace/reviews/`, one pathspec commit, push. Findings numbered DE35-Rn (yours, distinct from the coordinator's DE35-Cn); each with the line; for DE35-C1..C5 a CONFIRMED / CONTESTED with the reason; the rulings items 2, 4, 6 and 8 ask for, stated as rulings. RELEASE / HOLD on `27c1ccd` as round 36's base. You estimate; the coordinator routes; the USER decides (rule 14). Nothing further until the coordinator dispatches (rule 18).
