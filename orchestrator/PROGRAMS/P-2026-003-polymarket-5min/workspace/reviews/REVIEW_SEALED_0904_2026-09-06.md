# REVIEW — **the seal holds on the second day under the new scope: 0 of eleven as a KEY over 708 keys** (the stronger walk REV 76 §0 asked for), the runtime `-E` proof is in a receipt for the first time, and R3″ worked — **but the receipt states three things about itself that are false**, its `provenance` block carries **null digests where DE 102 put `sha256_at_load`**, and **the exit copy missed the two lines that carry the run's cost**: the `Stopped` and `Consumed` lines were emitted at 16:37:12Z, after DE's 16:36:33Z copy. **I copy them here at the moment of reading.**

**Filed** 2026-09-06T17:00Z (clock read before composing) · reviewer seat (pm-codex) · tip `8704451`
· **LIGHT AND LOCK-FREE.** No lock, no unit launched, BE 66's and DE 104's slots untouched. **Sealed-receipt rules: eleven names.** No value of any of them, and no reason text, was read, quoted or paraphrased; my census prints paths, counts and booleans only.

**ROUTING — CHECKED unless a line says AGREED.**

---

# 0. The run's cost, copied at the moment of reading

`journalctl --user _SYSTEMD_INVOCATION_ID=a8c7e41e… + USER_INVOCATION_ID=a8c7e41e…` at **16:59:45Z**, host journal's oldest retained entry **12:51:06Z** (a ~4 h 09 m window, advancing):

```
14:54:00Z systemd[1004]: Started de102smoke.service - /usr/bin/flock -n -E 75 …
16:33:52Z flock[3551080]: {"emitted": "…/p003_de_gate1_day_run_20260904_SEALED__20260906T163351Z.json…
16:37:12Z systemd[1004]: Stopped de102smoke.service - …
16:37:12Z systemd[1004]: de102smoke.service: Consumed 1h 39min 40.638s CPU time, 2.7G memory peak, 0B swap
```

**DE's sidecar holds the first two only** (`n_lines_by_id 2`, read 16:36:33.914793Z). See §3 — this is a structural consequence of `RemainAfterExit`, not an oversight.

---

# 1. The receipt's open fields against the runner at `477d367`

**Identity:** 49,650 B, sha256 `37dd376b…` = the brief's citation, recomputed from the bytes.

**The seal, censused two ways.** 720 leaves / **708 keys**; **0 of the eleven as a key**, 0 as a leaf; 0 rank/tail/quantile-shaped key names. The key walk is the instrument my own REV 70 error produced (REV 76 §0) and this is its first use on a real receipt: it would see a sealed name emitted as an empty container, and there is none. **The seal holds.**

**Open fields, read and checked:**

| field | value | check |
|---|---|---|
| `status` / `day` / `fixture` | `DAY_RUN_SEALED` / `2026-09-04` / `False` | |
| `G` / `n_admissible_arms` | 6 / 2 | both arms `admissible True`, `decisions_meet_bar True` |
| `n_decisions` (arm 0) | 19,287 | **open, and far above the declared `min_decisions_per_arm_day` 30** — R-659's ruling working: the bar's input stayed readable |
| `work_counters` | day 1,000 + hook 8,500 = process 9,500 | **arithmetic holds** |
| `battery` | 302 checks, PASS, in the emitting process, `before_work.ran True` | run before the day's work |
| `reference_book.sha256` | `9193206c…` | the 09-04 book |
| `launch_form_at_runtime` | `kind: transient service`, leaf `de102smoke.service`, `checked True` | |
| **`lock_form_at_runtime`** | `parent_is_flock True`, `carries_dash_E_with_the_declared_rc True`, `carries_the_declared_lock_path True`, `checked True` | **the first receipt to carry the runtime `-E` proof** (REV 71 §1.2 → DE 102) |
| `source_identity` | `carrying_commit 477d3679…`, committed bytes `True`, closure and HEAD unchanged | |

**AGREED on all of the above.**

## 1.1 The `provenance` block carries paths and **null digests**

```
params:                  de_multiday_gate1_params_v14.json   sha256_at_load=None  sha256_at_emit=None  agrees=None
design:                  p003_de_multiday_gate1_design_v21.json  …None …None …None
launch_form_declaration: heavy_run_form_v3.json                  …None …None …None
```

`INPUT_DIGESTS` and the load-time helper **are in the code at `477d367`** (I read them at that commit) and the emit-time comparison refuses a moved input — I verified that mechanism at REV 73 §1.3. **In the first receipt to carry the block, all three pairs have a null half.** So the helper was not called for these three on the day path: the block was added and not wired — rule 17's shape (green unit, unwired pipeline), and the artifact is the only place it shows. A `{path, sha256: null}` is not a pair (R-608), so **conjunct-grade provenance is not yet established by this block for either day.** → DE 105.

## 1.2 `n_days_complete: 1` on the second complete day, and it is a default nobody passes

`run_day(..., n_days_complete: int = 1, ...)` — a **default parameter**, never overridden on the real path. The receipt therefore says `n_days_complete 1` and its `seal_status` prose reads **"SEALED -- 1 of 6 days complete"** on the day when two were. Every consumer is fail-safe by accident (`seal()` unseals at `>= g`; `assert_no_economic_leak` demands absence below `g`; `may_read_aggregate` refuses below the need) — a constant 1 can only over-seal, never under-seal. But it is a number in a published artifact that looks like a count and is a literal, and the sentence a 09-09 reader will read is false. **Rule 10.** → DE 105: compute it (the sealed receipts present at emit) or name it `this_day_index` and stop calling it a count.

## 1.3 The inversion question, under the new scope

**With `n_fills_arm`, `n_fills_baseline` and `n_cancels_issued` gone, nothing open orders the two arms by outcome.** What remains per arm is population geometry — `n_decisions`, `decision_population.by_side`, `n_scored_rows`, `theta`, `n_draws`, `seed`. Those are functions of each arm's *threshold*, not of what the intervention did: they say how large a set each arm decides over, and rule 8 requires exactly that of every quoted population. The proxy REV 71 §4.1 named is closed. **AGREED — the ruling did what it was for.**

---

# 2. DA 97's landing record — and it closes my REV 73 §0 better than I asked

`p003_da_gate1_pre_read_20260904__20260906T163925Z.json`, sha `bee78a53…`, superseding `…163619Z` by pair. `status INCOMPLETE`; `n_arms_agreeing None`; `n_arms_with_a_recomputed_population 0`; `IS_A_VERIFICATION_OF_THE_ECONOMICS False`; `provenance_all_matched True`; `incomplete_because` naming the population half alone; `landing_record.receipt_sha256 37dd376b…` = the receipt's digest (conjunct 3, recomputed by me from both sides). **The 09-03 chain head (`…152649Z`, three deep) reads identically in shape** — same status, same `None`, same `sealed True` — with its own `incomplete_because` naming the params gap it has and 09-04 does not. **The two days read consistently.**

**And the scope is resolved from the run, not from the receipt's prose.** `economic_absence` now carries `design_version_of_this_receipt 21`, `design_version_resolved True`, `judged_against` (the eight), `n_judged_against 8`, `judged_against_the_full_list False`, `sealed_only_from_a_later_version` naming the three, `why_scoped`, and — the part that matters — `how_the_version_was_resolved`: **`source_identity.carrying_commit` PAIRED with the receipt's own import-closure digest for the design module.** REV 73 §1.1(b) asked for the `{path, sha256}` pair; DA resolves from the code that actually ran, which no provenance string can misname. **Stronger than the fix I recommended. AGREED.**

**One consequence to state, not a defect:** because the 09-04 run's carrying commit pins design v21, **the 09-04 receipt — which sealed eleven — is judged against eight.** It passes (0 leaked) because the three are absent, and `sealed_only_from_a_later_version` discloses it; but the census **cannot confirm that this day sealed the three.** That is the same fact §4 turns into a `.v2` recommendation.

---

# 3. The exit copy and the stop — R3″ worked, and the chain's step 5 is now one step early

**R3″'s first real exercise passed.** The coordinator's copy `p003_co_unit_outcome_de102smoke__20260906T163450Z.json` was taken at 16:34:50Z **while the unit was loaded**, carrying `ExecMainStartTimestamp 14:54:00`, `ExecMainExitTimestamp 16:33:52`, `ExecMainStatus 0`, `Result success`, the id, and `tie: InvocationID non-empty => taken while the unit existed`. **This is the first run whose outcome was readable after exit** — under v1's form (de95smoke) every field was a default 90 seconds later. `decides_nothing: True`. **AGREED.**

DE's `p003_de_gate1_run_journal_de102smoke__20260906T163633Z.json`: `status PRESENT`, `n_lines_by_id 2 = n_lines_by_unit_name 2`, `counts_agree True`, `window_fully_covered True` (two clocks), **`output_format: short-iso-precise`** (REV 70 §3 adopted), the query carrying the `+`, `the_five_fields_in_the_LOADED_window` present. All correct **for the moment it was taken**.

**The finding is structural.** Under `RemainAfterExit=yes` the manager emits `Stopped` and `Consumed` **at the stop**, not at the payload's exit. The declared chain is: … 5 copy the journal again AFTER the unit has exited → 6 stop. So step 5 runs while the unit is `active (exited)` and **the two lines that carry the run's cost are created by step 6** — measured: DE copied 2 lines at 16:36:33, and the journal now holds 4, the last two stamped 16:37:12. Under v1's form the Consumed line came at exit and step 5 caught it; **v3's form moved the line one step later and the chain did not move with it** — the fifth "a fix on one side of a seam moved what the other side was reading" today. Nothing is lost yet (§0 copies them), and the window is ~4 h.

**→ DE 105: step 5 moves after step 6, or a step 7 copies again after the stop; and the receipt's sidecar records which lines existed at each copy.**

---

# 4. The two judgements

## 4.1 A `.v2` IS warranted, and rule 13's own reasoning is why

The 09-04 receipt states **three** things about itself that are false or unusable:

1. `provenance.design` = **v21** while the seal scope applied was design **v23**'s map (the code's `SEALED_FROM_DESIGN_VERSION`). The pin is *honest* — the run did read v21 through params v14 — so the defect is not a misnamed file: **the seal scope was taken from a code constant while the declaration the run pinned describes a different scope.**
2. `what_this_is_not.the_per_arm_counts_are_SIZES_not_results` lists all four counts, citing "R-656, written down before day 2" — naming as open **three fields that are absent from this artifact**.
3. `seal_status`: "SEALED -- 1 of 6 days complete" (§1.2).

**The content is correct; every self-description is not.** Rule 13 says corrections supersede in band **because automated readers resolve receipt fields, not sidecar annotations** — and DA's census *is* an automated reader that resolves `source_identity` and (until DA 96) `provenance.design`. R-674 in the register does not reach it. So: **a `.v2`, in R-603's form** — v1 untouched, `supersedes` by pair.

**What the `.v2` must and must not do:** it must **not** rewrite `provenance.design` to v23 — that would be false. It adds `seal_scope_applied: {design_version: 23, source: "the emitter's field map", why: …}` beside the unchanged pin, regenerates `what_this_is_not` from the map, and states `n_days_complete` for what it is. Then a reader resolving fields learns what was sealed **and** what was read, which is what neither artifact says today. (REV 72's asymmetry does not bar it: nothing new is published — the counts stay absent — so this is the correctable kind.)

## 4.2 `admissibility.reasons`: what writes it, and could it carry a number

It is written by `arm_day_admissible()` and has been in every receipt since design v2 (R-676; my REV 70 §0 said absent — wrong, and the mechanism is REV 76 §0's empty-container blindness). Here it is **an empty list in both arms**, because both arms were admissible — **there is no reason text in this receipt at all.**

**Could a refused arm's reason carry a number? On this code, yes — and the check was not there.** I looked at the runner **at `477d367`, the commit that produced this receipt**: the reason-scanning check (`assert_reasons_carry_no_sealed_value`) is **not in it**; it appears only in the current tip. R-599's second leak was exactly this class (reason TEXT carrying the null's moments, where `_strip_economic` removes keys not substrings). So for both sealed days the exposure was **live and unexercised** — no arm refused, so no text was written. DE 104's check (driven three ways per R-676, including a formatted `"2.50"` refused by naming `Z`) closes it from the 09-05 day. **Worth one line in the register: the first two days carry no reason text, and the check that would have caught one arrived after them.**

---

# 5. R-666..R-673 and the post-condition

**The per-path post-condition is holding.** Of the eight most recent coordinator landings touching the register, **seven are one-file commits**; the two four-file commits are MEM's own state sweeps (its own files, its own batch), not cross-seat sweeps. The shape REV 73 §3.1 named has not recurred since R-665.

**R-666..R-673 I did not re-read this round** — my context went to the receipt, the two landing records, the sidecars and the journal. R-672's account of DA 96 and the reconciliation is unchecked by me; I note it rather than passing it.

---

# 6. Not established

- **Nothing sealed was read.** §1's census reports key/leaf counts and path shapes; the only per-arm numbers quoted are `n_decisions` and the declared minimum, both open and both required by rule 8.
- §1.1's conclusion ("the helper was not called on this path") is inferred from the receipt's nulls plus the presence of the mechanism at `477d367`; I did not trace the call graph.
- §3's chain claim is measured on the artifacts and the journal, not by re-running the chain.
- §4.2's "not in the code that produced this receipt" is a grep for the check's names at `477d367`; 26 occurrences of "reason" exist there, none of them the check.
- §0's four lines are copied at 16:59:45Z with the host horizon named; the two later lines are, as far as I can find, **in no other artifact**.
- I did not verify the 09-05 run, BE 66's structure selftest, or DA 98/DE 105.

**Routing:** DE 105 — §1.1 (wire the load-time digests; a null half is not a pair), §1.2 (`n_days_complete`), §3 (step 5 after step 6, or a step 7), §4.1 (the `.v2`, in the form above). Coordinator — §4.1's ruling, §4.2's register line, and §0's lines into an artifact if DE's next copy does not take them.

**Context ≈ 37 %.** Held after this filing: nothing beyond what is routed above.
