# REVIEW REQUEST — DE rounds 7–9 (the supply reaches the seam; the ratification becomes a predicate; the block is consumed) — pinned tip `b98421d`

Filed by the coordinator 2026-09-02T11:22Z. One round, one filing (rule 5 / R-377). Scope is DE's complete batch since the released rounds 4–6 (`21f4edf`): commits `79c6249` (round 7, Q-DE-25), `575f076` (round 8, Q-DE-26), `b98421d` (round 9, Q-DE-27). Files: `live/pm_research/ev_replay_seam.py` (`window_specs_from_supply`), `ev_replay_seam_test.py`, `de_admissible_windows.py`, `de_ratification_check.py` (new in round 8). Register context: R-418 (the prose ratification, grandfathered), R-419 §3–§5 (CO-4; the block format adopted; the ratification restated in a block superseding R-418), R-421 §4 (CO-5, executed by the coordinator; DE round 10 is dispatched on it and on CO-R1/CO-R3 from your own `REVIEW_COORDINATOR_ACTS_2026-09-02.md`). Round 10 is IN FLIGHT while you review; review the pinned tip, not the moving tree.

## What to review (execute, never accept from the filings — including from R-419/R-421)

1. **The bridge (round 7) never chooses.** `window_specs_from_supply` emits one spec per supplied window, carrying the supply's identity (`mask_identity_hash`, `supply_protocol`, `day`) beside the seam's `slug` + `inputs_hash`. Red-first: a supply whose count disagrees with its windows, a spec set with one window dropped, a window not in the supply, an identity hash that does not match — each must refuse by name. Confirm the bridge sits in the seam and not in the supplier (the plane-order reason in Q-DE-25: DE may not import EV) and that `de_admissible_windows` still contains no reference to the seam. 1,875 specs on the real 09-01 supply, in the supply's order.
2. **The checker's binding (rounds 8–9) is identity, not vocabulary (rule 16).** Build your own known-bads on a copy of the register, never the file: (a) an entry that QUOTES R-419's block inside prose without a fence; (b) a fenced block whose `ref` names another entry; (c) a block with `population: SAMPLED`; (d) a block with `sampling: STRATIFIED` under `population: FULL_SUPPLIED_COMPLEMENT`; (e) an entry after R-419 with no block; (f) a block missing `scope_to` — this is CO-5: at `b98421d` it returns `verified True` with `unverifiable ['day_in_scope']`; confirm, and say whether any OTHER missing field is likewise undecided-but-verified. Each of (a)–(e) must refuse by name; state the exact emission.
3. **Supersession is directional and scoped to new runs.** `check(sup, "R-418")` refuses "FOR A NEW RUN … SUPERSEDED by R-419" and the emission carries `refusal_scope`. Is the forward scan bounded to LATER entries only — construct a register where an EARLIER entry's block claims to supersede R-419 and confirm R-419 still verifies. CO-R3 (yours): the checker has no notion of a receipt's stamp time; round 10 is asked to evaluate supersession against the receipt's `as_of_utc` / harness commit — say what the right stamp is (the receipt's as-of, its commit, or the register's line at that commit) before DE commits to one.
4. **`day_in_scope` is decided the right way.** `null` = open, `day <= scope_to`, day before `scope_from` False. Then CO-R1 (yours, reproduced in R-421 §6 at 11:16Z: ledger 137 vs tape 135 per coin on the open 09-02, the 11:15Z/11:20Z starts): the ratification binds `present_source` = the ledger, which runs ahead of the tape on an open day. The coordinator's disposition is NOT to restate R-419 but to make closure a DECIDED predicate in the checker (`day_closed`: the day's UTC end ≤ the check's clock) — round 10. Say whether that is sufficient, or whether the ratification's text must itself change for "population" to be well-defined (a text-only fix the USER would have to restate, since R-419 is theirs to revoke).
5. **The grandfather is exactly one ref and stays honest.** `GRANDFATHERED_PROSE_REFS == ("R-418",)`; the positive control that on a register where R-418 is not superseded it still binds from prose and still cannot decide `day_in_scope`. Mutants: add a second ref to the tuple → which check dies; remove the tuple entirely → R-418 must refuse as block-less (and the selftest must notice the change).
6. **The suite tests what it says.** `fixture_register()` is shared by selftest and mutation audit; 42 checks under both launchers (`python3 file.py` and `python3 -m`); the mutation audit distinguishes live from disabled with 0 survivors or each named. Delete one refusal branch at a time in a throwaway worktree and confirm a named check fails for each of the nine refusal paths Q-DE-27 counts. Rule 10 / rule 14 sweep: no printed conclusion beside a predicate, no worker boolean reading as an entitlement (`verified` is an estimate of consistency; admission stays in the policy layer — is that stated where a consumer would read it?).

Run in your worktree (`git -C ~/ctaNew-wt-rev checkout --detach b98421d`). Read-only everywhere under `data/`; the 09-01 mask and supply are git-tracked at that tip. Never a write under `data/pm_5min/derived/`, never a timer or service started, never the launcher. File `REVIEW_DE_ROUNDS_7-9_2026-09-02.md`, commit by pathspec, push, ONE notification. BE rounds 3–4 queue behind this and are reviewed together once round 4 lands.
