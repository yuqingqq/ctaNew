# REVIEW 132 — DE 167 (`2969caa`) driven from the entry point

**REV, 2026-09-09T10:05Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`.

**VERDICT: DE 167 VERIFIES ON ALL THREE, AND THE ONE THAT CLOSES THE USER'S DEFECT CLOSES IT
AT ITS ROOT — REVIEW 129's EXACT THREE VICTIMS NOW REFUSE BY NAME. TWO RESIDUALS. THE
MATERIAL ONE: the expected set is now the RECEIPT'S OWN DECLARATION AND NOTHING RECOMPUTES
IT, so a truncated derivation reproduces `MATCHES, n_checked: 1` — driven, and driven to pass
while a real scoring module had moved. THE SECOND: `load_params` fails rule 33's third drive —
a params file with NO `protocol` key is ADMITTED.**

---

## (1) THE BOOK-CODE PREDICATE — the move to BE's derived eight

BE's derivation over the real 49-module recording gives **`scoring.n = 8`** — the exact eight
REVIEW 129 named, my predecessor's typed five plus `de_data_root.py`,
`de_multiday_gate1_runner.py` and `pm_tape_density.py`.

**PASS on the real thing.** A receipt shaped as the builder writes one today:

```
BOOK_SCORING_CODE_MATCHES  n_checked=8  n_declared=8  n_checked_equals_n_declared=True
expected_set_source = derived_closures.scoring (BE 117's derivation)
```

It reads `derived_closures.scoring.modules` **and** `.n`, and the count sits beside the key
set — REVIEW 129's requirement, met.

**FAIL on a known-bad — REVIEW 129's own three victims, re-driven.** Each in BE's derived
scoring set, none in the old typed five, each given a digest of sixty-four zeros:

| module | REVIEW 129 (before) | now |
|---|---|---|
| `de_multiday_gate1_runner.py` | `BOOK_SCORING_CODE_MATCHES n_checked=5` | **REFUSED `BOOK_BUILT_BY_DIFFERENT_SCORING_CODE`** |
| `de_data_root.py` | `BOOK_SCORING_CODE_MATCHES n_checked=5` | **REFUSED** |
| `pm_tape_density.py` | `BOOK_SCORING_CODE_MATCHES n_checked=5` | **REFUSED** |
| `de_head_scoring.py` *(control, was in the typed five)* | refused | refused |

**The false MATCH is gone, and the defence REVIEW 129 removed is not needed.**

**REFUSE A PARTIAL INPUT — four ways:**

```
the USER's one-module recording  -> REFUSED BOOK_SCORING_CODE_RECEIPT_INCOMPLETE
an EMPTY recording               -> REFUSED BOOK_SCORING_CODE_NOT_RECORDED
recording missing 3 derived names-> REFUSED BOOK_SCORING_CODE_RECEIPT_INCOMPLETE
n = 99 against 8 keys            -> REFUSED BOOK_SCORING_EXPECTED_SET_UNREADABLE
```

**The fallback does not become a typed list.** An unreadable derivation (`n` not an int,
`modules` empty, or the key absent) falls back to the **49-module recording** and names
itself `FALLBACK` in `expected_set_source`.

### REACHABILITY, WHICH IS THE REVIEW 126 QUESTION

**`derived_closures.scoring` is carried by 0 of the 12 receipts on disk.** So today the
fallback is the only path that runs, and under it a real receipt **refuses**
`BOOK_BUILT_BY_DIFFERENT_SCORING_CODE` — 49 recorded modules against a tree that has moved
all day. The builder does write the key (`be_daybook_build.py:1403`,
`derived_closures=_producing_closure_block(...)`), so the derived path goes live at the next
rebuild. **Wired, and unexercised.**

### RESIDUAL 1 — MATERIAL: THE STANDARD COMES FROM THE ARTIFACT UNDER TEST

`be_producing_closure` is **not referenced inside `assert_book_scoring_code`**. The predicate
reads the receipt's declared expected set and never re-derives it. Driven — a receipt whose
`derived_closures.scoring` declares two modules chosen to **exclude** `de_head_scoring.py`,
with `de_head_scoring.py` carrying sixty-four zeros in the recording:

```
BOOK_SCORING_CODE_MATCHES  n_checked=2  n_declared=2  equal=True
modules_checked = ['de_data_root.py', 'de_multiday_gate1_runner.py']
closure_coverage: 2 of 49, fraction 0.0408, TYPED_SUBSET_OF_A_RECORDED_CLOSURE
```

**A real scoring module moved and the predicate passed**, and at `n = 1` it returns the
user's own reading verbatim. The shortfall **is** published in `closure_coverage` — which is
rule 28 exactly: the evidence is recorded and the check is off.

Weighted honestly: the receipt is written by the builder in the same run as the book, so this
is not a live exposure today — it is the *standard* being taken from the artifact under test.
**The fix is one call: re-derive with `be_producing_closure.derive(mods)` at read time and
refuse a declared scoring set that differs.** DE already imports that module in its own
battery. **It is owed before the rebuild makes the derived path live, not after.**

## (2) THE PROTOCOL STRING — corrected, with a hole at the third drive

```
v26 version=26  protocol=…_V20      v28 version=28  protocol=…_V20
v27 version=27  protocol=…_V20      v29 version=29  protocol=…_V29   <- corrected
```

`load_params` now requires **equality**: the real v29 loads; `…_V20` at version 29 is
**REFUSED `PARAMS_VERSION_AND_PROTOCOL_STRING_DISAGREE`**.

**RESIDUAL 2 — a params file with NO `protocol` key is ADMITTED.** An absent string is never
compared. Driven at the consumer: `da_accrual_report.gate1_accrual` then raises
**`KeyError: 'protocol'`** — so it is **loud, not silent**, a crash where a named refusal
belongs. That is the same shape as the `de_section81_arms` hole DE fixed this round, one file
over.

**Readers, by operation rather than by spelling:** one **COMPARES** —
`da_gate1_day_verdict.py:2523`, `params["protocol"].endswith(f"V{max(nums)}")`, the computed
comparison a literal search cannot see (DA is re-running that gate; not duplicated here) —
and two **COPY** into emitted artifacts, `da_accrual_report.py:242` and
`da_gate1_day_verdict.py:2151`. No third comparison exists.

## (3) THE TWO DE 165 ITEMS — the holes are CLOSED, not described

**`de_section81_arms.assembly_shape`**, all five driven:

```
all PER-ROW dicts        -> PER_ROW_SCORES              (real shape, decides)
all bare floats          -> PER_GENERATION_SCORES       (real shape, decides)
MIXED dict beside float  -> REFUSED SECTION81_ASSEMBLY_SHAPE_UNDECIDABLE   (was a TypeError)
dicts carrying NO `gen`  -> REFUSED SECTION81_ASSEMBLY_SHAPE_UNDECIDABLE   (was 3 of 3 dropped, silent)
EMPTY assembly           -> REFUSED SECTION81_ASSEMBLY_IS_EMPTY
```

**The write-side pairing**, including the drive you named:

```
correct                                -> PAIRED
TWO LENGTH-500 LISTS, WRONG ORDER      -> REFUSED …NULL_DRAW_PAIRING_BROKEN
a DUPLICATE ordinal at position 7      -> REFUSED …NULL_DRAW_PAIRING_BROKEN
no recorded pairing                    -> REFUSED …NULL_DRAW_PAIRING_UNRECORDED
497 settle values against 500 draws    -> REFUSED …NULL_PARALLEL_LIST_LENGTH
```

and **reached from the entry point**: `write_ledger` calls `assert_null_draw_pairing`, and the
runner calls `write_ledger`.

**The case it admits, measured here rather than cited.** Right length, ordinals `0..n-1`,
settlement values copied from the 5-second draws — **R-825's own instance** — is ADMITTED.
On my fixture, with the observed value held fixed:

```
Z from the settlement moments (correct) = -0.804828
Z from the 5-second moments (R-825)     = +1.042697
```

**A different direction, not a different size** — the same shape REVIEW 115 found on HAZARD.
DE states this limit in its own docstring and points at the reader's re-derivation as what
catches it. Recorded so the guard is not trusted past its reach.

## MY OWN PROBES FAILED TWICE AND I RECORD BOTH

`sorted(scoring["modules"])[:2]` **included** the module I meant to exclude, so its refusal
proved nothing until I re-drove the case properly; and my first `assembly_shape` fixture
passed a **list** where the module wants a dict, so the real per-row shape read as a refusal.
Both were caught by their own controls and re-driven; the numbers above are the re-drives.

## ROUTED

1. **DE — re-derive the expected set at read time** (`be_producing_closure.derive(mods)`) and
   refuse a receipt whose declared scoring set differs. Owed **before** the rebuild.
2. **DE — a params file with no `protocol` key must refuse BY NAME at `load_params`**, not
   `KeyError` at a consumer.
3. **Coordinator — the derived path is wired and unexercised (0 of 12 receipts)**; the
   fallback is what runs today and it refuses on every landed book.
4. **Nothing here blocks.**
