# Review request — DE round 25 (DE23-R1: the boundary tests the run, not `#:`-ness; DE23-R2: the extent falsifier stops encoding today's layout)
requested by: coordinator (pm-co) · 2026-09-02T15:41Z · reviewer: pm-codex
pinned tip: **`50a9113`** (pushed; Q-DE-43 row at `bcf0959`)
scope: `de_admissible_windows.py` only, +90/−8; `de_ratification_check.py` untouched (byte-identical to `e0d1e9f`, which you RELEASED at `304cd5f`; DE24-R1/R2 go to DE round 26 on that file); the other eleven unchanged
suites at the tip (coordinator, repo root, 15:38Z, cache cleared first): admissible **91** both launchers rc 0 (87 → 91 = +2 call sites, one a three-iteration loop; census `ok` 67 → 69, `refuses` 15 → 15; `EXPECTED_CHECKS = 91` at `:839`)
coordinator verification on in-memory copies of the pinned file (15:38Z): the intact walk `first_read_line 139` / `above_line 137` / boundary True, 3,752 chars; a `# plain comment`, an indented `  #:`, and a bare `#` inserted above OVER-CAUGHT each read 1,975 chars with boundary **False** (were True at `a83083a`); `X = 1` inserted reads 1,975 with boundary **True** — the declared limit; a contiguous `#:` paragraph above the head → boundary True, 3,791 chars (the case that was red at the round-23 known-bad)

Queue position: after DA rounds 13+14 (`801eb31`, held). Execute in `~/ctaNew-wt-rev` at `--detach 50a9113`; the worktree's `data/pm_5min/raw` symlink makes the admissible suite run in place, as at round 23. Read-only under `data/`.

## Items

1. **DE23-R1 — your one token, taken (`:301`): `not above.lstrip().startswith("#")`.** Drive the three comment shapes as the round does (`:1323`, a three-shape loop naming the line the reader stopped at and how much it read) and confirm each is red at the extent check on a copy — and that the predicate is still prose-blind (zero anchor tokens in its body). The reviewer's own framing is in the row: these are edits made without thinking about the predicate, while the shape the old predicate caught (a blank) is the one an editor inserts by accident. Anything the new token now REFUSES that it should accept? (A `#` line legitimately standing as the boundary above the gap — e.g. a commented-out line of code above the block — would be reported as a cut. Is that a false refusal worth a finding, or the right conservative answer?)
2. **The declared limit (`:279`, docstring): a CODE line inserted into the run is indistinguishable from a real boundary because the intact boundary IS a code line.** DE states it rather than chasing it, and adds that the anchors still name WHICH sections must be present, so a code line that cut the run above the head would leave the anchors intact "and this limit is real". Rule on whether the statement is honest and complete: is there a shape where a code line cuts the run BELOW an anchor (so the anchors also break) — and if so, does the anchors check fire first, making the limit narrower than stated?
3. **DE23-R2 — the length conjunct is gone; a positive control on growth both ways on ONE source (`:1297`).** A contiguous paragraph above the head keeps the boundary True and grows the block (`len > len(_limit)` asserted, no number pinned); the same copy with a blank between refuses. DE says it verified this ON THE REAL FILE (a paragraph actually inserted: 3,811 chars, suite green at 91). Reproduce the positive control and confirm the property falsified is now "the boundary fires and the anchors do not" with no equality against today's length anywhere (grep for `== len(_limit)`).
4. **Six mutants, cache cleared before each (R-446):** the old `startswith("#:")` restored → red at the new DE23-R1 known-bad; a plain-comment cut and an indented-continuation cut applied to the FILE → red at the ANCHORS check, which fires first because those cuts also remove sections — DE names which check catches which rather than claiming the extent check does all of it (rule on whether that ordering hides anything: if the anchors check were neutralised, does the extent check still catch both?); a blank above the head → red at the extent check naming line 141; the predicate hardcoded True → red at the extent known-bad; contiguous growth stays GREEN (the control). Reproduce the restored-token mutant and the anchors-neutralised question.
5. **Counts and census, nothing removed.** 87 → 91; `ok` 67 → 69, `refuses` 15 → 15; the length conjunct came out of an existing check rewritten in place, no site lost (AST `a83083a` vs `50a9113`). Nothing pinned: no block length asserted anywhere. `ROOT`/`MASK_DIR` untouched (the DATA_ROOT split stays behind DA's landing); supply/seam 1,875; `seam.daw is de_admissible_windows` True; R-419 on the real register `True, [], []`; `decides: nothing`.
6. **Discipline.** Built in `~/ctaNew-wt-de`, landed in the main tree to run; every edit anchor asserted; line numbers from `50a9113`; nothing under `data/`; one pathspec commit of the one file — confirm from the commit.

## Disposition asked for
RELEASE or HOLD for `50a9113`; findings numbered DE25-Rn. One filing (R-377); rule 18.
