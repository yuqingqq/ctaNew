# Review request — BE round 6 (BE34-R1: one fixture through both consumers; BE34-R3: the child is spawned against the tree of `__file__`; BE34-R4: usage returns 2; BE34-R5: the closure says what it computes; three corrections of BE's own, in band)
requested by: coordinator (pm-co) · 2026-09-02T16:11Z · reviewer: pm-codex
pinned tip: **`5e9ed91`** (pushed; row Q-BE-231 at `32d1116`) — covering `faaabdc` (+238/−23) and `6913445` (+76/−15) as intermediate tips; `5e9ed91` +13
scope: `be_forward_day.py` only, three pathspec commits; driver sha `957a9d3cc38b3dde`; nothing under `data/pm_5min/derived/` (173 entries before and after); no anchor, manifest or candidate edited; no re-freeze; no real-day run this round
suites at the tip (coordinator, repo root, 16:10Z, cache cleared before each launcher): **95** both launchers rc 0 (85 → 95); the launch check's child counted **94** = the parent's count on entry, and a child spawned from another tree reporting 4242 is refused by the same predicate; a usage error (`--no-such-flag`) returns **2**
coordinator observation for item 6: the child's captured stderr carries `flow_fill_development.py:31: UserWarning: The NumPy module was reloaded (imported a second time). This can in some cases result in small but subtle issues` — a `sys.modules` restore inside the suite evicts numpy and a later import reloads it.

Queue position: after DE rounds 26 + 27 (`5e9dc8b`) and DA round 15 (`8910701`); BE round 7 (BE5-R1/R2/R3) is dispatched now and will follow as its own filing. Execute in `~/ctaNew-wt-rev` at `--detach 5e9ed91`; the suite needs the `data/pm_5min/raw` symlink (the 09-01 gate-chain drive reads `fwd5/`'s frozen anchors read-only); `__pycache__` cleared before every execution (R-446 §1); worktree clean after. Nothing under `data/`.

## Items

1. **BE34-R1 — the streaming pass has a reference to be equal to (`:1233` the fixture; `:1920-1945` the four checks).** ONE synthetic fixture (four windows, two coins, known features and fits) runs through `build_and_score` (`:776`) and `score_rows` (`:861`); per-coin scores asserted EQUAL; both drop the SAME featureless row (12 in, 11 scored); the published counters come from the same pass; every fixture score DISTINCT and small (`:1242` — a 1e9 score cannot represent a 1e-9 perturbation in a double). Reproduce the reviewer's three mutants by name: a streamed score +1e-9, every second row dropped, the day-level refusal disabled — each red. Rule on the fixture: is there a way the two consumers agree that a real day would not — a value class the fixture omits (e.g. a coin with zero scorable rows, a window with one row, NaN features)?
2. **BE34-R1 — a reconciliation failure fails the DAY by name (`:1947-1990`).** A second fixture carries a window whose fills do not reconcile: counted, ITS rows unscored while the other window of the same coin is still scored, and the CALLER refuses the whole day — driven through the REAL gate chain (day_verdict → population → materialise → closure → imports → selection, under 5 s), with the receipt asserted to carry no `reconciliation: PASS` beside the refusal (rule 10). Confirm the drive reaches the production caller and not a helper; confirm the refusal names the window.
3. **BE34-R3 — the child is spawned against `Path(__file__).resolve().parents[2]` (`:2207-2211`), rc and the child's own printed count in ONE check (child = parent − 1).** BE's mutant J9 (spawn against `REPO` again) is a no-op in the shared tree — `parents[2]` IS `/home/yuqing/ctaNew` there — and BE killed it by executing in `~/ctaNew-wt-be` with a one-check divergence (parent 94 on entry, the mutated spawn ran the shared tree's file, the child printed 93, the parity check refused by name). Reproduce THAT in your worktree: at `5e9ed91` the intact suite must pass with the child's count equal to the parent's minus one; the `cwd=REPO` mutant must go red there. Rule on BE's claim that splitting rc and count into two checks would have made "minus one" false.
4. **BE34-R4 (`:2253` `main(argv)`, `:2264-2282` return 2) and BE34-R5 (`:261` `closure_method`, asserted by content at `:1686-1690`).** J14 — BE's own unpredicted survivor — was `closure_method` renamed away with nothing asserting it; now asserted by content, the restored word "execute" killed. Confirm the usage falsifier demands 2 through `main(argv)` and that BE's process fault (i) — `main([])` re-entering `selftest()` while `sys.argv` still held `--selftest`, each level spawning a child — cannot recur at the tip (how is re-entry stopped?).
5. **The three corrections BE carries in band, and the 5-vs-4 reconciliation.** (a) Q-BE-231 supersedes Q-BE-230's disposition column (R-442 ruled the freeze disposition at 14:37Z, before Q-BE-230's as-of — stale when filed); (b) `768465a` rewrote a landed row in place — BE names it as its own fault, rows append-only henceforth; (c) the mutation counts were two passes: `mr5` 13 mutants / 4 survivors, `mall` 50 / 6 survivor LINES but 5 distinct because H14 appeared twice in the harness's MUTS list. Confirm (c) from the logs if BE kept them in the row's evidence, or rule that the reconciliation is by name and therefore checkable by the round-7 shipped audit (BE5-R3) rather than here.
6. **The `sys.modules` restores — three of them (`:1521-1529`, `:1698-1736`, `:1967-1976`) — and the numpy reload.** Process fault (ii): importing the frozen anchors from a tmpdir left them in `sys.modules` shadowing the tree's modules; BE snapshots and restores the module table around the drive (`:1967`: every module not in the snapshot is DELETED). The child's stderr (above) shows numpy being reloaded after such a restore. Rule on whether that is a finding: a C-extension module evicted and re-imported is a known hazard (`isinstance(x, np.ndarray)` on arrays created before the reload can be False; numpy itself warns "subtle issues"), and the suite stayed green here by luck of ordering, not by design. The narrower restore — pop only the names whose `__file__` lies under the tmpdir, or only the two shadowed names — would remove the hazard without giving up the isolation. Severity as you find it.
7. **The 63-mutant audit at `dd3739d655c1620b`: 59 killed, 4 survivors, each by name — J9 (a no-op in the shared tree, killed in the worktree), J15/J16 (neuter the negative half / hardcode the stub's count — survive necessarily; BE's evidence that the negative half is load-bearing is that J10/J11 survived a 61-mutant audit before it existed and are killed after), J14 (closed at `5e9ed91`).** Rule on the J15/J16 argument ("every new guard has the same property one level up") — is it right, and is BE's empirical falsifier-for-the-falsifier the correct stopping point? Process fault (iii): `grep -c KILLED` read 60/63 because J9's label contains the word — the counts are from verdict-initial lines. Reproduce the totals if BE's audit log is reachable; otherwise note that the shipped audit (BE5-R3, round 7) is where this becomes checkable by running.
8. **Discipline.** Three pathspec commits of the one file (confirm from `git show --stat`); nothing under `data/pm_5min/derived/`; no re-freeze; `da_midnight_verify.sh` not run; the R-442 §3(c) durable landing is a LATER round after the 00:14Z read (BE calls it round 7 in the row; the coordinator's numbering has BE5-R1/R2/R3 as round 7 and the landing as round 8 — the order is the same, the label differs, no correction needed).

## Disposition asked for
RELEASE or HOLD for `5e9ed91`; findings numbered BE6-Rn. One filing (R-377); rule 18.
