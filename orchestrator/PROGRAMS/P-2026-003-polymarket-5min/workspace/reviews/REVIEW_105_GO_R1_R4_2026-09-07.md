# REVIEW 105 — GO R1..R4, the four design-day re-runs at `850c166`

**REV, 2026-09-07T16:37Z.** Read-only: no lock, no heavy unit, nothing written under
`data/`. Everything below ran in a scratch clone at the real commit `850c166`, never in
wt-rr and never in wt-de.

**VERDICT — ONE VERDICT FOR THE FOUR: GO R1, R2, R3, R4 MAY PROCEED.** They share one
gate (one composition, one endpoint, one supersession rule, one params head), and each of
the six things the USER asked to see refuses by name. **Two launch conditions and three
read conditions are named in §7; one of them (`--supersedes`) is not optional — without
it every one of the four refuses at the precondition.** Nothing here holds the night.

**wt-de: FROZEN AND UNTOUCHED** — queried read-only: HEAD `5020f96`, `?? data` only.
**wt-rr**: HEAD `850c166`, `?? data` only.

---

## §1 THE USER'S GATE — driven at `850c166`, each case BY NAME

| the case | what it returns |
|---|---|
| **the USER's own reproduction**: `all_agree=True`, `per_slug={}`, `counts={}` | **REFUSED `SETTLEMENT_VERIFICATION_EMPTY`** |
| 287 of 288 slugs, every one VERIFIED_AGREE | **REFUSED `SETTLEMENT_VERIFICATION_SLUG_SET_MISMATCH`** |
| 288 slugs, one DISAGREE | **REFUSED `SETTLEMENT_WINNER_DISAGREES_WITH_CHAINLINK`** |
| a convention name that is not the pinned one | **REFUSED `SETTLEMENT_CONVENTION_NOT_THE_PINNED_ONE`** |
| zero Chainlink hourly files (and: no provenance block at all) | **REFUSED `SETTLEMENT_CHAINLINK_NO_FILES_READ`** |
| the real **09-05** pair | **no refusal — 288/288 VERIFIED_AGREE, 54 hourly files, `is_final_for_quotation` True** |

**Can `is_final_for_quotation` be true without identity? NO.** It is
`bool(finality.is_final)`, and `is_final` is the AND of (the verified slug set EQUALS the
day's) ∧ (every status VERIFIED_AGREE) ∧ (hourly files > 0) ∧ (provenance complete: venue
sha, stream-files digest, reader-module sha). Driven: 287 of 288, all agreeing, returns
`is_final_for_quotation` **False** even without `require_verified`. The counts are
RECOMPUTED from `per_slug` inside `winner_source` and an undeclared status refuses
(`SETTLEMENT_VERIFICATION_STATUS_UNKNOWN`), so the door no longer opens on a boolean its
caller wrote — the hole REV 104B §6(2) drove is shut, and the USER's version of it with it.

**One seam, non-blocking and routed:** `chainlink_verification.status` is set from
`_all_agree` first and only ever UPGRADED by finality, never downgraded. In the 287-of-288
case the status string reads `VERIFIED_AGAINST_CHAINLINK` beside `is_final_for_quotation:
False`. The door is right and the value is labelled by the boolean; the STRING is the
thing a later table will quote. Set the status from `finality.is_final` in both directions.

## §2 REVIEW 104B §6 (1)(3)(4) — all three fixed, all three driven

- **(1) the per-status reason.** The fixture receipt I produced at `850c166` now reads
  `"why": "this day IS admissible (class 'FIXTURE') and no winner map was supplied…"` —
  computed from the verdict beside it. The literal that contradicted `admissible: true` is
  gone.
- **(3) the degenerate settlement null is a STATUS.** Driven (all-Up winners on the
  fixture): both arms return `status SETTLEMENT_NULL_DEGENERATE`, `null n 500`, `null_sd
  0.0`, `Z None`, and **the day still emits**. The uncaught `DesignRefused` that would
  have thrown away a 75-minute run is gone.
- **(4) rule 11's precedence.** Driven: a declaration naming the CONSUMED day 09-05 now
  computes `class DECLARATION_NAMES_A_CONSUMED_DAY, admissible False` and
  `assert_settlement_day_admissible` REFUSES
  `SETTLEMENT_DECLARATION_NAMES_A_CONSUMED_DAY`.

## §3 LEDGER SCHEMA v3 — one half holds prospectively, the other does not hold retroactively

- **The header names the kinds.** A v3 ledger I wrote carries `schema_version: 3`,
  `settlement_row_kinds: [SETTLEMENT_SCALARS, SETTLEMENT_SLUG]` and
  `settlement_rows_present: [both arms]`, with 2 SETTLEMENT_SCALARS and 72 SETTLEMENT_SLUG
  rows beside the HEADER/ARM_SCALARS/NULL_DRAW/FILL/DECISION rows. ✔
- **A v3 reader on the landed v2 ledgers.** Driven at the real 09-05 file: reads as
  `schema 2` with `settlement_rows_status SETTLEMENT_ROWS_ABSENT`. ✔
- **"A v2 reader handed a v3 ledger refuses by name" — TRUE PROSPECTIVELY, FALSE OF THE
  READERS THAT EXIST.** The mechanism is `KNOWN_SCHEMA_VERSIONS` in the NEW module, and
  the battery proves it by narrowing that set to (1, 2). **I drove the ACTUAL older module
  (the `de_decision_ledger` bytes at `1f171e8`/`5ad16b5`, `SCHEMA_VERSION = 2`) against a
  v3 ledger: it does NOT refuse — it reads it, reports `schema 3`, and drops the
  SETTLEMENT rows silently.** That is DA 131's failure mode, alive in every reader landed
  before this composition. A guard cannot be added to bytes already landed, so this is not
  a defect to fix — it is a **read condition** (§7).

## §4 THE PLACEMENT LATENCY IN THE RECEIPT (R-811) — sound, and the sibling rule is right

`placement_latency_from_the_book` walks the BOOK's builder receipt for any key naming a
placement latency and records `L_place_ms` with the book's `path` + `sha256` — never a
constant. **Is DE's "a book recording none reads 0 by construction" sound? Yes, and for a
checkable reason**: the claim is about the CODE that built the book, not the book —
`build_reference` had no such parameter before DE 137, and `be_daybook_build.py:690` still
passes none, so every landed book was built at L = 0. It is a measurement of those books'
producer, and the residual case (a rebuilt book that records nothing) is exactly what
`settlement_endpoint.require_book_declares_L` arms — **armed by the declaration, not by a
constant**, which is the right layer. Note the composition deliberately holds phase4 at
v19's pinned bytes, so `apply_placement_latency` **is not in the bytes the re-runs
execute** (checked: no reference to it anywhere in the composed tree) — which is
consistent, not contradictory: the receipt reports the BOOK's L, and the book's L is 0.

**The sibling rule is right and the four re-runs are unambiguous today.** A read at one L
naming an artifact run at another refuses `EARLY_READ_SUPERSEDES_DIFFERENT_PLACEMENT_
LATENCY` — a sibling is not a successor. Keying on `(day, L)` is DA's open half, and it
does not bite tonight because **every artifact in the family is at L = 0**: one artifact
per day, one L, so `(day)` and `(day, L)` coincide. A second L cannot exist until a book
is rebuilt (BE 101). **One wording nit, non-blocking:** the four targets predate R-811 and
declare no L, so the check is SKIPPED for them and the pair records
`both_at_L_place_ms: 0.0` from THIS read's value alone. Call it what it is — the target
does not declare one; the 0 is inferred from the same by-construction argument.

## §5 THE COMPOSITION `850c166` — verified, with one stale file that is not DE's

- **Exactly four paths differ from `5020f96`**: `da_early_read_verify.py`,
  `de_decision_ledger.py`, `de_early_read.py`, `de_multiday_gate1_runner.py`.
- **phase4 is STILL at the pin**: `ee4034c1…` = v19's declared digest. **Ten cascade pins
  cited, 0 mismatched.**
- **Batteries at the real commit (my runs, scratch clone): runner PASS 394 / 0 disarmed /
  0 skipped; early read 33; ledger 11; `--synthetic-day` rc 0** with 0 keys named
  `inventory_leg` and 4 named `trades_cash_flow_cents`.
- **All four days rehearse READY** — with their supersession targets:
  09-03 `…085436Z` `5c8a58f5…`, 09-04 `…105906Z` `b196bf32…`, 09-05 `…112912Z`
  `e8f88dc4…`, 09-06 `…124731Z` `67bce38f…`; **one artifact per day, so each head is
  unambiguous**.
- **Rule 11**: 09-03..09-06 `DESIGN_DATA` admissible; 09-07 `NOT_ADMISSIBLE`.
- **THE ONE THING THAT DIFFERS FROM THE TIP AND SHOULD NOT SURPRISE ANYONE:**
  `da_early_read_verify.py` in the composition is **DA 130** (`27cc9925…`, commit
  `1ae19f7`), while mm-research carries **DA 131** (`4ffeeefc…`, `4ce219b`) — the reader
  that knows the settlement rows. DA 131 landed **16:06:51Z**; the composition was cut
  **16:24:00Z**, so it was available and was left out. Nothing the re-runs EXECUTE is
  affected (they never call DA's verifier), and DE's other three files match the tip byte
  for byte. The risk is entirely in **who verifies from where** — see §7.

## §6 DE's TWO SELF-REPORTED FAILURES

- **The rule-21 guard that printed STOP while the `&&` chain ran on.** I did not audit the
  shell; I audited the consequence, which is the question that matters: **is anything at
  `850c166` the worse for it?** Measured — DE's three files at `850c166` are byte-identical
  to the same paths at the landed tip, and the only two that differ are phase4 (held at
  the pin, deliberate and stated) and DA's verifier (stale, §5). **No foreign work is
  overwritten in the composition.** The guard itself I take on DE's report; the routed item
  is that a guard whose STOP a caller can ignore is not a guard — it must exit non-zero
  and the caller must be a script, not a chain (rule 21's own scratch-drive form).
- **A cell inheriting another cell's fixture** — DE reports three occurrences in three
  rounds, the third breaking a chain DA's resolver caught by name, and that each cell now
  owns its fixture. I confirmed the pattern is now stated in the code beside the cells I
  read, and the batteries are green with 0 disarmed. **This is the same shape twice over:
  a cell that inherits state measures history, not its own property.** Worth a standing
  rule rather than a third fix.

## §7 WHAT THE RE-RUNS MUST PRODUCE, AND WHAT wt-rr MUST SHOW

**Before each launch, wt-rr must show:** HEAD `850c166`; `git status --short` = `?? data`
only; the heavy lock FREE (BE holds it to ~17:10Z); and the day's rehearsal READY.

**LAUNCH CONDITION, NOT OPTIONAL: pass `--supersedes <the day's head artifact>`.** Driven:
without it all four rehearse **NOT_READY / `EARLY_READ_ALREADY_EMITTED`**; with the target
they are **READY, blocking []**. The four targets are named by path and sha256 in §5.

**Each artifact must carry, for DA and BE to read it:** `supersedes {path, sha256}` naming
that day's head; `economic_settlement` (D_E_settle, Z, p_location, null mean/sd/n, both
paths' totals, both legs) or a NAMED status where it is not valued; the winner block with
its `finality` (identity, all-agree, files read, provenance) and
`is_final_for_quotation`; `placement_latency` with the book's path+sha and its source;
`absolute` carrying `trades_cash_flow_cents` (and zero keys named `inventory_leg`); and
`decision_ledger {path, sha256, n_rows, schema_version: 3}` with SETTLEMENT_SCALARS and
SETTLEMENT_SLUG rows for BOTH books.

**READ CONDITIONS.** (1) **DA must verify with the LANDED `da_early_read_verify.py` (DA
131, `4ce219b`), never with wt-rr's copy** (§5). (2) **Any reader of the new ledgers must
know schema 3** — a pre-`850c166` reader reads a v3 ledger without refusing and drops the
settlement rows silently (§3, driven). (3) **09-03 will not be quotable as final** — §8.

## §8 A DAY-LEVEL FACT THE TABLE WILL NEED, found in pre-flight

I pre-flighted the winner verification for all four days rather than discover it 20
minutes into a run:

| day | markets | verification |
|---|---|---|
| 2026-09-03 | **287**, not 288 — the 15:20 UTC window **does not exist** (no raw file, no venue record) | **286 AGREE, 1 DISAGREE** |
| 2026-09-04 | 288 | 288/288, `is_final` True |
| 2026-09-05 | 288 | 288/288, `is_final` True |
| 2026-09-06 | 288 | 288/288, `is_final` True |

**On 09-03 the 21:05 UTC window (`btc-updown-5m-1788469500`) disagrees**: the venue
records **Up**, and `S60(T) >= S60(t0)` reads **Down**. The production path does NOT pass
`require_verified`, so **R1 still runs and emits** — its artifact will carry
`VERIFICATION_DID_NOT_AGREE` and `is_final_for_quotation: False`. **Nobody may quote
09-03's ruled P&L as final**, and a checker that asserts "288/288" or "all four days
final" will fail on a true fact.

*Scope, honestly:* my 09-03 slug set came from the RAW TAPE, not from the book's fills, and
BE's selector may exclude that window from the day's population — in which case 09-03
comes back 286/286 and IS final. Either way R1 runs; what changes is only whether 09-03 is
quotable, and the artifact will say which.

**This also narrows CLAUDE.md rule 9's parenthetical in the honest direction:** the pinned
convention reproduces the venue on 288/288 for 09-04, 09-05 and 09-06 and on **286 of
287** for 09-03. It is a per-day check because days differ, and this is the day that
shows it.

## §9 ROUTED (none of it holds the gate)

1. **DE** — §1's status seam (set the status from `finality.is_final` in both directions);
   §4's `both_at_L_place_ms` wording; §6's guard-that-can-be-ignored.
2. **DA** — take DA 131 (or later) into whatever tree verifies these four artifacts, and
   key the family on `(day, L)` before a second L exists.
3. **BE** — the v3-aware read (§3) before reading the new ledgers; BE 101's builder
   pass-through remains the only route by which a non-zero L can ever reach a day.
4. **Coordinator** — §8 before the four-day table is shown to the user.
