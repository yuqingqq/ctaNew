# REVIEW REQUEST — DE round 11 (DE-R1..R4 closed: the bridge compares the identity it copies, dynamic imports resolve or refuse, values are validated) — pinned tip `d07d901`

Filed by the coordinator 2026-09-02T11:46Z. One round, one filing (rule 5 / R-377). Scope is DE's round 11 alone: commit `d07d901` (Q-DE-29), files `live/pm_research/ev_replay_seam.py` (64 → 69 checks; bridge audit 9 guards), `live/pm_research/de_admissible_windows.py` (47 → 53), `live/pm_research/de_ratification_check.py` (58 → 66; 12 → 14 audited refusal paths). Register context: R-422 §3 (DE-R1..R4 as reproduced at `2282e5c`) and §5 (the dispatch), your `REVIEW_DE_ROUND_10` (DE10-R1..R3). DE round 12 (DE10-R1: parse timestamps, refuse unparsable by name) is IN FLIGHT on `de_ratification_check.py`; review the pinned tip.

## What to review (execute, never accept from Q-DE-29 or from the coordinator's R-423)

1. **DE-R1 — the bridge compares the identity it copies.** `window_specs_from_supply` recomputes `mask_identity_hash` with the supplier's own `daw._sha` (an import, not a restatement — confirm at the call site) and refuses by name on mismatch. Tamper EACH half: the hash zeroed; one window flipped inside `mask_identity` with the hash left; the hash present with no identity to recompute from. Does the refusal fire BEFORE any spec is emitted, and does a spec carry the recomputed value or the copied one?
2. **DE-R2 — dynamic imports resolve or refuse.** `imported_modules` resolves `importlib.import_module(<literal>)` and `__import__(<literal>)`; a non-literal argument becomes a sentinel that makes `reads_no_verdict` REFUSE, not pass; dotted names contribute first AND last segment. Then look for what it still cannot see — `from importlib import import_module`, an aliased `importlib`, `exec`/`eval`, `__import__` through a variable, a string built by concatenation — and say for each whether the predicate refuses, passes, or is blind. Blind-and-passes is a finding; blind-and-refuses is a limit to state. Confirm `de_admissible_windows`' own dynamic import (the RR11-1 demonstration) resolves to `da_content_liveness_rule`.
3. **DE-R3 — VALUE, never MISSING.** Each of `present_source` (== the ledger path constant, named once), `scope_days` (named set), `revocable_by` (`USER`), `population`, `sampling` refuses a wrong value by name with the word VALUE; a missing field still says MISSING; an undecidable field still lands in `unverifiable`. DE deliberately kept `kind` OUT of the vocabulary loop (a wrong kind is "not a ratification", a different complaint) and kept the sampling/population self-contradiction refusal separate from the vocabulary check — say whether those two separations are right. **Known and out of scope for this round:** `scope_to: not-a-date` still verifies clean and `now_utc='zzzz'` still reads as closed — that is your DE10-R1, dispatched as DE round 12; confirm it is unchanged at this tip and do not re-file it.
4. **DE-R4 — the audit at 14 paths.** The unknown-population branch and the nonsense-value branch each have a control; delete each new raise site in a throwaway worktree and name the check that dies. DE reports one measured redundancy — a missing `mask_identity_hash` is caught by BOTH the envelope guard and the identity comparison — and drives each guard on the case it uniquely owns. Confirm no guard is counted load-bearing on a case another guard also refuses (the audit must not launder defence-in-depth into coverage).
5. **Nothing under review moved.** `check()`'s `stamped_at` / `now_utc` semantics untouched: R-419 on 09-01 `verified_for_new_run True`; R-418 stamped 10:30Z `provenance True`; the bridge still emits 1,875 specs on the real 09-01 supply; 66 / 53 / 69 under both launchers from the repo root, rc 0.
6. **Rule 10 / rule 14 sweep** over the new code: the refusal messages compute what they state; no worker boolean encodes an entitlement; `reads_no_verdict` is a predicate on imports, not a verdict on admissibility.

Run in your worktree (`git -C ~/ctaNew-wt-rev checkout --detach d07d901`). Read-only under `data/`; never a write under `data/pm_5min/derived/`, never a timer or service started, never the launcher, never `DA_MIDNIGHT_MODE=production`. File `REVIEW_DE_ROUND_11_2026-09-02.md`, commit by pathspec, push, ONE notification. BE rounds 3–4 queue behind this, reviewed together once round 4 lands.
