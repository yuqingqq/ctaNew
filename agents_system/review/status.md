# review status
iteration: iter-022
state: done
fix-round: 1
updated: 2026-05-25
summary: Audited X131 sideways-regime XS-reversal ensemble re-rank (ALPHA track; rank key in side regime changes pred -> score=z_xs(pred)-z_xs(rel_ret_1d), equal-weight 0.5/0.5 untuned, bull/bear unchanged). VERDICT PASS (G1 look-ahead PASS; the REJECT is trustworthy). TWO CRITICAL CHECKS, both clean. (1) PIT of rel_ret_1d (decisive for IC validity): verified empirically vs raw klines that panel return_1d[t]=close[t]/close[t-288]-1 (corr=1.0, TRAILING, window [t-288,t] ENDING at decision bar t; NOT extra-shifted -- shift(+-1) drops corr to 0.996) and the forward target return_pct[t]=close[t+48]/close[t]-1 (corr~=1.0; preds exit_time-open_time=exactly 48 bars, window [t,t+48] STARTING at t). The trailing-1d and forward windows share ONLY boundary close[t] (known at t) -> NO overlap, NO look-ahead. XS demean is within-cycle groupby(open_time) PIT. Independently reproduced pre-check IC = -0.0360 (t -9.76, n 2458) EXACTLY -> the IC is REAL, not leakage; far below +0.10 leak red-flag and re-rank LOSES gross (opposite of leak signature). (2) Ensemble sign/logic (is REJECT a bug?): sign verified empirically -- LONG basket (top score) mean rel_ret_1d=-0.10 (recent relative LOSERS), SHORT basket=+0.07 (recent WINNERS) -> longs the reversal-favored side; -z(rel) not doubled/flipped; z_xs per-cycle cross-sectional. Base reproduces X117 EXACTLY @4.5bps (+1.93/-5674/+10472) from emitted parquet; X131 heldbook engine line-for-line X117 (regime map, make_w beta-neutral, active slice t-HOLD+1:t+1, net+=wt/HOLD, turn=Sum|net-prev|, pnl=cyc-turn*0.5*cost, np.isfinite guard); only side rank key changes; bull/bear identical (non-side PnL max diff 0.028 = legit HOLD-window bleed). GROSS-PnL collapse reproduced independently by extrapolating net-vs-cost {1,3,4.5}bps to cost=0: base gross +12272 -> ens +7241 (-41%), matching handoff -> loss is NOT cost artifact, pre-cost return falls. WHY no monetize: pred book already a beta-neutral XS mean-rev basket; -z(rel_ret_1d) ranks same names from noisier higher-turnover angle that trades AGAINST pred -> reversal alpha already absorbed by pred (iter-018 mode). G3 correctly WAIVED (untuned). RNG seeded 12345. No fixes needed.
blockers: none
